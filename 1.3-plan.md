# 1.3 计划

> **状态**：✅ **已立项（2026-09-17）** —— 范围、优先级、验收标准见 **`1.3-charter.md`（立项书，已签字）**，
> 本文自此只记**开工记录**，不再承担「建议范围」的角色。
> **候选输入**一律在 `1.3-candidates.md`（A–H 八组，逐条带出处），
> 本文不重复列候选，只记「决定做什么、为什么不做什么、做完了什么」。
>
> ⚠️ **§3「建议范围」保留原样以便对照当初的理由**，但**它不是现行范围** ——
> 现行范围以立项书 §2.2 为准；建议范围里与立项书冲突之处，**以立项书为准**
> （例：C2 在本表中未列，立项书已裁决「暂不做」而移入排除表）。
>
> 与 1.2 的关系：1.2「可信版」以「**可引的痕迹**」收尾（见 `1.2-plan.md` §7.1 与
> `references/acceptance-criteria.md` §8.3），1.3 处理的是**验收时归拢出来的缺口**。
> **开工起点** = git tag `v1.2.0`（本仓库第一个 tag，2026-09-17；落点说明见该 tag 注释）之后。
> ⚠️ 但**历史层面的「起点」并不干净**：B1 / B2 这两刀早已提交在 `v1.2.0` 的**祖先**里
> （`1.2-plan.md` §7.1 末、`CHANGELOG.md` 头部均有记录）—— 版本以 `VERSION` 文件为准，别用 tag 反推归属。

---

## §1 开工记录：第一刀（2026-09-17）

**范围**：B1 + B2 两个工具层缺陷（`1.3-candidates.md` B 组前两条），外加回归时暴露的改名涟漪。

**为什么先做这两条**：它们是候选清单里**唯一**同时满足「已定位 / 可机械修 / 无新判据设计 / 有实测数据可验」
的四条特征的一组 —— 不需要先做范围决策就能开工，且做完立刻提高后续每一次闸门留痕的可信度。

| # | 内容 | 落点 | 验证 |
|---|---|---|---|
| B1 | `alignment` 的**跨轴尺寸不同即跳过**从「不报也不计入」改为**留痕**（`alignmentCoverage`：`compared` / `skippedCrossSizeDiff` / `samples`） | `tools/layout-audit.mjs` | 变异测试 **60 PASS / 0 FAIL**（51 → 60） |
| B2 | `precheck --live` 的汇总行跟随在线核对更新：核对前标「离线汇总」，核对后必打「最终汇总（含在线核对）」并给 `已核对 / 核出不存在 / 未核对` **三个数** | `tools/precheck.mjs` | 变异测试 **75 PASS / 0 FAIL**（66 → 75）· 真 Bridge 实测一次 |
| 涟漪 | `tools/qa-export-mutation.mjs` 因 1.2 收尾改名而 **ENOENT 崩掉**（上一轮改名后未复跑，一直潜伏） | `tools/qa-export-mutation.mjs` | 复跑 **56 PASS / 0 FAIL** |
| 新发现 | **不存在的 node id 让插件挂 10s 后报「请检查网络」** → 记入 D3，根因在冻结区，**未改冻结文件** | `1.3-candidates.md` D3 · `lessons.md` #76 | 3/3 复现 |

**两条判据值得单独记**（已回写 `lessons.md`）：

1. **#67c 的正确修法是留痕，不是放宽判据。** 运行 B readback 实测：真正比过 **3 对**，
   因跨轴尺寸不同没比 **121 对**；放宽判据后新增 **5 条报告，5 条全是假阳性**
   （时间戳 231×27 vs 在线圆点 10×10 —— 圆点**居中**对齐文字）。**跨轴尺寸不同时视觉惯例是居中。**
   已立反向闸门（变异测试 #24）：居中对齐的合法布局**不得**报 alignment。
2. **改名的涟漪必须追到代码里的字面量，且必须复跑全套闸门**（#75）。`check-refs` 全绿**不代表**追净了 ——
   它只管文档引用，管不到代码里拼出来的路径。B2 这一刀之所以能顺带抓到它，只因为**跑了全套回归**。

---

## §2 债线打包（2026-09-17，第二刀）

**范围**：B4 + B3 + A2 + C1 —— `1.3-candidates.md` 里「**已定位 / 可机械修 / 无新判据设计**」的全部剩余债。

**为什么打包成一轮做**：**A2 必须在加任何新检查类之前完成** —— 否则新检查的档位参数又会变成手敲的
（`lessons.md` #73 根因重现）。顺序刻意排为 **B4 收紧引用兜底 → B3 补齐留痕 → A2 参数同源 → C1 装置收编**，
做完地基干净，再进空环线不返工。

| # | 内容 | 落点 | 验证 |
|---|---|---|---|
| B4 | 引用解析四规则收紧：带目录的引用**不再 basename 兜底**；产物**类型名**判 `artifact`（指向 `assets/templates/` 定义）；部署路径 / 占位符根**单列豁免**；同名多份 → `ambiguous` **WARN 不静默取首** | `tools/check-refs.mjs` | 变异测试 **31 PASS / 0 FAIL**（18 → 31）· 收口 658 已解析 / 悬空 0 / 歧义 0 |
| B3 | `qa-critic` / `qa-l2` / `qa-export` 各加「**评估覆盖**」段（缺哪份输入 ⇒ 哪些 QA 项整体跳过，带样例名与数量） | 三个校验器 | 变异测试 **52 / 20 / 57 PASS**（各 +1 条覆盖留痕用例） |
| A2 | `layout-audit` 新增 `--spec` 从 L2 `design-system-spec.json` 派生档位；`paramSource` 逐项记来源；`paramGaps` 记「spec 声明了但取不到」 | `tools/layout-audit.mjs` | 变异测试 **76 PASS / 0 FAIL**（60 → 76，含**反向对照**用例） |
| C1 | 运行 A 的一次性装置（Python + PIL）收编为随包零依赖工具 | **新增** `tools/pixel-proof.mjs` + `tools/pixel-proof-mutation.mjs` | 变异测试 **46 PASS / 0 FAIL** · 真实截图回归与收编前**逐位一致** |

**本轮三条实证**（不是「照着做完了」，而是「做完之后被数据改了认知」）：

1. **B4 的定性被实测推翻。** 原描述是「近半数引用靠兜底解析、目录写错全漏」。实测：263 条 basename 命中里
   **254 条是纯文件名**（兜底本就合理），带目录的仅 9 条、其中 8 条是合理豁免。**真正的规模是 38 条**——
   产物类型名在 `assets/templates/` 有官方同名，旧行为静默指向第一个实例（17 条**全**指向 `example-health`），
   制造「指的是某个具体文件」的假象。**危害不在"用得多"，而在"它悄悄替一个类型名挑了一个实例"。**
2. **A2 当场抓到一条已归档结论的偏差。** 运行 B 原始 `layout-audit.json` 的 `spacing` 档位手敲为
   `[2,4,8,16,24]`，Spec 只声明 `[8,16,24]` —— **多出的 2/4 是手敲时混入的 radius 值**。
   结论条数虽未变，但**尺子不同 ⇒ 数字不可比**。#73 的根因被**复现**，不是推测。
3. **C1 收编时抓到工具自身四处静默降级，其中一处违背它自己写在头部的契约**：
   `--json` 声称「机器可读」，却在 JSON 之后又打一行人话结论 ⇒ `JSON.parse(stdout)` 与 `| jq` 全废。
   另三处：`--grid abc` 静默变空网格 · `--region` 给了没给值被当成「没写」· `--expect-content` 落在末尾抛 TypeError。
   全部改为**退出码 2 + 可读原因**，并写进头部契约「选项取值不合法一律退出 2、**不静默回落**」。
   **这是变异测试第一次跑就抓到的**（46 条里 16 条初始 MISS），不是事后补的。

**收口实测（当场，全绿）**：主闸门 8 个 —— `check-refs` 658 / 悬空 0 / 歧义 0 ｜ `qa-l2` **47** ｜
`qa-critic` **134** ｜ `qa-export --strict` **935** ｜ `qa-install` **100** ｜ `contrast-audit` OK ｜
`layout-audit --spec`（运行 B readback，1 条 medium）｜ `precheck`（基准样例 42 步 / 错误 0）。
变异测试 **9 套 / 475 条断言 / 0 FAIL**（新增 pixel-proof 46 条）。

---

## §2.5 债线尾巴（2026-09-17，第三刀）

范围：**A1（layout-audit 吃 `get-node`）+ D1（`paintToHex`）+ B5 / B6（发版守卫）** ——
都是 §3 表里标注「可直接做 / 独立可做」的项。**开工前的预期是三件真活；实测后两条半站不住**：

| 项 | 原预期 | 实测后 |
|---|---|---|
| **A1** | 「readback 形状与 Bridge `depth` 语义不匹配」→ 需让工具吃 `get-node` | **需求早已满足**（`31c68cd` 引入工具即带 `shallowWarning`；`depth` 语义由 `e445a7f` 修好）⇒ 只剩残余：几何覆盖从「容器数」升为「节点数」 |
| **D1** | 「改 `tools/figma-harness.mjs`（**非冻结区，可直接做**）」 | **落点错**：`paintToHex` 只在**冻结区** `figma-plugin/code.js`（harness 是加载器，跑**一字未改**的源码）⇒ 与 D2/D3 同类，**须向冻结区提需求** |
| **B5** | 「不校 `VERSION` ↔ `CHANGELOG` 一致」 | **前提不成立**：`CHANGELOG.md` 第 7–11 行明写两者**语义不同、允许并存** ⇒ 重述为**校序关系**（`-dev` 段必须 > `VERSION`） |
| **B6** | 「无 `VERSION` ↔ `git tag` 守卫」 | ✅ 成立，照做 |

| # | 内容 | 落点 | 验证 |
|---|---|---|---|
| **A1 残余** | 几何覆盖率量化：产物新增 `geometryCoverage`（`auditedNodes` / `totalNodes` / `unauditedNodes` / `shallowParents` / `shallowNodes` / `auditedDepth` / `inputDepth`）；警告文案改为给出**节点数**（不再把 `depth:2` 说成「典型 depth:1 轻量回读」） | `tools/layout-audit.mjs` | 变异 **76 → 89 PASS**（+13 条，含反向对照） |
| **A1 证据固化** | **新增** `tools/layout-audit-readback-probe.mjs` —— 把 A1 的复核从 `.vibe/` 一次性探针提成仓库内可重跑的**跨模块契约探针**（真 `code.js` ←→ 真 `layout-audit`），4 条判据 | 新文件 | 探针 **4 PASS / 0 FAIL**（`1/2 · 2/5 · 5/5`，且钉住「浅容器数都是 1」） |
| **A1 顺带** | 桩补 `page.selection` —— `code.js` 的 `get-node` / `get-page-summary` 都读它，缺则**任何**走 `get-node` 的离线装置抛 `TypeError`（装置缺口被当产品缺陷报出去） | `tools/figma-harness.mjs` | 探针由崩栈转为正常返回；**`qa-install` / `precheck` 已复跑**（二者共享该装置） |
| **B5 + B6** | **新增** `tools/release-guard.mjs`（六条判据：VERSION 合法 · CHANGELOG 含定版段 · 段版本自上而下不增 · `-dev` 段 > VERSION · `v<VERSION>` tag 存在 · tag 落点处 VERSION 一致）与 `tools/release-guard-mutation.mjs` | 两个新文件 | 变异 **39 PASS / 0 FAIL**；真实仓库 **6 PASS / 0 FAIL**（`v1.2.0` → `9828edb`，`v1.2.0:VERSION` = `1.2.0`） |

**方法上的收获**：A1 的结论不是读代码猜的，而是**在真执行路径上验** —— `figma-harness.mjs` 加载
**一字未改**的 `code.js`，在桩画布上造一棵 5 节点树，走真实 `execute("get-node", …)`，
把响应落盘后喂给真工具。三组深度得覆盖率 **1/2 · 2/5 · 5/5** ——
而前两组**浅容器数都是 1**，漏掉的却分别是 1 个与 3 个节点。**「1 个容器」是代理指标，
读不出规模**；这就是本轮唯一有实质产出的改动。

> 这套复核已固化为 `tools/layout-audit-readback-probe.mjs`（自包含，不再依赖 `.vibe/`）。
> 定稿时顺手改掉两个**照推**出来的数：depth1 的覆盖率是 **1/2 而非 1/5**（浅回读把子树整段截掉，
> 漏掉的节点不在响应里，报出来的是**下界**）；第 3 种回读的问题条数**随树与档位变**（3 或 5），
> **不是契约**。两处都写进了探针的 `_meta` 与 `lessons.md` #84 —— 又一次「写完没人回头核」。

**收口实测（当场，全绿）**：主闸门 **11** 个（新增 `release-guard` 与 `layout-audit-readback-probe`）—— `check-refs` **751** / 悬空 0 /
歧义 0 ｜ `qa-l2` **47** ｜ `qa-critic` **134** ｜ `qa-export --strict` **935** ｜ `qa-install` **100** ｜
`contrast-audit` OK ｜ `layout-audit --spec`（1 条 medium）｜ `precheck`（错误 0）｜
`release-guard`（6 PASS）｜ **`layout-audit-readback-probe`（4 PASS）**。
变异测试 **10 套 / 527 条断言 / 0 FAIL**（新增 `release-guard` 39）。
> ⚠️ 上面这行是**第三刀收口当时**的数；**第四刀后为 533 条**（`qa-export` 57 → 63），引用数 **751 → 782**，见 §2.6。

**这一刀的教训**：**候选清单里的「可直接做」不能直接采信** —— 它常常写于当时、之后没人回头核
（`lessons.md` **#84**）。本轮四条候选：两条前提不成立、一条落点错，而三条**原描述都是当时的真实观察**。

---

## §2.6 立项第 0 项 · QA3 判据修正（2026-09-17，第四刀）

**这是立项书签字后的第一个动作** —— 顺序以 `1.3-charter.md` §3 为准（`(0) QA3 判据修正 → (1) F4 筛子 →
(2) F3 装置 → (3) A3/A4/A5 → (4) H 组 / D 组`）。本节只记「做了什么、实测怎样」，范围与验收标准在立项书里。

**做了什么**：`qa-export` QA3 的 `live-build` 分支，从「**必须有** `figmaFileKey`」改为
「**拿不到就如实标 `null` + 在 `_meta.note` 交代缺口**；已声明就必须非空」。

**为什么这不是「放宽校验器」**：A4 的教训（`lessons.md` #34 / 「改措辞不放宽校验器」）治的是
「文档措辞不严谨，却去松校验器」；本条病灶相反 —— **判据引用了本拓扑根本得不到的外部凭证**。
`figmaFileKey` 在 REST API 上游里是「按 fileKey+nodeId 回读」的必要条件，而本管线走 Bridge 直连插件
（凭 `nodeId` 单键即可），且插件**无任何 op 返回它**。⇒ 在本拓扑它不是判据，是噪音（两次实跑各撞一条永久 FAIL）。

**裁剪时踩到的坑（值得单独记）**：第一版把分支写成 `if (!fk)`，于是**空串**被归进「如实标 null」——
`figmaFileKey: ""` 只要补一句 note 就过关了；而旧判据的 `!!` **恰好**把空串拦下过。
⇒ 改为 `fk == null`（只认 `null` / 缺字段）+ 补一条「已声明就必须非空」。
**同一个字段有四种「没有」：`null` / 缺字段 / 空串 / 全空白 —— 它们不是一回事，裁剪时必须逐个过一遍**
（`lessons.md` **#85** 判据 ④）。

**验收（三条都落在数字上）**：

| 判据 | 实测 |
|---|---|
| 该放行的放行 | 运行 B `409 PASS / 1 FAIL` → **`410 PASS / 0 FAIL / 0 WARN`**（唯一那条永久 FAIL 消失） |
| **不该动的没动**（证明是「换判据」不是「删判据」） | 自带样例 `--strict` **`935 PASS`** 不变 ｜ 运行 A **`364 PASS`** 不变 ｜ 变异用例断言「两条路径各带**一条**凭证判据、数量相等，且各自断言行可见」 |
| 该拦的仍拦 | 新增三条变异用例：`null` 无 note / note 全空白 / 写成空串 —— 均 `exit 1` |

变异测试 **57 → 63 PASS / 0 FAIL**（+6）。**盲区如实记**：`_meta.note` 非空**不证明画布真的存在**，
只证明执行者交代了缺口 —— 画布是否存在由 `precheck --live` 的实际回读证明（那是另一条时刻的判据）。

---

## §2.7 立项第 1 项 · F4 筛子先行（2026-09-17，⏳ 进行中）

**为什么 F4 必须排在 F3 之前**（立项书 §3 已定）：并置比对要先知道「**比什么**」。没有判据原料就先建装置，
只会把 `unassessed` 从「**没装置**」改成「**有装置但不知道该看什么**」—— 那是把问题从「看不见」升级成
「看起来能看」，比原来更坏（`lessons.md` #82 同族）。

**分两步走，本次完成第 2 步**：

| 步 | 内容 | 产出 |
|---|---|---|
| **1** | 把**筛法**提为随包文档（原先三份试点产物全在 `.vibe/`、不随包发布 ⇒ `lessons.md` **#81** 点名要求补这一步） | `references/design-criteria-intake.md` 建立：四道筛 + `better-ui` 17 条 |
| **2** | `better-layout`（10 条）+ `better-typography`（19 条）逐条分档 | 该文扩为 **3 份来源 / 46 条原则** + **回读契约表** + 第二批八条发现 |

**第 2 步实测**：搬 **3** / 译 **5** / 交人 **1** / **无对象 20（43%）** / **拆 15（33%）** / 已有 **2**。

**两处口径变化（比数字更重要）**：

1. **分类从四档扩到五档 + 「拆」** —— 新增 **已有**（闸门 / 审计器已在做 ⇒ **不重做**，否则重复造轮子
   且留下两套判据口径）；**拆**不是落点，是「**还要再分一次**」。
   **「拆」的占比从 18%（3/17）升到 33%（15/46）** ⇒ **逐条筛不够，逐句筛才是常态**。
2. **S2 的先决条件被显式化**：新增 **回读契约表**（`get-node` 到底回传哪些字段）。
   不查表的后果很具体 —— 三条排版规则（行高 / 字距 / 字号下限）**判据明确、值也给全了**，
   却**全部死在同一个缺字段**上（`lineHeight` / `letterSpacing` / 数值 `fontWeight` 契约里没有）。
   ⇒ 这是 **S1.5「能力前提不成立」的新子类：不是缺 op，而是缺回读字段**。

**本轮新增 D 组需求单 3 条**（`get-node` 回传 `fontWeight` / `lineHeight` / `letterSpacing`）——
**一次加三个字段解锁 4~5 条外部规则**，是本次筛出的**性价比最高的需求**；冻结区**只提需求**
（立项书 §2.3：D 组不允许构建期改）。逐条与依据见 `1.3-candidates.md` 文末。

**另一条比继续筛更有用的动作**：**先列「回读契约里有哪些字段没有任何检查在读」，再按这个清单去找规则**
（`textAutoResize` / `textAlign` / `clipsContent` / `layoutSizing` 全属此类，其中 `textAutoResize`
**一条解锁三条规则**）⇒ F4 的产出密度取决于「**未使用字段清单**」，而不是「来源数量」。
**✅ 已执行（2026-09-18）**：`tools/contract-usage.mjs`（33 字段矩阵）+ 5 字段收编 →
`layout-audit` 3 类新检查（10→13）+ overflow 分级；详见 intake §7 与 `1.3-candidates.md` 文末。

**本轮零工具代码改动**（纯文档轮）⇒ 变异测试与其余闸门数字不变；
`check-refs` 引用条数**仍按 #74 上涨**（增量全部来自新写的引用），**该数不记绝对值、只看「悬空 0」**
—— 口径见 `1.3-status.md` §五。

**进度如实记**：**3/6 已过筛**；余 3 份（`taste-skill` 的 *AI Design Tells* / `review-checklist` /
`review-animations`）未做。**「筛法已定」≠「F4 已完成」**。
（次日补充：第 3 步「未使用字段清单」已执行，但**不消耗来源配额** —— 来源仍 3/6。）

---

## §2.7b 立项第 1 项 · F4 第 4 步：taste-skill 过筛（2026-09-18，✅ 4/6）

**来源**：`Leonxlnx/taste-skill` 主技能文件 §9「AI TELLS」—— 一手抓取 main 最新（87,253 字节，
该文件最新提交 3c7017d），存档 `.vibe/f4-sources/taste-skill.md`（二手榜单勿信，`lessons.md` #80 照旧）。
**50 条 bullet → 41 条原则**（口径与踩坑见 intake §2.4 / `lessons.md` **#89**：统计口径必须写死在表头，
初稿把 41 数成 43、又数成 44，逐行复算才锁定）。

**结果**：**搬 14（34%）/ 译 8 / 交人 9 / 无对象 9 / 拆 1** —— 搬率从前三批的 7% 跃升，
**原因不是来源更好，是判据域换了**：文本内容域（`characters` 可回读），而该字段至今几乎无人消费
（§7.6 顺序纪律第二次应验）。三条主发现：

1. **语言分档升为主杀器** —— 9.G 破折号禁令 vs 中文破折号「——」（U+2014 本尊）＝
   §1.6 隐含假定的旗舰样本；14 条搬里 7 条带语言分档标记，**字符串检查收编前必须先过语言表**；
2. **候选新审计器「文案 tell 审计」**（约 12 条搬共享 `characters` 正则形态）—— 本轮**刻意不实施**：
   先决问题是**判据校准**（中文等价 tell 名单 + 产物类型分档）不是代码，需求已登记；
3. **F4→F3 供料第 2 批**（3 条读图项：假产品截图 / 斜体断行标题 / 眉标下微说明）—— 待并入
   `read-image-checklist.md`。

**产物**：`references/design-criteria-intake.md` §2.4（41 条逐条表）+ §2.5（四来源合并统计 87 条）
+ §六 进度 4/6。**本轮零工具代码改动** ⇒ 变异与其余闸门数字不变。

---

## §2.7c 立项第 1 项 · F4 第 5 步：review-checklist 过筛（2026-09-18，✅ 5/6）

**来源**：`mattbx/shadcn-skills` 的 shadcn-component-review/references/review-checklist.md（8,300 字节，
最新提交 816b5a8），一手存档 `.vibe/f4-sources/`。**来源形态与前四批不同**：不是原则列表，
是**组件评审 checklist**（Quick Audit 9 项 + Detailed 7 类 ≈50 checkbox + 输出格式 + 修复对）。
**口径**：Quick Audit 逐条比对全是 Detailed 的重复展开 ⇒ 0 条；修复对全是正文示例 ⇒ 0 条；
React 工程细节与语义 HTML 族按「并 1 条」归并 ⇒ **20 条原则**（口径写死表头，#89 照办）。

**结果**：**已有 4（20%）/ 译 1 / 交人 3 / 无对象 10（50%）/ 拆 2** —— 两个第一：

1. **「已有」首次成为最大可执行档**：间距档位 / 触控目标 44px / WCAG 对比度 / 密度体系四个判据
   被一个互不知情的第三方评审清单**逐字复刻**（判据佐证第 3 次；`MIN_TOUCH=44` 等已当场回指
   `layout-audit.mjs` 源码复核，非凭印象）；
2. **「画布色 ↔ Spec token」缺口获第二处独立指认**：§3「禁裸色阶、必须语义 token」的可判定内核 =
   画布 hex ∈ DS Spec token 集 —— 与 intake §1.5 警告（F3② 发现 contrast-audit 不看画布）指向
   **同一个洞** ⇒ 该检查优先级上调，需求已登记（校准项：按 Preset 定白名单豁免，防 §3.1 式冲突）。

另：Quick Audit 的「gap 而非子级 margin」判 **无对象** 的依据是实测 377/377 节点 `layoutMode=NONE`
—— **auto-layout 域整体是暗的**（§1.5 已留痕），此处第三次独立确认。

**顺带修正**：第 4 步合并统计的合计行算错（搬曾写 20，逐列复算实为 **17**）—— #89 复算规则
当场又抓到一条。**产物**：intake §2.5 + §2.6（五来源合并 107 条：搬 17 / 译 14 / 交人 13 /
无对象 39 / 拆 18 / 已有 6）+ §六 5/6。**本轮零工具代码改动**。

---

## §2.7d 立项第 1 项 · F4 第 6 步：review-animations 过筛（2026-09-18，✅ 6/6 · **F4 筛线完成**）

**来源**：`emilkowalski/skills` 的 review-animations/SKILL.md（8,407 字节，最新提交 85e8e23），
一手存档 `.vibe/f4-sources/`。charter 预期「S1 通过率低」当场兑现：
**14 条原则 = 交人 5（全部是方法论）/ 无对象 9，实质 0 条可收**。

**两条主发现**：

1. **「方法论与实质要分开筛」成显式动作** —— 本来源实质全灭，但方法层 4 条独立佐证
   （findings 表 / 分层 verdict / 显式 Block/Approve / cite file:line / 反猜），与本仓
   「never approve what you did not inspect」纪律逐条同型（第 4 次）。分档表为此新增
   「交人（方法论佐证）」显式落点 —— **否则整份来源会被「无对象 64%」掩盖掉真正价值**；
2. **F5 启动包定档** —— 本批 + §2.1 #4–#7/#16 + §2.3 #14 已攒出完整 F5（动效维度）启动包：
   值表（原文自带 STANDARDS.md：easing 曲线 / 时长表 / spring 配置）+ 判据来源 + 修复层级
   （第一优先是删掉动效）。F5 将来立项的启动成本从「从零调研」降为「按包组装」。

**产物**：intake §2.6 + §2.7（六来源合并 **121 条**：搬 17 / 译 14 / 交人 18 / 无对象 48 /
拆 18 / 已有 6）+ §六 6/6。**F4 筛线（立项书 §2.2 第 1 项）至此完成**；
候选检查（文案 tell 审计 / 板外色信号）**收编动作另行裁决**（四件齐，#73）。

---

## §2.8 立项第 3 项 · A3：读图结论自动落结构化字段（2026-09-18，✅ 完成）

**修的环**：F3①③ 把读图升为必选项并给了留痕 Schema，但运行 B 的 `visionObservations`
是**人手把当轮素材结构化**的 —— 「自动落」没有装置，且「记录合格与否」没有机械裁判。

**做了什么（两个落点 + 一套变异）**：

1. **`tools/read-image-merge.mjs`（新工具）**：
   - **合并模式**：review 记录 → 自动落 `_evidence.visionObservations`（ok/issue 条目，带 source 留痕）
     与 `_evidence.unassessed`（n-a 条目，带维度可追溯），**绝不覆盖原始报告**（--out 同路径退出 2）；
   - **幂等**：先剔除既有自动条目再追加 + `_evidence.readImageMerge.sha256` 内容指纹 ——
     同输入重跑逐字节一致（#74「写记录推高数字」类漂移被指纹拦截）；落痕后**改记录**必须重落（--check 拦）；
   - **判据 validateReview**：R1–R11 全覆盖（**缺一条 = 没查**）· verdict 枚举 · ok/issue 必填
     evidence · n-a 必填 note（无对象必须给理由）· issue 缺 dimension 记 WARN（Schema 原文「建议填写」）；
   - **IS_MAIN 哨兵**（#88）：纯函数可 import，变异测试直接吃。
2. **`qa-critic.mjs` QA9（`--review` 接线）**：记录完备性 + 跨产物交叉核对 —— 档位一致
   （structured-only 下存在读图记录 = **伪造档位**，拦）· n-a ⊆ unassessed · ok-issue ⊆
   visionObservations · 落痕指纹一致。未提供 `--review` 且报告声称 structured+vision → **WARN**
   （未核对不得折算为通过，#72 同族）；三样例全 structured-only ⇒ **基线 134 PASS 零扰动**。
3. **变异测试 `read-image-merge-mutation.mjs`：37 PASS** —— 判据 12 类（含反向对照）+ 合并幂等 /
   保真 / 更新 + CLI 四态（0/1/2/防篡改）+ QA9 接线正反 + **#48 反向对照**。
   踩坑留痕：`includes("FAIL")` 永真（汇总行「0 FAIL / 0 WARN」自带 FAIL 字样）—— 断言输出要用
   **明细行形态**（`/^  FAIL  /m`），不能用关键词。

**登记**：`acceptance-criteria.md` §5 命令表 + 登记注 · `read-image-checklist.md` §一（分工改指 QA9 + merge）·
`assets/templates/read-image-review.json`（description 同步）· `1.3-status.md` §6.1（A3 移出）·
`1.3-candidates.md` A3 行 · CHANGELOG。

---

## §2.8b 立项第 3 项 · A4：qa-export 的「无清单」诚实降级（2026-09-18，✅ 完成）

**修的环**：清单不存在时旧路径只有两条——闸门静默不跑（「没查」与「查了且干净」不可区分，#72），
或 exit 1 的「清单解析失败」（把**没有产物可查**读成**产物被查出问题**，归因方向是错的）。

**做了什么**：`qa-export --if-absent not-run --absent-reason "<为何没清单>" [--out record.json]`——

1. **第三条路**：产出机器可读 NOT_RUN 记录（Schema `assets/templates/export-not-run.json`）+
   **退出码 3** —— 四态互斥：0 查过绿 / 1 查过红 / 2 用法错 / **3 没查**。「没查」从此有了
   专属输出形态，不许长得像任何一态；
2. **诚实纪律**：`--absent-reason` 必须非空（「为什么没清单」由人交代，空白拦）；记录裁决固定
   `UNVERIFIED —— 未核对，不得折算为通过；补齐清单后必须复跑`；
3. **两种降级形态合流**：「有清单但缺环境凭证」已由 D4 解决（`figmaFileKey=null` + `_meta.note`），
   记录 note 里互指；**无旗标时旧路径保持原样**（反向对照钉住：不悄悄放宽）。

**验证**：变异 63 → **70 PASS**（降级正路 / 理由空白 / 反向对照 / 清单存在时旗标被忽略）；
样例基线 **935 PASS 不变**（不带新旗标的行为逐字节不变）。

**登记**：`1.3-candidates.md` A4 行 · `1.3-status.md` §6.1 · `acceptance-criteria.md` §5 · CHANGELOG。

---

## §2.8c 立项第 4 项 · A5：precheck 吃增量批次计划（2026-09-18，✅ 完成）

**修的环**：运行 A / B 的实际操作都是「逐批小计划」，批次 2 的计划要引用批次 1 的产物
（跨批次 `$name` 与字面 id）。旧入口只吃单个计划 ⇒ 批次 2 独立预检时，跨批次引用被误判
「未知引用」（exit 1 假红），字面 id 被误算「待核对外部引用」（--live 白核对一批必然存在的 id）。

**做了什么**：`precheck.mjs <plan.json> --after <priorPlan.json> …`（可重复、按序）——

1. **前置批次 = 已执行上下文**：先模拟前置批次（复用同一套 `simulate` 判据），其 `as` 名、
   产物 id、`@last` 链播种进主批次 ⇒ 跨批次 `$name` 正常解析，产物字面 id 归入
   **「跨批次引用 N 处」**展示（不算待核对 —— 执行到该批次时它们必然在画布）；
2. **上下文不可信即拒**：前置批次自带结构错误 → exit 2 直接拒（带具体错误行），
   不带病上下文继续预检；前置批次**自身**的外部引用不泄入主批次待核对（它自己那轮 --live 核过）；
3. **顺手修两处**：`--after` 解析提前到主计划 file 探测之前（否则写在主计划前面时前置批次
   会被误当主计划）；`die` 声明提前（TDZ：解析循环先于 `const die` 调用会抛 ReferenceError、
   退出码 1 冒充「查过有错」——退出码四态纪律里这是把 2 冒充 1，必须钉死）。

**验证**：变异 63 → **101 PASS**（跨批次 $name / 字面 id 各带**不喂 --after 的反向对照**、
链式传递、四种拒收、外部引用不泄入、参数顺序鲁棒）；其中一条抓到**真缺陷**：字面 id 循环
`localIds.has(v)` 直接 continue，跨批次 id 被静默跳过 ⇒ crossBatch 分流是死代码、
「跨批次引用 N 处」永远不出现——已修（`priorIds` 集合，登记不静默丢）；真实批次冒烟
（d2-run-a batch1→batch2）通过。全套门禁复跑绿（check-refs 0 悬空 · qa-export 935 ·
qa-critic-mutation 52 · qa-l2-mutation 20 · qa-export-mutation 70 · A3 变异 37）。

**登记**：`1.3-candidates.md` A5 行 · `1.3-status.md` §6.1 · CHANGELOG。

---

## §2.8d H 组分类收紧：六处实测盲区一次裁决（2026-09-18，✅ 完成）

**修的环**：1.2 三轮盲测共识的 6 处「文档没说、只能猜」（`1.3-candidates.md` H1–H6）。
立项书裁决过分类：**H1/H3/H4 收紧为唯一解**（多解必歧义的映射类），**H2/H5/H6 给缺省+区间**
（有合理范围的取值类，一刀切唯一值会压平产品差异）。

**做了什么**（全部落 `references/design-system.md`，纯文档层，不触冻结区）：

| 项 | 新规则 | 内容 |
|---|---|---|
| H1 | **TY-5**（§6） | 字阶串逐档贪心映射：队首两段 `b > a` 即「字号-行高对」，否则单字号 + TY-2 推行高；display→heading→body→caption 按序；number 恒复用 body 档（TY-1），剩余合法对作数字强调档；富余段留痕不静默丢。few-shot 对照：`example-health` 逐档复现，`example-saas` 的 number 档属收紧前自由度（历史产物不改） |
| H3 | **LD-11**（§11.2） | web-admin 断点缺省两档 **1440 / 1024**：≥1440 满档、1024–1439 半档（KPI 2 列 + Sidebar 折叠 64）、<1024 降档（抽屉化/单列/表格横滚）；Brief 显式给则覆盖记 `briefOverrides` |
| H4 | **ST-1 补判据**（§5） | 黄系与橙系并存时取 `chartColors` **数组顺序第一个**命中项（先到先得，可复现），`assumptions` 留痕 |
| H2 | **SP-4**（§7） | `spacing.base`=8(4–12) · pagePadding=24(16–32) · cardPadding=16(12–24) · 区块纵距=24(16–40)；恒 4 的倍数；出区间记 `assumptions` |
| H5 | **AC-3 / AC-4**（§7） | focusRing `2px brand.primary` offset 2（宽 1–3 / offset 0–4）；touchTarget 桌面 ≥30（按 preset）、触屏 ≥44（40–48，gov ≥48） |
| H6 | **TY-6**（§6） | 标题 500（500–600，Hero 可 700 须留痕）、正文/caption 400（禁 <300 与整段 ≥500）、number 500；标题与正文字重差 ≥100 为硬下限 |

**验证**：`qa-l2` 样例（`example-saas` spec）复跑 **18 PASS / 0 FAIL** 不变（历史产物未被新规则追溯改判）；
`check-refs` 复跑悬空 0。新规则对历史 few-shot 的吻合度在 TY-5 里逐字注明（吻合的作为锚、
不吻合的注明属收紧前自由度）——**规则必须能复现它宣称的锚，否则锚是假的**。

**登记**：`1.3-candidates.md` H 组裁决注 · `1.3-status.md` §6.1 · CHANGELOG。

---

## §2.8 立项第 2 项 · F3 锚点并置比对装置（2026-09-18，✅ 完成）

**立项书 §4 判据 1（主题验收）的主交付** —— 消掉运行 B 的 L4 报告里那条「Commercial 商业产品相似度
（40% 权重）未评，理由：需并置比对、链路无装置」。三步（① ③ 已于 F3 第 1 步清掉，本节记 ②）。

**做了什么**：

1. **锚点源核实（S1.5 先行）**：`nexu-io/open-design` 真实存在，`design-systems/` 下 **151 个品牌
   DESIGN.md 设计契约**，主体 Apache-2.0；抽样 linear-app / stripe 两份确认**机器可解析**
   （色板反引号 hex + 字阶层级表 + 字族标记）。**「随包锚点集」改判为「随包协议与装置」**——
   锚点集外部可选、不 vendored（许可证 / 会漂 / 体积），快照留痕可回放。
2. **装置 `tools/anchor-compare.mjs`**：锚点**声明** token ←→ 画布**实际用** token（色板 RGB 欧氏距 /
   字族 / 字阶）。判据 J1–J5 **全是装置纪律**（出处四件套 / 两侧解析非空 / 快照 sha256），
   **没有一条是「像不像」**——相似度结论属读图 R11 + 人（H3）。变异测试 **43 PASS**。
3. **协议 `references/anchor-compare.md`** + **Schema `assets/templates/anchor-compare-report.json`**；
   `qa-critic` QA7 断言不变（structured-only 下观感仍不可判，装置只给机械证据）。

**首轮真跑即抓到**：字族解析不设「第 3 列含 px」签名会把阴影表 / 断点表混进来；`#fff` 三位 hex
要扩展再比；**CLI 顶层副作用杀死模块可测性**（import 即被 `process.exit` 杀掉）⇒ `IS_MAIN` 哨兵
分离纯函数，记 **`lessons.md` #88**（87→88，README ×2 / SKILL 同步）。

**首批实测（运行 B × linear-app）**：13 色仅 1 精确命中（均距 65.5）；字族 `Inter` vs 锚点
`Inter Variable`（按字面比、不猜）；字号 4 档命中 2 档。报告 `.vibe/d2-run-b/anchor-compare-linear.json`。

**闸门（本轮复跑）**：`check-refs` 悬空 0 ｜ 变异 ×12 全绿（+`anchor-compare-mutation` 43）｜
冻结区零改动。**F3 三件全清 ⇒ 立项书 §4 判据 1 可验收。**

---

## §3 建议范围（待定稿）

按「**先收口已知缺陷，再谈新能力**」排，建议顺序（**第 1–4 项已于 §2「债线打包」完成**，保留原样以便对照当初的理由）：

| 序 | 项 | 理由 | 组别 |
|---|---|---|---|
| ~~1~~ ✅ | **B4 · `check-refs` 的 basename 兜底**（**已于 §2 完成**） | 原判「**近半数**引用靠兜底解析、「目录写错、名字对」全漏」—— **实测部分推翻**：真正的规模是 38 条产物类型名被静默指向第一个同名实例。与 `qa-export` QA9 的「前缀合法但路径不存在」确为**同一类病** | B |
| ~~2~~ ✅ | **B3 · 其余校验器补 `unchecked` 留痕**（**已于 §2 完成**） | #72 只在 `layout-audit` 落地了。**其余校验器「没查」仍然看不出** —— 这正是 1.2 两次实测反复踩的坑 | B |
| ~~3~~ ✅ | **C1 · 效果类参数可见性装置收编**（**已于 §2 完成** → `tools/pixel-proof.mjs`） | #56 是运行 A **最值钱的发现**（负载参数缺少能否证它的观察通道），此前只活在 `.vibe/` 运行目录里 —— **下次跑还会重新踩** | C |
| ~~4~~ ✅ | **A2 · 闸门参数从 L2 Spec 派生**（**已于 §2 完成** → `layout-audit --spec`） | 1.3 开工时再次撞见：同一份 readback 用默认档位得 `medium 2`、用存档档位得 `medium 1`。**参数不同源 ⇒ 数字不可比** | A |
| 5 | **A3 / A4 / A5 · 命令接入点对齐**（**A1 已于 §2.5 复核为「需求早已满足」，A6 已于 §2 顺手修**） | 1.2 实测显示漏跑成因是「不知道该跑它」+「参数不同源」，不是命令多。**须在 A2 之后做**才有意义（A2 已就位） | A |
| 6 | **D1 / D2 / D3 · 向冻结区提需求**（**D1 已于 §2.5 改判：落点在冻结区、不是「可直接做」**；且 C1 的像素通道已是补偿。**D4 的断言侧已由 §2.6 做掉**） | 装置缺口，**须走提需求**而非构建期改（QA7 禁区 = `figma-plugin/` + `bridge/`） | D |
| ~~7~~ ✅ | **B5 / B6（`VERSION` ↔ CHANGELOG ↔ git tag 三方守卫）** —— **已于 §2.5 完成**（`tools/release-guard.mjs`）；B5 由「校一致」**重述为校序关系** | 1.2 定版时新发现，两条都**无守卫**且**已各被现实踩到一次**（`VERSION` 停 `1.2.0-dev` 而 CHANGELOG 开到 1.3；`v1.2.0` 纯手工打） | B |

**建议明确不做的**：E 组（语义性缺失）**不是待办** —— 它是判定的能力边界，写脚本查不出来；
真正可做的是把读图评审变成流程必选项。F 组（MCP 外壳 / 图片填充）**属新能力**，须先立评估再动手，
不要混进「修缺陷」的批次里做。

---

## §4 纪律（沿用 1.2）

- 收编一个装置 = **代码 + 前提 + 留痕 + 变异测试**，四件一起搬（`lessons.md` #73）。
- **收编一个装置时，先查它自己有没有「静默降级」**（`lessons.md` #82）—— 一次性脚本的失败模式是「没人读它的输出」，
  成品工具的失败模式是「有人信它的输出」，同一行静默降级会从「看不见」升级为「被当事实复用」。
  故：选项取值不合法一律**退出码 2 + 可读原因**，**不静默回落默认值**；能力边界（不支持的输入形态）
  **显式抛出并写进头部**，不假装支持没实现的路径。变异测试要**用自造夹具反查正确性**，
  不能拿真实产物当基准（那是循环论证）。
- **收紧一条兜底规则前，先量「兜底命中的是什么」，别只量「命中了多少」**（`lessons.md` #83）——
  「命中率高」是假警报，「命中目标是**类型名**」才是真病灶。默认值（如"取第一个同名文件"）必须被看见。
- 新增能力项 → 默认拒绝，记入 `1.3-candidates.md`（`1.2-plan.md` §8 的规则继续有效）。
- 闸门参数必须**来自规格原文**（L2 Spec），不跟着构建脚本顺手写的一份走（`lessons.md` #72②）。
- 每一刀做完：**复跑全套闸门**，并把**真实汇总行 + 退出码**写进记录（同 1.2 的留痕纪律）。
- 改名 / 收编 / 冻结解冻之后，**必须复跑全套变异测试**（`lessons.md` #75）。
