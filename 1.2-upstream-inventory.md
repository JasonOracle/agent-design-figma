# 上游资产清查 — `figma-vibe-bridge` 1.0.0 → 本仓库

> 日期：2026-09-16 ｜ 用途：**1.2 范围的决策输入**，不是交付物
> 来源：`C:\Users\Administrator\figma-vibe-bridge`（开发仓库）+ `release/ai-ui-designer-1.0.0/`（已发布正式包）
> 背景：这是「先盘家底，再规划」的第一次执行。**盘完之前 1.2 的范围不算定稿。**

---

## 1. 一句话结论

**本仓库是上游 1.0.0 发布包的换名版 + 我们的 1.1/1.2 工作，技能包层面零缺失。**

真正没搬过来的，是**上游仓库根的开发侧资产**——QA 体系、阶段设计文档、发布流水线。而这三类恰好是 1.2 要还的三笔债的证据来源。

> ⚠️ **上一轮判断需要修订**：我曾说「提炼时丢了一大批资产」。核实后不准确——**"丢失"的部分本来就不在发布包里**，不是提炼过程丢的。这个区别很重要：说明问题出在**上游的发布口径**，而不是我们的搬运。

---

## 2. 谱系确认（证据）

用文件级比对钉死本仓库与 1.0.0 的关系（比对时归一化 CRLF，排除换行符干扰）：

| 检查项 | 结果 |
|---|---|
| 1.0.0 发布包 51 个文件 → 本仓库 | **1.0.0 有、本仓库没有 = 0 个**（零缺失） |
| 本仓库有、1.0.0 没有 | 7 个 = 5 个真新增 + 2 个 `.vibe/` 运行时产物 |
| `figma-plugin/code.js` 初始提交 ① vs 1.0.0 | 差 **4 行**，全部是品牌改名（`Vibe Bridge` → `agent-design-figma Bridge`） |
| `figma-plugin/manifest.json` | 差 **1 行**：`"name"` 中的品牌名 |
| `assets/templates/design-system-spec.json` | sha256 **完全相同**（`e48cc777054ed1cf…`） |

① 本仓库首个提交 `d5a4070`。

→ **本仓库基线 = 1.0.0**。所有 20 个「内容不同」的文件都可归因于我们在 1.1/1.2 的主动修改。

### 净增的 5 项（1.0.0 没有）

`assets/style-library/web-marketing.json` · `references/bridge-ops.md` · `tools/layout-audit.mjs` · `tools/qa-bridge.mjs` · `tools/qa-plugin.mjs`

### 安全核查

- `.vibe/token` **未被 git 跟踪**，`.gitignore:2` 的 `.vibe/` 覆盖到位 ✅
- 安装副本 `~/.workbuddy/skills/agent-design-figma/` 无 `.vibe/` 残留（干净分发）✅

---

## 3. 真正缺的三类资产

### 3.1 QA 体系 — 5 个脚本，**零第三方依赖**

这是那 12 处「失效引用」指向的真实实现。全部只用标准库（Python：`json/re/sys/pathlib/subprocess/hashlib/zipfile`；Node：`fs/http/path/child_process`）。

| 脚本 | 覆盖层 | 规模 | 断言数 | 可移植性 |
|---|---|---|---|---|
| `stage10-4-qa.py` | **L2** DS Spec（QA1 Schema / QA2 Token 无未知色 / QA3 组件覆盖 / QA4 DS 单源） | 204 行 | **47 / 0** | **高** — 输入是 `assets/examples/*.dsspec.json` + Preset，通用 |
| `stage10-6-qa.py` | **L4** Visual Critic（Schema / 评分 / 证据 / 路由 / loop 自洽） | 144 行 | **90 / 0** | **高** — 输入是 critic-report + Brief + DS Spec，通用 |
| `stage10-7-qa.py` | **L5** Export（Schema / 文件存在 / node id 回读 / 映射完整 / Token 回溯 / 无孤儿 / 冻结零修改） | 237 行 | **652 / 0** | **高** — 输入是 export-manifest + 文件树，通用 |
| `stage10-7-install-qa.py` | 安装体验（README / SETUP / USER_GUIDE / 探针四方契约一致） | 109 行 | **56 / 0** | **高** — 纯文件内容校验 |
| `stage10-5-qa.mjs` | **L3** 构建（QA1 分区 / QA2 DS / QA3 Layout / QA4 Token / QA5 冻结） | 124 行 | 5 类 | **低** — 连真实 Bridge，且**硬编码了项目名与 token 白名单**（`AI Health /`、`Stage 10.5 /`）→ 是**项目级审计**，须重写为通用版才能随包发布 |

**注**：断言数来自上游 `CHANGELOG.md`「质量」节（10.4: 47/0、10.6: 90/0、10.7: 652/0、安装体验: 56/0）。SKILL.md 里那句「90+ 断言」实指 10.6。

### 3.2 阶段设计文档 — `docs/` 27 篇 4136 行

按「该不该搬」逐篇判定：

| 文档 | 行数 | 本仓库对应物 | 处置 |
|---|---|---|---|
| **`stage10-4-design-system-generation.md`** | **281** | **无（L2 是空白层）** | ✅ **搬 + 适配 → `references/design-system.md`（即 A1）** |
| **`lessons-learned.md`** | **54**（30 条） | 无 | ✅ **建议搬** — 全部是实测踩坑验证过的不变量，价值密度最高 |
| `stage7-visual-regression.md` | **553** | 无 | ⚠️ **评估** — 程序化像素 diff 完整方法（阈值/SSIM/网格聚类/11 区域统计）。依赖 `numpy` + `Pillow`；且对象是「Figma ↔ Vue 两稿对比」，与 L4「单稿五维打分」不同 |
| `bridge-architecture.md` | 305 | `references/bridge-ops.md`（285 行） | ⚠️ 对照补漏 — 其「命令 ↔ Plugin API 映射」章节可查我们是否漏了 op 语义 |
| `stage10-1-skill-architecture.md` | 189 | `SKILL.md` L3 段 + `bridge-ops.md §4` | ⚠️ 对照补漏 — §2 执行规则（READ before WRITE / WRITE→READBACK / NO_RESULT≠未执行 / 冻结协议）我们已覆盖要点，但**冻结协议没有** |
| `stage9-design-system.md` | 408 | 无 | 参考 — 真实 L2 产物记录（可作 D2 校准样本） |
| `stage10-backend-plan.md` | 323 | 无 | ❌ 不搬 — 后端 API 设计，超出本 Skill 范围 |
| `stage9-ia.md` | 284 | 无 | 参考 |
| `stage10-2-ai-ui-designer-skill.md` | 129 | `SKILL.md` | ❌ 不搬 — 架构记录，已体现在 SKILL.md |
| `stage8-interaction-regression.md` | 126 | 无 | ❌ 不搬 — 前端交互回归，超范围 |
| `stage9-4-qa.md` / `stage9-2a/2b/2c/3` | ~420 | 无 | 参考 — L5 QA 的现场记录 |
| `stage10-7-e2e-validation.md` | 92 | `USER_GUIDE.md` | ❌ 不搬 — 但**D2 两次实测可照它的走查步骤做** |
| `stage10-7-export-layer.md` | 86 | `references/export-mapping.md`（105 行，更全） | ❌ 不搬 |
| `stage10-5-l3-generation-validation.md` | 82 | 无 | ❌ 不搬 — 验证报告，非规则 |
| `stage10-3-l1-design-intelligence.md` | 80 | `references/design-intelligence.md`（104 行，更全） | ❌ 不搬 |
| `final-install-ready-validation.md` / `STAGE11-release-validation.md` | 77 / 67 | 无 | 参考 — A3 版本治理可参照 |
| `stage10-6-visual-critic.md` | 63 | `references/visual-critic.md`（139 行，更全） | ❌ 不搬 — 其 §6 设计决策记录可作 C2 参考 |
| `communication-log.md` | 94 | 无 | ❌ 不搬 |
| `stage3/4/5-figma2code.md`、`stage3-ia.md` | ~355 | 无 | ❌ 不搬 — 历史阶段产物 |

**关键**：本仓库的 `references/*.md` 是**产品化规则手册**（比上游对应文档更全），上游 `docs/` 是**开发期实现报告**。两者不是同一体裁，多数不该直接搬——**只有 L2 那一篇是「规则手册缺失、上游有现成设计」**。

### 3.3 发布流水线与其他

| 资产 | 规模 | 处置 |
|---|---|---|
| `tools/stage11-release-package.py` | 10 KB | ⚠️ **评估** — 完整性打包 + 敏感扫描 + 文档引用校验 + RELEASE-MANIFEST(sha256)。**「文档引用校验」正是能拦住 12 处失效引用的那一步** |
| `tools/stage11-release-check.py` | 89 行 | ⚠️ 评估 — 发布前硬性项扫描（个人路径/ token / 测试残留 / 历史项目名） |
| `tools/stage11-cleanroom-run.mjs` | 3.7 KB | ⚠️ 评估 — 清洁环境首跑模拟（对应 D2 校准） |
| `skills/ai-ui-designer/release-checklist.md` | — | ✅ 建议搬 — 被发布包**明确排除**的开发者 QA 文档，含发布前硬性项清单 |
| `skills/figma-agent-skill/` | 307 行 | ⚠️ **对照** — 上游**第二个技能**；其 `references/bridge-api.md`（95 行）是我们 `bridge-ops.md` 的上游对应物，同为 op 手册。**它自己也有失效引用**：通篇引用的 `failure-playbook.md` 不存在 |
| `release/ai-ui-designer-1.0.0/` | 51 文件 | ✅ 机制可搬 — RELEASE-MANIFEST 的 sha256 清单机制（A3 版本治理的加强项） |
| `tools/selftest.js` + `mock-plugin.js` | 27 KB + 8 KB | ⚠️ **对照** — Bridge 自测（上游 25/25）vs 我们的 `qa-bridge.mjs`（13 项）。可查我们漏了哪些反向用例 |
| `stages/`（9 个阶段）+ `stage6-element-admin/` | 大量 | ❌ 不搬 — 历史项目产物，但 stage7 的视觉回归数据是 C 项的方法参照 |

---

## 4. 「12 处失效引用」的真正根因（重新定性）

上一轮我定性为「上游有实现、我们没搬」。**不准确**。真实根因是**上游自身的口径矛盾**：

> 上游 `docs/stage10-4-design-system-generation.md` §9 明确写：
> 「四项验证全绿才算完成（脚本：`tools/stage10-4-qa.py`，**随仓库入库，可被第三方 AI 独立复核**）」
>
> 而 `tools/stage11-release-package.py` 的排除项写着：
> 「排除：`release-checklist.md`（开发者内部 QA 文档）、任何 `.vibe/` 测试产物 / 临时脚本」
> `RELEASE-MANIFEST.json` 的 `exclusions` 进一步列明：「`release-checklist.md`、测试产物、开发日志、临时脚本」

**QA 脚本被设计为「随包发布、供第三方复核」，打包时却把它们连同 `tools/` 全部排除。** 产品文档里的引用因此永远指向包外。

更直接的证据：上游自己的 `release-checklist.md` 勾选了
> `[x] docs 可以独立阅读 — ……SKILL.md / references 中的仓内 QA 脚本引用已改为通用描述`

**但没改干净**——1.0.0 的 `SKILL.md` 第 87 / 98 / 110 行仍留着「Python QA 脚本（90+ 断言）」「L3 QA1–QA5」「Python QA 脚本（QA1-QA7）」。**人工勾选项靠人查，漏了。**

### 这条发现对 1.2 的价值

它把 1.2 的核心命题从「还债」升级成一个**可直接验证的产品缺陷**：

> Skill 的第一价值观是「未跑的层不得假装跑过」，但 **L5 Export Gate 是空的**——验收脚本不在包里，用户的 Agent 走到出口只能照着清单假装核对。**上游有 845 条断言（10.4: 47 + 10.6: 90 + 10.7: 652 + 安装体验: 56，另有 L3 五类），用户一条都拿不到。**

这也说明：**1.2 必须带自动化校验**。不是「想做好」，是「不做就是自欺」。

---

## 5. 逐项处置清单（决策表）

| # | 资产 | 处置 | 支撑哪个 1.2 项 | 成本 |
|---|---|---|---|---|
| 1 | `stage10-4-design-system-generation.md` → `references/design-system.md` | **搬 + 适配** | **A1** | 低（翻译整理，非创作） |
| 2 | `lessons-learned.md` 30 条 | **搬**（作 `references/lessons.md` 或 SKILL 附录） | 全局（防复踩） | 极低 |
| 3 | `stage10-4-qa.py` → `tools/qa-l2.mjs` | **Node 重写** | **C（L4 证据）/ D1** | 中（204 行纯逻辑直译） |
| 4 | `stage10-6-qa.py` → `tools/qa-critic.mjs` | **Node 重写** | **C** | 中（144 行） |
| 5 | `stage10-7-qa.py` → `tools/qa-export.mjs` | **Node 重写** | **D1（零目测评分）** | 中（237 行） |
| 6 | `stage10-7-install-qa.py` → `tools/qa-install.mjs` | **Node 重写** | **D 验收** | 低（109 行，纯文本校验） |
| 7 | `stage10-5-qa.mjs` | **不搬，重写为通用版**（上游硬编码项目名，不可随包） | C2（L4 要逐节点实测） | 中 |
| 8 | `references/design-system.md` 的 QA 依据 | 与 #3 同源，文档 §9 直接引用 | A1 | — |
| 9 | `release-checklist.md` | **搬**并改造为本仓库的发布前检查 | A3 | 低 |
| 10 | `stage11-release-check.py` 的**引用校验**逻辑 | **搬其思路**（Node 化） | **A2 的守卫**（防失效引用复发） | 低 |
| 11 | RELEASE-MANIFEST sha256 机制 | **评估** | A3 加强 | 低 |
| 12 | `bridge-architecture.md` op 映射章节 + `figma-agent-skill/references/bridge-api.md` | **对照补漏**。附带发现：该 op 手册自称 **35 个 handler**，实际 `code.js` 是 **36 个**（`tools/qa-plugin.mjs` 断言 `36`）→ **上游自己的 op 手册也是过时的**，可反证「无权威 op 文档」是长期系统问题，不只出在我们这一版 | 无需新增 | 低 |
| 13 | `stage10-1-skill-architecture.md` §2.5 冻结协议 | **补进 SKILL.md**（我们只有 READ/WRITE 纪律） | 全局 | 低 |
| 14 | `selftest.js` + `mock-plugin.js` | **对照补漏**我们的 `qa-bridge.mjs` 反向用例 | 全局 | 低 |
| 15 | `stage7-visual-regression` 方法 | **评估，暂不搬**（依赖 numpy/Pillow，且对象不同） | C 的增益档 | 高 |
| 16 | `stages/`、`stage6-element-admin/`、`stage9-*`、`stage10-backend-plan` | **不搬** | — | — |

**合计**：#1–#2、#9–#13 是低成本高收益（半天内可完成量级）；#3–#7 是本体工作量；#15 单独评估。

---

## 6. 对 1.2 范围的影响（相对 `1.2-plan.md` 定稿的变化）

### 6.1 项的性质变了

| 项 | 定稿说法 | 盘完后 |
|---|---|---|
| **A1 L2 契约文档** | 「从零写，是红线下的次优解」 | **改为「搬运 + 适配上游 281 行成熟设计」**。上游文档质量高：五段线性流水线、三条不变式、五类 token、**source 五种前缀规范**、派生规则白名单 RD-1/2/3 + ST-1、规则编号（TY-1..4 / SP-2,3 / CD-1..4 / LD-2,3,5 / GEO-1）、State 矩阵、三平台 Layout 模板、QA 四项判据。**工作量下降、质量上升**——但「次优解」的定性不变（上游同样只有设计、没有实现，见其 §10.4 自述「规则引擎的实现未编码」） |

### 6.2 新增应纳入的项

| 新增 | 理由 |
|---|---|
| **C3 QA 体系 Node 化（4 个脚本）** | 原定稿把「L5 Export Gate 从空规则变真闸门」当作副产品，现在有了确定的母本（845 条断言的设计），应作为**独立交付项**，它是 D1 唯一可执行的支撑 |
| **A4 引用校验器** | 12 处失效引用的机制性根因是「人工勾选靠人查」。加一个自动校验（扫全仓库文件引用），才是根治 |
| **B0 lessons.md** | 30 条实测不变量，成本极低、防复踩价值高 |

### 6.3 应下调优先级的项

| 项 | 变化 |
|---|---|
| `stage7-visual-regression` 移植 | 依赖 numpy/Pillow（破零依赖红线）+ 对象不同（两稿对比 ≠ 单稿打分）。**明确留在「增益档之外」，1.2 不做** |
| 图片填充 | 判断不变（等验收数据），但**盘完更清楚该等**：上游 8 个阶段从未做过图片填充（已核 `plugin/code.js`：`IMAGE` / `setImage` / `imageHash` **零命中**），说明它不在主线能力上 |

### 6.4 被证否/澄清的

- ~~「提炼过程丢失资产」~~ → 未发生。**本仓库零缺失**。
- 「L2 是唯一空白层」→ **仍然成立**，且上游有现成设计可搬。
- 上游 L2 §10.4 自承「`web-site` 平台模板与 responsive 细则未覆盖」→ **我们 1.1 已补 `web-marketing` Preset，领先上游**。

---

## 7. 拍板结果（2026-09-16 已定）

| # | 决策点 | 结论 | 落点 |
|---|---|---|---|
| 1 | 移植语言 | **Node 重写**，不引入 Python | `1.2-plan.md` §3.2.1 |
| 2 | QA 体系是否纳入 1.2 | **纳入**，作为独立交付项 C3（4 个脚本） | `1.2-plan.md` §4.C3 |
| 3 | 移植范围 | **分层**：必做 = A1 + **C3 本体** + A4；顺手 = A5 / #9 / #13；缓做 = #11 / #12 / #14；不做 = #15 | `1.2-plan.md` §3.2.3 |

**关键修正**：第 3 条原问法（「#1/#2/#9–#13 全做」）漏了 **#3–#7（QA 本体）**——那是第 2 条决策的载体，不做则第 2 条落空。故范围改为按「必做 / 顺手 / 缓做 / 不做」四层划分，而非按成本一刀切。

**编号对应调整**：本文件原称的 `B0 lessons.md` 在定稿中并入 A 段，改称 **A5**（本质是「隐式知识显式化」，属补契约，不属拦截）。

---

## 附：本文件未做的事

- 未逐一阅读 `docs/` 全部 4136 行（只读了 L2 全文 + release-checklist + lessons-learned + 各文档结构）
- 未运行上游任何脚本（避免动上游仓库状态）
- 未改动本仓库任何实现代码；`1.2-plan.md` 已于 §7 拍板后同步更新（第 3.2.3 节等）
