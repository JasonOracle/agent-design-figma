# 1.5 候选清单

> **来源**：v1.4.0「兑现版」定版（2026-09-18）后归拢。承接 1.4 裁决项 ① 留下的语言相关子集，以及 1.4 章程 §2.3 明确排除但标记「留 1.5」的条目。
>
> **性质**：这是**输入**，不是计划。⚠️ **1.5 尚未立项** —— 是否纳入、按什么顺序、怎样算做完，须先写 `1.5-charter.md`（立项书）并由决策者签字。
>
> **分组**：A 语言相关 tell / B 响应式维度 / C 动效维度 / D 生成侧 / E 冻结区 / F 缓做债 / G 遗留口径。
>
> **本文件属开发者内部文档**（`tools/check-refs.mjs` 的 `DEV_ONLY` 模式自动豁免）。

---

## 零、1.4 裁决项回顾

1.4 章程 §5 裁决项 ① 已定：
- **语言无关子集** → 本版必做 ✅（`fake-perfect-numbers` 已落地）
- **语言相关子集** → 保守初版，**不进闸门**，1.5 或决策者醒来后确认

1.4 章程 §2.3 排除但标记「留 1.5」：
- **F6 响应式落画布**：规则已定（H3/LD-11: 1440/1024），画布只建单帧 ⇒ **记候选，留 1.5**
- **F5 动效维度**：表达层缺口（op 面无原型操作）⇒ **不单独立项，另评估**

---

## A. 语言相关 tell 子集（1.4 裁决项 ① 的延续）

> **共同前提**：taste-skill 的 tell 假定产物是**营销落地页**，而运行 A/B 是**仪表盘**。
> 语言相关 tell 的卡点不在代码，在**判据校准**：
> - ① 中文等价 tell 名单（如「张三/李四/示例」「第一步/步骤 1」「某某科技」）
> - ② 产物类型分档（哪些 tell 对营销类生效、对仪表盘豁免）

| # | 条目 | 证据与现状 | 落点 | 代价 |
|---|---|---|---|---|
| **A1** | **中文占位符 tell**（「张三/李四/示例用户/某某公司」） | taste-skill #11 "No placeholder names"；英文等价 "Lorem ipsum" 已在语言无关子集处理 | `layout-audit` 扩展检查类 | ⚠️ **先决条件**：名单由决策者核准。样本太少（<5 条）= 假阳性风险；样本太多 = 维护成本 |
| **A2** | **顺序词 tell**（「第一步/步骤 1/首先/其次/最后」） | taste-skill #15 "No 'Step 1, Step 2' labels"；仪表盘场景**合法**（流程图需要步骤标签） | `layout-audit` 扩展检查类 | ⚠️ **产物分档**：仪表盘豁免，营销类必查 |
| **A3** | **品牌名占位**（「某某科技/XX 公司/品牌名」） | taste-skill #14 "No placeholder brand names"；仪表盘场景**合法**（内部工具不需要品牌） | `layout-audit` 扩展检查类 | ⚠️ **产物分档**：同上 |
| **A4** | **动词直接做标签**（taste-skill #32） | "Use verbs as labels" —— 如「查看详情」而非「详情按钮」 | `layout-audit` 扩展检查类 | 中等：需语义理解（NLP），非纯字符串匹配 |

### A 组准入条件

| 条件 | 说明 | 当前状态 |
|---|---|---|
| **中文 tell 名单核准** | 决策者给「最小可运行列表」≥5 条 | ❌ 未核准 |
| **产物类型分档** | 营销类 / 仪表盘 / 其他 三档定义 | ❌ 未定义 |
| **保守初版不进闸门** | 只在报告里留痕，不阻塞流水线 | ✅ 已由 1.4 裁决项 ① 确定 |

---

## B. 响应式落画布（F6，1.4 标记「留 1.5」）

> **现状**：DS Spec 有 `responsive.baseline / breakpoints / rules`，但 L3 **只建一个 `frameBaseline`**（大屏恒 1920×1080），**不建多断点**。
> H3 / LD-11 已于 2026-09-18 把 web-admin 断点收紧为确定值（1440 / 1024）。

| # | 条目 | 证据与现状 | 落点 | 代价 |
|---|---|---|---|---|
| **B1** | **L3 多断点画布生成** | 规则已定（baseline=1920×1080, tablet=1440×1024, mobile=375×812）；缺口 = 「L3 只建单帧」 | `templates/design-brief.json` + L3 `bridge/server.js` 扩展 | **中等**：改 L3 产出形状（单帧 → 多帧）⇒ 与历史产物不可比 |
| **B2** | **多帧回读比对** | 需回读 ≥2 帧的节点一致性（组件跨断点不变） | `precheck.mjs` 扩展 + `qa-export` 新闸门 | 中等：需验证 L3 实际能输出多帧 |
| **B3** | **帧间一致性闸门** | 新闸门：同组件在不同断点的节点 ID/name/层级应一致 | `qa-export.mjs` 新增 QA 项 | 低：机械比对 |

### B 组风险评估

| 风险 | 等级 | 缓解 |
|---|---|---|
| **历史产物不可比** | ⚠️ 高 | CHANGELOG + `_meta` 留痕说明 |
| **L3 改动面大** | ⚠️ 中 | 先做探针验证 L3 是否支持多帧 |
| **闸门数字变动** | ✅ 可控 | `qa-l2` / `qa-critic` 允许数字变动，只要不变红 |

---

## C. 动效维度（F5，1.4 标记「不单独立项」）

> **现状**：`visual-critic.md` 五维（Layout / Color / Consistency / Commercial / Usability）**没有动效维度**。
> intake 已攒出完整启动包（值表：STANDARD.md 的 easing / 时长 / spring；判据来源）。

| # | 条目 | 证据与现状 | 落点 | 代价 |
|---|---|---|---|---|
| **C1** | **动效维度判据定义** | intake §3.2 已有值表；缺口 = 「画布表达能力」 | `visual-critic.md` 扩展 | **低**：纯文档 |
| **C2** | **op 面动效操作** | `bridge-ops.md` 的 op 集**没有任何原型 / 动效操作** | `figma-plugin/code.js` + `bridge/server.js` | **高**：冻结区改动，需单独立项 |
| **C3** | **动效检查类** | 依赖 C2 落地后才能写 | `layout-audit` 扩展 | **阻塞于 C2** |

### C 组结论

- **C1（判据定义）可做**，但「做了没人用」（op 面无动效操作）
- **C2（op 面）必须单独立项**，成本比 B 组高一个量级
- **建议**：C1 并入 1.5（纯文档，成本低），C2/C3 留 1.6 或单独评估

---

## D. 生成侧（Preset 配方升级）

> **现状**：`assets/style-library/*.json`（4 个 Preset）自 1.1 起一字未动。
> intake §1.4 列出约 7 条配方（better-ui #8 / #6, better-layout #1, taste-skill #6/#32, better-typography #5）。

| # | 条目 | 证据与现状 | 落点 | 代价 |
|---|---|---|---|---|
| **D1** | **Preset 配方升级** | 7 条配方待落地；改 Preset = 改生成规则 | `assets/style-library/*.json` | **高**：牵连 `qa-l2`（47 断言）与历史产物可比性 |
| **D2** | **L1 设计智能文档更新** | `references/design-intelligence.md` 自 1.1 未动 | 同上 | 低 |

### D 组结论

- **不纳入 1.5**：属「怎么做得更好」而非「怎么查得更准」
- **另立评估**：需单独立项，且与 1.5 主题（补维）不同源

---

## E. 冻结区解冻（D 组需求单）

> **状态**：§4 结案记录显示 **FR-1~FR-6 全部已完成**（2026-09-18）。
> 契约字段 33 → 38（`imageFills` / `lineHeight` / `letterSpacing` / `fontWeight` / `fillOpacities`）。

| # | 条目 | 状态 |
|---|---|---|
| **E1** | FR-1 `ping` 回传 `figma.fileKey` | ✅ 已做 |
| **E2** | FR-2 `get-node` 回传数值 `fontWeight` | ✅ 已做 |
| **E3** | FR-3 `get-node` 回传 `lineHeight` | ✅ 已做 |
| **E4** | FR-4 `get-node` 回传 `letterSpacing` | ✅ 已做 |
| **E5** | FR-5 `paintToHex` 回传 `fillOpacities` | ✅ 已做 |
| **E6** | FR-6 未知 id 快速失败 `NODE_NOT_FOUND` | ✅ 复核结案（需求已在既有代码中满足） |

### E 组结论

- **全部结案，无待办**
- 但 **FR-2/3/4 解锁的字段**（fontWeight / lineHeight / letterSpacing）尚未有检查类消费
- ⇒ 建议作为 **1.6 候选**（" typography 三条业务判据"）

---

## F. 缓做债项（1.2 划出 → 1.3 → 1.4 仍未纳入）

| # | 项 | 出处 | 备注 |
|---|---|---|---|
| **F1** | `#11` sha256 清单机制 | `1.2-plan.md` §3.2.3 | 「有增益但非阻塞」 |
| **F2** | `#12` op 映射对照 | 同上 | 同上 |
| **F3** | `#14` selftest 反向用例 | 同上 | 同上 |

### F 组结论

- **成本极低**（纯文档/工具），但**非阻塞**
- ⇒ 建议 **G 组顺手清**，不单独立项

---

## G. 遗留口径清理

| # | 条目 | 实测（2026-09-18） |
|---|---|---|
| **G1** | **`imageFills` 口径澄清**（非 bug，§7.2 已说明） | `references/design-criteria-intake.md` §7.2 已澄清：`contract-usage` 扫描**显式排除 `*-mutation.mjs`**（变异测试是造数据、不算生产消费），故工具报「零引用」是生产侧正确结果；§7.2「非零引用」指变异侧，两者口径不同但不矛盾。1.5 只需顺手核对 §7.2 表述无误 | **极低**：确认文字即可 |
| **G2** | 已完成 D 组需求单是否仍有「待批准」措辞 | `references/frozen-zone-requests.md` §4 已回填，但 `1.3-candidates.md` D 组表头仍保留「原始记录」；须核对是否有读者会误读成「未决」 |

### G 组结论

- **G1/G2 顺手可清**，成本 < 1 小时
- ⇒ **纳入 1.5 范围**（作为「收尾动作」）

---

## 八、候选优先级矩阵

| 候选 | 价值 | 成本 | 阻塞 | 建议优先级 |
|---|---|---|---|---|
| **B1+B2+B3（F6 响应式）** | 高（补维度） | **高**（三项零基建设：template/L3/qa-export） | 无 | **P1（必做）** |
| **A1+A2+A3（语言相关 tell）** | 中（补判据） | 低 | ⚠️ 名单核准 | **P2（条件必做）** |
| **C1（动效判据定义）** | 低（无人用） | 低 | 无 | **P3（可选）** |
| **G1+G2（口径清理）** | 低（纯文档） | 极低 | 无 | **P4（顺手）** |
| **F1+F2+F3（缓做债）** | 低 | 极低 | 无 | **P5（可选）** |
| **D1+D2（Preset 配方）** | 高（生成侧） | 高 | 无 | **❌ 不纳入 1.5** |
| **C2+C3（动效 op 面）** | 高（表达层） | 很高 | 冻结区 | **❌ 单独立项** |

---

## 九、1.5 推荐范围（待签字）

### 推荐档位：**B（推荐）**

| 序 | 项 | 类型 | 验收条件 |
|---|---|---|---|
| **1** | F6 响应式落画布（B1+B2+B3） | 新维度（三项零基建设） | 多帧生成 + 帧间一致性闸门 |
| **2** | 语言相关 tell 保守初版（A1+A2+A3） | 收编 | 名单核准后落地，不进闸门 |
| **3** | 口径清理（G1 确认 + G2 核对） | 文档 | 口径一致，无悬空 |
| **4** | 动效判据定义（C1） | 文档 | `visual-critic.md` 新增动效维度段 |

#### 明确排除

| 条目 | 去向 |
|---|---|
| A4（动词直接做标签） | 1.6 候选（需 NLP） |
| FR-2/3/4 解锁字段（fontWeight / lineHeight / letterSpacing） | 1.6 typography 判据候选 |
| F1+F2+F3（缓做债） | 后续独立清（非阻塞） |

### 否决条件

- 中文 tell 名单**无法核准** ⇒ A 档降级为「语言无关子集扩展」
- 多断点画布**破坏历史可比性且无留痕方案** ⇒ F6 降级为「预留接口」
