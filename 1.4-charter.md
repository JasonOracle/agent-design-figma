# 1.4 立项书

> **用途**：定义 1.4 的 **范围 / 优先级 / 验收标准**，并由决策者签字。
> **候选输入**见 `1.4-candidates.md`（七组 + 四条互斥主线，逐条带证据强度与代价）。
>
> **状态**：⏳ **待签字** —— 决策者已选定主线 **A 兑现版** 与验收形状 **可数优先**（2026-09-18），
> 尚待确认 §5 裁决项 ①。签字后**范围外请求一律默认拒绝并记候选**。
> **时点**：2026-09-18（v1.3.0 定版当日；**先立项、后开工**）。
> **本文件属开发者内部文档**（`tools/check-refs.mjs` 的 `DEV_ONLY` 模式 `^\d+\.\d+-[^/]*\.md$` 自动豁免）。

---

## §0 这一次与 1.3 不同在哪

**1.3 是「做完三刀才补立项」**：三刀按「不需要先做范围决策就能开工」挑出来，后果不是做错事，
而是**每刀单独成立、合起来拼不成一个可验收的范围**，且没人能回答「1.3 到底要干什么」。
更贵的是第三刀实测出「两条半站不住」（`lessons.md` **#84**）。

**1.4 反过来**：本文件落盘**在任何新条目动手之前**，且支撑它的三条事实都是**定版当天的回指复核实测**所得
（`1.4-candidates.md` §A/§B/§C），不是从旧文档照抄：

| 事实 | 实测（2026-09-18） | 它对范围的作用 |
|---|---|---|
| **1.3 的投资未兑现** | F4 筛线 6/6 完成、**121 条原则**过筛（搬 17 / 译 14），但**收编 0 条**（除第 3 步的 5 个字段） | 这就是本版的正题 —— **原料与裁判都在，缺的是兑现** |
| **A2 只完成一半** | `paramSource` 只有 4 项；Spec 的 `accessibility` 是 required 一节（touchTarget / minFontSize / focusRing），`layout-audit.mjs` **grep 不到 `accessibility`**，`--min-touch` 硬编码 44 | 它是**前置项**：#73 纪律要求「参数同源必须在加新检查类之前完成」 |
| **一条判据的数据源是现成的** | Spec 的 `accessibility.minFontSize`（运行 B = `{value:14, source:"rule:TY-3…"}`）**正是** intake §2.3 #16「字号下限」要的那个同源下限 | 说明「兑现」往往比「新建」便宜 —— 先找现成数据源，再谈新字段 |

> ⚠️ **本条纪律沿用 1.3**：本文件落盘之前，不抢跑任何新条目 —— 哪怕是标注「便宜」的 A5。
> 「能开工」不等于「该开工」（`lessons.md` **#9**）。

---

## §1 本版主题（**待签字**）

> **1.2 是「可信版」** —— 让每一步留下可引的痕迹。
> **1.3 是「裁判版」** —— 让每一环都有可证伪的裁判。
> **1.4 是「兑现版」** —— 让裁判**真的看得见东西**：把已过筛的判据兑现成跑得起来、带反向对照的检查。

由此得出一条判据，用来回答「这条该不该做」：

> **它是否把一条「已过筛的判据」变成「跑得起来 + 带反向对照的检查」？**

| 候选组 | 过得了 §1 判据吗 |
|---|---|
| **A 判据收编**（文案 tell / 板外色 / 排版三条 / 字号下限） | ✅ **正题** —— 逐条都是「已筛、判据齐、只差装进闸门」 |
| **B 参数同源余量** | ⚠️ 间接，但**必要** —— 它是 A 的**前置**（#73），不是并列项 |
| **C 生成侧**（配方 / Preset） | ❌ 不属主题 —— 配方是「该怎么做」，不是「你错了」，**装不进闸门** |
| **D 新维度**（F6 / F5） | ❌ 不属主题 —— 它们连**已筛的判据**都还没有（F5 更缺表达能力） |
| **E / F 零引用字段与缓做债** | ❌ 不属主题 —— 按 #73 顺序纪律「先有判据来源再写检查」，它们**恰恰是判据来源还没到位的那批** |

---

## §2 范围

### 2.0 档位（**已选定**）

| 档 | 内容 | 代价 | 裁决 |
|---|---|---|---|
| **A · 兑现线** | 只做「已过筛 + 依赖已就位」的判据收编（§2.2 第 1–5 项 + 前置第 0 项） | 真成本在**判据校准**（需要产品判断），不在代码 | ✅ **已选定（2026-09-18）** |
| B · 兑现 + 语言相关子集 | A 档 + 文案 tell 里**依赖中文名单**的那半边 | 需要决策者逐词核准名单 | ⏳ 由 §5 裁决项 ① 决定是否并入 |
| C · 兑现 + 生成侧 + 维度 | 再加 C 组与 D 组 | 范围膨胀，且三条都**不过 §1 判据** | ❌ 未选 —— 建议另立评估 |

### 2.1 已完成（不在此重复）

**1.3 全量（B 档「收口 + 空环」，判据 1–7 全部对账）** 见 `1.3-charter.md` 与 `CHANGELOG.md` 的 `[1.3.0]` 段。
本文件只记「从哪继续」：**1.3 造好了裁判、备好了原料，收编动作一条未做。**

### 2.2 本版范围

| 序 | 项 | 类型 | 落点 | 依赖 / 约束 |
|---|---|---|---|---|
| **0** | **参数同源余量**：把 Spec 的 `accessibility` 一节接上（`touchTarget` / `minFontSize` / `focusRing`），`--min-touch` 与 `--font-family-max` 纳入 `CLI > spec > default` 三档并进 `paramSource` | **前置**（#73） | `tools/layout-audit.mjs` | ⚠️ **必须区分「不适用」与「阈值为 0」** —— 运行 B 的 `touchTarget.value = 0` 意思是「本产物无触控要求」，该检查应进 `unchecked` 而**不是**静默通过（#12 判据 ②：`null`/缺字段/空串/全空白不是一回事） |
| **1** | **字号下限 `font-size-floor`** | 收编 | `layout-audit` 新检查类 | **数据源现成**（Spec `accessibility.minFontSize`，运行 B = 14）⇒ 做完第 0 项即可做。判据来自 intake §2.3 #16 |
| **2** | **「画布色 ↔ Spec token」板外色信号** | 收编 | `contrast-audit` 或 `layout-audit` 新信号 | **两处独立指认**（intake §1.5 + §2.5 #4）。⚠️ **报候选计数交人，不得直接判违规** —— 「连续公式 vs 离散白名单」哲学冲突（§3.1）未裁决，且须按 4 个 Preset 定豁免白名单 |
| **3** | **排版三条业务判据**（字重下限 / 行高按角色 / 字距） | 收编 | `layout-audit` 新检查类 | 字段已就位（FR-2/3/4 真机验证）。⚠️ **字距那条原文未给值**（"a little" / "often look better"）⇒ 只到「**译**」档（可疑信号），**不得冒充判定** |
| **4** | **文案 tell 审计器** | 收编 | 新审计器（或 `layout-audit` 新检查类） | ⚠️ **本版最重的一条，且卡点不在代码**：先决问题是**判据校准** —— ① 中文等价 tell 名单 ② 产物类型分档（taste-skill 的 tell 假定**营销落地页**，而运行 A/B 是**仪表盘**：hero/scroll 域结构性缺席、等宽 KPI 卡在仪表盘上**合法**）。⇒ 见 §5 裁决项 ① |
| **5** | **F4 → F3 供料第 2 批**（3 条读图项：假产品截图 / `<br>` 斜体断行标题 / 眉标下微说明） | 文档 | `references/read-image-checklist.md` | 并入时**按 R 系列续号 + 全线数字联动**（#74）。**最小的一条**，可穿插 |
| **6** | **G 组遗留口径**（intake §7.2 与 `contract-usage` 输出对 `imageFills` 说了两套话；D 组需求单是否仍有「待批准」措辞会误导读者） | 文档 | `design-criteria-intake.md` / `1.3-candidates.md` | **建议无条件纳入**（半小时量级，纯口径清理） |

### 2.3 建议明确排除（连同理由）

| 项 | 排除理由 |
|---|---|
| **C 生成侧**（配方写进 Preset / L1 规则） | 配方是「该怎么做」，**装不进闸门**（intake §1.4 分流的原意）。且改 Preset 会牵动 `qa-l2` 契约与历史产物可比性 ⇒ 建议**另立评估**（`1.2-plan.md` §8 的规则继续有效） |
| **F5 动效维度** | 最贵的一条，且理由不在「没值表」（值表已定档）—— intake §3.2 实测 **op 集里没有任何原型 / 动效操作** ⇒ 「表达能力缺口 × 维度缺口」**两层叠加**。**先动 op 面才谈得上立项** |
| **F6 响应式落画布** | 有真实缺口（H3 已定 1440/1024 断点、画布只建一个 baseline），但**不过 §1 判据**（不是「已筛判据的兑现」）。⇒ **记候选，留 1.5** |
| **同心圆角** | 形态前提不成立（真实产物上**可检查对象 = 0**）⇒ 除非先裁决「连续公式 vs 离散白名单」的哲学冲突 |
| **`layoutSizing` 判据** | 第三态（376/376 恒 `FIXED`）⇒ **恒真假绿** |
| **E 组零引用字段判据** | `strokeWeight` / `vectorPathCount` 判据可写，但**判据来源还没到位**（外部规则那条被判无对象）⇒ 按 #73 顺序纪律**本版不做** |
| **F 组缓做债（sha256 清单 / op 映射对照 / selftest 反向用例）** | 「有增益但非阻塞」，且**不过 §1 判据** ⇒ 继续留 |
| **继续扩外部来源** | intake §4.6 / §七 已证「产出密度取决于**未使用字段清单**，不是来源数量」，且 6/6 来源已筛完 |

---

## §3 优先级与顺序

```
1.3 全量（已定版 v1.3.0，判据 1–7 对账）
   │
   ├─(0) 参数同源余量 ──▶ 必须在(1)之前：否则新检查的下限参数又是手敲的（#73 根因重现）
   │
   ├─(1) font-size-floor ─▶ 最便宜的一条，直接吃(0)的产出 —— 用它先把「收编四件齐」的流程走通一遍
   │
   ├─(2) 板外色信号 ─┐
   ├─(3) 排版三条  ─┴▶ 各自四件齐（代码 + 前提 + 留痕 + 变异测试）
   │
   ├─(4) 文案 tell ───▶ 卡点在校准（§5 裁决项 ①）⇒ 语言无关子集可先行，语言相关子集待核准
   │
   ├─(5) F4→F3 供料第 2 批  ── 纯文档，穿插
   └─(6) G 组口径清理      ── 纯文档，穿插
```

**为什么(1)先于(2)(3)(4)**：它是唯一一条**数据源与判据都已现成**的收编，
用它把「四件齐」的完整流程（含反向对照）跑通一遍，后面三条即可照此办理 ——
**先用最便宜的一条把流程校准，再上贵的**。

**为什么(4)不排第一**：它的难点在校准而非实现，且校准需要决策者输入（§5 ①）；
把它排在(2)(3)之后，可以让「不依赖名单的那部分」与「依赖名单的那部分」**分两刀**，
前者不被后者卡住。

---

## §4 验收标准（**已选定：可数优先**）

| # | 判据 | 怎么验 |
|---|---|---|
| **1** | **主题验收（可数）**：至少 **8 条**已过筛的判据变成「跑得起来」的检查 —— 逐条列出**条目名 + 落点 + 变异测试断言数** | 当场跑 `node tools/<name>-mutation.mjs` 与全套闸门，把**真实汇总行 + 退出码**写进 `CHANGELOG.md` |
| **2** | 每项收编**四件齐**：代码 + 前提 + 留痕 + 变异测试 | `lessons.md` #73 |
| **3** | **新增检查类必须带反向对照**（合法布局不得被报），且**真实产物零扰动**（新检查因前提不成立而不触发时，`summary` 与改动前一致） | 同 #4；对照 1.3 第 3 步的护栏做法 |
| **4** | 全套闸门 **0 FAIL / 0 未核对**（未核对项必须逐条解释，**不得折算为通过**） | 当场复跑，写真实汇总行 |
| **5** | **参数同源**：新检查的全部档位来自 `CLI > Spec > 默认`，逐项进 `paramSource`；spec 声明了但取不到 ⇒ 回落并**如实标注** `paramGaps`，**不假装来自规格** | `--json` 看 `paramSource` / `paramGaps` |
| **6** | **冻结区零改动**（`figma-plugin/` + `bridge/`）—— 本版**不含**任何冻结区需求 | `git log -- figma-plugin bridge` 应无本版提交 |
| **7** | 范围外请求**默认拒绝并记候选**，不夹带做掉 | `1.4-candidates.md` |
| **8** | 发版前 `release-guard` **0 FAIL / 0 未核对**，再打 `v1.4.0`（落点处 `VERSION` = `1.4.0`） | `node tools/release-guard.mjs` |

> **判据 1 为什么用「条数」而不是「检出率」**（决策者选定）：1.3 的判据 1 回答「这版做完多了什么」，
> 靠的是「一条从未被评过的项变成可评」。本版同构 —— **「已筛」→「跑得起来」的条数**就是「多了什么」的直接计量；
> 而「检出率」依赖单份产物样本、且要跑画布，成本高且结论外推性差。
> ⚠️ **盲区如实记**：条数**不证明这些判据有用** —— 它们可能与真实产物完全不相交（1.3 第 3 步的实例：
> 3 类新检查在运行 B 上**全部不触发**）。本版靠判据 3 的「反向对照 + 零扰动」保证**不产生假阳性**，
> 但**不保证产生真阳性**；后者的证据要等 1.5 的真实产出。

---

## §5 待裁决（**待签字**）

| 代号 | 待决 | 问题 | 选项与代价 | 执行者建议 |
|---|---|---|---|---|
| **①** | **文案 tell 审计器的判据校准输入由谁给** | 收编前必须先定 ① 中文等价 tell 名单（如「张三/李四/示例」「第一步/步骤 1」）② 产物类型分档（哪些 tell 对营销类生效、对仪表盘豁免）。**定不出来就是「永远报 0 条」的假绿灯**（#87 同族），而 verify 不出来的检查比没有检查更坏（#72） | **已决（C + B）**：语言无关子集**本版必做**（不需要任何名单）；语言相关子集**保守初版但不进闸门**，只在 1.5 或你醒来确认后启用 |
| **②** | **1.3 的四类裁决是否需要在 1.4 重述** | `1.3-charter.md` §5 的裁决（QA3 裁剪 / H 组分类收紧 / C2 暂不做 / 只立 F3+F4）**已全部执行完毕** | **不重述**：它们不是「待办」，重述会让读者误以为还有未决项 | **不重述**，仅在 `1.3-charter.md` 标「裁决已全部兑现」 |

**执行者可自定、无须裁决的处置**（列出以便被推翻）：

- **字距那条**（intake §2.3 #8，原文只有 "a little" / "often look better"）：**只做「异常值」信号**（与同字号同类文本的字距显著不同），档位记「译」，报告里**显式标注「这不是判定」**；
- **板外色豁免白名单**：直接从现有 **4 个 Preset**（`enterprise-dashboard` / `gov-digital-screen` / `premium-saas` / `web-marketing`）的 `visualSystem` + `chartColors` + 行业映射色读出，**不新造名单**；
- **G 组口径清理**：**无条件纳入**（§2.2 第 6 项）。

---

## §6 签字

| 角色 | 签署内容 | 状态 |
|---|---|---|
| **决策者** | §1 主题「兑现版」 · §2.0 档位 A · §4 验收形状「可数优先」 · §5 裁决项 ① | ⏳ **待签 · 2026-09-18**（主线与验收形状已选定，① 待定） |
| **执行者**（AI） | 按签定范围执行；**范围外默认拒绝并记候选**；每一刀复跑全套闸门并留痕；**冻结区零改动** | 本文件落盘即生效 |

**签字后须同步更新三处指向**（避免又出现「结论写下了，改动来时没人回头核」）：
`1.4-candidates.md` 头部状态行（「1.4 尚未立项」→ 指向本文件）·
`CHANGELOG.md`（开 `[1.4.0-dev]` 段，**序关系必须大于 `VERSION`**，由 `release-guard` 守卫）·
`.workbuddy/memory/MEMORY.md`（「1.4 未立项」→ 已立项 + 范围）。
