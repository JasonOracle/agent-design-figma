# Changelog

本 Skill 的版本变更记录。版本号基线：**对外契约** = `SKILL.md` 的五层规则 + `bridge/` 与 `figma-plugin/` 的 op 面；改动这两者的行为即需升版本。

格式参考 Keep a Changelog；版本号遵循语义化版本。

**`VERSION` 文件与本文的关系**：`VERSION` 记的是**最后一个已定版（已验收）的版本**；
而本文顶部可能同时存在标着「未发布」的 `-dev` 段——那是**已进入开发但尚未定版**的改动。
两者语义不同、允许并存：**`VERSION` 答「这份稿子由哪个已发布的版本产出」，本文答「累积了哪些变更」**。
此前这层关系没写明，于是 1.2 定版前 `VERSION` 停在 `1.2.0-dev`、本文却已开到 `1.3.0-dev`，
读起来像是矛盾（实为两个问题混在一个号上）。

**`git tag` 与本文的关系**：本仓库自 **`v1.2.0` 起**才开始打 tag（`1.0.0` / `1.1.0` 均未打）。
tag 只用于标记**某个版本的「收尾完成点」**，**不宣称它是纯净快照** —— 本仓库 1.2 与 1.3 的提交在历史上
**相互交织**（`v1.2.0` 的祖先里已含标着 1.3 的工具层修复 `B1`/`B2`，其落点又同时含 1.3 的开发中文档）。
**故判断「这份稿子是不是某版本」，一律以 `VERSION` 文件为准**；逐 tag 的落点与边界说明见该 tag 的注释
（`git tag -n99 v1.2.0`）。

---

## [1.4.0] — 2026-09-18（代号「兑现版」—— 已定版）

**主题**：**「兑现版」** —— 让裁判**真的看得见东西**：把 F4 筛过的判据兑现成跑得起来、带反向对照的检查。
验收形状：**可数优先** —— ≥8 条「已筛」判据变「跑得起来」。

### 1.4 已完成（截至 2026-09-18 晚间）

| 任务 | 内容 | 状态 |
|---|---|---|
| **0** | 参数同源余量：accessibility section 接入 `layout-audit` | ✅ |
| **1** | font-size-floor 检查类（字号下限） | ✅ |
| **2** | 板外色信号审计器 `off-palette-audit.mjs` | ✅ |
| **3** | 排版三条 → D 组需求单（fontWeight/lineHeight/letterSpacing 不回读） | ✅ |
| **4** | 文案 tell 语言无关子集：fake-perfect-numbers | ✅ |
| **5** | F4→F3 供料第 2 批：R12-R14 读图项 | ✅ |
| **6** | G 组口径清理：imageFills 引用口径澄清 | ✅ |

### 闸门状态（全部通过）

| 工具 | 结果 |
|---|---|
| `check-refs.mjs` | 1022 refs / 悬空 0 / 歧义 0 |
| `qa-l2.mjs` | 47 PASS / 0 FAIL |
| `qa-critic.mjs` | 134 PASS / 0 FAIL / 0 WARN |
| `qa-export.mjs --strict` | 935 PASS / 0 FAIL / 0 WARN |
| `qa-install.mjs` | 100 PASS / 0 FAIL / 1 WARN |
| `layout-audit-mutation.mjs` | 129 PASS / 0 FAIL |

### 新增工具

- `tools/off-palette-audit.mjs` — 板外色信号审计器（画布色 ↔ Spec token，不判违规，只报候选计数）
- `tools/layout-audit-mutation.mjs` — 新增 test 30-33（fake-perfect-numbers + font-size-floor 变异测试）

### 新增检查类

- `font-size-floor` — 字号下限检查（intake §2.3 #16）
- `fake-perfect-numbers` — 假完美数字检测（taste-skill #13）
- `alignmentCoverage` — 对齐覆盖范围追踪（1.3 B1）

### 新增读图项

- **R12** — 假产品截图（taste-skill #17）
- **R13** — `<br>` 斜体断行标题（taste-skill #26）
- **R14** — 眉标下微说明（taste-skill #30）

---

## [1.3.0] — 2026-09-18（代号「裁判版」—— 已定版）

**主题**：**「裁判版」—— 让每一环都有可证伪的裁判**（1.2 是「可信版」：让每一步留下可引的痕迹）。
范围、优先级与验收标准见 **`1.3-charter.md`（立项书，2026-09-17 已签字；档位 B「收口 + 空环」）**；
候选输入见 `1.3-candidates.md`。⚠️ `1.3-plan.md` §3 的「建议范围」**只是历史对照**，现行范围以立项书为准。

### 开工第一刀（2026-09-17）—— B1 / B2 两个工具层缺陷

- **B1 · `layout-audit` 的对齐检查补第五个留痕段 `alignmentCoverage`** —— `alignment` 有一句「跨轴尺寸不同就跳过」，**既不上报也不计入**，于是「比过 3 对」和「比过 900 对」在产物里长得一模一样，都写 `alignment: 0`（`lessons.md` #72 的同一类错）。**修法是留痕，不是放宽判据** —— 实测（运行 B readback）：真正比过 **3 对**，因跨轴尺寸不同**没比 121 对**；放宽判据后新增 **5 条报告，5 条全是假阳性**（时间戳 231×27 vs 在线圆点 10×10 —— 圆点**居中**对齐文字，上边缘当然差 8px；**跨轴尺寸不同时视觉惯例是居中，边缘对齐不是正确判据**）。现报告含 `compared` / `skippedCrossSizeDiff` / `samples`（抽样 12 条，带各自 `edgeDelta` 供复核），控制台单列「对齐覆盖」行。变异测试 **51 → 60 PASS**，其中 #24 是**反向闸门**：居中对齐的合法布局**不得**报 alignment —— 将来若有人把判据"修"回去，它立刻变红。
- **B2 · `precheck --live` 的汇总行跟随在线核对更新** —— 旧版只在核对**之前**打一行「待核对 N」，核对完不再更新，而 `acceptance-criteria.md` §6 要求留痕的恰是**汇总行** ⇒ 产物里只剩一句过期的话，人把「已核过」记成「没核」。这与 `lessons.md` #72 是**镜像**（那次是「没查」像「查了」，这次是「查了」像「没查」），病根同一：**结论行的状态与它描述的事实脱节**。现改为：核对前那行加限定词「离线汇总」，核对后**必再打一行**「最终汇总（含在线核对）」，并给出 `已核对 / 核出不存在 / 未核对` **三个数**（三条路径分别落位 `1/0/0` · `0/1/0` · `0/0/1`）；最终汇总行里**不得**再出现「待核对」措辞。变异测试 **66 → 75 PASS**，另用**真 Bridge + 真画布**实测一次（`7:328` 存在 → 已核对 1；`999:999` → 未核对 1）。
- **回归时抓到的改名涟漪（`lessons.md` #75）** —— 1.2 收尾给导出包副本改名（`health-*` → `healthcare-*`）时，`tools/qa-export-mutation.mjs` **被打断了而没人发现**：它里面 `["saas","health","highway"]` 一个列表**兼作两用**（拼核心示例目录 `assets/examples/example-health/` **和**拼随包清单 `example-healthcare-export.json` 的**旧拼写**——那是「example-health-export.json」这个**已不存在的**文件名），改名后两者不再同名 ⇒ ENOENT，整份变异测试崩掉。**根因不是改名本身，是改名后没有复跑全套闸门** —— `check-refs` 当时全绿，因为它只管文档引用、管不到代码里拼出来的路径。已修（拆成 `CORE_EXAMPLES` / `EXPORT_NAMES` 两个具名列表 + `manifestKey()` 显式转名），复跑 **56 PASS / 0 FAIL**。`1.2-plan.md` §7.1 的「变异测试全 0 FAIL」一行已加追补说明（那行是**改名之前**的版本）。
- **新发现 D3（记录，未修）· 不存在的 node id 让插件挂满 10 秒，然后报「无法连接 Figma，请检查网络」** —— 排查 B2 时顺带实测出来：对不存在的 id 调 `get-node`，插件既不返回 `NODE_NOT_FOUND`（`code.js` 里 `if (!node) throw fail(..., "NODE_NOT_FOUND")` 的分支因此**永不触发**），也不快速失败，而是挂 10s 后由 Figma 宿主抛出这句**指向错误方向**的错误（实测 3/3：`999:999` / `1:2` / `7:99999` 均 10.1~11.0s；真实存在的 `7:328` 54ms 就回）。成因疑为 `documentAccess: "dynamic-page"` 下对缺失 id 会去尝试加载对应页面。**两个后果**：① 诊断陷阱（把「id 不存在」说成「网络问题」）；② `precheck --live` 设计里的「核出不存在」专门路径在本环境永不触发，所有缺失 id 落进「意外响应」并判 FAIL —— **好在 FAIL 的方向是安全的**（不会把没核成说成通过），但 `核出不存在` 计数恒为 0。根因在**冻结区**，本轮**未改**任何冻结文件；工具层**不应用关键词猜**（会把真·连接故障一起吞掉），已记入 `1.3-candidates.md` D3 与 `lessons.md` #76 向冻结区提需求。

### 闸门（2026-09-17 开工第一刀后的复跑 —— ⚠️ **已被下方「债线打包后复跑」表取代**）

> **保留原因**：它是**第一刀当时**的真实快照，用于对照「B1/B2 改完但 B3/B4/A2/C1 未做」时的基线。
> **引用条数请用下方新表** —— 本表的 `check-refs` 与变异测试计数均已因债线打包而变化
> （尤其 `check-refs 586 → 658`、`basename 259 → 243`：**旧表里「已解析 586」混着 46 条产物类型名的静默兜底**）。

| 闸门 | 汇总行 |
|---|---|
| `check-refs.mjs` | `引用总数 586 ｜ 已解析 586 ｜ 悬空 0`（`path=325 / basename=259 / glob=2`）→ `0 FAIL —— 文档引用全部落地（DOC REFS ALL GREEN）` |
| `qa-export.mjs --strict` | `935 PASS / 0 FAIL / 0 WARN —— L5 Export Gate ALL GREEN` |
| `qa-l2.mjs` | `47 PASS / 0 FAIL —— L2 QA ALL GREEN` |
| `qa-critic.mjs` | `134 PASS / 0 FAIL / 0 WARN —— L4 QA ALL GREEN` |
| `qa-install.mjs` | `100 PASS / 0 FAIL / 0 WARN / 0 装置缺口 —— 安装契约 ALL GREEN` |
| 变异测试 ×8 | `layout-audit 60 · precheck 75 · check-refs 18 · qa-critic 51 · qa-export 56 · qa-install 82 · contrast 36 · qa-l2 19(ok/MISS)`，**全 0 FAIL** |

（按 `lessons.md` #74：不记「全 0 FAIL」之外的绝对条数——上表条数为当场复跑所得，引用时请重跑。）

---

### 债线打包（2026-09-17）—— B4 / B3 / A2 / C1 四项一次做完

**为什么打包**：四项都是「已定位、可机械修」的债，且 **A2 必须在加任何新检查类之前完成** ——
否则新检查的档位参数又会变成手敲的（`lessons.md` #73 根因重现）。顺序刻意排为
**B4 收紧引用兜底 → B3 补齐留痕 → A2 参数同源 → C1 装置收编**，做完地基干净，再进下一段不返工。

- **B4 · `check-refs` 引用解析四规则收紧（含一次定性纠偏）** —— 原怀疑是「近半数引用靠 basename 兜底、目录写错全漏」。**实测推翻了定性**：263 条 basename 命中里 **254 条是纯文件名**（无目录，兜底本就合理），带目录的仅 9 条、其中 8 条是合理豁免（`~/.workbuddy/…` 部署路径、`/skill/…` 占位符根）。**真正的规模是 38 条**：`critic-report.json`×17 / `design-system-spec.json`×6 / `export-manifest.json`×15 在 `assets/templates/` 有**官方同名定义**，而旧行为会静默指向全仓库第一个同名实例（那 17 条**全**指向 `example-health` 的那一份），在文档里制造出「指的是某个具体文件」的假象 —— 实际上它指的是一个**产物类型**。修法四条：① **带目录的引用不再走 basename 兜底**（目录错即判 `dangling`，这是原缺陷的正解）；② 同名**产物类型名**判为 `artifact`，落点写明定位到 `assets/templates/` 的定义；③ 部署路径 / 占位符根**单列豁免计数**（不再混进"已解析"里充数）；④ 同名多份返回 `ambiguous` 并 **WARN，不静默取首**。**上线顺带暴露 1 条真悬空**：`CHANGELOG.md:172` 那条引用**漏写了 `assets/examples/` 前缀**（正确应为 `assets/examples/example-health/critic-report.json`，旧版被兜底吞掉了），已修。收口：`引用总数 658 ｜ 已解析 658 ｜ 歧义 0 ｜ 悬空 0`（`path=367 / basename=243 / artifact=46 / glob=2`）。变异测试 **18 → 31 PASS**（10 → 15 类）。

- **B3 · 三个校验器补齐「评估覆盖」留痕** —— `qa-critic` / `qa-l2` / `qa-export` 此前都有一类共同病（`lessons.md` #72）：**因输入缺席而整体跳过时，输出与「查了且干净」长得一模一样**。运行 B 的 L4 报告补做前，`qa-critic` 的 QA2–QA9 全部**静默跳过**，读者无从分辨。现三者各加一段「评估覆盖」：报出**样例名与数量**（缺 `critic-report.json` 的样例 / 缺同级 Brief 或 DS Spec 的样例 / `source.dsSpec` 不可读的样例），并写明「哪些 QA 项因此整体跳过」。变异测试 `qa-critic 51 → 52`、`qa-l2 19 → 20`、`qa-export 56 → 57`，各加一条**覆盖留痕**用例。

- **A2 · `layout-audit` 的档位参数改从 L2 Spec 派生（`--spec`）** —— `lessons.md` #73 的根因是**闸门参数与规格不同源**：运行 B 上一轮「全绿」的真相是 `--min-touch 0`（触控红线被关掉）+ 无 `--baseline`（页框检查没跑）+ `--font` 里混进 16（比 Spec 声明的字阶宽）⇒ 三条检查缺席或放宽。现新增 `--spec <design-system-spec.json>`，从 `tokens.spacing` / `tokens.radius` / `tokens.typography` / `responsive.baseline` 派生档位。优先级 **`显式 CLI` > `--spec 派生` > `内置默认`**，逐项来源写进产物 `paramSource` 段、控制台打印一行「参数来源」；spec 声明了该项但**取不到**时回落默认并**如实标注** `paramGaps`（**不假装来自规格**）。**当场复现了 #73 的根因**：运行 B 原始产物 `.vibe/d2-run-b/layout-audit.json` 手敲的 `spacing` 为 `[2,4,8,16,24]`，而 Spec 只声明 `[8,16,24]` —— **多出的 2/4 是手敲时混入的 radius 值**；结论条数虽未变（均 1 条 spacing），但**尺子不同 ⇒ 数字不可比**。变异测试 **60 → 76 PASS**（新增用例 25–29，含**反向对照**：不给 `--spec` 时 22 确实报 font-size，证明用例 25 的"不报"来自 spec 而非判据失灵）。

- **C1 · 效果类参数的可见性装置收编为随包工具（新增 `tools/pixel-proof.mjs`）** —— 这是 D2 运行 A 最值钱的发现（`lessons.md` #56「承重参数必须有一条能证实它的通道」/ #57「效果需要一个可被效果作用的对象」/ #59「导出成功 ≠ 有内容」），但它此前**只活在 `.vibe/` 运行目录里、不随包发布，下一次跑还会重新踩**。按 #73 的收编判据（**代码 + 前提 + 留痕 + 变异测试**，不是「搬进 `tools/` 就算数」）做成零依赖 node 工具：内置 PNG 解码（8 位非隔行，colorType 0/2/3/4/6，手动 unfilter）→ 直方图（颜色数 / Top-N 占比 / 底色 / 非底色占比）→ ASCII 密度图（看内容**分布**而非总量）→ A/B 逐像素差异；并把「有差异 / 无差异 / 非纯色」变成**退出码**（`--expect-diff` / `--expect-same` / `--expect-content`），使它**能当闸门用而不只是一段给人看的输出**。**能力边界显式抛出、不静默降级**：16 位或 Adam7 隔行直接退出 2；调色板 `tRNS` 忽略（报告写明）；A/B 要求同尺寸，**拒绝裁剪或缩放**（那会伪造可比性）；**不提供「够不够明显」这类阈值**（撞 H3「零目测评分」，只报数字与「是否完全相同」）。**收编过程中抓到工具自身四处静默降级**，其中一处**违背它自己写在头部的契约**：`--json` 声称「机器可读」，却在 JSON 之后又打一行人话结论 ⇒ `JSON.parse(stdout)` 与 `| jq` 全废（现改为**只输出 JSON**，判据结果在 `verdicts` 里、退出码照样反映，并把 64×64 的 `cells` 压成「每行一个字符串」）；另三处 `--grid abc` 静默变空网格 / `--region` 给了没给值被当成「没写」/ `--expect-content` 落在末尾抛 TypeError，全部改为**退出码 2 + 可读原因**，并把「选项取值不合法一律退出 2、**不静默回落**」写进头部契约。新增 `tools/pixel-proof-mutation.mjs`（**46 PASS / 0 FAIL**，自实现极简 PNG 编码器造各 colorType 与全部 5 种 filter 字节的夹具**反查解码正确性**，因为拿真实截图当基准是循环论证）；真实截图回归与收编前**逐位一致**（198 色 / 底色 `#f7f7f9` 46.45% / 非底色 53.55%）。

- **A6（本轮登记并顺手修）· 编排文档的命令表漏列闸门 —— 「诊断写了，修的是别的层」** —— 登记 `pixel-proof` 时才发现，`references/acceptance-criteria.md` §5 那张标注「**可直接复制**」的逐阶段命令表里**连 `layout-audit` 都没有**，`SKILL.md` 的「一次跑完全部闸门」清单同样漏它。而同一份文档 §8.2 早就诊断过「运行 A 漏跑 3 条：`precheck --live` / `layout-audit` / `check-refs`」——那 3 条里**只有 `layout-audit` 一直没进过命令表**。**命令表就是「知道要跑它」的唯一载体，漏一条等于没这条闸门**：这正是 1.2 实测的漏跑成因（「不是命令多到记不住，而是不知道该跑它」）在**文档侧**的实例。两处命令表均已补（含 `layout-audit --spec` 与 `pixel-proof`），并立纪律：**收编新工具必须同步登记进命令表，否则收编等于没收编**。

- **顺带修掉的文档陈旧项** —— `README.md` / `README_EN.md` 的工具清单里有一行**重复**（`qa-export-mutation` 连写两遍）和**五处条数陈旧**（`qa-install` 96→**100**、`qa-install-mutation` 78→**82**、`precheck-mutation` 63→**75**、`qa-critic-mutation` 51→**52**、`qa-export-mutation` 56→**57**，以及 `lessons.md` 的「**48 条**」→**「83 条」**，实测 `grep -cE "^[0-9]+\. "` = 83）；两个 README 均补登 `pixel-proof` / `pixel-proof-mutation`，并把**从未进过 README** 的 `layout-audit-mutation` 补上。这批数字**当时都是对的，之后内容长了没人回头改** —— `lessons.md` #74 的又一实例。

### 闸门（2026-09-17 债线打包后复跑 —— ⚠️ **已被下方「债线尾巴后复跑」表取代**）

| 闸门 | 汇总行 |
|---|---|
| `check-refs.mjs` | `引用总数 658 ｜ 已解析 658 ｜ 歧义 0 ｜ 悬空 0`（`path=367 / basename=243 / artifact=46 / glob=2`）→ `0 FAIL —— 文档引用全部落地（DOC REFS ALL GREEN）`｜⚠️ **这是最终值**：收紧兜底后、尚未写本轮记录时是 `595 / artifact=38`；**写这几行文档这件事本身**又把它推到 `658`（登记 A6 + 补两个 README 的清点又新增了 `path 347→367`、`basename 235→243`）—— `lessons.md` #74「回写顺序会让文档里的数静默漂移」的**现场版**；本行是写完全部文档后**最后一次复跑**取的数，且**此后只做数字替换、不再新增引用**，故它现在稳定 |
| `qa-l2.mjs` | `47 PASS / 0 FAIL —— L2 QA ALL GREEN` |
| `qa-critic.mjs` | `134 PASS / 0 FAIL / 0 WARN —— L4 QA ALL GREEN` |
| `qa-export.mjs --strict` | `935 PASS / 0 FAIL / 0 WARN —— L5 Export Gate ALL GREEN` |
| `qa-install.mjs` | `100 PASS / 0 FAIL / 0 WARN / 0 装置缺口 —— 安装契约 ALL GREEN` |
| `contrast-audit.mjs` | `通过 —— 正文对比度全达标，声称值与复算一致（CONTRAST AUDIT OK）` |
| `layout-audit.mjs --spec`（运行 B readback） | `共 1 条：high 0 / medium 1`（spacing，档位 `[8,16,24]` **派生自 Spec**），退出码 0 |
| `precheck.mjs`（基准样例 `example-health.ops.json`） | `42 步：通过 42 ｜ 错误 0 ｜ 装置缺口 0 ｜ 待核对 1` |
| 变异测试 ×**9** | `check-refs 31 · contrast 36 · layout-audit 76 · precheck 75 · qa-l2 20 · qa-critic 52 · qa-export 57 · qa-install 82 · pixel-proof 46` = **475 条断言 / 0 FAIL** |

（按 `lessons.md` #74：不记「全 0 FAIL」之外的绝对条数——上表条数为当场复跑所得，引用时请重跑。）

---

### 债线尾巴（2026-09-17）—— A1 残余 / D1 改判 / B5 + B6 发版守卫

**这一刀最有价值的产出不是代码，是「三条候选的定性都不可直接采信」。** 开工前按候选清单挑了三件
「已定位、可机械修」的活；实测后**两条半站不住**：

| 项 | 候选清单原话 | 实测后 |
|---|---|---|
| **A1** | 「readback 形状与 Bridge `depth` 语义不匹配，整条 L4 布局闸门**当场跑不了**」 | **需求早已满足。** `31c68cd` 引入 `layout-audit` 时即带 `shallowWarning`（「内部自己处理几何缺失」已实现），`depth` 语义由 `e445a7f`「修复 set-effects 毛玻璃必崩与 depth 语义失效」修好 —— 判据就是那次修复留在 `code.js` `nodeInfo` 里的注释 |
| **D1** | 「改 `tools/figma-harness.mjs`（**非冻结区，可直接做**）」 | **落点错了。** `figma-harness.mjs` 是**加载器**（用 `vm` 跑**一字未改**的 `code.js`），改它不会改变回读行为；`paintToHex` 的真身**只在冻结区** `figma-plugin/code.js` ⇒ 与 D2/D3 同类，**须向冻结区提需求**，不在构建期改 |
| **B5** | 「`VERSION` 与 `CHANGELOG` 可各说各话」（= 不校两者一致） | **前提不成立。** `CHANGELOG.md` 第 7–11 行**早就写死**「`VERSION` 记最后已定版的版本，顶部允许并存更大的 `-dev` 段」⇒ **「不校一致」本就是对的**，它想校的那个「一致」是个错前提 |
| **B6** | 「无 `VERSION` ↔ `git tag` 守卫」 | ✅ 成立，照做 |

**⇒ 一条半是真活（A1 残余、B6），另两条是改判（D1、B5）。** 而三条原描述**都是当时的真实观察** ——
坏的从来不是记录，是**没人回头核**（`lessons.md` #74 的同族，本轮立为独立条目 **#84**）。
两个「前提不成立」还共同指向 **F4 试点校准新增的筛子 S1.5「前提是否成立?」**：
它**必须排在「可判定性」之前**，否则两道筛子都会漏过这种错。

- **A1 残余 · 几何覆盖率从「容器数」升为「节点数 + 可审深度 / 输入深度」（`tools/layout-audit.mjs`）** ——
  本轮唯一有实质产出的改动。复核方法本身值得记：**不读代码猜，而是在真执行路径上验** ——
  用 `tools/figma-harness.mjs` 加载**一字未改**的 `code.js`，在桩画布上造一棵 5 节点三层树，走真实的
  `execute("get-node", …)`，把响应原样落盘后喂给真工具。**这套复核已固化成仓库内的探针
  `tools/layout-audit-readback-probe.mjs`**（自包含、不再依赖 `.vibe/`；4 PASS / 0 FAIL）。三组深度实测：

  | `get-node` 参数 | 子级几何 | `layout-audit` 结果 |
  |---|---|---|
  | `{depth:1}`（默认） | 连 `id/name` 都没有（**子树整段截掉**） | 覆盖率 **1/2**，只报 baseline |
  | `{depth:2}` | **带完整几何**（子级 `200x200@24,24`） | 覆盖率 **2/5**，孙级仍查不到 |
  | `{depth:3, detail:true}` | 全层带几何 + style | 覆盖率 **5/5**，一次跑出 **3 条** |

  ⇒ **后两种回读都能被直接吃下**（旧描述「`depth≥2` 剥掉几何」不成立）。但前两种下**浅容器数都是
  1**，而漏掉的分别是 **1 个与 3 个节点** —— **「1 个容器」是个代理指标，读不出规模**。故产物新增
  `geometryCoverage`（`auditedNodes` / `totalNodes` / `unauditedNodes` / `shallowParents` /
  `shallowNodes` / `auditedDepth` / `inputDepth`），控制台多打一行「几何覆盖 N/M 个节点带几何（审到第
  X 层 / 输入深 Y 层）｜ K 个节点未参与检查」，警告文案也不再把 `depth:2` 说成「典型 depth:1 轻量回读」。
  变异测试 **76 → 89 PASS**（+13 条，含**反向对照**：几何给足时**不得**报浅回读）。

  > ⚠ 定稿时改掉两个**照推**出来的数（`lessons.md` #84 的现场实例）：depth1 的覆盖率是 **1/2 而非
  > 1/5** —— 漏掉的子树**不在响应里**，工具报的「1 个节点未参与」只是**下界**（真树 5 个节点实际漏了
  > 4 个）；第 3 种回读的问题条数**随树与档位变**（3 或 5），**不是契约** —— 契约是「覆盖率随深度严格递增」。

- **顺带修掉一个装置缺口 · `figma-harness.mjs` 的桩缺 `page.selection`** —— `code.js` 的 `get-node`
  （写 `info.selection`）与 `get-page-summary`（读 `currentPage.selection`）**都会碰它**；桩少了这个
  成员会让**任何**走 `get-node` 的离线装置抛 `TypeError: … reading 'map'`。这正是该文件头部第 2 条
  原则要避免的形态：**装置缺口被当成产品缺陷报出去**。补法遵循它自己的成文原则（「覆盖面按源码枚举」
  —— 宿主 API 面从 `code.js` 实际用到的成员反推）。

- **B5 + B6 · 新增 `tools/release-guard.mjs`（六条机械判据，零依赖）** —— 在此之前 `qa-install`
  只校「探针 ↔ `VERSION` 文件」，而 `VERSION` 与 `CHANGELOG`、与 `git tag` 的关系**一条守卫都没有**。
  六条：`VERSION` 合法 semver · `CHANGELOG` 含该版本的定版段 · 版本段**自上而下单调不增**（防新段落
  插到底部）· 顶部 `-dev` 段 **>** `VERSION`（B5 的真形态）· `v<VERSION>` tag 存在（B6）·
  **tag 落点处的 `VERSION` 与文件一致**（抓「tag 打在 `VERSION` 还没更新的提交上」，即「落点不纯」里
  **可机械判**的那一半）。
  ⚠️ **校的是序关系、不是相等** —— 这条同时写进头部与 JSON 的 `_meta.judged`，并由变异测试的
  **反向对照**钉住（`VERSION` 与顶部 `-dev` 段**不相等**但序关系正确时**必须全绿**），免得下一次有人
  「顺手」把它改回校相等。**核不了必须说核不了**：取不到 git 时 E/F 记入 `unchecked` 并写明原因，
  **绝不折算为通过**，`--strict` 下未核对也阻塞（与 `qa-export` 的档位惯例一致）；历史欠账
  （`1.0.0` / `1.1.0` 无 tag）只校当前 `VERSION`、**不判为当下之错**。真实仓库实测
  **6 PASS / 0 FAIL / 0 未核对**（`v1.2.0` → `9828edb`，`v1.2.0:VERSION` = `1.2.0`）；配套新增
  `tools/release-guard-mutation.mjs`（**39 PASS / 0 FAIL**）。
  **它自己的第一版就编了个错理由**：`VERSION` 读不出时报 `B FAIL`、说「CHANGELOG 里一个段都没解析到」
  —— 而出问题的是 `VERSION`、CHANGELOG 通常好好的。**这是变异测试第一次跑就抓到的**（39 条里 1 条初始
  FAIL）。修法与 `lessons.md` #61 同源：**报 FAIL 必须说清「是产物错了」还是「是我核不了」**，
  现改为记入 `unchecked`。

- **文档改判（本轮的另一半产出）** —— `references/acceptance-criteria.md`：§8.1 表格第 2 行
  「接口不匹配」就地标注**已被推翻**并附「⚑ 1.3 复核」小节（含上表三组实测）；§8.3 的
  「未核对 · 1 条」**成因改判**为「当轮产物是**扁平几何表**（缺父子关系）」—— **接口能补，数据不能补**；
  §5 命令表补登记 `release-guard`。`1.3-candidates.md`：A1 / D1 / B5 / B6 四行改判或重述。
  `SKILL.md` / `README.md` / `README_EN.md`：登记新工具 `release-guard` / `layout-audit-readback-probe`、
  `layout-audit-mutation` 76 → 89。`1.2-plan.md` / `1.3-plan.md` / 本文档：同步改正 A1 的三处实测数字。

- **A1 的复核证据就地固化 · 新增 `tools/layout-audit-readback-probe.mjs`** —— 上面的「三组实测」原本只活在
  `.vibe/` 里的一次性探针（脚本 + 它落盘的几份响应 JSON）。而 `.vibe/` **不随包发布**：结论留下来了，
  **能复核它的装置没留下来** —— 下一次评审只能照抄我的数，无法自己重跑（#84 的又一形态）。现提成仓库内
  **自包含**的跨模块契约探针：真 `code.js`（`figma-harness` 以 `vm` 加载**原文**，不碰冻结区）→ 真
  `execute("get-node")` → 真 `layout-audit --json`，4 条判据（**4 PASS / 0 FAIL**）：① 覆盖率随深度**严格
  递增**（`1 < 2 < 5`）；② 只有 `{depth:3,detail:true}` 能报出**第 2 层节点**上的问题；③ 浅回读必须
  **如实报出**（`shallowWarning` + 未参与节点数）；④ `{depth:1}` 的未参与节点数是**下界**（< 真树的未审数）。
  它与 `layout-audit-mutation.mjs` **互补而不重复**：变异测试喂**合成树**，验「形状对了工具判得对不对」；
  本探针喂**真 `code.js` 产出的响应**，验「真形状是不是就是那样」。

### 闸门（2026-09-17 债线尾巴后复跑 —— ⚠️ **已被下方「立项第 0 项后复跑」表取代**）

| 闸门 | 汇总行 |
|---|---|
| `check-refs.mjs` | `引用总数 751 ｜ 已解析 751 ｜ 歧义 0 ｜ 悬空 0`（`path=420 / basename=283 / artifact=46 / glob=2`）→ `0 FAIL —— 文档引用全部落地（DOC REFS ALL GREEN）` |
| `qa-l2.mjs` | `47 PASS / 0 FAIL —— L2 QA ALL GREEN` |
| `qa-critic.mjs` | `134 PASS / 0 FAIL / 0 WARN —— L4 QA ALL GREEN` |
| `qa-export.mjs --strict` | `935 PASS / 0 FAIL / 0 WARN —— L5 Export Gate ALL GREEN` |
| `qa-install.mjs` | `100 PASS / 0 FAIL / 0 WARN / 0 装置缺口 —— 安装契约 ALL GREEN` |
| `contrast-audit.mjs` | `CONTRAST AUDIT OK` |
| `layout-audit.mjs --spec`（运行 B readback） | `共 1 条：high 0 / medium 1`（spacing，档位派生自 Spec），退出码 0 |
| `precheck.mjs`（基准样例 `example-health.ops.json`） | `结构与契约错误 0（PRECHECK OK）` |
| **`release-guard.mjs`（本轮新增）** | `6 PASS / 0 FAIL / 0 未核对 —— 发版一致性 OK` |
| **`layout-audit-readback-probe.mjs`（本轮新增，跨模块契约探针）** | `4 PASS / 0 FAIL —— 回读形状与工具输入契约成立`（`{depth:1}→1/2` · `{depth:2}→2/5` · `{depth:3,detail:true}→5/5`） |
| 变异测试 ×**10** | `check-refs 31 · contrast 36 · layout-audit 89 · precheck 75 · qa-l2 20 · qa-critic 52 · qa-export 57 · qa-install 82 · pixel-proof 46 · release-guard 39` = **527 条断言 / 0 FAIL** |

> ⚠️ **`qa-install` 与 `precheck` 必须在本轮之后复跑** —— 它们共享 `figma-harness.mjs`（本轮改了它的桩）。
> 已复跑，均绿。

### 立项（2026-09-17）—— 补上这条链上从未发生的一步

**新增 `1.3-charter.md`（立项书，已签字）** —— 1.3 在此之前**已做三刀、11 条改动，却从未定过范围**：
候选清单是 1.2 收尾的产物（**输入**，不是计划）；三刀由执行者按「已定位 / 可机械修 / 无新判据设计 /
有实测数据可验」四条特征挑出，理由是「**不需要先做范围决策就能开工**」。
**代价不是做错事，而是没人能回答「1.3 到底要干什么」** —— 每一刀单独看都成立，合起来拼不成可验收的范围。
第三刀把这个代价变成了实测数字：那批「已定位、可直接做」的候选里**两条半站不住**（`lessons.md` **#84**）
⇒ 范围没定，筛选标准就只剩「能不能做」，「**该不该做**」没有抓手。

**签字内容（四类裁决全部按执行者建议通过）**：

| 代号 | 待决 | 裁决 |
|---|---|---|
| ① | 范围档位（A 只收口 / **B 收口 + 空环** / C 全量） | ✅ **B 档**，连同主题「**裁判版**」一并采纳 |
| ② | `qa-export` QA3 对 `live-build` 的 `figmaFileKey` 强制要求 | ✅ **裁剪**，但须走一次正式的「判据修正」流程（改判据不改编造 + 变异测试 + 文档留痕），**不许顺手改** |
| ③ | H1–H6 六处「约束内的自由」 | ✅ **分类收紧**：字阶映射 / 状态色 / 断点收紧；pagePadding / focusRing+touchTarget / 字重保留自由但**须给合理区间** |
| ④ | C2 增量补齐工具 | ✅ **暂不做**（`get-node` 输入形状经 A1 复核本来就能用）⇒ 留作纪律 |

**主题与判据**：1.3 =「**裁判版**」—— 让每一环都有**可证伪的裁判**。判据一句话：
**它是否让某一环从「没有裁判」变成「有裁判」，或让已有的裁判变得可证伪？**
三刀 11 条全部落在判据的**左半边**（修「裁判自己的可信度」），**右半边一条没做** —— 那正是 F3 必做的原因。

**验收标准（7 条，见立项书 §4）**，其中**判据 1 是「已立项」与「没立项」的分水岭**：
L4 报告 `_evidence.unassessed` 里 **Commercial 的「商业产品相似度」（占该维 40% 权重）不再是「无装置可评」**。
其余六条（四件齐 / 闸门 0 FAIL 且 0 未核对 / 变异测试 0 FAIL 且新增检查类带反向对照 /
冻结区零改动 / 范围外默认拒绝 / 发版前 `release-guard` 6 PASS）都是**过程无瑕疵**，
只有判据 1 回答「**这版做完多了什么**」。

**顺序**：`(0) QA3 判据修正 → (1) F4 筛子 → (2) F3 装置 → (3) A3/A4/A5 → (4) H 组收紧 / D 组需求单`。
**F4 必须排在 F3 之前** —— 并置比对要先知道「比什么」；没有判据原料就先建装置，只会把 `unassessed`
从「没装置」改成「有装置但不知道该看什么」，比原来更坏（`lessons.md` #82 同族）。
**第 0 项排第一不是因为重要，是因为便宜且污染面大** —— 不清掉它，此后每次收口都要额外解释
「FAIL 的那条是环境凭证、不是产物质量」。

**同步更新的指向**：`1.3-plan.md` 顶部状态行 · `1.3-status.md` §一 / §二 / §6.2 / §七 ·
`1.3-candidates.md` 第 4 行 · 本文件 1.3.0-dev 的主题行。
（补这一步的直接动因是 `lessons.md` #84 的同族形态：**结论写下了，改动来时没人回头核**。）

### 立项第 0 项 · QA3 判据修正（2026-09-17 · D2 / D4）

**撤掉 `qa-export` QA3 对 `live-build` 档位的 `figmaFileKey` 强制要求**（立项书 §2.2 第 0 项 / 裁决 ②；
按约束**走正式判据修正流程**，不是顺手改）。

**为什么这不是「放宽校验器」**：A4 的教训（`lessons.md` #34 /「改措辞不放宽校验器」）治的是
「**文档措辞不严谨，却去松校验器**」；本条病灶相反 —— **判据引用了一个本拓扑根本得不到的外部凭证**。
`figmaFileKey` 的用途是「按 fileKey+nodeId 回读」，那在**基于 REST API 的上游**里是必要条件；
而本管线的回读通道是 **Bridge 直连插件**（`get-node` 凭 `nodeId` 单键即可），且插件
**没有任何 op 返回 `figma.fileKey`**（冻结区缺口，运行 A / 运行 B **两次独立确认**）
⇒ **在本拓扑下它不是判据，是噪音**：本环境永远 1 条 FAIL，且每次收口都要额外解释
「FAIL 的是环境凭证、不是产物质量」。系统教训见 `lessons.md` **#85**。

**改法：把「必须持有」换成「必须交代」**：

| | 原判据 | 现判据 |
|---|---|---|
| `live-build` 且无凭证 | `必须有 figmaFileKey`（无条件硬要求）→ FAIL | **如实标 `null` + 在 `_meta.note` 交代缺口** → 放行 |
| `live-build` 且已声明 | 同上 | **必须非空** —— 空串 `""` ≠ `null`，它是「静默留空」的另一种写法 → FAIL |
| 静默留空 / note 全空白 | FAIL | **仍 FAIL**（不变） |
| `design-phase` | 必须诚实标 `null` | **不变**（未被顺带放宽） |

> ⚠️ **裁剪时踩到的坑（值得单独记）**：第一版把分支写成 `if (!fk)`，于是**空串**被归进「如实标 null」——
> `figmaFileKey: ""` 只要补一句 note 就过关了；而旧判据的 `!!` **恰好**把空串拦下过。
> ⇒ 改为 `fk == null`（只认 `null` / 缺字段）+ 补一条「已声明就必须非空」。
> **同一个字段有四种「没有」：`null` / 缺字段 / 空串 / 全空白 —— 它们不是一回事**（#85 判据 ④）。

**验收：三条判据都落在数字上**

| 判据 | 实测 |
|---|---|
| 该放行的放行 | 运行 B **`409 PASS / 1 FAIL` → `410 PASS / 0 FAIL / 0 WARN`**（那条永久 FAIL 消失） |
| **不该动的没动**（证明是「换判据」不是「删判据」） | 自带样例 `--strict` **`935 PASS`** 不变 ｜ 运行 A **`364 PASS`** 不变 ｜ 变异用例断言「两条路径各带**一条**凭证判据、数量相等，且各自断言行可见」 |
| 该拦的仍拦 | 新增三条变异用例：`null` 无 note / note 全空白 / 空串 —— 均 `exit 1` |

`tools/qa-export-mutation.mjs` **57 → 63 PASS / 0 FAIL**（+6）。
**盲区如实记**：`_meta.note` 非空**不证明画布真的存在**，只证明执行者**显式交代了缺口** ——
画布是否存在由 `precheck --live` 的**实际回读**证明（那是另一条时刻的判据，两者不许互相顶替）。

**文档改判**：`assets/templates/export-manifest.json`（字段描述）· `references/acceptance-criteria.md`
（§1.3 加「**别反推**」注 + 盲区 · §5 L5 命令块 · §8.3 边界表「环境不可达 · 1 条」**移出并留痕为一条注**）·
`SKILL.md` · `README.md` / `README_EN.md` · `references/lessons.md`（**#85** + 守卫覆盖表）·
`1.3-charter.md` §2.2 第 0 项标已完成 · `1.3-plan.md` §2.6 · `1.3-status.md` §三 / §五 · `1.3-candidates.md` D2 / D4 两行。

### 闸门（2026-09-17 立项第 0 项后复跑，均为当场实测）

| 闸门 | 汇总行 |
|---|---|
| `check-refs.mjs` | `引用总数 782 ｜ 已解析 782 ｜ 歧义 0 ｜ 悬空 0`（`path=435 / basename=299 / artifact=46 / glob=2`）→ `0 FAIL —— 文档引用全部落地（DOC REFS ALL GREEN）`（751 → 782 仍是 #74：增量全部来自本轮在受扫文档里**新写的引用**；替换数字不改变引用数，复跑确认稳定。**⚠️ 下一节 F4 又写了新引用 ⇒ 该数此后为 800**，见 `1.3-status.md` §五口径「看悬空这个结论即可」） |
| `qa-l2.mjs` | `47 PASS / 0 FAIL —— L2 QA ALL GREEN` |
| `qa-critic.mjs` | `134 PASS / 0 FAIL / 0 WARN —— L4 QA ALL GREEN` |
| `qa-export.mjs --strict` | `935 PASS / 0 FAIL / 0 WARN —— L5 Export Gate ALL GREEN` |
| `qa-export.mjs --manifest`（**运行 B 真实清单**） | `410 PASS / 0 FAIL / 0 WARN`（本刀的主要产出） |
| `qa-install.mjs` | `100 PASS / 0 FAIL / 0 WARN / 0 装置缺口 —— 安装契约 ALL GREEN` |
| `contrast-audit.mjs` | `CONTRAST AUDIT OK` |
| `layout-audit.mjs --spec`（运行 B readback） | `共 1 条：high 0 / medium 1`，退出码 0 |
| `precheck.mjs`（基准样例） | `结构与契约错误 0（PRECHECK OK）` |
| `release-guard.mjs` | `6 PASS / 0 FAIL / 0 未核对 —— 发版一致性 OK` |
| `layout-audit-readback-probe.mjs` | `4 PASS / 0 FAIL` |
| 变异测试 ×**10** | `qa-export` **57 → 63**；其余九套不变 —— 合计 **533 条断言 / 0 FAIL** |

### F4 第 1 步：把吸纳筛法提进仓库（2026-09-17 · 立项书 §2.2 第 1 项，⏳ 进行中）

**新增 `references/design-criteria-intake.md`（随包发布）** —— F4 的**筛子正本**。

立项书第 1 项是「逐条过筛外部设计 skill，能落到画布几何 / 属性的做成新检查类」，而它的**前置**是
「**筛法本身要随包**」：此前三份试点产物（逐条分档工作稿 / 同心圆角闭环试点 / 探针脚本）**全在 `.vibe/`**
—— 被 gitignore 挡住、**不随包发布** ⇒ **结论留下来了，能复核它的过程没留下来**，
下一次评审只能照抄结论（`lessons.md` **#81** 点名要求补这一步）。

**四道筛（顺序不可换）**：**S1 静态？→ S1.5 前提是否成立？→ S2 可判定？→ S3 检查还是配方？**
答案只有四种、且每种都带落点：**搬**（→ 闸门）/ **译**（→ 闸门出候选 + 人裁决）/ **交人**（→ 评审清单）/
**无对象**（→ 不收编，但记下来）。
两条附加纪律：**逐条筛不够，还要逐句筛**；**引进必须带「依赖能力」栏**（否则得到一堆「写好了但永远不生效」的规则）。

**样例：`better-ui` 全部 17 条（一手回指原文件）**

| 分档 | 条数 | 占比 |
|---|---|---|
| **搬**（可机械判定，含配方） | 2 整条 + 2 部分 | 12% |
| **译**（可算可疑信号） | 1 整条 + 3 部分 | 6% |
| **交人**（不可判定，须显式标注） | 1 整条 + 1 部分 | 6% |
| **无对象**（静态画布表达不了） | **9 整条** + 2 部分 | **53%** |

比分类更值钱的六条发现（择要）：
① 「**连续公式撞离散白名单**」—— 同心圆角 `outer = inner + padding` 与我们「档位精确匹配」的纪律
**互斥 5/8（62.5%）**（运行 B 实际档位 `[2,4]`）；处置**不是**改判据，而是**形态前提不成立**
（真实画布上**可检查对象 = 0**）—— 它正是 **S1.5 这道筛子**被补进来的样本；
② **结构性错位** —— 这批 skill 的主战场是**运行时**（17 条里 9 条），我们的产物是**静态画布**、
op 集里没有任何原型 / 动效操作 ⇒ **「学」之前先按「静态 / 运行时」筛一遍，否则白学一半以上**；
③ 有一条可**反向利用**：「Motion is never the only feedback channel」的可判定内核是**静态**的
（状态之间必须存在静态差异）⇒ **筛的时候不能只按表面主题丢**。

**进度如实记**：`better-ui` **1/6 已过筛**；其余 5 份（`taste-skill` 的 *AI Design Tells* / `better-layout` /
`better-typography` / `review-checklist` / `review-animations`）**尚未过筛** —— **不要把「筛法已定」读成「F4 已完成」**。

顺带订正两处陈旧条数（`lessons.md` #74 同族）：`SKILL.md` 里 `lessons.md` 的「**48 条**」→ **86 条**；
两个 README 的「**84 条**」→ **86 条**。本轮并为「引用外部来源」新记一条 **#86**：
**反引号在本仓库是「仓库内存在此路径」的声明**（由 `check-refs` 机械执行），故外部（外网 / 别家仓库）
路径一律写**裸 URL**；本轮把一手来源写成反引号路径，当场被 `check-refs` 判 `悬空 1` —— 那是闸门在
**如实执行**本仓库的约定，错的是「一个符号装两个意思」（#83 同族）。F4 还要再引 5 份外部来源，这条会反复用上。

### F4 第 2 步：三份来源合并过筛（2026-09-17 · 立项书 §2.2 第 1 项，⏳ 进行中）

**`better-layout`（10 条）+ `better-typography`（19 条）逐条分档完成** —— 与 `better-ui` 合计
**3/6 份来源、46 条原则**过筛完毕。`references/design-criteria-intake.md` 由「1 份样例」扩为
「3 份来源 + **回读契约表** + 第二批发现」。两份来源与 `better-ui` **同仓库**（`jakubkrehel/skills`）。

**筛选口径扩两档**（原四档 → 五档 + 「拆」）：

- **已有** —— 闸门 / 审计器**已经在做这件事** ⇒ **不做，只记参照**。不单列的后果是**重复造一遍同样的检查**
  （还会带来两套判据口径漂移）。本次命中 2 次：`alignment` 检查（`better-layout` #3）、`font-size` 字阶检查（`better-typography` #5）。
- **拆** —— 一条规则内部**结论不同**，必须逐句分开定档。**它不是一种落点，是「还要再分一次」。**

| 来源 | 搬 | 译 | 交人 | 无对象 | 拆 | 已有 | 合计 |
|---|---|---|---|---|---|---|---|
| `better-ui`（17） | 2 | 2 | 1 | 9 | 3 | 0 | 17 |
| `better-layout`（10） | 1 | 2 | 0 | 2 | 4 | 1 | 10 |
| `better-typography`（19） | 0 | 1 | 0 | 9 | 8 | 1 | 19 |
| **合计** | **3**（7%） | **5**（11%） | **1**（2%） | **20**（**43%**） | **15**（**33%**） | **2**（4%） | **46** |

**两条读法**：① 「无对象」**稳定在四成以上**（首份 53% → 本次合并 43%）——「不能整包吸收」**不是单一样本的偶然**；
② **「拆」从 18% 涨到 33%** —— **逐条筛不够，逐句筛才是常态**。`better-typography` 尤其极端
（19 条里 8 条要拆：声明 / 值 / 症状三件事往往分属不同域），且它**整条可搬 = 0**，
可搬产出**全部藏在半句里**（字族计数 / 宽度上限 / 标点 / 字号下限 / 两端对齐）——
**只看条目级结论会一条都拿不到。**

**第二批八条发现（择要）**：

① **回读契约是硬边界** —— 「行高按角色」`1.1`/`1.5–1.6`、「字距按字号」、「`18px` 以下字重 ≥ `400`」
这三条排版规则**判据明确、值也给全了，却全部过不了**：`lineHeight` / `letterSpacing` /
**数值 `fontWeight`**（只有 `fontName.style` 字符串如 `SemiBold`）**不在回读契约里**。
⇒ 这是 **S1.5「能力前提不成立」的一个新子类：不是缺 op，而是缺回读字段** ——
判据可写、算法可写、样本可造，**跑起来永远是 0 条**（`lessons.md` #72 的假绿灯形态）。
处置：**进 D 组需求单**（**一次加三个字段解锁 4~5 条规则**，本次性价比最高的需求），
**冻结区不得构建期改**。同时如实记下一条**明确不采用**的近似路径：行高可用单行文本的
`height ÷ fontSize` 反推 —— 多行需估行数，**与「从 padding 反推内边距」同类误报风险**，
按既有纪律「宁可如实声明没查，也不给会误报的近似」办。

② **规则自带让位条款** —— `better-layout` 原文即写明「有密度系统就按它来，别换成熟面孔的替代品」
且「**无**密度系统时从 `12px` 起」。我们**有**密度系统 ⇒ 与「离散档位」的冲突**外部已主动让位**，
**不照搬数值**、按 Preset 换算（§3.1 同族，但比同心圆角省事 —— 那条要**我们裁决**，这条**外部已让**）。

③ **「视口 / 滚动 / 浏览器」是共享的缺失维度** —— 8 条「无对象」死于**同一个**形态前提，
归类比逐条省；与候选 **F6**（响应式落画布）同域，立项书 §2.3 已排除（不是遗漏，是已裁决）。

④ **运行时规则的可判定内核落在静态域（第 2、3 次出现）** —— `tabular-nums` → **数值列对齐**；
坏换行 / 混排顺序 → **读图症状**。`better-ui` 那次记的是「不能只按表面主题丢」，
现在有 3 个样本，它应当成为**筛子的固定条目**。

⑤ **F4 → F3 的供料通道第一次打通** —— 直接供出 F3③ 读图清单**第一批 4 条**
（孤字 / 坏换行 / 截断后完整值是否可达 / 混排顺序 / 阅读顺序）。这正是立项书坚持
「**F4 必须先于 F3**」的**实证**；进 F3 时须标「**这是读图项，不是闸门项**」，不得混进闸门冒充机器判定。

⑥ **最大发现是「一个已回读、却没人用的字段」`textAutoResize`** —— **一条字段解锁三条规则**；
同类还有 `textAlign` / `clipsContent`。对照契约表：`textAutoResize` / `textAlign` / `layoutSizing` /
`clipsContent` **全部可回读，而除 `fontSize` 外，文本类字段当前没有任何检查在读**。
⇒ 给 F4 加一个专门动作：**先列「回读契约里有哪些字段没有任何检查在读」，再拿这个清单去找规则** ——
顺序反过来，命中率更高（**F4 的产出密度取决于「未使用字段清单」，而不是「来源数量」**）。

⑦ **代理指标第 4 次出现** —— `better-layout` #2/#6/#7 与 `better-typography` #6 的可落地部分，
**全部依赖「按节点名正则认角色」**（与既有 `TOUCH_NAME` 同源）⇒ 必须标「**这不是判定**」。
本项目已 4 次：`shallowParents` · `issues-count` · `figmaFileKey` · **`TOUCH_NAME`**。

⑧ **S1.5 要问第三问：隐含假定（语言 / 平台 / 输入设备）** —— 标点规则（弯引号 / en dash / 单个省略号）
是**拉丁排版惯例**，照搬到**中文产物**会**系统性假阳性**（中文本来就用「——」「……」）⇒
只能**按语言分档**，不能整条搬。这一问同时批量解释了一批「无对象」（那批的隐含假定是「浏览器 + 视口 + CSS」）。

**顺带订正一处陈旧引用**：`better-ui` 表第 1 行原写「见 §四.1」是**错的**（§四 是结论），
所指应是「连续公式撞离散白名单」所在的 §3.1 —— 已改。

**进度如实记**：**3/6 已过筛**；余 3 份（`taste-skill` 的 *AI Design Tells* / `review-checklist` /
`review-animations`）**未做** —— **「筛法已定」≠「F4 已完成」**。

**顺带记一条新不变量 + 删一条陈旧重复行**：

- `references/lessons.md` 新增 **#87 ——「判据完备 ≠ 能触发」**（`get-node` 回读契约的边界；
  **与 #85 镜像但处置相反**：那条换拓扑失真 ⇒ **换判据**，这条是装置缺口 ⇒ **补字段，不降判据**）；
  条数 **86 → 87**，各 README 与 `SKILL.md` 的引述同步（`lessons.md` #74 同族）。
  `SKILL.md` 同时加一句「**写任何新检查类之前先查回读契约表**」。
- **删除守卫覆盖表里一条陈旧重复行**：`#73`「闸门参数与规格不同源」**同时存在两行** ——
  「✅ 已收编（1.3 债线打包 · A2，`--spec` + `paramSource` / `paramGaps`）」与
  「❌ 无守卫，靠纪律（建议做进 L4 编排层）」；后者是 A2 **之前**的旧态，留着会让读者以为
  「档位仍然只能靠纪律抄」。⇒ 删旧留新。

**闸门（本轮复跑）**：`check-refs.mjs` `引用总数 826 ｜ 悬空 0 ｜ 歧义 0`（上一节那条 ⚠️ 里的
「此后为 800」应读作「**此后不再固定**」；#74 同族，只看悬空结论）· `qa-install.mjs` `100 PASS`。
**本轮零工具代码改动**（纯文档），故变异测试与其余闸门数字不变。

---

### F4 第 3 步：把「未使用字段清单」跑掉（2026-09-18 · 立项书 §2.2 第 1 项的并行产出线）

> F4 第 2 步（§4.6）的结论：**F4 的产出密度取决于「未使用字段清单」而不是「来源数量」** ——
> 本轮把这条建议当场兑现。两个工具层提交（`1ce6953` / `62b8d83`）+ 本文档层。

**C1 · 新工具 `tools/contract-usage.mjs`（+ 变异测试 `contract-usage-mutation.mjs`，42 PASS）**
—— 回读契约的**使用矩阵**：现场**解析** `figma-plugin/code.js` 的序列化段（`styleInfo` 的 `out.X =` +
`nodeInfo` 的对象字面量键 + `info.X =`）得 **33 个契约字段**，扫 `tools/*.mjs`（**排除 `*-mutation.mjs`
—— 变异测试是造数据的，不是在读契约**）的属性访问，三档：**用 / 待复核（裸词提及）/
零引用（= 清单本体）**。判据带牙齿：每个字段要么有消费者、要么在 `UNUSED_OK` 写明
「为什么现在不做」，**双向不一致即 exit 1** —— 防清单静默腐化。
首跑：**20 在用 ｜ 3 待复核 ｜ 10 零引用**；待复核 3 个（`opacity` / `fills` / `parentId`）
人工核实全是**写侧**（op 参数 / mock 构造），不是读者。

**C2 · 收编：5 个字段 → `layout-audit` 三类新检查 + 一处分级（`textAutoResize` / `textAlign` /
`clipsContent` / `characters` / `fontName`）**，按 `lessons.md` #73 四件齐：

- `text-container-fixed`（`textAutoResize=NONE` ⇒ 文本增长只能溢出或被裁，medium）——
  解锁自 `better-layout` #10 / `better-typography` #9、#12 的**存在性内核**；
- `text-justified`（`textAlign=JUSTIFIED`，medium）—— `better-typography` #20 的 Fix 项；
- `font-family-count`（字族去重 > 上限，默认 3，`--font-family-max` 可调，medium）—— #4a；
- `overflow` 在父级 `clipsContent=true` 时升 **high**（**静默隐藏** ≠ 看得见的溢出）；
  `TRUNCATE` 文本**只进 `textTruncated` 清单、不报问题**（已开省略号是有意行为）；
- 全字段 `fieldCoverage` 留痕：输入缺字段 ⇒ 对应检查进 `unchecked`，**不许把「没查」伪装成「查过没问题」**。

**实测护栏（数字不变 = 加的是哨兵，不是噪音）**：run-B 归档回读（377 节点）改动前后 `summary`
**完全一致**（真实产物无 NONE 文本 / 单字族 / 无 JUSTIFIED ⇒ 新检查全部因前提不成立而不触发）。
变异测试 **89 → 125 PASS / 0 FAIL**；检查类 **10 → 13**。另发现并修复测试架子缺陷：
`reports()` / `silent()` 是**文本正则**断言，`--json` 输出喂进去会**空转恒 PASS** ——
新增 **#48 反向对照**（`--json` 输出喂 `reports()` **必须抛**），并加 `已截断文本` 到元信息行过滤。

**顺带修正三处「凭印象写的文档」**（都在 `references/design-criteria-intake.md`，§1.5 已重写）：

- 初版契约表「谁在读」至少 **4 处错**（`fills` ≠ `contrast-audit`——它读 DS Spec 的 token；
  `effects` / `vectorPathCount` ≠ `pixel-proof`——它读导出的 PNG；`opacity` **节点级其实一直可回读**，
  取不到的只是填充级 `paint.opacity`）—— **「谁在读」是可实测的事实，不该凭印象填**（#80 同族）；
- 新增**第三态**：`layoutSizing` **可回读但恒为常量**（376/376 `FIXED`，与 92 个文本的
  `WIDTH_AND_HEIGHT` 互相矛盾）⇒ **「能回读」≠「有信息量」**，不得做判据（恒真假绿）；
  §2.3 #9 的落点相应收缩为只看 `textAutoResize === "HEIGHT"`；
- 新增 **§七** 完整记录本轮（7.1 清单工具 / 7.2 首跑结果 / 7.3 修契约表 / 7.4 第三态 / 7.5 收编 / 7.6
  剩余清单**是待办有理由，不是忽略**）。

**登记**：`acceptance-criteria.md` §5 命令表新增「契约维护」段（`contract-usage` +
`contract-usage-mutation`）并补登记注；README ×2（layout-audit 10→13 类、mutation 89→125、
两新工具条目）；`SKILL.md` 命令清单 + 「写新检查类先查回读契约表」段补工具指针与第三态提醒。

**闸门（本轮复跑）**：`layout-audit-mutation` `125 PASS / 0 FAIL`（89→125）·
`contract-usage-mutation` `42 PASS / 0 FAIL`（新）· `contract-usage` 判据通过（33 字段双向一致）·
其余全套 0 FAIL（`check-refs` / `qa-install` / `qa-l2` / `qa-critic` / `qa-export --strict` /
`release-guard` / `readback-probe` / 各变异测试）。**冻结区 `figma-plugin/` + `bridge/` 零改动**
（contract-usage 只**读** `code.js`）。

---

### F3 第 1 步：读图评审升必选项 + 随包读图检查清单（2026-09-18 · 立项书 §2.2 第 2 项的 ①③）

> F4 的筛法与「未使用字段清单」两条产出线已跑通、供料通道已打通（§4.5）⇒ F4 先于 F3 的前置成立，
> 进 F3。本步做 **①（消措辞矛盾）+ ③（清单落地）**，均为文档层；**②（锚点并置比对装置）单独成步**
> —— 它是立项书 §4 **判据 1（主题验收）** 的对象，要配锚点源 + 快照 + 协议 + 机械裁判，体量配得上独立一刀。

**F3① · `visual-critic.md` §1.6 消掉「增益档可合法不读」的措辞矛盾** —— 原文「读图是增益，不是前提」
只挡了一种滥用（无能力硬出分），却**允许了反方向的滥用**（有读图能力且已导出 PNG，仍可合法跳过读图），
而 E1 的注明说「真正可做的是把读图评审变成流程必选项」—— 两者方向不一致。现改为**两句方向相反的纪律同时成立**：

1. 对**能力**：读图不是出分的前提 —— 无读图环境照常通用档出分 + `unassessed` 如实列缺；
2. 对**义务**：**有读图能力且已导出 PNG 的环境，读图评审是必选项** —— 「能读而不读」= 违规。
   `acceptance-criteria.md` §4.3 加镜像规则同款表述。档名「增益 → 读图」在 §1.6 标题注明（别处别名沿用）。

**F3③ · 新文档 `references/read-image-checklist.md`（随包正本，首批 11 条）** —— 全部标注
**「读图项，不是闸门项」**（H3 零目测评分：产出是带可回放证据的观察，不是分数）：

- **R1–R4**（E1 语义性缺失）：无轴标 / 无单位 / 无空态提示 / 多系列无图例 —— 几何完全合法、
  数值审计原理上不可见（运行 B 的 168 格热力图即 R1 实例）；
- **R5–R8**（F4 供料通道 §4.5 的第一批 4 条）：孤字坏换行 / 截断后完整值可达性（**与 `layout-audit`
  的 `textTruncated` 清单交叉核对**）/ 混排顺序 / 阅读顺序与重要性一致；
- **R9–R11**（`visual-critic.md` 自有增益项）：icon 风格混用 / 视觉焦点失位 / **商业产品相似度** ——
  R11 在 **F3② 装置建成前只允许 verdict=`n-a` 且理由写「装置未建成」**（显式空环，不许假装评过）。

**记录格式升为随包 Schema `assets/templates/read-image-review.json`**：每条 verdict
（`ok` 须写「看了哪」/ `issue` 须带「看到什么 + 节点或区域 + 影响哪一维」/ `n-a` 须带理由）、
`items` 覆盖全部 id、`structured+vision` 才允许产出本记录。**顺手消掉一次 `check-refs` 悬空 ×3**
—— `read-image-review.json` 原写法触发了 #86 同族（反引号 = 仓库内存在断言），按仓库惯例
**补 Schema 到 `assets/templates/`**（产物类型名特例自动解析，与其他三类产物同构），
比改写法或加豁免都更对：这份记录格式本来就该有正式 Schema。

**登记**：README ×2（新文档条目 + templates 清单）· `SKILL.md` L4 段新增读图必选一条 ·
`acceptance-criteria.md` §4.3 镜像规则 · `design-criteria-intake.md` §4.5 标注供料已落地 ·
`1.3-charter.md` 第 2 项备注（①③ done / ② 待做）· `1.3-status.md` §六。

**闸门（本轮复跑）**：`check-refs` `875 引用 / 悬空 0 / 歧义 0` ｜ `qa-install` `100 PASS`。
**零工具代码改动**（纯文档 + 1 份新 Schema）；冻结区零改动。
**F3 进度**：① ✅ ③ ✅（清单首批）· ② ⏳ —— 下一步为 F3 第 2 步（锚点源 + 快照 + 并置协议 +
`qa-critic` QA7 接线机械裁判，立项书判据 1 的主交付）。

---

### F3 第 2 步：锚点并置比对装置（2026-09-18 · 立项书 §2.2 第 2 项的 ②，判据 1 的主交付）

> 修的环：Commercial「商业产品相似度（40% 权重）」此前**结构性无法评估**——运行 B 的 L4 报告未评它的
> 理由是「需与锚点产品并置比对」，而链路里**没有装置**。本轮把「并置比对」从一句要求变成
> 可执行、可回放、有变异测试的五步协议。

**新增工具 `tools/anchor-compare.mjs`（+ 变异测试 43 PASS）**：锚点品牌 DESIGN.md **声明**的 token
（色板 / 字族 / 字阶）←→ 画布回读**实际用**的 token（fills+strokes 的 hex / `fontName.family` / `fontSize`）。
锚点源 = `nexu-io/open-design`（2026-09-18 核实：151 个品牌设计契约，Apache-2.0）——**外部可选、
不 vendored**（许可证 / 会漂 / 体积），立项书「随包锚点集」的措辞据此改判为「**随包的是协议与装置**」。

**判据 J1–J5 全是「装置有没有如实运转」，没有一条是「像不像」**：出处四件套（source URL / license /
sha256 / fetchedAt，缺一不出报告）· 锚点侧解析 ≥3 色 ≥1 字族（空锚点 = 假证据）· 画布侧 ≥1 色 ≥1 字族
（无 token 如实失败，不许空报告装绿）。相似度结论属读图 R11 + 人（H3 零目测评分）；
`qa-critic` QA7 的「structured-only 必有 unassessed」**不变**（装置给机械证据，观感仍需读图）。

**首轮真跑即抓到两个提取坑（变异测试钉住）**：① 字族解析若不设「第 3 列含 px」的形态签名，
阴影表的 `rgba(...)` 与断点表的 `640–768px` 会混进字族；② `#fff` 三位 hex 要扩展成 6 位再比。
另踩到一条工具层通病：**CLI 顶层副作用杀死模块可测性**（变异测试一 import 就被顶层 `process.exit`
杀掉）⇒ 纯函数与主流程用 `IS_MAIN` 哨兵分离，记 **`lessons.md` #88**（条数 87→88，README ×2 / SKILL 同步）。

**首批实测（运行 B × linear-app）**：画布 13 色，1 色精确命中锚点板（均距 65.5）；
字族 `Inter`+`Microsoft YaHei` vs 锚点 `Inter Variable`+`Berkeley Mono`（按字面比、不猜）；
字号 4 档命中 2 档。报告落 `.vibe/d2-run-b/anchor-compare-linear.json`（快照 sha256 可回放）。

**登记**：新协议文档 `references/anchor-compare.md` + 新 Schema `assets/templates/anchor-compare-report.json`
（产物类型名自动登记）· `read-image-checklist.md` R11 从「装置建成前只允许 n-a」改判**已可评** ·
`visual-critic.md` §1.6 分档表（Commercial 行拆「机械证据 / 观感对照」两半）· README ×2 · SKILL ·
`acceptance-criteria.md` §5 命令表 + §8 登记注。

### F4 第 4 步：taste-skill 过筛（2026-09-18 · 立项书 §2.2 第 1 项，4/6）

**来源**：`Leonxlnx/taste-skill` 主技能文件 §9「AI TELLS (Forbidden Patterns)」——一手抓取 main 最新
（87,253 字节，文件最新提交 3c7017d），存档 `.vibe/f4-sources/`（`lessons.md` #80 纪律照旧：只吃一手原文）。

**结果**：50 条 bullet → **41 条原则**（口径见 intake §2.4；初稿把 41 数成 43、又数成 44，
**逐行复算才锁定** —— 记 **`lessons.md` #89**：统计口径必须写死在表头，88→89 条，README ×2 / SKILL 同步）。
分档 **搬 14（34%）/ 译 8 / 交人 9 / 无对象 9 / 拆 1**。

**三条主发现**：
1. **搬率从前三批的 7% 跃升到 34%，原因是判据域换了** —— 文本内容域（`characters` 可回读），
   而该字段在回读契约里至今几乎无人消费（只有 `textTruncated` 在用）⇒ §7.6 顺序纪律第二次应验；
2. **语言分档升为主杀器** —— 9.G 破折号禁令 vs 中文破折号「——」（U+2014 本尊）＝ §1.6 隐含假定的
   旗舰样本；14 条搬里 7 条带语言分档标记 ⇒ **字符串检查收编前必须先过语言表**；
3. **候选新审计器「文案 tell 审计」**（约 12 条搬共享 `characters` 正则形态）本轮**刻意不实施**：
   先决问题是判据校准（中文等价 tell 名单 + 产物类型分档）不是代码，需求已登记；
   另产 **F4→F3 供料第 2 批**（3 条读图项：假产品截图 / 斜体断行标题 / 眉标下微说明），待并入读图清单。

**本轮零工具代码改动** ⇒ 变异与其余闸门数字不变。
**登记**：`design-criteria-intake.md` §2.4（41 条逐条表）+ §2.5（四来源合并 87 条）+ §六 4/6 ·
`1.3-plan.md` §2.7b · `1.3-status.md` §六 · `1.3-candidates.md` F4 行（顺带把滞留的 1/6 修正为 4/6）·
`lessons.md` #89。

### F4 第 5 步：review-checklist 过筛（2026-09-18 · 立项书 §2.2 第 1 项，5/6）

**来源**：`mattbx/shadcn-skills` 的组件评审清单（review-checklist.md，8,300 字节，最新提交 816b5a8），
一手存档 `.vibe/f4-sources/`。**口径**：Quick Audit 9 项逐条比对全是 Detailed 的重复展开 ⇒ 0 条；
修复对全是正文示例 ⇒ 0 条；React 工程细节与语义 HTML 族并 1 条 ⇒ **20 条原则**（口径写死表头，#89 照办）。

**结果**：**已有 4（20%）/ 译 1 / 交人 3 / 无对象 10（50%）/ 拆 2**。两条主发现：
1. **「已有」首次成为最大可执行档** —— 间距档位 / 触控目标 44px / WCAG 对比度 / 密度体系四个判据
   被互不知情的第三方评审清单**逐字复刻**（判据佐证第 3 次）；「已有」判定全部当场回指
   `layout-audit.mjs` 源码复核（`MIN_TOUCH=44` 等），非凭印象；
2. **「画布色 ↔ Spec token」缺口获第二处独立指认** —— §3「禁裸色阶、必须语义 token」的可判定内核 =
   画布 hex ∈ DS Spec token 集，与 intake §1.5 警告（F3② 发现 contrast-audit 不看画布）指向同一个洞
   ⇒ 该检查优先级上调，需求已登记（校准项：按 Preset 定白名单豁免，防 §3.1 式冲突）。

**顺带修正**：第 4 步合并统计合计行算错（搬曾写 20，逐列复算实为 **17**）—— #89 复算规则当场再抓一条。
**本轮零工具代码改动** ⇒ 变异与其余闸门数字不变。
**登记**：intake §2.5 + §2.6（五来源合并 **107 条**：搬 17 / 译 14 / 交人 13 / 无对象 39 / 拆 18 / 已有 6）
+ §六 5/6 · `1.3-plan.md` §2.7c · `1.3-status.md` §六（含第 4 步数字同步修正）· `1.3-charter.md` ·
`1.3-candidates.md` F4 行。

### F4 第 6 步：review-animations 过筛 —— F4 筛线完成（2026-09-18 · 立项书 §2.2 第 1 项，6/6）

**来源**：`emilkowalski/skills` 的 review-animations/SKILL.md（8,407 字节，最新提交 85e8e23），
一手存档 `.vibe/f4-sources/`。charter 预期「S1 通过率低」当场兑现：
**14 条原则 = 交人 5（全部方法论）/ 无对象 9，实质 0 条可收**。

**两条主发现**：
1. **「方法论与实质要分开筛」成显式动作** —— 本来源实质全灭，方法层 4 条独立佐证（findings 表 /
   分层 verdict / 显式 Block-Approve / cite file:line / 反猜），与本仓
   「never approve what you did not inspect」纪律逐条同型（**第 4 次**）。分档表为此新增
   「交人（方法论佐证）」显式落点 —— 否则整份来源会被「无对象 64%」掩盖掉真正价值；
2. **F5 启动包定档** —— 本批 + §2.1 #4–#7/#16 + §2.3 #14 已攒出完整 F5（动效维度）启动包：
   值表（原文自带 STANDARDS.md：easing 曲线 / 时长表 / spring 配置）+ 判据来源 + 修复层级
   （第一优先是删掉动效）—— F5 将来立项的启动成本从「从零调研」降为「按包组装」。

**本轮零工具代码改动** ⇒ 变异与其余闸门数字不变。
**登记**：intake §2.6 + §2.7（六来源合并 **121 条**：搬 17 / 译 14 / 交人 18 / 无对象 48 /
拆 18 / 已有 6）+ §六 6/6 · `1.3-plan.md` §2.7d · `1.3-status.md` §六（F4 筛线完成）·
`1.3-charter.md` · `1.3-candidates.md` F4 行。
**⚠️ F4 收口口径**：**「筛完」≠「收编完」** —— 候选检查（文案 tell 审计 / 板外色信号 /
F3 读图第 2 批并入）均为**需求已登记、待四件齐裁决**（`lessons.md` #73）。

### A3：读图结论自动落结构化字段（2026-09-18 · 立项书 §2.2 第 3 项）

**修的环**：F3①③ 把读图升为必选项并给了留痕 Schema（`read-image-review.json`），但运行 B 的
`_evidence.visionObservations` 是**人手结构化**的 ——「自动落」没有装置，「记录合格与否」没有机械裁判。

**新增工具 `tools/read-image-merge.mjs`**：
- **合并模式**：review → 自动落 `visionObservations`（ok/issue，带 source 留痕）+ `unassessed`
  （n-a，带维度可追溯）；**绝不覆盖原始报告**（`--out` 同路径退出 2）；
- **幂等 + 防篡改**：先剔除既有自动条目再追加 + `readImageMerge.sha256` 内容指纹 —— 同输入重跑
  逐字节一致（#74 类漂移拦截）；落痕后**改记录**必须重落（`--check` 拦）；
- **判据 `validateReview`**：R1–R11 全覆盖（**缺一条 = 没查**）· verdict 枚举 · ok/issue 必填
  evidence · n-a 必填 note · issue 缺 dimension 记 WARN（Schema 原文「建议填写」）。

**`qa-critic.mjs` 新增 QA9（`--review` 接线）**：记录完备性 + 跨产物交叉核对 —— 档位一致
（structured-only 下存在读图记录 = **伪造档位**）· n-a ⊆ unassessed · ok-issue ⊆ visionObservations ·
指纹一致。未提供 `--review` 且报告声称 structured+vision → **WARN**（未核对不得折算为通过）。
三样例全 structured-only ⇒ **基线 134 PASS / 0 WARN 零扰动**。

**变异测试 `read-image-merge-mutation.mjs`：37 PASS**（判据 12 类 + 合并幂等/保真/更新 + CLI 四态 +
QA9 接线正反 + #48 反向对照）。踩坑：`includes("FAIL")` 永真（汇总行自带 "0 FAIL" 字样）——
断言输出要用明细行形态（`/^  FAIL  /m`），不能用关键词。

**登记**：`acceptance-criteria.md` §5 命令表 + 登记注 · `read-image-checklist.md` §一 ·
Schema description · `1.3-plan.md` §2.8 · `1.3-status.md` §6.1（A3 移出）· `1.3-candidates.md` A3 行。

### A4：qa-export 的「无清单」诚实降级（2026-09-18 · 立项书 §2.2 第 3 项）

**修的环**：清单不存在时旧路径只有两条——闸门静默不跑（「没查」与「查了且干净」不可区分，#72），
或 exit 1 的「清单解析失败」（把**没有产物可查**读成**产物被查出问题**，归因方向错）。

**新增 `qa-export --if-absent not-run --absent-reason "<为何没清单>"`**：
- 产出机器可读 **NOT_RUN** 记录（Schema `assets/templates/export-not-run.json`）+ **退出码 3** ——
  四态互斥：0 查过绿 / 1 查过红 / 2 用法错 / **3 没查**，「没查」从此有专属输出形态；
- `--absent-reason` 必须非空（为什么没清单由人交代，空白退出 2）；记录裁决固定
  `UNVERIFIED —— 未核对，不得折算为通过；补齐清单后必须复跑`；
- **两种降级形态合流**：「有清单但缺环境凭证」已由 D4 解决（`figmaFileKey=null` + `_meta.note`），
  记录 note 互指；无旗标时旧路径原样（反向对照钉住，不悄悄放宽）。

**验证**：变异 **63 → 70 PASS**（降级正路 / 理由空白 / 反向对照 / 清单存在时旗标被忽略）；
样例基线 **935 PASS 不变**。
**登记**：`1.3-candidates.md` A4 行 · `1.3-status.md` §6.1 · `1.3-plan.md` §2.8b · CHANGELOG。

---

### A5：precheck 吃增量批次计划（2026-09-18 · 立项书 §2.2 第 3 项）

**修的环**：运行 A / B 的实际操作都是「逐批小计划」，批次 2 的计划要引用批次 1 的产物
（跨批次 `$name` 与字面 id）。旧入口只吃单个计划 ⇒ 批次 2 独立预检时，跨批次引用被误判
「未知引用」（假红 exit 1），字面 id 被误算「待核对外部引用」（--live 白核对一批必然存在的 id）。

**新增 `precheck <plan> --after <priorPlan> …`（可重复、按序）**：
- **前置批次 = 已执行上下文**：先模拟前置批次（同一套 `simulate` 判据），其 `as` 名、
  产物 id、`@last` 链播种进主批次 ⇒ 跨批次 `$name` 正常解析，产物字面 id 归入
  「跨批次引用 N 处」展示（不算待核对 —— 执行到该批次时必然在画布）；
- **上下文不可信即拒**：前置批次自带结构错误 → exit 2 直接拒（带具体错误行）；
  前置批次**自身**的外部引用不泄入主批次待核对（它自己那轮 --live 核过）；
- **顺手修两处真缺陷**：① `--after` 解析提前到主计划 file 探测之前（写在主计划前面时
  前置批次会被误当主计划）；② `die` 声明提前（TDZ：解析循环先于 `const die` 调用抛
  ReferenceError、退出码 1 冒充「查过有错」——把用法错（2）冒充成查过红（1），四态纪律不容）；
- 变异测试另抓到一条：字面 id 循环 `localIds.has(v)` 直接 continue ⇒ 跨批次 id 被静默跳过，
  crossBatch 分流是**死代码**、「跨批次引用 N 处」永远不出现 —— 已修（`priorIds` 集合，登记不静默丢）。

**验证**：变异 **63 → 101 PASS**（跨批次 `$name` / 字面 id 各带不喂 `--after` 的反向对照、
链式传递、四种拒收、外部引用不泄入、参数顺序鲁棒）；真实批次冒烟（d2-run-a batch1→batch2）通过；
全套门禁复跑绿（check-refs 悬空 0 · qa-export 935 · 变异五套全 0 FAIL）。
**登记**：`1.3-candidates.md` A5 行 · `1.3-status.md` §6.1 · `1.3-plan.md` §2.8c · CHANGELOG。

---

### H 组分类收紧：六处实测盲区一次裁决（2026-09-18 · 立项书 §2.2 第 4 项）

**修的环**：1.2 三轮盲测共识的 6 处「文档没说、只能猜」（`1.3-candidates.md` H1–H6）。
按立项书裁决分类落地，全部落 `references/design-system.md`，纯文档层不触冻结区：

- **收紧为唯一解**（多解必歧义的映射类）：
  - **TY-5 字阶串映射（H1）**：逐档贪心——队首两段 `b > a` 即「字号-行高对」，否则单字号 +
    TY-2 推行高；display→heading→body→caption 按序；number 恒复用 body 档（TY-1），
    剩余合法对作数字强调档；富余段 `assumptions` 留痕不静默丢。few-shot `example-health`
    被逐档复现；`example-saas` 的 number 档属收紧前自由度，历史产物不改；
  - **LD-11 web-admin 断点（H3）**：缺省 **1440 / 1024** 两档（满档 / KPI 2 列 + Sidebar 折叠 / 抽屉化降档）；
  - **ST-1 补判据（H4）**：黄/橙并存取 `chartColors` **数组顺序第一个**（先到先得，可复现）。
- **给「缺省 + 区间」**（有合理范围的取值类）：**SP-4**（base 8(4–12) · pagePadding 24(16–32) ·
  cardPadding 16(12–24) · 区块纵距 24(16–40)，恒 4 的倍数）· **AC-3/AC-4**（focusRing 2px+offset2，
  宽 1–3/offset 0–4；touchTarget 桌面 ≥30 / 触屏 ≥44(40–48)）· **TY-6 字重**（标题 500(500–600) ·
  正文 400 · number 500 · 字重差 ≥100 硬下限）。

**验证**：`qa-l2`（example-saas）复跑 **18 PASS / 0 FAIL** 不变（新规则不追溯改判历史产物）；
`check-refs` 复跑悬空 0。
**登记**：`1.3-candidates.md` H 组裁决注 · `1.3-status.md` §6.1 · `1.3-plan.md` §2.8d · CHANGELOG。

---

### D 组需求单定稿（2026-09-18 · 立项书 §2.2 第 5 项）

**交付物**：**`references/frozen-zone-requests.md`** —— D1/D2/D3 与 F4 新增的三条回读字段需求
归并为正式需求单 **FR-1 ~ FR-6**（`ping` 回传 `fileKey` / 数值 `fontWeight` / `lineHeight` /
`letterSpacing` / `paintToHex` 回传填充级 `opacity` / 未知 id 快速 `NODE_NOT_FOUND`），
每条含**落点（code.js 具体位置）· 解锁什么 · 验收方式 · 风险**，按性价比排序。

**纪律**：纯文档层交付——冻结区（`figma-plugin/` + `bridge/`）**一行未动**，待用户逐条批准解冻；
§3 预写批准后执行纪律（一次解冻窗口 / 每条独立提交 + 全套闸门复跑 / FR-6 必须真机验证）。
全部条目 additive，不破坏既有 33 字段回读契约。

**登记**：`1.3-candidates.md` D 组归并注 · `1.3-status.md` §6.1 · CHANGELOG。

---

### F2：图片填充（2026-09-18 · 立项书 §2.2 · **用户授权解冻**）

**能力**：`set-image-fill` op —— 把节点 fills 换成一张图片（`asset` = `.vibe/assets/` 里的纯文件名，
`scaleMode` = `FILL|FIT|CROP|TILE` 缺省 FILL，`opacity` 可选）。

**字节链路**（1.2 附录既定路径，不改 manifest）：主线程 → `figma.ui.postMessage(vibe:fetch-asset)` →
UI 同域 `GET /v1/asset?name=…` fetch → base64 postMessage 回主线程 → `figma.base64Decode` →
`figma.createImage` → `imageHash`。主线程 30s 超时兜底；迟到应答不误配对。

**改动面**：
- `figma-plugin/code.js`：新 op + `fetchAssetBytes` UI 往返 + `styleInfo.imageFills` 回读字段
  （IMAGE paint 的 `image:<hash>:<scaleMode>`；`paintToHex` 语义未动，`fills`/`fillCount` 不变）；
  **回读契约 33 → 34 字段**（additive，零破坏）；
- `figma-plugin/ui.html`：`vibe:fetch-asset` 应答（分块 base64，防 `String.fromCharCode` 爆栈；成败必应答）；
- `bridge/server.js`：`GET /v1/asset` —— token 保护 / 纯文件名防穿越 / 扩展名白名单
  （`.png .jpg .jpeg .webp`）/ 8MB 上限（postMessage 载荷还要 base64 膨胀 33%）/ `sendBytes` 原始字节响应；
- `tools/make-test-asset.mjs`：**零依赖合成 PNG** 测试资产（8 色带 + 对角渐变，可分辨 FILL/FIT 行为）。
  agnes 生图 key 未动 —— 真设计场景生成 PNG 落 `.vibe/assets/` 即插即用；
- `tools/figma-harness.mjs`：补桩 `createImage`（确定性 hash）/ `base64Decode` / UI 取图自动应答 /
  vm 上下文补 `setTimeout`（真沙箱有、装置此前缺）。

**验证（离线全链路 + bridge 真跑；真机复测待用户）**：
- precheck 变异 **101 → 111 PASS**（37 op 全通跑含新 op；scaleMode 越界 / 穿越名 / 缺 asset 均 BAD_PARAM）；
- bridge 端点真跑五分支：200（字节级一致 + PNG 签名合法）/ 404 `ASSET_NOT_FOUND` /
  400 `BAD_ASSET_NAME` / 415 `UNSUPPORTED_ASSET_TYPE` / 401；
- `qa-plugin` **21 项全过**（op 面 36 → 37 断言已更新）；`qa-l2` 样例 18 PASS 不变；
- **⚠️ 未核对（真机）**：Figma 沙箱里的 `figma.createImage` / UI fetch 链路需重载插件后实测
  （步骤记录在 `1.3-status.md` §6.1 F2 行）。**离线桩证明不了宿主行为，此处如实留白。**

**登记**：`1.3-candidates.md` F2 行 · `1.3-status.md` §6.1 · `bridge-ops.md` §5.4+asset 端点 ·
`design-criteria-intake.md` §1.5 · CHANGELOG。

---

### 夜班收口（2026-09-18 凌晨 · 全套闸门复跑，均为当场实测）

**自主执行段完成**：F4 筛线 6/6 · A3 · A4 · A5 · H 组裁决 · D 组需求单 · F2（用户授权解冻）。

| 闸门 | 汇总行 |
|---|---|
| `check-refs.mjs` | `引用总数 1000 ｜ 已解析 1000 ｜ 歧义 0 ｜ 悬空 0` → `0 FAIL`（定版口径见下「封版收口」：1013 / 0） |
| `qa-export.mjs` | `935 PASS / 0 FAIL / 0 WARN —— L5 Export Gate ALL GREEN` |
| `qa-install.mjs` | `100 PASS / 0 FAIL / 0 WARN / 0 装置缺口` |
| `qa-l2.mjs`（saas 样例） | `18 PASS / 0 FAIL —— L2 QA ALL GREEN` |
| `qa-critic.mjs`（run-b） | `45 PASS / 0 FAIL / 1 WARN —— L4 QA ALL GREEN` |
| `qa-plugin.mjs` | `21 项全过`（op 面 36 → 37；定版口径 25 项） |
| 变异 ×13 | `precheck 111 · layout-audit 125 · qa-install 82 · qa-export 70 · contract-usage 42 · qa-critic 52 · anchor-compare 43 · read-image-merge 37 · check-refs 31 · contrast 36 · pixel-proof 46 · release-guard 39 · qa-l2 20(ok)`，**全 0 FAIL** |

**留白（如实）**：F2 的 Figma 沙箱内链路（`createImage` / UI fetch）**未经真机验证** —— 离线桩证明不了
宿主行为；复测步骤见 `1.3-status.md` §6.1 F2 行。冻结区其余改动仍为零（F2 的解冻范围仅限本条目）。

---

### FR-1~6：冻结区需求单实现与结案（2026-09-18 · **用户批准全部 6 条，一次解冻窗口**）

| 条目 | 内容 | 提交 | 真机验证（插件 `· F2b` + bridge 45677） |
|---|---|---|---|
| FR-1 | `ping` 回传 `figma.fileKey` | `3953e48` | 键在（未保存草稿 `null` 属正常，管道通） |
| FR-3/4 | `get-node` 回传 `lineHeight` / `letterSpacing`（原生 `{value,unit}`；混排回 `null`） | `7acd368` | 默认 `{unit:"AUTO"}`（三态确认）/ `{value:0,unit:PERCENT}` |
| FR-2 | 数值 `fontWeight`（`STYLE_WEIGHTS` 逆映射与 `WEIGHT_STYLES` 同源；混排回 `null`） | `6bf8211` | Regular→**400**；加强用例 `set-font Bold`→**700** |
| FR-5 | `fillOpacities` **平行数组**回传 paint 级透明度（不用 `@` 后缀写法——保护 anchor-compare 消费方） | `bb770ac` | `[0.5]` 回包+独立回读一致；`fills` 格式未变 |
| FR-6 | 未知 id 快速失败 —— **复核结案：既有代码已满足，冻结区零改动** | — | `999:999` → **418ms** `NODE_NOT_FOUND`（需求单里「挂 10s 报网络问题」是过期时点快照，#10） |

- 契约计数 33 → **38**（`contract-usage.mjs` 当场实测：在用 27｜弱引用待复核 2｜零引用 9，双向判据通过）；
  `qa-plugin` 21 → **25 项**全过；`check-refs` 1008 引用悬空 0。
- F2 真机复测同日先行通过（`b8cb1ff`，五项全绿 + 出图肉眼验证）。
- **排障教训**（已入 `SETUP.md` / `1.3-status.md` §6.1）：Figma 按 manifest `id` 缓存开发插件包，
  重跑/重启都不重读 `code.js`；可靠解法 = 改 manifest（名字加后缀）后**重新 Import**，
  插件窗口 ops 清单是加载版本的指纹。
- 结案回填与执行纪律兑现记录：`references/frozen-zone-requests.md` §4。QA7 冻结恢复。

---

### 封版收口（2026-09-18 20:10 · 定版前最后一次全套复跑，均为当场实测）

> F2 真机复测（`b8cb1ff`）与冻结区需求单 FR-1~6（`3953e48`…`a1116a9`）发生在夜班收口之后，
> 故按立项书判据 3 再跑一次，下表为**定版口径**：

| 闸门 | 汇总行 |
|---|---|
| `check-refs.mjs` | `引用总数 1013 ｜ 已解析 1013 ｜ 歧义 0 ｜ 悬空 0` → `0 FAIL` |
| `qa-export.mjs` | `935 PASS / 0 FAIL / 0 WARN —— L5 Export Gate ALL GREEN` |
| `qa-install.mjs` | `100 PASS / 0 FAIL / 0 WARN / 0 装置缺口 —— 安装契约 ALL GREEN` |
| `qa-l2.mjs`（saas 样例） | `18 PASS / 0 FAIL —— L2 QA ALL GREEN` |
| `qa-critic.mjs`（run-b） | `45 PASS / 0 FAIL / 1 WARN —— L4 QA ALL GREEN` |
| `qa-plugin.mjs` | `全部通过：25 项`（op 面 37，含 `set-image-fill`） |
| 变异 ×13 | `precheck 111 · layout-audit 125 · qa-install 82 · qa-export 70 · contract-usage 42 · qa-critic 52 · anchor-compare 43 · read-image-merge 37 · check-refs 31 · contrast 36 · pixel-proof 46 · release-guard 39 · qa-l2 20(ok)`，**全 0 FAIL** |

**立项书 §4 判据对账（定版记录）**：

1. **判据 1（主题）** ✅ —— anchor-compare 装置 + 首批实测留痕（运行 B × linear-app，13 色 1 命中 / 均距 65.5）；
2. **判据 2（四件齐）** ✅ —— 各收编项均带变异测试；
3. **判据 3/4（闸门 / 变异）** ✅ —— 上表，`0 FAIL / 0 未核对`；
4. **判据 5（冻结区零改动）—— ⚠️ 经用户授权修订**：F2 与 FR-1~5 先后解冻并改动 `figma-plugin/` + `bridge/`，
   **每一条都有显式批准与独立提交**（F2 见本文件上节；FR-1~6 见 `references/frozen-zone-requests.md` §4）；
   结案后 QA7 冻结恢复。「零改动」承诺在「批准解冻」面前失效，但「批准 + 留痕 + 逐条提交」的纪律全程兑现；
5. **判据 6（范围外拒绝记候选）** ✅；
6. **判据 7（release-guard → tag）** —— `VERSION` `1.2.0` → **`1.3.0`**，tag `v1.3.0` 打在定版提交上
   （guard 的 E/F 检查要求 tag 已存在，故 6 PASS 终验在 tag 之后补跑，结果记入 tag 注释）。

**遗留背债（如实，均有留痕有去处，不拦定版）**：F4 搬档剩余条目未全部收编（文案 tell 审计器待语言表校准 ·
同心圆角档位冲突 · image outlines 依赖 F2 现已解锁）—— 见 `design-criteria-intake.md` §2.4 / §7.6；
「画布色 ↔ Spec token」一致性判据未立（`contract-usage` 的 `UNUSED_OK` 有名）。

---

## [1.2.0] — 2026-09-17（代号「可信版」）

> **定版说明**：2026-09-17 定版。1.2 验收通过 —— 判据、闸门退出码与留痕见 `1.2-plan.md` §7.1，
> 两次实测的逐闸门记录见 `references/d2-calibration.md` §5.1 / §5.2，验收结论与边界见
> `references/acceptance-criteria.md` §8.3。
> ⚠️ **定版时带 4 条如实边界，其中 2 条已于当日补做消除；现存 3 条**：
> 一条「**环境不可达**」（运行 B 的 L5 `qa-export` —— 已补做并核对，`409 PASS / 1 FAIL`，
> 唯一 FAIL 是 `QA3` 要求 `figmaFileKey` 而本环境取不到，**FAIL 的是环境凭证、不是产物**；
> 与运行 A 的同一 FAIL 构成**第二次独立确认**）、
> 一条「**未核对**」（运行 A 的 `layout-audit`，已复核证明**事后不可补**）
> + 一条**能力边界**（数值审计对语义性缺失原理性失明）。
> 另两条已由补做消除：①「A1 的 DoD 是同一 Agent 自验」→ **独立盲测**补成实证；
> ②「运行 B 的 `qa-critic` / `qa-export` 未核对」→ **L4/L5 出口补做**（L4 全绿、L5 只剩环境那一条）。
> 两者见下方同名小节。
> **引用时应与边界一起引，不得只引「通过」二字。**
> **`git tag` 落点**：`v1.2.0`（本仓库**第一个** tag）打在 **1.2 收尾补做完成**的那个提交上，
> 即在下方两条「定版后补做」落地**之后**；落点**不纯**（祖先里已含标着 1.3 的 `B1`/`B2` 工具层修复，
> 落点又同时含 1.3 的开发中文档），**如实记于 tag 注释，不粉饰**。判断版本仍以 `VERSION` 为准。
> 下方各条目的实测数字均为**当时**留下，引用前请按 `lessons.md` #74 复核或重跑。

**主题**：让「跑一次完整链路」从**可能翻车**变成**可预期、可拦截、可复现、可验收**。**零能力新增，纯还债。**

完整范围见 `1.2-plan.md`。

### 定版后补做 ①：A1 的独立盲测（2026-09-17）

定版时留下的 4 条边界里，「A1 的 DoD 是同一 Agent 自验」是**当日先补的一条**
（另 3 条当时记为：2 条「未核对」源于当轮走了替代路径、1 条已复核证明事后不可补）。
当日用**独立盲测**补成实证；**随后又补掉了运行 B 的 L4/L5 出口**（见下一小节）。

**做法**：在与仓库无关的系统 temp 目录里重建最小环境，只放 `references/design-system.md`
+ `assets/templates/design-system-spec.json` + `assets/style-library/` 下的四套 preset + 一份 Brief；
**禁止执行者读 / 搜 / 列举目录外的任何路径**；**不给 few-shot 产物与校验脚本**（否则它会抄现成产物或按断言反推）；
要求它把「文档没说清、只能靠猜」的地方逐条写进一份自记笔记；产物用真闸门 `tools/qa-l2.mjs` 判分
（**不看执行者自己的合规自评** —— 它会写「判定合规」并附免责，自评不算数）。

| 轮次 | 材料 | `qa-l2.mjs` 结果 | 执行者自记「无依据」卡点 |
|---|---|---|---|
| 一 | 文档 + Brief（无 Schema 正本、无 Preset 正本） | **15 PASS / 3 FAIL** | 9 条 |
| 二 | 文档（已补 preset 指针）+ Schema + Preset | **18 PASS / 0 FAIL** | 7 条 |
| 三 | 文档（**未补指针**）+ Schema + Preset（**对照轮**） | **18 PASS / 0 FAIL** | 6 条 |

- **DoD 的口径同时被修正**：原写「仅凭文档 + 一份 Brief」—— 第一轮证明该字面条件**不成立**
  （缺随包资产时必然失败）。真实条件是「契约文档 + **随包资产（Spec Schema 正本 + Style Preset 正本）** + 一份 Brief」。
  L2 本来就要取这两份随包资产，把它们排除在外不是「更严」，而是**不成立**。
- **第三轮对照是必须的**（新增 `lessons.md` #78）：二、三两轮只差「文档有没有补 preset 指针」，结果却同为全绿
  ⇒ **「文档没给 preset 文件位置」不是阻塞性缺陷** —— 执行者能从文档里 schema 的正本路径与开篇的 few-shot 路径
  推断出 `assets/` 的目录结构，自行探索到 preset 库。补指针属**改善**而非**修 bug**；
  **跳过对照轮就会把「改善」写成「修复」、把巧合写成因果**。
- **诚实限定**：盲测执行者与本人**同基座模型**，「通过」证明的是「文档 + 随包资产足以约束出合规产物」，
  **不是「文档已无歧义」**，也不等于第三方人类评审。
- **顺带修掉一处真实缺口**：`references/design-system.md` §4.2 把色值的唯一合法来源指向 Style Preset，
  却**全文未给 preset 文件位置**（`assets/style-library/` 在该文档里零命中，而 schema 与 few-shot 的路径都是写明的）
  —— 已在 §4.2 与 §12.1 各补一处指针。这类缺口**只有独立盲测暴露得出来**：自验者知道文件在哪，永远不会撞上。
- **顺带得到的资产**：三轮笔记去掉材料性缺口后，剩 **6 处共识盲区**（字阶映射规则 / pagePadding 等
  非 cellPadding 档位 / web-admin 断点 / status.warning 的黄橙二选一 / focusRing 与 touchTarget 数值 / 字重），
  已记入 `1.3-candidates.md` 作为将来改文档的输入。
- **产物**：三轮 Spec 与执行者笔记归档在 `.vibe/a1-blind/`（运行目录，不随包发布）。

### 定版后补做 ②：运行 B 的 L4/L5 出口（2026-09-17）

「运行 B 的 `qa-critic` / `qa-export` 未核对」是另一条可补的边界（其余两条已复核确认：运行 A 的
`layout-audit` **不可补**、语义性缺失属**能力边界**）。当轮这两道闸门**工具存在且断言最多**（935 / 134 条），
漏跑的原因是**当轮 L4/L5 的产出形态没往它们入口对齐**（L4 走读图 + `layout-audit`、L5 只做逐组件 PNG 导出）。

**做法**：**不重跑画布**，把**当轮已有的真实素材**结构化成两个出口 —— L4 报告素材来自 `layout-audit`
收编后抓出的 5 条真缺陷、人工读图观察、翻车点 #15/#16/#17；L5 清单由**从 L2 DS Spec 精确派生**的脚本生成
（`tokens` 的 `value` / `source` 一律从 Spec 派生，**不手抄** —— 手抄一个数字就等于伪造一次证据）。

| 出口 | 产物 | 闸门汇总行 | 退出码 |
|---|---|---|---|
| L4 Critic | `.vibe/d2-run-b/critic-report.json` | `45 PASS / 0 FAIL / 0 WARN —— L4 QA ALL GREEN` | 0 |
| L5 Export | `.vibe/d2-run-b/export-manifest.json` | `409 PASS / 1 FAIL / 0 WARN —— L5 Export Gate 未通过` | 1 |

- **L4 已核对通过**。⚠️ 评的是**修复后终态**，故 `_loop.history` 只记终态一项，并在 `note` 里声明
  **不虚构中间轮次分数**（当轮未逐轮打分，CL-5「修复必复评」不可追溯 —— 如实写，不补几个好看的数字）。
- **L5 的 1 条 FAIL 是环境而非产物**。409 PASS 覆盖 QA1–QA9 九组（QA5 逐条核了 **253** 条 value 快照与
  source 继承、QA9 解析 **21** 条 preset 源路径、QA2 核 **14** 个 PNG 逐一落盘）。唯一 FAIL：
  `QA3 live-build 必须有 figmaFileKey` —— **本环境取不到 file key**（插件支持的 op 只有
  `ping` / `get-page-summary` / `get-node` / `set-name` / `create-frame` 等；实测 `ping` 的 `data` 里只有
  `plugin/page/pageId/editorType/documentAccess`，另试 `get-file-info` / `get-document-info` 均回
  `UNSUPPORTED_OP`），根因在**冻结区**。
- ⚠️ **这是「运行 A 唯一 FAIL」的第二次独立确认** —— 同一闸门、同一断言、同一根因，**稳定复现**
  ⇒ 本管线的 L5 `live-build` 档位**结构性不可达**，不是偶发。**没有为了让闸门变绿而改写产物**
  （把 `status` 改成 `design-phase`、或手填一个 key，都能骗过断言）—— 按 A4 的教训，**改措辞而不是放宽校验器**。
- **净效果：「未核对」由 3 条降到 1 条**，剩下那条已复核确认不可补；新增这条「环境不可达」精确到
  具体断言与根因，比原来的「未核对」信息量大得多。
- **产物**：两个出口文件在 `.vibe/d2-run-b/`（运行目录，不随包发布），产物 `_meta` 均**如实标注「事后补做」**。

### 已完成

- **A2 修失效引用** —— 全量核查后为 **12 处**（`SKILL.md` 4 / `critic-mapping.md` 3 / `visual-critic.md` 2 / `export-mapping.md` 1，另 2 处是把 Build Plan 误当独立文件），全部指向一套从未随包发布的 QA 脚本。全仓库 63 处文件引用扫描：真缺失 **0**。
- **A3 版本治理** —— 新增 `VERSION` 与本文件；`tools/runtime-check.mjs` 输出增加 `version` 字段，路径无关（任意 cwd 可跑）。
- **A1 L2 契约文档** —— 新增 `references/design-system.md`，填补链路唯一的无文档层：五段流水线、三条不变式、五类 Token、白名单派生（RD-1/2/3 + ST-1）、组件决策矩阵（CD-1..4）、四件套状态矩阵、四平台布局模板、QA 四项判据。`SKILL.md` 补 L2 章节。上游可搬运部分已适配，另补上游缺失的 `web-site` 平台模板与字体降级链。
- **C3（部分）L2 校验脚本** —— 新增 `tools/qa-l2.mjs`，把 L2 出口校验从「人工照清单核对」变成实际执行。四项检查共 **47 项断言**（Schema / Token 无未知色 / 覆盖与数量 / DS 单源），零依赖。除校验自带 few-shot 外，支持 `--spec` + `--brief` 校验**用户自己的产物**（这是 D1「零目测评分」的前提：校验必须能作用于真实产物，而不只是仓库示例）。另附 `tools/qa-l2-mutation.mjs` —— 逐项注入 16 个已知错误确认校验会报警：**只跑一次全绿不能证明校验有效**，一个永远返回 PASS 的脚本也能全绿。
- **A4 引用校验器** —— 新增 `tools/check-refs.mjs`：扫全部随包发布的 `.md`，把「命令式引用」（`node` 后跟脚本路径）与「反引号路径」（`` `references/y.md` ``）逐条解析，四条判定（仓库根 / 文档同级 / 全仓库同名 / glob）全不中即判悬空，报警并非零退出。治的是 12 处失效引用的**机制性根因**——上游把「检查文档引用」列为人工勾选项，勾了却没做干净；人工项治不住，机械闸门才治得住。三类豁免**均写明理由**：① 运行时产物（`.vibe/` 下的探针输出，本来就只在跑完之后才存在，引用它属于描述产出位置）；② Token 点路径（`tokens.radius.md` 是 radius 下的 md 档、不是 markdown 文件；判据收紧到「无 `/` + 至少 3 段 + 首段是已知对象根」，故真文件 `manifest.json` 不被误豁免）；③ 角度占位符（`<skill 根目录>/tools/runtime-check.mjs`、`<readback.json>` 先剥占位符段，剥完以 `/` 开头的按仓库根锚定）。**现仓库实测：140 条引用全部落地，零误报。**
- **A5 实测不变量清单** —— 新增 `references/lessons.md`：上游七个阶段沉淀的 **30 条**实测踩坑不变量（协议 / Plugin API / 数据流 / 测试 / Windows 环境 / 协作），按本仓库命名与实现适配，并逐条标注**已被工具守卫（⚙️）还是只能靠纪律（📏）**；另补本仓库 1.1–1.2 新得的 8 条（安装副本与开发仓库漂移、校验器判断顺序本身是缺陷来源、校验器必须配变异测试、人工勾选治不住文档脱节、打包排除项会悄悄删掉质量资产、筛选要用正向形状而非反向排除、算得出来的数字必须配复算、判定阈值不许先舍入），共 **38 条**。`SKILL.md` 增加入口段，指明「动手前先读」。
- **B1 构建计划静态预检** —— 新增 `tools/precheck.mjs`：把「画到一半才发现参数错」提前到开工之前。输入是**你正要发出去的那串 op**（`{steps:[…]}` / `{ops:[…]}` / 裸数组三形状等价）。**契约的事实源是真 `code.js` 本身**——用 vm 离线跑一遍（装置抽成 `tools/figma-harness.mjs`，与 `qa-plugin` 共用同一份严格效果桩），而不是再抄一份 op 形状表（抄一份就多一处将来必然腐坏的副本，而"必填参数 / 颜色格式 / 参数类型 / 父级可否容纳子节点"的判定本来就写在 `code.js` 里）。判定分三档且**边界如实打印**：结构错误（离线可判定）、契约错误（真源码的裁决）、**装置缺口**（桩没覆盖到，不算产品缺陷，单独列出提示补桩）。**逐步试跑而非整批提交**——`run` 是 fail-fast 的，一次只报第一个；预检必须一次报全。
- **B2 在线引用核对**（`precheck … --live`）—— 开工前连 Bridge，把计划里引用的画布已有 id 逐个用 `get-node` 问一遍，缺哪个就报「**哪一步的哪个 id**」。**离线查不出这件事**（画布是桩的），所以字面 id 一律归入「待核对」而不是错误；装置还会按参数名给出期望类型提示（`componentId` → COMPONENT），免得凭空撞出 `NOT_A_COMPONENT` 这种假错误。边界照写进输出：**这是核对，不是仿真**——Figma 无事务，做不到「先跑一遍再回滚」。
- **离线装置的完整性被测试锁住** —— `tools/precheck-mutation.mjs`（63 条断言）：13 类注入错误 + 「一次报全部」（防退化成 fail-fast）+ **36 个 op 全通跑**（0 错误 0 装置缺口，等价于"装置对全部 op 都不缺件"）+ B2 端到端（真 Bridge + mock 插件：id 存在 → 通过 / id 缺失 → 报出具体步骤与字段 / 插件掉线 → 失败且**不得**说"全部存在"）。
- **`assets/examples/example-health.ops.json`** —— 一份真实规模的 L3 构建计划样例（42 步，覆盖建帧 / 建文 / 建组件 / 实例 / 效果 / 读回 / 字面 id 引用），可直接喂给 `precheck.mjs`，也是 B1「对合法计划零误报」的基准样本。
- **C1 对比度审计（Color 维）** —— 新增 `tools/contrast-audit.mjs`：从 DS Spec 的 `tokens.color` 复算「文本 × 表面」矩阵的 WCAG 比值与 AA/AAA 档位，并把 `accessibility.contrast` 里**手写的每条声称值**与复算逐条比对，另交叉验证「声称里写的 hex」与「token 实际值」是否一致（抓「token 改了、声称没跟着改」）。判据刻意为硬/软两档：只有正文（`text.primary`/`regular` ≥ 4.5:1）与「声称值必须复算得上」进退出码；辅助/占位文本、品牌色、状态色、边框色的组合只作**信息项**——DS Spec 并不声明「某 token 用在哪个背景上」，把这些组合一律当硬性要求等于工具自己发明用法、凭空造出违规（如 `example-highway` 的 `#00D4FF` 是高亮色而非按钮填充，要求白字达标就是假的）。判定一律用**未舍入**原值（`4.478:1` 显示成 `4.5:1` 也不许当通过），贴阈值时打 `⚠边界`；非 `#RRGGBB` 取值记为「无法计算」并计入退出码——**算不出不能当通过**。配套 `tools/contrast-audit-mutation.mjs`（**36 例**，含边界灰 / 非白背景 / 非 hex 取值 / 多份聚合）。
- **C2 L4 证据链两档化** —— `visual-critic.md` 新增 §1.6「证据两档制（通用 / 增益）」：**通用档**（所有环境，结构化回读的实测值）恒须产出；**增益档**（只有能读图的 Agent，导出 PNG + 逐条观察）为附加，**不得替代**通用档——**读图是增益、不是前提**。给出逐维分档表（如 Commercial 的「商业产品相似度 40%」属增益档、其「模板感」在通用档用结构化代理判定：`Frame 123` 式默认命名 / lorem 文本 / `#D9D9D9` 占位块），并立下四条硬约束：三维通用档分数恒须产出 · **未评估项不得当作通过**（`_evidence.unassessed` 为空才允许声称完整评估）· 增益档观察必须具体可回放 · `_meta.reviewer` 须写明档位。**关键的一条是权重处理**：通用档下无法评估的检查项要**剔除权重并对余项重新归一化**，因为把「没看」按满分或零分计入**都是在编**（前者放过缺陷，后者凭空扣分）。`critic-report.json` 增开可选 `_evidence` 字段（`mode` / `unassessed` / `visionObservations`）承载该规则——顶层本就是开放对象，故三份既有样例仍合法。§4「审查执行方式」的前两条同步改写为两档的执行规则。
- **C3·qa-critic L4 产出校验** —— 新增 `tools/qa-critic.mjs`（**134 条断言**），把 L4 出口校验从「人工照清单核对」变成实际执行。**八组检查**：QA1 Schema 契约（含 `_evidence.mode` 枚举）· QA2 顶层字段 · QA3 五维评分与 `average` 实算（half-up 一位小数，**用未舍入原值判等**）· QA4 issue 必备证据（`evidence` 须含**量或核对动作**）· QA5 `targetLayer ∈ {L1,L2,L3}` · QA6 Critic Loop 自洽（`round ≤3` / `maxLoop==3` / `action` 与分数互推 / **有 `critical` issue 时禁止 PASS**）· **QA7 证据档位契约**（把 C2 的 `_evidence` 从「可选」升为「必填 + 校验」，并立一条可推导的硬判据：`mode=structured-only` 时 `unassessed` **必须非空**——§1.6 分档表里 Commercial「商业产品相似度 40%」在结构化档不可判定，声称「零未评估项」即把没看当看过）· **QA8 跨产物一致**（`project` ↔ `brand.name` ↔ `product.name`、`page ∈ Brief IA`、`_meta.inputs` 路径存在，以及**证据里引用的每个可解析量都要与 DS Spec 对得上**）。除校验自带 few-shot 外支持 `--report`（可配 `--brief` / `--spec`）校验**用户自己的产物**。判据同样刻意分**硬 / 软两档**：`issue.evidence` 是自由文本，从中抽出的量只能当交叉核对提示，记为 `WARN`；`--strict` 可把 `WARN` 升为失败（仓库自带样例走这一档，等于给「样例自身必须干净」装了门禁）。配套 `tools/qa-critic-mutation.mjs`（**51 条断言**：注入 30+ 类已知缺陷 + 一次全报 + `--strict` 升档 + 基线零告警）。
- **C3·qa-export L5 出口闸门** —— 新增 `tools/qa-export.mjs`（**935 条断言**），把 1.2 之前**唯一的空规则**（L5 Export Gate）变成实际闸门。**九组检查**：QA1 **真 Schema 校验**（内置最小 draft-07 校验器，覆盖 `type` / `required` / `properties` / `items` / `enum` / `const` / `pattern` / `minimum` / `minItems` / `minLength` / `allOf` / `if-then-else` / `$ref`——上游只查了 `required` 与一个枚举，Schema 里其余 40 余条约束从来没人执行）· QA2 导出物存在 · QA3 `nodeId` 可回读（live-build 交叉引用 `exports ⊆ rootNodeIds`）· QA4 组件映射完整 · **QA5 `value` 快照必须与 DS Spec 原值逐字相等 + `source` 必须继承自 Spec 叶子 + `cssVariable` 命名规则 + 点路径回溯** · QA6 无孤儿映射 + Export Gate 自洽（`criticScore<8` 只允许 design-phase、`audit.criticScore == criticReport.average`）· QA7 冻结禁区（`figma-plugin/`、`bridge/`）零修改，**核不了必须说核不了** · **QA8 身份一致**（`project` == `source.brief.product.name` == `source.dsSpec.brand.name`；随包副本必须与追溯源同项目）· **QA9 可追溯性**（`preset:<id>.<路径>` 必须能在 `assets/style-library/` 的对应预设里真解析出来，`derived:` 必须给出 `@<源 token>` 指向）。判定分**硬 / 软两档**，`--strict` 把 WARN 升为失败（仓库自带样例走这一档）。配套 `tools/qa-export-mutation.mjs`（**56 条断言**：注入 30+ 类已知缺陷 + Schema 条件分支正反两例 + 基线零告警 + 冻结态「核不了必须说核不了」+ 无 `.git` 路径 + `--freeze` 覆盖）。另把 `references/export-mapping.md` §5 里**此前无任何文档定义**的 `value` 快照约定（字阶写 `size/lineHeight/weight`）与「`source` 继承 + 可解析」写成明文规则。
- **C3·qa-install 安装契约校验（C3 收口，四个脚本至此齐备）** —— 新增 `tools/qa-install.mjs`（**96 条断言**），校验「新用户照文档装完能不能真的用起来」，即 README / SETUP / USER_GUIDE / `references/runtime-capability.md` / 探针五方是否自洽。移植自上游的安装 QA 脚本（stage10-7-install-qa.py，109 行 / 56 断言），但把「grep 关键 token」换成**真跑探针、拿它自己的输出当事实源**——理由是上游那种写法只能证明「这串字在文件里出现过」：文档写 `未检测到 Figma 连接能力，仅可生成设计资产`，探针实际吐的是带尾注的更长串，两者都「含有关键 token」，检查全绿而**用户拿到的提示与文档不符**（本轮即抓到这一条，见下）。**九组检查**：QA1 安装入口交付物齐备 · QA2 探针静态约束（只读 / 零新协议 / **零外部依赖** / 公开端点不带 token）· **QA3 三模式端到端**（用真 bridge 取一份真实 `/health` 当模板，起 mock 插件翻 `plugin.connected` 得 FULL_MODE，再用假 `HOME` 造出 READ_ONLY 与 OFFLINE，**逐态对照 §3 Capability Matrix**——上游只在「本机恰好是什么模式」下跑过一次）· **QA4 只读承诺**（`--no-write` 不得落盘、不带则须落在文档声称的默认路径）· **QA5 cwd 无关性**（从别的目录跑，仍读得到自身 `VERSION`、仍写技能目录、不在 cwd 误建 `.vibe/`）· **QA6 四方口径一致**（模式枚举锁死：包内文档不得出现第四种 `*_MODE`；以及**用户可见提示语逐字复算**——凡以引号给出的提示，必须与探针实跑输出一字不差，形状前缀由真值派生而非写死；另要求「FULL_MODE 下 `figmaRead` 同为 true」这条**反直觉规则**在五份说明它的文档里都写着）· **QA7 安装合约事实**（端口**四处一致** + 运行时再从 `/health` 回声核对一次 · 插件侧必须是 `localhost` 拼写（Figma 校验器拒绝 `127.0.0.1`）· bridge 必须**同时绑 `::1`**（否则 `localhost` 解析到 `::1` 时插件连不上）· token 文件路径且**跨重启保持**（真起两次 bridge 对比）· `manifest.main`/`ui` 指向的文件存在、插件名与 SETUP 让用户选的一致 · `.gitignore` 必须忽略 `.vibe/`（否则 token 会被提交））· QA8 SKILL.md 注册与降级铁律 · QA9 落盘产物形状（软）。行为检查跑不起来时记「**装置缺口**」单列，不算产品缺陷（与 `precheck` 同一套三档口径），`--no-behavior` 可整体跳过行为段。配套 `tools/qa-install-mutation.mjs`（**78 条断言**：搭自包含「假安装包」夹具——真探针 + 真 bridge + 真清单 + 真文档，注入 30 类缺陷 + 基线零告警 + 一次报全部 + 降级路径 + 软档命中非零）。
- **D1 硬验收标准**（D 块收口） —— 新增 `references/acceptance-criteria.md`。原三条（零人工补丁 / 零失效引用 / 零目测评分）是**行为承诺**，而行为承诺无法验收：跑完之后产物上没有痕迹证明执行者当时守没守规矩——这与 A4 治的 12 处失效引用同源（上游把「检查文档引用」列成人工勾选项，勾了却没做干净）。故 D1 的落地方式是**把三条挂到链路出口的机械闸门上**，每条给出确切命令、退出码与失败归因，**判定由工具做**。四件事：① **档位先行** —— 先用探针定 `FULL_MODE` / `READ_ONLY_MODE` / `OFFLINE_MODE` 再判，并立「**低档不得冒充高档**」为硬判据（离线档产物不得出现 `liveBuild` / `nodeId` 已回读 / `figmaFileKey` 非 `null`，由 `qa-export` QA2·QA3 与 SKILL.md 诚实铁律守卫），三档各自的可判范围列成表；② **H1 零人工补丁**的判据不是「手有没碰过文件」（不可观测），而是**产物里每个数字要么算得出、要么指得出源** —— `qa-l2` QA2 派生色复算 / `contrast-audit` 声称值复算 / `qa-export` QA5 `value` 快照 + `source` 继承 / `qa-critic` QA3 `average` 实算 / `qa-critic` QA8 · `qa-export` QA9 引用量可解析；这组判据**有区分度**（上线即抓到 9 条被编造的对比度值、32 处对不上的产物值、17 条假溯源），不是为 D1 新写来「看起来很严」的；③ **H2 零失效引用**拆成三个时刻三个裁判（写文档时 `check-refs` / 发计划前离线 `precheck` / 有图时 `precheck --live`），并明文规定无图环境下记「**未核对**」而**不得折算为通过**；④ **H3 零目测评分**落到实处：`qa-critic` QA4（证据须带量或带核对动作，判形状不判关键词）/ QA6（有 `critical` 禁止 PASS）/ QA7（`structured-only` 下 `unassessed` 必须非空）/ QA8，加 `contrast-audit` 与 C2 §1.6 的权重重新归一化。文档另附**逐阶段命令表**与**验收记录表模板** —— 记录表要求粘贴工具输出的汇总行与退出码，「通过」两个字不算留痕（一个永远返回 0 的脚本也能让人写出「通过」）；附三档归因表（结构错误 / 契约错误 / 装置缺口）与**如实列出的盲区**（H1 只能观测到「算得出 / 指得出源」，无法直接证明「没被手工编辑」）。**范围**：只编排已有闸门、零新增判定，守 §8 净范围不膨胀；「固化成一次性出口检查（薄编排器）」列为**待定强化项**，取决于 D2 两次实测的手工成本，现在不预先实现。

### 修复

- **`design-system-spec.json` 的 `stylePresetId` 枚举缺 `web-marketing`** —— 1.1 新增的第四套 Preset 一直无法通过 DS Spec 校验。
- **派生色校验可被绕过**（移植上游脚本时发现的逻辑漏洞）—— 上游先判「值是否在 preset 色板内」、再判是否派生，导致**派生 token 的值只要填成任意一个色板色就能蒙混过关**（`hover` 错填 `#000000` 或 `#FFFFFF` 均不报警）。改为：带 `derived:` 前缀即强制走公式复算。
- **`check-refs.mjs` 的路径正则漏掉点目录**：首字符类不含 `.`，导致 `.vibe/runtime-capability.json` 这类**点目录开头**的路径整条连不上——既不报错也不进豁免，等于运行时豁免规则空转（真实仓库里「豁免 运行时=0」即是证据）。已由变异测试的「豁免计数非零」断言守住。
- **A4 上线即抓到一处真实问题**：`references/lessons.md` 初稿把**不存在**的 package.json 加了反引号。按本仓库文档惯例，反引号包住路径即声明「仓库内有此文件」，故改的是文档措辞（改为叙述 npm 的通用行为），不是放宽校验器。
- **离线装置"多给属性"会凭空造出契约错误**：`figma-harness.mjs` 起初给**所有**节点都补上 `layoutMode: "NONE"`，但真实 Figma 里 TEXT / RECTANGLE **没有** `layoutMode`（那是 AutoLayoutMixin 的属性，只有 FRAME / COMPONENT / COMPONENT_SET / INSTANCE 才实现）。于是 `set-layout-sizing` 里 `"layoutMode" in node` 这个判断被误触发，把合法的 `FILL` 判成 `NO_AUTO_LAYOUT`。**桩"多给属性"和"少给属性"一样危险**——已改为按类型（照 mixin 划分）给属性，并加断言锁住。
- **`precheck --live` 会把"没查成"说成"查过了"**：核对里出现意外响应（超时 / 掉线 / 未领取）时，它仍然打印「外部引用全部存在于画布」。已改为：只有**零意外响应**时才敢下"全部存在"的结论，否则一律判未通过。
- **`qa-l2` 挑 Brief 用的是反向排除法**（「`example-` 开头且**不是** `.dsspec.json`」）——本轮往 `assets/examples/` 加了一份 L3 构建计划样例 `example-health.ops.json`，它立刻把这个非 Brief 当 Brief，报出「同名 Spec 缺失」。**靠「排除已知的坏东西」写的规则，会在出现未知的新东西时失效**。已改为正向形状 `^example-[^.]+\.json$`。已记入 `references/lessons.md` #36。
- **三份样例的对比度声称值有 9 条是错的（C1 上线即抓到）** —— DS Spec 的 `accessibility.contrast` 要求逐条给出对比度结论，但那些数字从来只有人写、没人算。`contrast-audit.mjs` 首次运行即发现 13 条声称值里 **9 条**与 WCAG 公式不符，偏差 0.17~1.64 且**方向不一致**（不是同一个错误公式，是根本没算）；其中 `example-saas` 的 `text.regular` 声称 `7.0:1` 实为 `6.11:1`（若按 AAA 7:1 判即翻盘）、`text.secondary` 声称 `4.2:1` 实为 `3.08:1`。更值得记的是：这个错值还被抄进了 `design-system-spec.json` 的 `contrast` 字段描述，**成了下游的「参考事实」**——文档里的数字会被当事实复用。已按复算值修正三份样例（并补上此前自相矛盾的标签：`example-highway` 的 `3.3:1` 却标 `FAIL-TEXT`，实为 `PASS-LARGE`）与 schema 描述，并从 schema 描述里明确了标签词表（`PASS` ≥4.5 / `PASS-LARGE` ≥3 / `FAIL-TEXT` <3），此前该词表无任何文档定义。
- **三份 critic-report 的「实证数字」全都有与 DS Spec 对不上的地方（C3·qa-critic 上线即抓到，同 C1 那类病）** —— ① `example-highway` 的 `page` 写 `主驾驶舱`，而它自己的 Brief 里 IA 页面叫 `养护总览驾驶舱`（Schema 明文要求 `page` 取自 `brief.informationArchitecture.pages[].name`、逐页出报告）；② 同一份报告把 `text.secondary` 写成 `#6B7A99`、卡面写成 `#10224A`、对比度写成 `3.2:1`，而 Spec 里该 token 是 `#5A7CA6`、`surface.card` 是 `#0F2547`、实算 `3.5:1`——**三个数字没一个对**（结论「不达标」倒仍成立）；③ 它引用的档位写 `4/8/12/16/24/32` 与 `14/16/20/24/32`，而大屏 preset 声明的是 `8/16/24` 与 `14/20/22/26`（抄的是 Web 后台那套），连修复建议「对齐 12px」「归档到 TY 32 档」也指向不存在的档；④ `example-health` 一条 issue 声称「12px caption 低于 `accessibility.minFontSize=14`」，而 Spec 里 caption 是 13、`minFontSize` 是 **11**——按真实数据这条 issue 根本不成立。**写在产物里的数字会被下游当事实复用，而它们从来只有人写、没人算。** 已逐一按 Spec 修正三份样例，并补上此前缺失的 `_evidence` 档位声明。
- **`qa-critic` 自己的「实测痕迹」判定太松（写变异测试时被反例抓出）** —— 照搬上游的关键词袋 `["px","#",":","%","档","级","分",…]`，其中「级」「分」会命中「不够**高级**」「大部**分**」，于是「整体观感不够高级」这种**纯观感话术**照样通过——而它正是 `critic-report.json` 明令禁止的（`suggestion` 不得写「不够高级」）。已改为判**形状**：要么带量（`#RRGGBB` / 带单位的数字），要么带明确的核对动作词（实测 / readback / 复算 / 逐节点 / 档位…），并由变异测试的反例用例钉住。已记入 `references/lessons.md` #40。
- **B2 的「插件掉线」用例在赌时序（偶发假绿）** —— 该用例原本是「停掉 mock 轮询 → 睡 300ms → 断言核对失败」，但 Bridge 判定插件在线用的是 **45s** 的 stale 窗口、mock 的在途长轮询最长 500ms，300ms 后它仍可能把停摆前的最后一条命令答掉；同一份代码连跑两次会一绿一红。已改为让 mock 在停摆后**连在途命令也不回**（把「不应答」变成确定性事实，而不是等更久），并另补一条**从未连接**的离线路径（起一台全新 Bridge，`/health` 直接说未连接 → 当场判失败、不发命令）。已记入 `references/lessons.md` #39。

- **`export-manifest.json` 里一句「描述说允许、语义上不允许」的谎（C3·qa-export 上线即抓到，并用 ajv 独立复核）** —— 该 Schema 把 `exports.png.minItems: 1` 写在**基座**，再在 `allOf[0].then` 里给 design-phase `minItems: 0`。但 JSON Schema 的 `allOf` 是**合取**，`then` 撤不掉基座的约束——净效果仍是 `minItems: 1`。于是 `description` 写着「design-phase 允许 exports.png 为空」，**语义上根本不允许**：随包的 `example-highway-export.json`（design-phase + 空 png）一直是 invalid，只是从来没人真校验过。修法：把 minItems 上限挪进 `else`，并给 `if` 补 `required: ["_meta"]`（否则缺 `_meta` 的清单会被 `properties` 的「只校验存在的键」漏洞**静默当成 design-phase** 放过——修完前者之后这个洞会变成 fail-open，必须一起堵）。另把 `tokens.color` / `tokens.typography` 的 `$ref` + `minItems` 兄弟键改成 `allOf` 写法（draft-07 规定兄弟键被忽略、ajv 等实现却生效，不该依赖这个歧义），并给三处 `properties` 补 `type`。同时修正两处**过度声称**的字段描述：`figmaJson.path` 原文写「design-phase 为 null」，但真实构建也可能只导了 PNG/SVG；`criticReport` 原文暗示路径必填，而其类型本就允许 null。已记入 `references/lessons.md` #41。
- **出口清单里 32 处「抄来的值」与其声明的 DS Spec 对不上（C3·qa-export 上线即抓到，属 C1 那一类病）** —— `export-manifest.json` 的 `tokens[].value` 是**发给前端的最终值**、`project` 是产品名，本该从 DS Spec / Brief 抄来，却从没人核对过：`example-health` 的品牌色写 `#5A5CF0`（Spec 是 `#0FB5AE`）、`brand.hover/active` 同理、`status.success` 写 `#10B981`（Spec 是 `#00B578`）、圆角写 8/12/16（Spec 是 6/8/10）、字阶 value 写 `15/21/400`（Spec 是 `14/20/400`）、`project` 多一个「 App」后缀（Schema 明文要求与 `Brief.product.name` 一致）；`example-highway` 的警告色写 `#F5C542`（Spec 是 `#FFB547`）、字阶 value 直接写成**自由文本**「14（TY-3 大屏提升档）」——这是机器读的字段，说明文字不该混进去。已写脚本**从 Spec 取值**逐一回填（32 处），避免手抄复发。
- **17 条 `source` 是「假溯源」（前缀合法但路径不存在）** —— token 映射的 `source` 写 `preset:enterprise-dashboard.visualSystem.typeScale`，五类前缀合法、看着很像，但四套 Style Preset 里**都没有 `typeScale` 这个键**（真名是 `typography`），三份清单 15 条字阶映射全中。另 `example-health` 把 radius 的来源写成 `preset:...radius`，而 Spec 明说它来自 `brief:visualSystem.radius` 的显式覆盖（Brief 覆盖 vs 预设缺省是完全不同的溯源）；`example-saas` 的 `tokens.radius.md` 同样丢掉了 Spec 里的 `geometry.cardRadius`。**前缀校验只能证明「格式对」，证明不了「指的东西存在」。** 已按各清单自己声明的 Spec 逐条回填，并在 QA9 里立起「`preset:` 源必须真解析一次路径」的硬判据。已记入 `references/lessons.md` #42/#43。
- **`example-health` 这个名字下住着两个不同项目（C3·qa-export 期间发现的**上游继承问题**，未擅自改写）** —— 核心示例 `assets/examples/example-health.json` / `.dsspec.json`（以及 `assets/examples/example-health/critic-report.json`）是 **「AI 型衣 / 美业」**：品牌紫 `#5A5CF0`、字阶 24/17/15/13/15、圆角 8/12/16、行业映射命中美业；而导出包的随包副本（原文件名前缀为 health-design-，**1.2 收尾已统一改为 healthcare-**）是 **「AI 智能健康管理 / 医疗」**：青绿 `#0FB5AE`、字阶 26/16/14/13/16、圆角 6/8/10。两边各自内部自洽，所以单份校验全绿——只有把它们放在一起比才暴露。佐证：`assets/examples/example-health/critic-report.json` 的 `_meta.inputs` 自己写着依据是「AI **医疗** App 真实 Critic Loop 记录（同型问题）」，即核心示例是按照真实医疗项目的记录**重建**成美业的。**本轮只做两件事**：① 把文档里说错的地方改对（`SKILL.md` L4 段原写 `health = 消费健康 App`、`README.md` 原写示例是「医疗 App」）；② 在 `SKILL.md` 的 few-shot 与 `README` 里显式标注同名不同物，并记入 `references/lessons.md` #44。**重命名或对齐哪一边属于数据创作决策，留给 D2 之后定，不各按各的改——那只是把分裂固化。** ✅ **1.2 收尾已闭环**：经用户裁决采用**最窄的一种** —— 只给**导出包随包副本改名**（清单改为 `assets/examples/export/example-healthcare-export.json`，随包 brief / spec / build-log 三个文件的前缀 health-design- 与 health-build- 一并改为 healthcare-，含清单内 5 处交叉引用与 spec 的 `_source` 叙述），**核心示例 `example-health.*` 保持不动**（它是全链路 few-shot 主样例，8 个工具与全部文档都依赖它）。改后 `qa-export` 实测 **935 PASS / 0 FAIL** 未变。判据与取舍见 `references/lessons.md` #44。
- **`SKILL.md` L4 段把 `health` 示例描述成「消费健康 App」（C3·qa-export 期间发现）** —— 该行指向 `assets/examples/example-health/critic-report.json`，而那份报告的 `project` 是「AI 型衣」、其 issue 讲的是 `BookingFlow 预约按钮` / `StyleGalleryCard 热度数字`（美业），与「消费健康」不符。已改为「美业 AI 试发 App『AI 型衣』」，并注明与 L5 随包副本不同源。
- **canonical 文档把 OFFLINE 提示截短了（C3·qa-install 上线即抓到）** —— `references/runtime-capability.md` §3 的 Capability Matrix 把 OFFLINE 模式的「用户可见提示」写成 `未检测到 Figma 连接能力，仅可生成设计 asset`，而探针实际输出的是 `未检测到 Figma 连接能力，仅可生成设计资产（Brief / DS Spec / Build Plan）`——文档比真实提示少了一个尾注，用户照着文档核对会以为探针有问题。**这类缺陷用「关键词是否出现」查不出来**（两串都含关键词，上游脚本正是这么查的、所以一直是绿的），只有拿实跑输出逐字比才现形。已按实跑值改齐（`qa-install` 的 QA6 由此立起「引号住的提示语必须与实跑逐字一致」的判据）。
- **`SETUP.md` 把 token 文件说成「运行目录」（C3·qa-install 期间发现）** —— bridge 用 `path.resolve(__dirname, "..")` 取根目录、token 落在**仓库目录**的 `.vibe/token`，与 cwd 无关；而 `references/bridge-ops.md` 写的是 `<repo>/.vibe/token`。两份文档对同一个文件给了不同锚点，照 `SETUP.md` 从别处启动会得到错误预期。已改为「仓库目录（与你在哪个目录敲命令无关）」，并在 QA7 立起判据：凡提到 `.vibe/token` 的那一行必须把它绑定到仓库而非 cwd。
- **`qa-install` 自己的三处缺陷（写变异测试时全部被抓出，无一靠肉眼发现）** —— ① **缺文件时崩成堆栈**：第一版直接 `read("SETUP.md").match(...)`，SETUP.md 一被删就 ENOENT 崩掉——**它连它要抓的那个缺陷（缺文件）都报不出来**，用户只看到一段栈而真正的问题没被说出口。已改为所有读取容缺、缺文件本身交给 QA1 如实报出（并记入 `references/lessons.md` #45）。② **`includes(名字)` 被注释喂饱**：`includes("figma-developer-mcp")` 会被探针头部注释里的同一串字满足——把真正的判定分支删掉，检查照样绿；`includes("::1")` 同样被注释里的 `::1` 满足。已改为判**形状**（断言落在真实的 `haystack.includes("x")` / `const LOOPBACK_V6 = "::1"` + `.listen(PORT, LOOPBACK_V6)` 上），且 MCP 期望名单改为**从 canonical 文档派生**而非写死（记入 #46）。③ **降级开关「连坐」**：`--no-behavior` 本意只跳过要起进程的检查，但提示语复算的**真值**来自那次实跑，于是被一并跳过——**最值钱的检查在降级模式下静默消失**，而汇总里四组「装置缺口」看着还挺诚实。已把提示语真值改为轻量恒跑（只跑探针两次，不起 bridge/mock，记入 #47）。
- **一条「一直绿、却从来没测到东西」的用例（同上，由变异测试抓出）** —— QA3 的「bridge 可达但插件未连 → 读写皆 false」用例一直是绿的，但把探针的 `figmaWrite` 改成 `bridge.reachable` 之后**它还是绿的**。根因是装置把 `url` 返回成了**含 `/health` 的完整 URL**，探针自己去拼 `${BRIDGE_URL}/health` 得到 `/health/health` → 404 → 一路退化成「没有 bridge」，于是这条用例在「连不上」的假象里通过，**从来没真的走到那个分支**（真正去连真 bridge 的只有 QA3 的 `/health` 形状三条断言）。已改为返回 base URL；修完之后该变异立刻变红。**判据：变异之后仍然绿的用例，比变异之后变红的用例更值得看**（记入 `references/lessons.md` #48）。

- **D2 执行手册** —— 新增 `references/d2-calibration.md`。D1 定义了「什么叫通过」，但没定义「怎么把通过的过程记成可比的数据」，D2 两次实测若只凭临场发挥，产出的两行结论无法横向比较。手册给出：**环境前置**（必须真 `FULL_MODE`，双 `true` 才成立——**不降档跑一遍交差**）· **两次选题原则**（行业与复杂度两个维度都要拉开，禁两次同类）· **产物目录约定**（两次产物不可混放，`qa-l2`/`qa-export` 会扫同名）· **逐闸门记录字段**（每道闸门除汇总行外各自额外要记的量）· **预检收益的对照实验**（错计划 vs 修好的计划各跑一次，用 `failedAt.step` 算出「若不预检会在第几步炸」）· **手工成本记账五项指标 + 事先定好的阈值判据**（把 D1 §8「取决于 D2」这句悬着的话换成可判的结论）· **翻车点归因四档**（三档归因 + 新增「本轮新暴露的隐式知识」，后者正是对 `1.2-plan.md` §2.1 主线判断的验证）· 运行记录表模板与回写清单。**核心立场写进开篇：翻车点是数据，不是失败**——若两次跑完 `lessons.md` 一条未增，必须在验收记录里明说这既是「债已还清」也是「主线判断未被验证」，不许合并成一句「通过」。

- **「token 在启动时打印，也存在 `.vibe/token`」是过度声称（真实环境实测发现）** —— 该说法把「默认路径下成立」写成了无条件成立。`loadOrCreateToken()` 实有四条返回路径，**只有最后一条会写 `.vibe/token`**：`--token <值>` 与 `VIBE_TOKEN` 都是直接返回、**不落盘**。于是带 `--token` 启动时文件里是上一次的旧值——**照文档取值必然 401**（2026-09-16 实测踩到：探针报 FULL_MODE，但 `.vibe/token` 里是 14:02 的旧值，与在跑的 bridge 不一致）。设计上没错（`--token`/`VIBE_TOKEN` 正是为测试与临时隔离实例准备的，各 QA 脚本都这么起，**以免污染用户的 `.vibe/token`**），**错在文档**。按本仓库规矩改文档、不改实现：`SETUP.md` 与 `references/bridge-ops.md` 均改为「**以启动日志打印的那一行为准**」并写明该边界。记入 `lessons.md` #49。
- **QA7 补 4 条断言（96 → 100）** —— 上一条为何能潜伏：QA7 的「token 跨重启保持稳定」只跑了**默认路径**（起两次比对是否一致），从没验证过 `--token` 分支；而 `qa-bridge.mjs` 恰好是用 `--token` 起的、却不检查文件——**两处各自看都对，合起来才暴露**。新增：① 带 `--token` 启动**不得**把该值写入 `.vibe/token`；② 该启动不得破坏已存在的 token 文件；③ 提到 token 取值的文档都写明「以启动日志为准」（**纯静态检查，恒跑**）；④ 装置自检 —— `<TMP>/bridge/server.js` 副本必须存在。同时把原「绑定到仓库目录而非 cwd」的判定从**判行**改为**判形状**（只查真正在陈述 token 文件位置的行），否则「`--token` 不写入 `.vibe/token`」这类讨论落盘行为的句子会被误伤。
- **写补丁时自己踩了三个坑，全部由变异测试抓出**（值得记的是第 2、3 条：**修正过程本身就在复现 lessons 里已有的教训**）：
  ① 把新断言错放进 `if (!NO_BEHAVIOR)` —— 而 `expectCase` 默认就带 `--no-behavior`，于是断言被静默跳过（这是 #47「降级开关连坐」的同一个坑，**我隔一章又踩了一次**）。文档一致性检查已改为恒跑。
  ② 行为函数把 bridge 脚本路径写成 `<TMP>/skill/bridge/server.js`，而夹具实际放在 `<TMP>/bridge/` → **脚本不存在 → 进程起不来 → 返回 `null` → 走 `gap()` 静默跳过 → 断言一条没跑，汇总却全绿**（#48 的同一个坑）。已加装置自检断言，让「夹具路径错了」判 FAIL 而不是混成「装置缺口」。
  ③ 修正前后断言**都是绿的**（`res.value !== dictToken` 在读到 `null` 时也恒真）——差别只在消息里：修前打「实际 文件不存在」，修后打「实际 "fd64bb91…"」。**"读到空值"与"读到真实值"在断言里长得一样，只有把值打进消息才能分辨**（已立为 #51）。
  配套变异用例 **78 → 82**（新增：`--token` 路径也落盘 → 必须被抓；删掉文档告警 → 必须被抓）。

### 待办

- ~~D2 两次实测校准~~ ✅ **已完成（运行 A 2026-09-16/17 · 运行 B 2026-09-17）** —— 手册 `references/d2-calibration.md`；**逐闸门留痕（汇总行 + 退出码）见该文件 §5.1 / §5.2**。D1 §8 待定强化项的结论已下：**立项形态 = 「命令接入点对齐」，不做薄编排器**（见本文运行 B 一节与 `references/acceptance-criteria.md` §8.2）。
- ~~**待决策**：`example-health` 同名两物（美业「AI 型衣」vs 医疗「AI 智能健康管理」）~~ ✅ **已裁决（2026-09-17）**：采用**最窄方案** —— 只给导出包随包副本改名（清单改为 `assets/examples/export/example-healthcare-export.json`，随包三个文件的前缀 health-design- 与 health-build- 统一改为 healthcare-），**核心示例 `example-health.*` 保持不动**。理由与实测见 `references/lessons.md` #44。
- `example-health` 的 `critic-report.json` 已按核心示例（美业）对齐；若后续决定以医疗为准，该报告与其 `_meta.inputs` 需一并重做。

---

## [1.2.0 · D2 运行 A] — 2026-09-17

**选题**：iOS 记账 App（用户点名「适配 iOS 的液态玻璃 / 毛玻璃」）。**档位**：`FULL_MODE`。
**产物目录**：`.vibe/d2-run-a/`。**完整记录**：`.vibe/d2-run-a/RUN-RECORD.md`。

**结论一句话**：链路走通了（L2 零 FAIL → L3 210 ops → L4 两轮收敛 8.0→8.7 PASS → L5 363 PASS / 1 FAIL），
但**冒出 5 条新的隐式知识**——即 `1.2-plan.md` §2.1「问题全是同一类——隐式知识」的主线判断
**在本轮得到验证**（若一条不增反而该质疑该判断）。

### 收尾补齐：L5 装置缺口闭合 + 预检对照实验

运行 A 结束后用户提供了 Figma 文件链接，两条原本记「未核对 / 装置缺口」的项得以在**真实环境**补做。

**① `figmaFileKey` 回填 → L5 Export Gate 从「363 PASS / 1 FAIL」转全绿（364 PASS / 0 FAIL）**

回填前做了**两步独立验证**，没有因为「URL 里有串字符」就直接抄进去（lesson #30「不要猜」）：
插件 `ping` 返回 `pageId 0:1`，与链接里 `node-id=0-1` 对应，确认链接指向的就是本轮这张画布；
再 `get-node` 回读两个 `rootNodeIds`，均真实存在且几何完整。

顺带修了 `export-manifest.json` 的 `_meta.note`——它还写着「figmaFileKey 为 null：……这不是缺字段，
而是本运行模式的事实」。字段回填后这段描述**变成了谎**，正是 lesson #41「描述说允许、语义上不允许」
的同一形态（**产物里的散文描述也会过期，回填字段时必须一起改**）。

**② `precheck --live` 补跑 → 全绿；并暴露出该闸门的一条重要边界**

3 个含字面 id 引用（`parentId=7:353`）的批次逐批核对通过。但实测发现：
**`--live` 只核对字面 id 引用。** 全部走 `$name` 的批次跑 `--live` 时输出是
「没有字面 id 引用需要核对」——闸门绿了，**其实什么都没核对**。
故 `--live` 的绿 ≠「引用都没问题」，只等于「字面 id 引用都没问题」；
`$name` 引用链的完整性归**离线** precheck 管。**两条闸门分工，任一单独跑都不足。**

**③ 预检收益对照实验（手册 §2.2 点名要的那个数）**

运行期没留住错误版本的计划，故以运行目录下的一批真实计划（20 步）为底**构造**对照，注入 4 类
**本轮真实犯过或近亲**的错误 —— **全部被一次报出**（9 条离线错误，非 fail-fast 只报第一条），
每条都带**步号 + 操作名 + 字段级说明**：

| 注入位置 | 错误内容 | 报在第几步 |
|---|---|---|
| step 3 | 真实字面 id `7:353` 错加 `$` 前缀 → `$7:353` | **step 3** |
| step 8 | `BACKGROUND_BLUR` 多写 `color`（上游 P0 事故形态） | **step 8** |
| step 12 | 引用不存在的名字 `$nosuchname` | **step 12** |
| step 16 | `create-text` 缺必填 `characters` | **step 16** |

这直接回答「预检省了多少无效绘制」：step 3 那个 `$` 前缀错误若不预检，
会在 `run` 执行到该步时抛 `unknown batch reference "$7:353"`——而 `run` 是 fail-fast 的，
届时画布上已躺了前 2 步的成果，且**整批 20 步全部作废**。

**④ 但对照实验也暴露了预检的能力边界（本轮最有信息量的副产物，已立为 lessons #62）**

把**原始未拆分**的那批计划（**117 ops**，正是运行期触发 Bridge chunk 断裂、
`$name` 断链、返 `executed=0` 的那一批）喂给 precheck：

```
117 步：通过 117 ｜ 错误 0 ｜ 装置缺口 0 ｜ 待核对 0
结果：通过 —— 结构与契约错误 0（PRECHECK OK）
```

**静态完全干净，却在运行期炸了。** precheck 校验的是「计划本身对不对」，
校验不了「这个计划以这个大小发出去会怎样」——「批太大 → Bridge 强拆 chunk →
`$name` 命名空间断裂」**不是计划里的错误，是传输层的约束**，而 precheck 的输入世界里没有「传输」这一维。
**别把「precheck 全绿」误当成「可以整批发车」**，批大小必须靠纪律 + 显式告警守。

> 另记一条收尾时踩的小坑（lessons #51 的同族形态）：`get-node` 的回包信封是 `data` **本身**，
> 不是 `data.node`。我第一版解析脚本按 `data.node` 取，得到 `KeyError` **并一度以为根节点不存在**。
> **「读到空值」与「真的不存在」在断言里长得一样**——处置是去看原始回包形状，而不是顺着报错怀疑画布。

### 实测新增的隐式知识（已回写 `references/lessons.md`）

- **#57 效果类参数需要一个「非常量」的输入** —— 毛玻璃做在纯色底上，`BACKGROUND_BLUR` 回读正确、
  结构校验全绿，**导出图却毫无玻璃感**（模糊一块纯色 = 那块纯色）。补 `Bg/Scene` 背景场景层后
  A/B 像素对照才成立（卡内 `#E8E8F9` vs 空底 `#F4F4F8`）。与 #56 是同一根因的两个面。
- **#58 超时是残缺的回执；重挂类 op 不幂等** —— 10 个 `append-child` 塞一个 `run` 撞超时，
  重试时组件**已从画布消失**，只剩空帧，两侧都不报错。正确顺序改为「先 `history` + 回读现状，再决定重发」。
- **#59 `move-node` ≠ `append-child`**（名字都像「把 A 放进 B」）—— 前者**只改 x/y 不换父级**，
  后者换父级但**重算坐标**；误用导致总览帧导出为 100% 空白的 PNG，而结构回读对这类错误全盲。
- **#60 `clips` 是仅创建期可写字段**（`clipsContent`）—— 事后无任何 op 能补，L3 计划阶段必须定稿。
- **#61 闸门可以比环境能力更严** —— L5 唯一 FAIL 是 `figmaFileKey` 缺失（插件 `ping` 不返回该字段，
  且 `figma-plugin/` 处于冻结区不可现场改）。**闸门规则没错，环境缺一个字段**；
  正确动作是「如实记为装置缺口」，不是降档或改软判据。（收尾时用户提供链接，缺口已闭合。）
- **#62 静态预检看不见「传输层约束」** —— 117 ops 的原始大批 precheck **0 错通过**，
  运行期却因 Bridge 强拆 chunk 导致 `$name` 断链、返 `executed=0`。
  **预检的输入世界里没有「传输」这一维**：它验计划本身对不对，验不了「以这个大小发出去会怎样」。
  批大小必须靠纪律 + 显式告警守，别把「precheck 全绿」误当成「可以整批发车」。

### 闸门留痕（汇总行）

| 闸门 | 结果 |
|---|---|
| `runtime-check.mjs` | `FULL_MODE`（figmaRead/Write 双 true） |
| `qa-plugin.mjs` | 全绿（写通道含 `BACKGROUND_BLUR` 可用） |
| `qa-l2.mjs` | **14 PASS / 0 FAIL** |
| `contrast-audit.mjs` | OK（11 条核对，**声称值 vs 复算值 0 条不符**） |
| `precheck.mjs`（离线） | 14 批逐批预检，**0 错发车** |
| `precheck.mjs --live` | **未核对**（超时分段构建流程里无自然插入点） |
| `qa-critic.mjs` | Round1 **8.0 FIX** → Round2 **8.7 PASS**（QA6 拦下过「action 与分数不自洽」的写法） |
| `layout-audit.mjs` | **未核对**（`get-node` 的 `depth` 会剥掉几何字段，工具吃不下；改用自建的页面几何回读脚本顶替） |
| `qa-export.mjs` | 首跑 **363 PASS / 1 FAIL**（`figmaFileKey` 缺）→ **用户补链接后 364 PASS / 0 FAIL，Gate 全绿** |
| `check-refs.mjs` | 运行 A 期间漏跑，**收尾时补跑**：**3 FAIL**（均为本轮新增文档引入的悬空引用，已修，见下）→ 归零 |
| `precheck.mjs --live` | 运行期间漏跑（分段构建无「完整计划」输入），**收尾补跑含字面 id 的 3 批，全绿** |

### D1 §8 待定强化项（薄编排器）—— 结出结论

手册 §3 事先定好判据：**任一次漏跑 → 立项**。本轮**漏跑 3 条**（`precheck --live` / `layout-audit` / `check-refs`），
按字面即**命中立项**。但要如实补一句：**漏跑原因不是「命令多到记不住」，而是「我不知道该跑它们」**
——`layout-audit` 需要的 readback 形状与 Bridge 实际返回对不上；`precheck --live` 在分段构建里没有插入点。
所以立项形态应改为：**先修这三条命令的接入点**（例如让 `layout-audit` 直接吃 `get-node` 输出、让 `precheck --live` 可接受增量计划），
而不是做一个「一次性跑完 10 条命令」的薄编排器。**D1 §8 的「取决于 D2」至此改为：立项，但范围重定义为「命令接入点对齐」，归入 1.3。**

### 本轮同步的文档修订

- `references/bridge-ops.md` —— §3 **新增 3.0 超时语义**（超时后的正确顺序三步 + 批大小按「最贵的 op」估）；
  §3.1 补「字面 id 不得加 `$`」「`run` 的 `data.ops` vs `/v1/batch` 的 `data.results` 信封差异 + 条数校验」；
  §5.2 补 `clips` 的仅创建期警告；§5.3 补 `move-node` / `append-child` 语义对照表。
- `references/lessons.md` —— 新增 **#55–#63**（9 条），守卫覆盖表新增「效果类参数可见性」一行（标为 ⚠️ 临时装置待收编）。
- **`check-refs.mjs` 在收尾时抓到 3 条悬空引用——全部是本轮写文档时新引入的**（这正是把「漏跑」
  补回来的价值：该闸门在运行 A 期间没跑，一跑就抓到自己刚犯的错）：
  ① 本文档把「页面几何回读脚本」写成了不存在的文件名（它只是运行期临时脚本，
     **未曾随包发布**，已改为叙述性描述）；
  ② `references/lessons.md` #56·#57 提到的像素验证装置确实存在于运行目录 `.vibe/d2-run-a/`，
     但它属**运行时产物**、不随包发布——已在文中改写成显式路径以避免歧义**而非**给校验器开豁免；
  ③ 守卫覆盖表里我预写的「待收编」目标文件尚不存在，已改为叙述性措辞、
     不再把尚未存在的路径写成反引号形式。
  **修的是文档写法，不是放宽校验器**——这是 `references/lessons.md` #54 的同一条纪律
  （「新加的检查让旧样例变红时，先怀疑样例，别急着豁免」）。修完后复跑：**悬空 0**。

> 本版本尚未发布，完成后补齐具体条目。

---

## [1.2.0 · D2 运行 B] — 2026-09-17

**选题**：大屏驾驶舱（充电站运营态势屏），用户选定 **3840×2160**、规模档 **适中（200–300 ops）**。
与运行 A（iOS 记账 App，210 ops，移动端单列布局）在**行业**与**复杂度**两个维度均拉开，
满足 `references/d2-calibration.md` §1.2 的选型约束。**档位**：`FULL_MODE`。
**产物目录**：`.vibe/d2-run-b/`。**完整记录**：`.vibe/d2-run-b/RUN-RECORD-B.md`。

**结论一句话**：链路**第二次走通**（L2 14 PASS → L3 **388 ops / 26 批** → L4 审计 **21 → 1 条**、
另抓出并修复 **5 条真缺陷** → 14/14 组件 362 子节点零失配），
且**新增 8 条隐式知识（#64–#71）**——`1.2-plan.md` §2.1 的主线判断**第二次得到验证**。

### 本轮最有价值的一件事：`layout-audit` 全绿 ≠ 结构正确

初版记录：**21 条告警全部是假阳性，而 3 条真实缺陷一条都没被报出来。** 两个方向都错了。

**收编后的定论**：这两个方向的错**同源** —— 判定所依赖的**前提没有写出来**。
补上两条前提（`localSameSpace` 共享坐标系、`bandByCross`+`isDataDrivenSpacing` 等距数据标注），
20 条假阳性归因清楚、`spacing` 降到 1 条；同时新增 `overlap`/`duplicate` 两个检查后，
工具**反过来抓出了 5 条真缺陷**（字号系统性越档、触摸区不足、2 处文字重叠、1 处间距越档）。

**本次最刺痛的一条**：从留存的旧报告 `.vibe/d2-run-b/layout-audit.json` 里读出 `minTouch: 0` ——
**上一轮把触控红线关掉了**。所以旧报告里的「touch 0 条」不是"合格"，是"**这条检查没在跑**"。
同一份参数里 `baseline: null`（基准检查也没跑）、`font:[…,16,…]`（字阶被放宽到含 16，
于是 16px 的系统性偏差顺利通过）。**「闸门全绿」实际是在三条检查缺席或放宽的情况下取得的。**
→ 处置：四个留痕段（`unchecked`/`skipped`/`dataDrivenSpacing`/`inputCoverage`）已补进工具 ——
**让"没看"在产物里是可见的**，而不是靠人记住自己传了什么参数。

假阳性来源已定死：17 条是**二维网格被单轴排序**造成的「伪相邻」——`Cell/12 (830,44)` 与
`Cell/13 (16,98)` 被排到一起判间距，实际两者**既不同行也不同列**，报出来的「间距」其实是**行距/列距**；
另 4 条是图表时间轴刻度与网格线（间隔由「绘图区 ÷ 刻度数」决定）。
**判据越机械，越要问「这个判据的隐含前提在这份数据上成立吗」**——
`spacing` 的前提是「列表是一维的」，热力图/图表不满足。

真缺陷则全部落在**读图才能看见**的地方（导出 PNG 后逐张看）：
① **TitleBar 右侧 71px 重叠** —— 时间戳 `2026-09-17 01:12:44` 占 x=[1560,1791]，
与在线状态簇（`OnlineDot` x=1720 / `系统在线` x=[1738,1802]）压在一起；
② **UtilizationHeatmap 有 168 个格子却没有一根轴标** —— 纯可读性缺陷，几何上完全合法；
③ **TrendChart 9 个轴标签被重复创建**（像素级重合），结构上多 9 个子节点。

已记入 `references/lessons.md` #67（`layout-audit` 三类盲区），并**如实定性为已知缺陷**
而非「误报」：`spacing` 对 `gap < -TOL`（重叠）直接 `continue` 并注释「另由 overflow 反映」，
而 `overflow` 只在子节点**逃出父级**时才触发——**兄弟 vs 兄弟的重叠两边都不管**。

> **收编后这三条各自的结局**（本轮已全部处置）：
> ① **已由工具抓出** —— TitleBar 的 71px 是**早期**版本，后来修过一次；收编后 `overlap` 检查
>    又抓出同组件**另一处** 2px 重叠（主/副标题），说明这类缺陷是成串出现的，不是孤例。
> ② **仍是盲区** —— 语义性缺失（"该有的东西没有"）**数值审计结构性看不见**，只能靠读图。
>    已写进 `acceptance-criteria.md` §8.2 作为"闸门绿 ≠ 结构正确"的根因。
> ③ **已由工具抓出** —— `duplicate` 检查在真实数据上抓到 **21 组**像素重合兄弟（不只 9 个）。

### 本轮抓出的错误（按归因四档）

| # | 现象 | 归因 | 处置 |
|---|---|---|---|
| 9 | `4b0`/`5c0`/`5d0` **稳定**卡在第 3 步，`executed=0` | **结构错误（自己的代码）** | 行生成器把批内**变量名** `"rl"` 当字面 id 传（没加 `$`），插件视为不存在的 id。修 `isLiteralId` 判定后三批**一次通过** |
| 10 | `3b` 报 `unknown batch reference "$mp"` | **契约错误** | `$name` 命名空间**批内局部**，跨批必须用字面 id（`^\d+:\d+$`） |
| 11 | 12 次 `/v1/batch` 返 `executed=0/remaining=N`，画布却实际多了 0–6 节点 | **装置缺口** | **插件会话退化态**（见下节）；其中 3 次已归因到 #9 |
| 12 | `collect-readback` 报 `NODE_NOT_FOUND: 13:794` | **结构错误** | 运行目录下的 id 登记表是**只追加**的，重跑批次留下死 id 与活 id 混在一起；改为**从画布实时发现**根节点 |
| 13 | TrendChart 25 个子节点（应为 16） | **结构错误** | `batch2b1` 重跑未带 `cleanPrevious`，而 `run` **不幂等**；删 9 个重复 |
| 14 | 删除重复后**导出 PNG 的 md5 一字未变** | **隐式知识** | 像素一致**不能**证明结构正确（重合的重复节点改不动一个像素） |
| 15 | Heatmap 168 格无轴标 | **隐式知识** | 语义性缺失在数值审计里**不可见**，需靠「产物是否可读」这层判据 |
| 16 | `5bx` 前两次部分成功（6/12），重试仍卡 | **装置缺口** | 退化态下**整批重试**无效，**读回后按缺口增量补齐 6 步**却一次成功 |
| 17 | 左侧残留 `0:00` / `日` 重复 | **结构错误** | 部分回执造成的半成品；按 id 删除 |
| 18 | 探针垃圾节点 13 个（`__probe_*` / 裸 `Frame` / `Row/1` / 裸数字） | **结构错误** | 清理；**刻意保留 `7:542`**（属运行 A 的记账画布，不越界删别人的东西） |

### 插件退化态：一次系统性定位（写入 `lessons.md` #65）

「6 步卡死」先后出现 12 次。跑了 **7 组隔离实验**排除各种猜想：

| 实验 | 结果 |
|---|---|
| op 数量 | **不是原因**——2–12 个纯 `create-frame` 全部 27–75ms 成功 |
| 内容 | **不是原因**——8 个中文 / 1 个中文 / 10 个拉丁 / 10 个数字**失败形态完全相同** |
| op 类型 | `create-rect` n=10 → 106ms **全部落图**；`create-frame` n=10 → 102ms **全部落图** |
| **同脚本内对照** | `create-text` n=10 → **15080ms，恰好落 6 个**；同一脚本里 `create-text` n=20 → **1924ms，20 个全落** |
| 等待 | **0 / 3 / 10 / 30 秒均不恢复** |
| 回读核对 | 与回执一致 |

结论：**状态相关，非输入相关。** 同一份代码在同一进程里，前后两次行为可以完全不同。
由此立的可执行纪律是：**中文 `create-text` 批控制在 ≤24 op，但真正的守卫不是固定的 op 上限，
而是「读回 → 按缺口增量补齐」**（#16 已证明整批重试在退化态下无效、增量补齐有效）。
`5b0` 涨到 38 op 时命中退化态，故拆出独立的 `5bx` 批。

### 实测新增的隐式知识（已回写 `references/lessons.md`）

- **#64 「多批同一形态地失败」时先查自己的代码** —— `4b0`/`5c0`/`5d0` 三批形态一致地卡在第 3 步，
  一度被归为环境问题（与 #11 混在一起）。**稳定复现的失败点是 bug，漂移的失败点才是环境**——
  这条判据把一次可能持续很久的误诊断当场收口。
- **#65 插件会话退化态**（含上表 7 组隔离实验数据）—— 见上。
- **#66 二维网格单轴排序产出假阳性** —— `layout-audit` 的 `spacing` 对二维网格失效的机理与实例。
- **#67 `layout-audit` 三类盲区** —— ① **兄弟重叠**（`spacing` 跳过负数间距、`overflow` 只管父子）；
  ② **语义性缺失**（无轴标、无单位、无空态提示，几何上完全合法）；
  ③ **跨轴长度不等即跳过**（`alignment` 要求 `Math.abs(prev[ax.cross]-cur[ax.cross]) <= TOL`，
  于是 27 高的行与 21 高的行配对比被静默跳过）。**三类的共同点：判据本身都成立，但它们各自带一个
  从没被写下来的前提**——这正是「隐式知识」的教科书样本。
- **#68 「像素一致」不能证明「结构正确」** —— 删除 9 个重合的重复节点，导出图 md5 与字节数
  （`eb35355a…`, 32539B）**完全未变**。**用像素一致性当结构正确性的代理会系统性漏掉重合类缺陷。**
- **#69 `cleanPrevious` + 超时 = 净损失** —— 「先清后建」在退化态下会清完就卡住，
  **净结果是比不清更糟**（从「有旧内容」变成「什么都没有」）。
- **#70 回读阶段真相源只能是画布** —— 一切本地记账（回执、id 文件、脚本内变量）都是**可能过期**的缓存；
  本项目已因信本地记账而错两次（#12 / #13）。
- **#71 本地 id 登记文件会失效** —— 具体到运行目录下那份 id 登记表：只追加的登记表在**重跑**场景下必然同时含
  死 id 与活 id，且无法从文件本身区分。**这类文件不该当真相源**（同 #70）。

### 基线问题 —— ✅ 已裁决（2026-09-17）

用户选定的画布是 **3840×2160**，但规则库把**大屏的设计基准定义为 1920×1080**，
3840×2160 是它的 **@2x 出图倍率**（`references/design-system.md` 大屏布局模板）。两者不是同一个东西，而**影响的是 L2**：

- 若 3840×2160 是**倍率** → **现状正确**，无需改动；
- 若它是**真实物理分辨率、不做缩放**（如 LED 拼接屏按 1:1 出图）→ **L2 的 `typography` 与 `spacing` 需整体 ×2 重推**，本轮全部字号/间距都要改。

**裁决：1920×1080 是设计基准，3840×2160 只是 @2x 出图倍率、不进 `frameBaseline`** ——
已锁进 `references/design-system.md` **LD-10**。三条独立依据：① 倍率与设计尺寸是两层
（同 iOS @2x / CSS `devicePixelRatio`）；② 规则库所有大屏数字都定义在 1920 上
（`TitleBar 8%`=86 由 `GEO-1`「1080×8%」算出；正文 ≥14px 是 3–10m 视距红线），
在 3840 上重推会**整体失效**；③ ×2 会背离 `dense` 与「图表 ≥50%」的意图。
**例外**已单独划出：LED 拼接屏按点对点 1:1 出图时，`typography` / `spacing` 必须整体重推 ——
那是一次**规则切换**，不是同一条规则的参数调整。

### 闸门留痕（汇总行）

| 闸门 | 结果 |
|---|---|
| `runtime-check.mjs` | `FULL_MODE`（figmaRead/Write 双 true） |
| `qa-l2.mjs` | **14 PASS / 0 FAIL** |
| `contrast-audit.mjs` | OK（**声称值 vs 复算值 0 条不符**） |
| `precheck.mjs`（离线） | **26 批逐批预检，0 错发车** |
| `layout-audit.mjs`（**收编后重跑**） | **1 条（0 high / 1 medium）**：AlertTicker 两端对齐（见下「保留项」）。**收编前为 21 条**，其中 20 条经证明是等距数据标注的假阳性；新增 `overlap`/`duplicate` 后另抓出 **5 条真缺陷**（已全修）。`padding`/`itemSpacing` 经探测为**输入不具备**（`unchecked` 留痕） |
| `layout-audit-mutation.mjs`（**本轮新增**） | **51 PASS / 0 FAIL**（22 类：能抓错 10 + 不误报 7 + 留痕 5） |
| 结构核对（自建） | **14/14 组件、362 子节点、0 失配、0 完全重合兄弟** |
| `precheck.mjs --live` | **未核对**（分段构建无完整计划可提交，与运行 A 同因） |
| `qa-export.mjs` | **未核对 → 收尾补做**（当轮 L5 只做逐组件 PNG 导出、未生成 manifest）→ `409 PASS / 1 FAIL`，唯一 FAIL 为 `QA3 figmaFileKey`，**属环境**，见上文「定版后补做 ②」 |
| `qa-critic.mjs` | **未核对 → 收尾补做**（当轮 L4 由 `layout-audit` + **人工读图**承担）→ `45 PASS / 0 FAIL / 0 WARN`，**已核对通过** |
| `check-refs.mjs` | **未核对 → 收尾补跑**（见下） |

**与运行 A 的对照（两次实测的横向对比，供 D1 §8 判据使用）**：

| 维度 | 运行 A | 运行 B |
|---|---|---|
| 行业 / 形态 | iOS 记账 App（移动端单列） | 大屏驾驶舱（桌面二维网格） |
| ops / 批数 | 210 / 14 | **388 / 26** |
| L2 | 14 PASS | 14 PASS |
| L4 结论（收编后） | 两轮 Critic 收敛 8.0 → 8.7 PASS | 审计 **21 → 1 条**（20 条假阳性归因为数据驱动）；工具**新抓出 5 条真缺陷**；**语义性缺失仍是盲区**（1 条靠读图） |
| L5 | Export Gate 全绿（364 PASS） | 未跑（改走逐组件 PNG 导出） |
| 新增隐式知识 | **9 条**（#55–#63） | **8 条**（#64–#71）＋收编 **2 条**（#72–#73） |
| 漏跑闸门 | 3 条 | **4 条**（+ `qa-export` / `qa-critic`） |
| 翻车点中最耗时的一项 | 效果类参数不可见（#57） | **插件退化态**（#65，12 次，占本轮绝大部分时间） |
| **闸门参数与规格是否同源** | 同源 | **⚠️ 否**：上一轮 `--min-touch 0` + `--baseline` 缺省 + `--font` 含 16，**三条检查实际缺席/放宽**（本轮修正） |

### 收编结果（两条临时装置 → 随包发布的 `tools/layout-audit.mjs`）

用户裁决「**收编**」，两条装置已并入 `tools/layout-audit.mjs`，**并附两条必要前提**
（前提不写出来它们就是错报机器 —— 这正是本轮的主教训）：

| 收编内容 | 本质 | 在真实数据上的效果 |
|---|---|---|
| `overlap` 检查（**限 TEXT×TEXT**） | 补「兄弟重叠」盲区 | 256 条假阳性 → **2 条，全真**（TitleBar 2px、GaugeChart 2px） |
| `duplicate` 检查 | 补「像素级重合」盲区 | 抓出 **21 组**重复兄弟 |
| **前提 ①** `localSameSpace` | 子节点须真共享坐标系 | 293→213 条假阳性被消（合成页根场景） |
| **前提 ②** `bandByCross` + `isDataDrivenSpacing` | 先分带、再按形态聚类、才判等距 | `spacing` **21 → 1** |
| **留痕 4 段**：`unchecked` / `skipped` / `dataDrivenSpacing` / `inputCoverage` | 让「没看」在产物里可见 | 查出 `padding`/`itemSpacing` 是**死检查**；旧报告 `minTouch:0` 的假阴性被暴露 |
| **配套变异测试** `layout-audit-mutation.mjs` | 本仓库惯例（每个校验器一个） | **51 PASS / 0 FAIL**；**在写测试的过程中抓出真缺陷** |

> **收编不是"把临时脚本搬进 tools/"，是"把它依赖的前提一起搬"**。
> 两个装置之所以在两个方向上都错过（假阳性 + 漏报），根因相同：
> **判定所依赖的前提没有写出来**。前提写进代码与文档，工具才从"错报机器"变成"可用的尺子"。

### 本轮另外查出并修复的真缺陷（5 条，全部来自收编后的工具）

| # | 缺陷 | 修法 | 严重度 |
|---|---|---|---|
| 1 | **系统性字号越档**：5 个面板标题建在 16px，而 L2 字阶是 14/20/22/26 | 18 处全部改回档位 | medium |
| 2 | **触摸区不足**：TimeRangeTabs tab 最小边 36 < 44 | tab 改 76×44，容器 320×48 | high |
| 3 | **文字重叠**：TitleBar 主/副标题重叠 2px | 副标题 y 52→54 | high |
| 4 | **文字重叠**：GaugeChart `78.4` 与 `%` 重叠 2px | `%` x 138→142 | high |
| 5 | **间距越档**：TitleBar 品牌竖条与标题间距 12（不在档位） | 竖条 x 32→36 → 间距 8 ✓ | medium |

修复后 `high 0`，14/14 PNG 已重新导出。

### 两条临时装置 —— ✅ 已收编（用户裁决）

本轮为补审计盲区临时写了两个检查，**都在真实数据上抓到了东西**：

1. **重复兄弟节点检查** —— 判「同名兄弟且几何完全重合」，抓到了 TrendChart 的 9 个重复标签
   （#68 已证明这类缺陷**导出图看不出来**）。
2. **兄弟重叠检查** —— 抓到了 TitleBar 的 71px 重叠，正是 `layout-audit` **两个检查之间的缝**
   （#67①）。

**已并入随包发布的 `tools/layout-audit.mjs`**（检查项 8 → 10：`overlap` 收窄为 **TEXT×TEXT**、新增 `duplicate`），
**并附两条必要前提**（不带它们就是错报机器 —— 见上文「收编结果」）；
配套新增 `tools/layout-audit-mutation.mjs`（**51 PASS / 0 FAIL**）。
`references/lessons.md` 守卫覆盖表两行已由 ⚠️ 改判为 **✅ 已收编**。

### 阶段闸门漏跑——D1 §8 的判据第二次命中

手册 §3 的事先判据是「**任一次漏跑 → 立项**」。运行 B **漏跑 4 条**（与 A 相比多出 `qa-export` 与
`qa-critic`），**再次命中**。但漏跑原因与 A **同源且更清楚**：不是命令多，是**接入点不对**——

- `precheck --live` 需要「一份完整计划」，而分段构建天然没有；
- `layout-audit` 需要的 readback 形状与 `get-node` 实际返回对不上（`depth:1` 剥几何）；
- `qa-export` / `qa-critic` 需要 L4/L5 的**结构化产物**，而本轮的 L4 走的是「审计 JSON + 人眼」，
  **没有产出 `critic-report.json`**——工具在，但**喂不进去**。

这**强化了运行 A 的结论**：立项形态应为「**命令接入点对齐**」（让 `layout-audit` 直接吃 `get-node` 输出、
让 `precheck --live` 接受增量计划、让 L4 的读图结论有**结构化落点**），**而不是**做一个「一次性跑完 10 条命令」
的薄编排器。**第二次实测一致指向同一结论，可以定案了。**

### 本轮同步的文档修订

- `references/lessons.md` —— 新增 **#64–#71**（运行 B，8 条）· **#72–#73**（收编，2 条）· **#74**（验收，1 条），累计 **74** 条（编号 `#1–#74` 连续，无缺号无重复，已核）。
  守卫覆盖表新增 4 行：`layout-audit` 的 spacing / overlap **已由本轮收编补齐**（原标"已知缺陷"），
  重份节点检查**已收编**为 `duplicate`（原标 ⚠️ 临时装置 → 改 ✅），插件退化态标为 **📏 纪律**（无工具可守），
  另补一行记录 `precheck --live` 汇总行不更新的**镜像缺陷**（❌ 无守卫，记 1.3）。
- `CHANGELOG.md` —— 本节。
- `references/acceptance-criteria.md` §8.2 —— 运行 B 记录（见该文件）。
- `tools/layout-audit.mjs` —— 收编两条装置 + 两条前提 + 四个留痕段 + 输入能力探测。
- `tools/layout-audit-mutation.mjs` —— **新增**（补齐本仓库"每个校验器配一个变异测试"的惯例）。

### 收尾时 `check-refs.mjs` 的补跑

运行 B 期间同样漏跑，收尾补跑——**这是第二次「一补跑就抓到自己刚写的文档」**（运行 A 也如此）。
说明该闸门的价值不在于「发布前扫一遍」，而在于**写作过程本身会引入悬空引用**，
故它的正确位置是**每次改完 `.md` 就跑**，不是收尾一次。

> 本版本尚未发布，完成后补齐具体条目。

---

## [1.1.0] — 2026-09-16

基于一次完整链路真实运行（移动端官网，4 页 / 296 ops / 2 轮 Critic）的修复批次。提交 `d5a4070`..`16df195`。

### 修复

- **`set-effects` 毛玻璃必崩**：模糊类效果被无条件写入 `color` 字段，新版 Figma Plugin API 严格校验整条拒绝（`Unrecognized key(s) in object: 'color'`）——`BACKGROUND_BLUR`（毛玻璃唯一路径）从未真正可用。改为按类型构造字段。
- **`nodeInfo` 的 `depth` 失效**：`depth` 只在同时传 `detail:true` 时才生效，`get-node {depth:2}` 的孙级永远是不含几何的壳。改为 `depth>1` 即递归，`depth:1` 形状不变。
- **探针 `figmaRead` 误报**：只认 MCP 配置，导致 FULL_MODE 下输出 `figmaWrite:true / figmaRead:false` 的自相矛盾结果。改为 `bridge读 ∥ mcp读`。
- **字体写死 `PingFang SC`**：Windows 的 Figma 无此字体，`loadFontAsync` 直接抛错。三套 Preset 与 L1 规则改为**降级链**（PingFang SC → Microsoft YaHei → Inter 兜底）。
- **安装体验**：clone 命令改 HTTPS（原 SSH 地址要求先配 SSH key）；技能目录 `.workbuddy/skills` 与 `.codebuddy/skills` 并列为主安装位；自检命令改绝对路径。

### 新增

- **`POST /v1/batch` 批量通道**：296 个 op 的 296 次长轮询往返压到 8 次；支持 `onError=abort|continue`，失败时 `failedAt.step` 即断点、可续跑。
- **`references/bridge-ops.md`**：36 个 op 的参数形状、`run` 批量语法（`@last`/`$name`）、`depth`/`detail` 语义、错误码、效果字段对照表。**此前仓库内 op 文档为 0**。
- **`tools/qa-plugin.mjs`**（21 项）：用 `vm` 加载真实 `code.js` + 严格效果校验桩，离线给插件做回归测试。
- **`tools/qa-bridge.mjs`**（13 项）：起真实 Bridge 子进程 + mock 插件走完整长轮询链路。
- **`tools/layout-audit.mjs`**：L4 布局审计八类检查（spacing / padding / radius / font-size / alignment / overflow / baseline / touch）。

### 规则

- **avoid 仲裁**：确立「Prompt 显式点名 > Preset avoid」，覆盖时从 avoid 剔除 → 写 `style.avoidOverrides` → 同步传播 L2/L4 共用一份裁决。
- **新增第四套 Preset `web-marketing`**（品牌官网 / 营销落地页）——此前"网站/官网/landing"不在任何 Preset 内。
- **行业枚举补快消 / 餐饮 / 食品**，并确立「用户显式品牌色 > 行业映射 > Preset」。
- **`description` 覆盖 L0–L5 全链路并补英文触发词**（`d5a4070`）——原描述只声明 L1，L4/L5 场景根本触发不到本技能。

---

## [1.0.0] — 2026-09-16

首个自包含开源版本（`16b3587`）。五层架构（L0 探测 → L1 Brief → L2 DS Spec → L3 构建 → L4 审查 → L5 导出）、三套 Style Preset、Bridge + Figma 插件写通道、示例与 JSON Schema。
