# 1.3 候选清单

> **来源**：1.2「可信版」收尾验收（2026-09-17）时归拢。1.2 期间按 `1.2-plan.md` §8「**新增能力项 → 默认拒绝，记入 1.3 候选**」积累的条目，以及两次实测（D2 运行 A / 运行 B）暴露的缺口，**全部收在这里**，1.2 的文档不再散记。
> **性质**：这是**输入**，不是计划。1.3 的范围、优先级与取舍**已于 2026-09-17 立项定稿** ——
> 见 **`1.3-charter.md`（立项书，已签字：B 档「收口 + 空环」）**。本清单中文末标着「待评估」的 F 组条目
> **不等于待办**：是否纳入以立项书 §2.2 / §2.3 为准。
> **分组**：A 已定案 / B 已定位缺陷 / C 待收编装置 / D 装置缺口 / E 能力边界 / F 新能力 / G 缓做债项 / H 文档盲区；组内按建议优先级排。
> **本文件属开发者内部文档**（`tools/check-refs.mjs` 的 `DEV_ONLY` 模式 `^\d+\.\d+-[^/]*\.md$` 自动豁免），允许引用包外路径与运行产物。

---

## A. 已定案：命令接入点对齐

**来源**：D1 §8 的待定强化项（「薄编排器」做不做）已由 D2 两次实测回答 ——
**立项，但范围重定义为「命令接入点对齐」，不是薄编排器**。

**判据**（不是感觉，是两次实测的数据）：

- 运行 A 漏跑 3 条、运行 B 漏跑 4 条，**成因都不是「命令多到记不住」，而是「不知道该跑它」**
  （`1.2-plan.md` §7.1；`references/acceptance-criteria.md` §8.1 / §8.2）。
- 更根本的一条：**闸门参数与规格不同源** —— 上一轮「全绿」的真相是
  `--min-touch 0` / 无 `--baseline` / `--font` 含 16，**三条检查缺席或放宽**（`lessons.md` #73）。
  ⇒ 光把命令串起来没用，**得让参数本身有源**。

**证据**：`CHANGELOG.md` 运行 B 一节 · `references/acceptance-criteria.md` §8.2 · `1.2-plan.md` §7.1

| # | 子项 | 现象 / 出处 | 建议优先级 |
|---|---|---|---|
| **A1** | 让 `layout-audit` 直接吃 `get-node` 输出 | ✅ **1.3 复核：这条需求其实早已满足** —— `31c68cd` 引入工具时即带 `shallowWarning`（「内部自己处理几何缺失」已实现），`depth` 语义由 `e445a7f`「修复 set-effects 毛玻璃必崩与 depth 语义失效」修好。本轮用 `figma-harness.mjs` 走**一字未改**的真 `code.js` 实测三组：`{depth:1}` → 覆盖率 **1/2**、`{depth:2}` → **2/5**、`{depth:3,detail:true}` → **5/5** ⇒ **工具直接可吃**。**1.3 只补了残余**：几何覆盖从「容器数」升为「节点数 + 可审深度 / 输入深度」（`geometryCoverage`）—— 实测 `{depth:1}` 与 `{depth:2}` **浅容器数都是 1**，而漏掉的分别是 **1 个与 3 个节点**。**运行 A 当轮仍不可补**，但成因改判为「产物是**扁平几何表**（缺父子关系）」：**接口能补，数据不能补**。证据已固化为仓库内探针 `tools/layout-audit-readback-probe.mjs`（4 PASS） | ✅ **已做**（含成因改判，见第三刀） |
| **A2** | **闸门参数从 L2 Spec 派生** | 手敲 CLI 是 #73 的根因。应读 L2 的 `spacing` / `typography` 直接生成参数，比靠纪律可靠。**✅ 已做（2026-09-17 债线打包）**：`layout-audit` 新增 `--spec`，档位优先从 L2 `design-system-spec.json` 的 `tokens.spacing` / `tokens.radius` / `tokens.typography` / `responsive.baseline` 派生；优先级 `显式 CLI > --spec 派生 > 内置默认`，逐项来源写进产物 `paramSource` 段并在控制台打印；spec 有该项但取不到时**回落默认并如实标注** `paramGaps`，**不假装来自规格** | 高 |
| **A3** | L4 读图结论要有**结构化落点** | `critic-report._evidence.visionObservations` 字段已开，但运行 B 的 L4 走读图时**没有产物承载** ⇒ `qa-critic` 无输入，只能记「未核对」 | ✅ **已做（2026-09-18 · 1.3 A3）**：`tools/read-image-merge.mjs`（review 记录 → 自动落 `visionObservations` / `unassessed`，幂等 + 内容指纹防篡改）+ `qa-critic` QA9（`--review`：记录完备性 + 跨产物交叉核对）+ 变异 **37 PASS**；见 `acceptance-criteria.md` §5 |
| **A4** | `qa-export` 需要诚实的「**无 manifest**」降级路径 | 运行 B 的 L5 改走逐组件 PNG，`export-manifest.json` 未生成 ⇒ 闸门无输入。**「没查」要有专门的输出形态**，不能只靠人记得。**1.2 收尾补出 manifest 后新得一条输入**：即使清单齐了，`live-build` 档位仍被 `figmaFileKey` 卡住（见 **D2 / D4**）⇒ 降级路径要同时考虑「**无清单**」与「**有清单但缺环境凭证**」两种形态 | ✅ **已做（2026-09-18 · 1.3 A4）**：`qa-export --if-absent not-run --absent-reason`（清单缺失 → 机器可读 NOT_RUN 记录 + **退出码 3**，四态互斥：0 绿/1 红/2 用法/3 没查；理由空白拦；Schema `assets/templates/export-not-run.json`）。第二形态已由 D4 解决（`figmaFileKey=null` + `_meta.note`）。变异 63 → **70 PASS**，样例基线 935 不变 |
| **A5** | `precheck --live` 接受**增量计划** | 运行 A / B 的实际操作都是「逐批小计划」，而 B2 的核对入口是按「完整计划」设计的 | ✅ **已做（2026-09-18 · 1.3 A5）**：`precheck <plan> --after <prior> …`（可重复按序；前置批次=已执行上下文：跨批次 `$name`/字面 id 正常解析并归类「跨批次引用 N 处」、不算待核对；前置批次自带结构错误 exit 2 拒——上下文不可信；前置批次自身外部引用不泄入主批次）。变异 63 → **101 PASS**（各正向用例带不喂 `--after` 的反向对照；抓到 crossBatch 死代码真缺陷已修） |
| **A6** | **编排文档的命令表漏了 `layout-audit`** —— `references/acceptance-criteria.md` §5「逐阶段闸门命令表（可直接复制）」与 `SKILL.md` 的「一次跑完全部闸门」清单里**都没有 `layout-audit`**。该文件 §8.2 **自己诊断过**「运行 A 漏跑 3 条：`precheck --live` / `layout-audit` / `check-refs`」，但那 3 条里**只有 `layout-audit` 一直没进过命令表** ⇒ **诊断写在文档里、修的却是别的层**，下一个执行者照抄命令表仍会漏它 | ✅ **已修（2026-09-17 债线打包顺手，属 C1 登记的必然连带）**：`acceptance-criteria.md` §5 与 `SKILL.md` 两处命令表均补 `layout-audit --spec` + `pixel-proof`，并加注「收编新工具必须同步登记进本节」 | 中 —— **这是 1.2 实测漏跑成因（「不是命令多，而是不知道该跑它」）在文档侧的实例**；命令表就是"知道"的唯一载体 |

---

## B. 已定位缺陷（可机械修，无需新设计）

**1.3 开工第一刀已做掉 B1 / B2**（2026-09-17，见本文末「1.3 开工第一刀」），**债线打包已做掉 B3 / B4**（同日，见本文末「债线打包」）；**B5 / B6 待做**。

| # | 缺陷 | 守卫现状 | 1.3 状态 | 出处 |
|---|---|---|---|---|
| **B1** | `alignment` 在**跨轴长度不等**时**静默跳过** —— 不报，也不计入 | ✅ **已收编（留痕式）**：`layout-audit.mjs` 第五个留痕段 `alignmentCoverage`（`compared` / `skippedCrossSizeDiff` / `samples`）+ 控制台「对齐覆盖」行 + 变异测试 #23/#24 | ✅ **已做** | `lessons.md` #67c |
| **B2** | `precheck --live` 的**汇总行不随后续在线核对更新** —— 汇总打印 `待核对 1`，紧随其后的 B2 段却已 `ok 存在`；而 §6 要求留痕的恰是**汇总行** ⇒ 把「**已核过**」读成「**没核**」 | ✅ **已收编**：「离线汇总 / 最终汇总（含在线核对）」两行 + `已核对 / 核出不存在 / 未核对` 三计数 + 变异测试 F 组 | ✅ **已做** | 1.2 收尾新发现 |
| **B3** | 其余校验器缺 `unchecked` / `skipped` / `inputCoverage` 同类留痕 | ✅ 仅 `layout-audit` 有（现为**五个**留痕段）；**其余全都没有** | ✅ **已做（2026-09-17 债线打包）**：`qa-critic` / `qa-l2` / `qa-export` 各加「**评估覆盖**」段，把「本闸门这次**没查**什么、因为哪份输入缺席」写成一行。修的是 #72 的要害——**闸门因缺输入而整体跳过时，旧输出与「查了且干净」长得一模一样**。变异测试 52 / 20 / 57 全绿 | `lessons.md` #72 |
| **B4** | `check-refs` 的**全仓库同名兜底**会让「**目录写错、文件名对**」通过 | ❌ **无守卫**（**近半数**引用靠 basename 兜底解析——精确占比会随文档增补而变，**别写死**；复核：跑 `tools/check-refs.mjs` 读它打印的 `已解析 N：path=… / basename=… / glob=…` 一行） | ✅ **已做（2026-09-17 债线打包）**：**实测推翻了原描述的定性** —— 263 条 basename 命中里 **254 条是纯文件名**（无目录，兜底本就合理）、仅 **9 条带目录**（其中 8 条是合理豁免：`~/.workbuddy/…` 部署路径、`/skill/…` 占位符根）。**真正的规模是 38 条**：`critic-report.json`×17 / `design-system-spec.json`×6 / `export-manifest.json`×15 在 `assets/templates/` 有官方同名，旧行为静默指向第一个实例（17 条**全**指向 `example-health`），制造「具体文件」假象。修法：① 带目录的引用**不再走 basename 兜底**（目录错即 `dangling`）；② 同名的产物**类型名**判为 `artifact`（落点定位 `assets/templates/` 定义）；③ 部署路径 / 占位符根**单列豁免计数**；④ 同名多份返回 `ambiguous` 并 **WARN，不静默取首**。**顺带暴露 1 条真悬空**（`CHANGELOG.md:172` 那条引用漏写 `assets/examples/` 前缀，正确应为 `assets/examples/example-health/critic-report.json`，已修）。收口：已解析 603 → **630**（path 347 / basename 235 / artifact 46 / glob 2）、悬空 0、歧义 0 | `lessons.md` #72 · 1.2 收尾新发现：`design-system.md` 曾把 Brief schema 写成 `assets/templates/design-brief.json`（真身在仓库根的 `templates/`），`check-refs` **未报** |
| **B5** | ~~`VERSION` 与 `CHANGELOG` 可各说各话~~ → **真形态：`VERSION` 与顶部 `-dev` 段的序关系无守卫**（`qa-install` 只校「探针 ↔ `VERSION` 文件」一致，管不到 CHANGELOG） | ⚠️ **原定性前提不成立**：`CHANGELOG.md` 第 7–11 行**已经写死**「`VERSION` 记最后已定版的版本，顶部允许并存更大的 `-dev` 段」⇒ **「不校两者一致」本就是对的**，B5 想校的那个「一致」是个错前提。**真形态 = 校序关系**（顶部 `-dev` 段版本必须 **>** `VERSION`）。⚠️ 这是 **F4 的筛子 S1.5「前提是否成立?」**该拦下的第二例（第一例见 A1 的成因改判） | ✅ **已做**（`tools/release-guard.mjs`，与 B6 合并为一个工具） | 1.2 定版时发现（2026-09-17）· 1.3 第三刀重述并收编 |
| **B6** | **发布流程无 `VERSION` ↔ `git tag` 一致性检查** —— 没有任何守卫检查「`v$(cat VERSION)` 是否存在、落点是不是 `VERSION` 已更新的提交」；`qa-install` 只校「探针 ↔ `VERSION` 文件」，管不到 tag | ❌ **无守卫**（`v1.2.0` 纯手工打；`1.0.0` / `1.1.0` 至今无 tag） | ✅ **已做**（`release-guard.mjs` 的 E / F 两条：tag 存在 + **tag 落点处 `VERSION` 与文件一致** —— 后者抓的正是「tag 打在 VERSION 还没更新的提交上」，即「落点不纯」里**可机械判**的那一半）。实测 `v1.2.0` → `9828edb`，`v1.2.0:VERSION` = `1.2.0` ✓。历史欠账（`1.0.0`/`1.1.0` 无 tag）**不判为当下之错**——本工具只校当前 `VERSION` | 1.2 定版时引入 tag（2026-09-17） |

> **B4 值得单独看**：它与 `qa-export` QA9 抓到的「`preset:` 前缀合法但路径不存在」是**同一类** ——
> **格式对了，指的东西不一定在**。校验器的兜底规则越宽，「指错地方」越容易漏。

> **B1 的修法值得单独看（反直觉，别再"修"回去）**：`alignment` 跨轴尺寸不同就跳过，看着像"漏检"，
> 于是最自然的动作是**放宽判据**。实测（运行 B readback）这个动作**是错的**：
> 真正比过 **3 对**、因跨轴尺寸不同没比 **121 对**，放宽后在 121 对里新增 **5 条报告 —— 5 条全是假阳性**
> （时间戳 231×27 vs 在线圆点 10×10：圆点**居中**对齐文字，上边缘当然差 8px）。
> **跨轴尺寸不同时视觉惯例是居中，边缘对齐不是正确判据。** 故正确修法是**留痕**：
> 把「没比多少对、是哪些对、差多少」全部写进产物，让人知道这块没被边缘判据覆盖。
> 反向闸门见 `layout-audit-mutation.mjs` #24（居中对齐的合法布局**不得**报 alignment）。

---

## C. 待收编的临时装置

| # | 装置 | 现状 | 出处 |
|---|---|---|---|
| **C1** | **效果类参数的可见性检查**（像素直方图 + 密度图） | ✅ **已收编（2026-09-17 债线打包）** → `tools/pixel-proof.mjs`（零依赖 PNG 解码 + 直方图 + ASCII 密度图 + A/B 逐像素差异 + `--expect-diff/--expect-same/--expect-content` 断言开关）+ `tools/pixel-proof-mutation.mjs`（**46 PASS / 0 FAIL**） | `lessons.md` #56 / #57；守卫覆盖表 |
| **C2** | **增量补齐工具** —— 喂「期望节点清单 + 目标父级」，它自己回读、算差、只补差集 | 📏 纪律，无工具；运行 B 的临时脚本已完成使命 | `lessons.md` #65 / #71 |

> **C1 已收编**（2026-09-17 债线打包）：#56 是运行 A「最值钱的发现」（负载参数缺少能否证它的观察通道），
> 但它此前只活在运行目录里、**下一次跑还会重新踩**。收编判据按 #73 —— 不是「搬进 `tools/` 就算数」，
> 而是**代码 + 前提 + 留痕 + 变异测试**四件齐；能力边界（只解 8 位非隔行、调色板 `tRNS` 忽略、
> A/B 拒绝不同尺寸）在头部**显式抛出**，不静默降级。

---

## D. 装置缺口（受冻结区或宿主 API 限制，非产品缺陷）

> **✅ 已归并定稿（2026-09-18 · 立项书 §2.2 第 5 项）**：D1–D3 与 F4 新增的三条回读字段需求
> 合并为正式需求单 **`references/frozen-zone-requests.md`**（FR-1 ~ FR-6，含落点 / 解锁 / 验收 / 风险 /
> 批准后执行纪律）。**✅ 全部 6 条已获用户批准并于 2026-09-18 实现完毕 + 真机验证通过**（FR-1~5 改冻结区，
> 提交 `3953e48`/`7acd368`/`6bf8211`/`bb770ac`；**FR-6 复核结案：既有代码已满足，真机 418ms 快速 `NODE_NOT_FOUND`，
> 下表 D3 描述的「挂 10s」是过期时点快照**）——结案回填见需求单 §4。下表保留原始记录。

| # | 缺口 | 影响 | 处置 |
|---|---|---|---|
| **D1** | `paintToHex()` 不回传 `paint.opacity` | 填充透明度**不可结构化观测**，只能读图 ⇒ L4「材质一致性」缺通用档证据 | ⚠️ **落点改判：它在冻结区，不是「可直接做」**。原文写的 `tools/figma-harness.mjs` 是**加载器**（用 `vm` 跑**一字未改**的 `code.js`），改它不会改变回读行为、也不该改；`paintToHex` 的真身**只在** `figma-plugin/code.js` —— 而 `qa-export` QA7 的禁区 = `figma-plugin/` + `bridge/` ⇒ **D1 与 D2/D3 同类，须向冻结区提需求**，不在构建期改。**另：C1 已给出补偿通道**（`pixel-proof.mjs` 的像素级观测）—— 结构化通道仍缺，但「不可观测」已不成立。实测复核：给 paint 声明 `opacity:0.5`，回读仍是 `fills=["#e6e6e6"]` + `fillCount=1` + 节点级 `opacity=1`，**paint 级透明度无处承载** |
| **D2** | 插件 `ping` 不返回 `figma.fileKey`，且 `code.js` 属冻结区 | **L5 Export Gate 的 `live-build` 档位在本环境结构性不可达** —— **已两次独立确认**：运行 A（唯一 FAIL）与运行 B（1.2 收尾补做 L5 出口时复现，`409 PASS / 1 FAIL`，唯一 FAIL 即此条） | **插件侧**：向冻结区**提需求**，不在构建期改（运行 A #11 已因当场改冻结文件被 QA7 拦下）。**两次实跑撞上同一条断言、同一根因 ⇒ 不是偶发**。**断言侧**：✅ **已由 D4 的判据修正消除**（2026-09-17 · 立项第 0 项）—— 「结构性不可达」不再表现为一条永久 FAIL |
| **D3** | **不存在的 node id 让插件挂满 10s，然后报「无法连接 Figma，请检查网络」**（`figma-plugin/code.js` 的 `getNodeByIdAsync` 在 `documentAccess: "dynamic-page"` 下对缺失 id 会去加载对应页面，失败后由 Figma 宿主抛出这句网络错误；代码里 `if (!node) throw fail(..., "NODE_NOT_FOUND")` 的分支因此**永不触发**） | ① **诊断陷阱**：把「id 不存在」说成「网络问题」，把排查引向无关方向；② 每次核对缺失 id 白花 10s；③ `precheck --live` 设计里的「核出不存在」专门路径**在本环境永不触发** ⇒ 所有缺失 id 落进「意外响应」并判 FAIL，`核出不存在` 计数恒为 0 | 根因在**冻结区**，本轮**未改**任何冻结文件。工具侧**不应用关键词猜**（会把真·连接故障一起吞掉）——如实报 FAIL 是本环境唯一不撒谎的选择，已在 `lessons.md` #76 写明该边界。可向冻结区提需求：`get-node` 对未知 id 先查页面清单再决定，或限定加载范围 |
| **D4** | **`qa-export` 的 QA3 要求 `live-build` 必须带 `figmaFileKey`，但本管线的回读通道是 Bridge 直连插件、凭 `nodeId` 单键即可回读，`fileKey` 在功能上没有用途** —— 这条断言来自基于 REST API 的上游（1.2 收尾补做 L5 出口时发现） | 与 D2 叠加：本环境的 `live-build` 档位被一条**对本管线拓扑无必要**的凭证要求卡住；给「1 条 FAIL」的归因增加了一层噪音（得解释「FAIL 的是环境凭证、不是产物质量」） | ✅ **已做**（2026-09-17 · **立项第 0 项**，见文末「第四刀」）。原文「本轮不改工具」的理由（A4 教训「改措辞不放宽校验器」）**不适用于本条** —— 那条教训治的是「文档措辞不严谨却去松校验器」，本条是**判据引用了本拓扑得不到的外部凭证**（#85）。裁剪办法是**换判据**：拿不到就如实标 `null` + 在 `_meta.note` 交代缺口；**静默留空 / 全空白 / 空串**三条仍拦 |

---

## E. 能力边界（明确「补不了」，记录以免反复立项）

| # | 边界 | 说明 |
|---|---|---|
| **E1** | **语义性缺失**：无轴标 / 无单位 / 无空态提示 / 无图例 | 几何**完全合法**，数值审计在原理上不可见（运行 B 的 168 格热力图无一根轴标）。只能靠读图评审，**且目前没有机械裁判** |

> 记在这里的用途是**防止它被当成待办反复立项**：它是**判定的能力边界**，不是实现缺口。
> 真正可做的是「把读图评审变成流程必选项」，而不是「写个脚本查它」——后者写不出来。
> ⇒ 这条「真正可做的事」已立为 **F3**（见 F 组，2026-09-17 补），连同「锚点产品并置比对」一起。

---

## F. 新能力（需评估，不是债）

| # | 能力 | 状态 | 出处 |
|---|---|---|---|
| **F1** | **MCP 外壳「门铃式」中间态** | 1.3 单独评估。MCP server **不内化规则**，只暴露「按本 SKILL.md 执行」的入口，规则留文档形态 | `1.2-plan.md` §3.1.3 / §5 |
| **F2** | **图片填充** | 实现路径已定（见 `1.2-plan.md` 附录）。受 `figma-plugin/manifest.json` 的 `allowedDomains` 只允许 `localhost:45677` 限制 —— 插件拉不了任意 URL，须走本地中转 | ✅ **已做（2026-09-18 · 1.3 F2，用户授权解冻）**：`set-image-fill` op（字节链路：主线程 → UI 同域 `GET /v1/asset` fetch → base64 → `figma.base64Decode` → `figma.createImage`，不改 manifest）+ bridge asset 端点（白名单扩展名 / 8MB 上限 / 防穿越 / token 保护）+ 回读字段 `imageFills`（契约 33→34）+ 合成测试资产工具 `make-test-asset.mjs`。已验证：offline 全链路（变异 101→111 PASS）· bridge 端点五分支真跑（字节级一致）· qa-plugin 37 op。**⚠️ 真机复测待用户**：Figma 里重载插件后跑一次 `set-image-fill`（步骤见 `1.3-status.md` §6.1） |
| **F3** | **L4「设计判断」闭环 —— 把链上唯一没有裁判的一环补上**（① 读图评审从「增益档」升为**有图环境的必选项**；② 建「**锚点产品并置比对**」装置 —— 随包锚点集 + 并置协议；③ 把 E1 的语义性缺失做成**读图检查清单**并要求逐条留痕） | ✅ **三件全清（2026-09-18，1.3 立项书 §2.2 第 2 项）**：①③ 见 `read-image-checklist.md` / `visual-critic.md` §1.6；② 装置 `tools/anchor-compare.mjs` + 协议 `references/anchor-compare.md`（锚点源 `nexu-io/open-design` 151 品牌契约核实、不 vendored，**「随包锚点集」改判为「随包协议与装置」**；判据 J1–J5 是装置纪律，相似度结论属 R11 + 人） | 见下方「F3 来历」 |
| **F4** | **外部设计判据的机械化吸收** —— 把成熟设计 skill 的检查条目（`taste-skill` 的 *AI Design Tells*、`jakubkrehel` 的 `better-ui`／`better-layout`／`better-typography`、`mattbx/shadcn-skills` 的 `review-checklist`、`emilkowalski` 的 `review-animations`）**逐条过筛**，能落到画布几何／属性的做成新检查类接进 `layout-audit` 或新审计器 | ⏳ **进行中（1.3 立项书 §2.2 第 1 项）**。**与 F3 同源：给空环找原料**。**筛子已由一轮试点从 3 个增到 4 个**（静态 → **前提是否成立** → 可判定 → 分流；新增的「前提」须排在「可判定」**之前**）。**筛法已于 2026-09-17 提为随包文档 `references/design-criteria-intake.md`**（原先三份试点产物全在 `.vibe/`、**不随包发布** —— `lessons.md` #81 点名要求）⇒ **6/6 来源已过筛（F4 筛线完成，2026-09-18）**（`better-ui` 17 + `better-layout` 10 + `better-typography` 19 + `taste-skill` 41 + `review-checklist` 20 + `review-animations` 14 = **121 条原则**：搬 17 / 译 14 / 交人 18 / 无对象 48（40%）/ 拆 18 / 已有 6；关键发现见 intake §2.4–§2.7：文本内容域搬率 34% + 语言分档主杀器、「已有」档与第三方佐证、「方法论与实质分开筛」、F5 启动包定档）。**收编动作（文案 tell 审计 / 板外色信号）待四件齐裁决** | 见下方「F4 来历」+「**F4 试点校准**」 |
| **F5** | **`Motion`（动效）不在 L4 五维里** —— `visual-critic.md` 五维 = Layout / Color / Consistency / Commercial / Usability，**没有动效维度**。Figma 有原型与智能动画、节点级 `transition` 可回读，故「评不了」不成立，是**从没立过这个维度** | ⏳ 待评估 | 见下方「F4 来历」 |
| **F6** | **「响应式」只落在声明里、不落在画布上** —— DS Spec 有 `responsive.baseline / breakpoints / rules`（`design-system.md` §12 八字段映射），但 L3 只建**一个** `frameBaseline`（LD-10：大屏恒 1920×1080），**不建多断点**；且 A1 盲测的 **H3** 指出 web-admin 断点**取值规则缺失** ⇒ H3 不只属「文档盲区」，可能是**能力缺环** | ⏳ 待评估 | 见下方「F4 来历」 |

> **F3 来历（2026-09-17，用户问「是不是把设计的功能给丢掉了」时查出来的）**
>
> **查证结论：设计产出能力没丢，但「设计判断」这一环 1.2 一字未动，且它本来就是整条链里唯一没有裁判的一环。**
>
> 三条实测证据：
>
> 1. **改动史** —— `references/design-intelligence.md`（L1 八条转换规则：风格路由 / 平台判定 / 行业映射 /
>    信息架构模板 / Token 推导 / 组件预期清单）与 `references/design-style-library.md` 最后一次改动都是
>    **1.1.0 的 `c9c1f35`**（那次加的是 `web-marketing` Preset、`avoid` 仲裁、行业枚举）；**1.2 全程未碰**。
>    1.2 动过的设计相关文件只有 `visual-critic.md` / `critic-mapping.md`，而改的全是**证据链与闸门** ——
>    `visual-critic.md` §1.1–1.5 五维评分模型与 §2 评分锚点**一个字没动**。
> 2. **闸门对「好不好看」零覆盖** —— `contrast-audit` 算 WCAG 比值、`layout-audit` 查几何、`qa-*` 校 Schema，
>    全是数值／结构检查。运行 B 最要命的三条真缺陷（TitleBar 时间戳重叠 71px / 热力图 168 格无一根轴标 /
>    TrendChart 9 个重复标签）**闸门全绿也发现不了**（`lessons.md` #68：像素一致不能证明结构正确）。
> 3. **最硬的一条** —— 2026-09-17 补做的 L4 报告（`.vibe/d2-run-b/critic-report.json`）**档位已是
>    `structured+vision`（真的读了图）**，五维均分 `8.5`、`action = PASS`，但 `_evidence.unassessed` 里躺着 5 项，
>    其中 **Commercial 的「商业产品相似度」占该维 40% 权重**，未评，理由是「**需与 Apple / Stripe / Linear /
>    Ant Design Pro / Material 3 同场景并置比对**，本轮只做了单图」；`Consistency` 的 **icon 风格（线性 vs 面性）**
>    同理。⇒ **「并置比对」这个动作，链路里根本没有装置** ⇒ 设计感最核心的项**结构性无法评估**。
>
> **张力（需在 1.3 裁决）**：`visual-critic.md` §1.6 把读图定为「**增益档**」（附加、可上调分值，不得替代通用档），
> 而 E1 的注却说「真正可做的是把读图评审变成**流程必选项**」—— 两者方向不一致：
> **现行措辞下，即便 Agent 有能力读图，也可以合法地不做**。F3 的第 ① 条就是消掉这个不一致。
>
> ⚠️ **不要把它误读成「1.2 做坏了」**：1.2 的自我定位是「零能力新增，纯还债」，且其核心纪律
> 「**未评估不得当通过**」恰恰是**把这块空环照实暴露**才对（剔除权重 + 逐条声明，而不是假装评过）。
> 没有 1.2，这个空环会被「五维 8.5 分 PASS」盖住。

> **F4 / F5 / F6 来历（2026-09-17，用户提供一份「顶尖开源 UI 设计 Skills 前十」二手清单并问「能否融合」）**
>
> **先做了一手核对** —— 逐条访问仓库，**8 个仓库全部真实存在**（`ui-ux-pro-max` / `taste-skill` /
> `anthropics/skills` / `emilkowalski/skills` / `ibelick/ui-skills` / `jakubkrehel/skills` /
> `vercel-labs/agent-skills` / `sugarforever/open-design-skill` / `sleekdotdesign/agent-skills` /
> `mattbx/shadcn-skills`），但**清单里的数字与定性有两处硬错**（详见 `lessons.md` #80）：
> ① `ui-ux-pro-max` 文中写「50+ 风格 / 161+ 色板」，其 README 实为 **79 种风格 / 192 色板**；
> ② `sleekdotdesign/agent-skills` 被描述成「移动端原生 UI/UX 规则技能」，实为**付费 SaaS 代理**
> （需 Sleek 账号 + `SLEEK_API_KEY` + Pro 计划 $49.99/月）。**定性错比数字错更贵**。
>
> **为什么不「融合」**：本 Skill **不是**「前端代码审美提升技能」，而是**往真实 Figma 画布出图**、
> 且产物可被**机械复算**的设计器。上面这批 skill 的产物是**代码**、验收靠**人看**。
> 照单融合的功能面 ⇒ 变成「第 11 个 taste skill」，**丢掉唯一两样别人没有的**：
> ① 真实画布产出（Figma 节点，不是代码）；② 可复算的证据链（8 套变异测试 397 条断言）。
>
> **该吸收的是三层，且只有第二层要写进本仓库**：
>
> | 层 | 做法 | 是否改本仓库 |
> |---|---|---|
> | **会话层组合** | 这些 skill 与本 Skill **并列**，一次会话可同时加载（用户级已装 `design-taste-plus` / `apple-design`，与本清单第 2、4 名同族）。**但是** 两套规则同时在场会**打架** —— 本仓库 L2 规定色值唯一来源是 Style Preset + RD-1/2/3 派生白名单，外部 skill 是另一套旋钮体系。**必须先定裁决顺序**（同 `lessons.md` #44「同名两物交人裁决」） | ❌ 不改，但要写进文档 |
> | **判据层** | 把外部条目**逐条过「可判定性」筛**：能落到 `get-node` 可回读的几何／属性的 → 做成**新检查类进闸门**；判不了的（如 optical centering 的观感部分）→ 只进文档，**不许进闸门**（否则就是 H3「目测评分」） | ✅ **F4** |
> | **资产层** | `open-design-skill` 背后的 `nexu-io/open-design` 有 **150+ 品牌 `DESIGN.md`**，正是 **F3 第 ② 条**「锚点产品并置比对」缺的锚点集。**但不能 vendored**（许可证 / 会漂 / 体积）⇒ 做成「**可选外部锚点源 + 快照 + 留痕**」，比对结论必须能回放到具体快照 | ✅ 并入 **F3** |
>
> **顺带照出两个真缺环**（比「融合」本身值钱）：**F5 动效维度缺失**、**F6 响应式只声明不落画布**。
>
> **F4 试点校准（2026-09-17，用户点「先试试」后做的一轮闭环）**
>
> **做法**：拿 `better-ui/SKILL.md` 的**一手原文**（`raw.githubusercontent.com`）逐条分档，
> 样例表见 `.vibe/triage-better-ui.md`；再取分档结果里**最像能落地**的那条 —— **同心圆角**
> （判据是纯算术、不依赖读图）—— 做完整闭环：判据 → 真实画布 → **合成样本正向验证** →
> 冲突穷举 → **真实闸门实测**。完整报告与复现命令：`.vibe/pilot-concentric-report.md`。
>
> **结论：同心圆角「不收编」，但理由不是判据不可算。**
> 判据经 5 个合成样本正向验证 **5/5 正确**（能分辨合规／违规／不适用／无约束四种情形）；
> 真正的问题是**可检查对象 = 0**：
>
> | 实测项 | 数字 |
> |---|---|
> | 运行 B 真实画布（377 节点）上的同心圆角候选 | **0** |
> | 其中「有圆角的容器」（`FRAME`/`COMPONENT`/`INSTANCE`/`GROUP` 且 `cornerRadius > 0`） | **0** |
> | 圆角实际分布 | 14 个组件帧**全为 0**；全树仅有的正圆角是 **224 个叶子 `RECTANGLE r=2`** + 1 个 `r=4` |
> | 同心公式要求值 × 运行 B 实际档位 `[2,4]` 的互斥率 | **5/8 = 62.5%** |
> | 真实 `layout-audit` 对「守同心」样本（父 4/gap 1/子 3）的裁决 | 报 `cornerRadius=3 不在档位 [2,4]`（**medium**，**不改退出码**） |
> | 同上、对「守档位」样本（子改 2）的裁决 | **零 issue**（但同心判据判 FAIL） |
>
> **两条硬结论**：
>
> ① **筛子要从 3 个增到 4 个，且新增的那道必须排在「可判定性」之前** ——
> 原有的三道见 `.vibe/triage-better-ui.md` §一（**静态? / 可判定? / 检查还是配方?**）；
> 本轮在「静态」与「可判定」**之间**插入 **S1.5「前提是否成立?」**
> —— 它问的是「**产物形态里有没有可检查对象**」，与 S2「**能不能算**」方向相反、最容易漏。
> 完整序列：**静态 → 前提 → 可判定 → 分流**。
> 一道规则完全可能**判据可算、却没有任何可检查的对象**。若顺序颠倒（先写判据、进闸门），
> 得到的是一个**永远报 0 条的检查**，而「报 0 条」会被读成「这项查过没问题」——
> 正是 `lessons.md` **#72** 那个形状的**假绿灯**。同心圆角正是通过 S2、却死在 S1.5 的样本。
>
> ② **「无对象可施」必须分两种**（诊断动作与处置都不一样）：
> **能力前提不成立** —— 需要某个 op，而它不存在（如 F2 的图片填充）⇒ **补能力后可收编**；
> **形态前提不成立** —— 能力都有，产物形态里不出现这种结构（同心圆角需要「有圆角的嵌套容器」，
> 而产物是「直角卡片 + 内部微圆角小条」）⇒ 一般**不收编**，因为它要改的是**风格**（产品决策）。
> 混成一类「用不上」就会丢掉这个区别，而这个区别正是「该不该为它立项」的分界线。
>
> **⚠️ 顺带订正一处数字**：同心圆角用的档位 `[2,4]` **直接派生自 Preset** ——
> `tokens.radius.{sm,md}.source = "preset:gov-digital-screen.visualSystem.radius(2/4)"`
> （出处 `.vibe/d2-run-b/design-system-spec.json`）。也就是说 **62.5% 的互斥率，根源是
> 「政府数字大屏只用 2/4 两个圆角」这个风格选择**，不是工具纪律定得太苛。
> 引用这条时不要写成「档位太窄」—— 那会把「风格决策」误报成「工具缺陷」。
>
> **本轮未为让结论好看而改任何仓库代码**：`layout-audit.mjs` 一行未动，同心圆角没有进闸门。

---

## G. 缓做的债项（1.2 明确划出，防止范围膨胀）

| # | 项 | 出处 | 备注 |
|---|---|---|---|
| **G1** | #11 sha256 清单机制 | `1.2-plan.md` §3.2.3 | 有增益但非阻塞 |
| **G2** | #12 op 映射对照 | 同上 | 同上 |
| **G3** | #14 selftest 反向用例 | 同上 | 同上 |

---

## H. 文档盲区（来自 A1 独立盲测的实测清单）

**来源**：2026-09-17 的 A1 独立盲测（见 `1.2-plan.md` §4.A1 与 `CHANGELOG.md` 的 1.2 段）。
三轮执行者（均为全新 context、只读材料）各自记下「文档没说清、只能靠猜」的点。
**去掉第一轮「材料不全」造成的色值类缺口**后，剩 **6 处两轮以上共识的盲区** ——
这是 `references/design-system.md` 的**实测缺口清单**，可直接作为改文档的输入。

| # | 盲区 | 出现轮次 | 说明 |
|---|---|---|---|
| **H1** | **typography 字阶映射规则** | 一 / 二 / 三 | Brief 给的是 12 段数字串（如 26-32-16-24-14-20-13-20-12-18-11-16），文档只说有五类 Typography 子组，**没给「数字串 → Display/Heading/Body/Caption/Number 各取哪个」的规则**。三轮执行者各猜一套，结果互不相同 |
| **H2** | **pagePadding / spacing.base 等非 cellPadding 档位** | 一 / 二 / 三 | §7 只由 SP-2 规定了 cellPadding（dense=12），其余 spacing 档位没有取值规则 |
| **H3** | **web-admin 的响应式断点** | 一 / 二 / 三 | 文档未对 web-admin 平台规定 breakpoints |
| **H4** | **status.warning 取黄还是橙** | 二 / 三 | ST-1 说「按色相从 chartColors 映射」，但黄/橙二选一无判据 |
| **H5** | **focusRing 与 touchTarget 的具体数值** | 一 / 二 / 三 | 字段存在，取值规则缺失 |
| **H6** | **字重取值** | 二 / 三 | §6 未给字重规则 |

> **这 6 处都不是 bug，是「约束内的自由」。** 三轮里第二、三轮均 `qa-l2` **18 PASS / 0 FAIL** ——
> 即文档规范了**必须满足什么**，但没规定这些具体值取多少。所以这是一批
> **「要不要把自由收紧」的产品决策**，不是「缺陷修复」批次：
> 收紧 → 降方差但文档更厚；不收紧 → 保留执行者的判断空间。
> **建议在 1.3 定稿范围时一并裁决**，别直接塞进"修缺陷"的批次里做掉。
>
> **✅ 裁决（2026-09-18 · 1.3 H 组分类收紧）**：**H1 / H3 / H4 收紧为确定性规则**
> （TY-5 字阶串逐档贪心映射 · LD-11 web-admin 断点 1440/1024 · ST-1 黄/橙并存按序首个），
> **H2 / H5 / H6 给「缺省值 + 允许区间」**（SP-4 · AC-3/AC-4 · TY-6）——区间保留执行者
> 对具体产品的判断空间，出区间才须记 `assumptions`。全部落 `references/design-system.md`，
> 历史 few-shot 产物不改（`example-saas` 的 number 档属收紧前自由度，规则里已注明）。
> 收紧方式刻意区分：**映射/断点/映射判据是「多解必歧义」→ 收紧为唯一解**；
> **间距/触控/字重是「有合理范围」→ 给区间而非唯一值**，一刀切唯一值会把产品差异压平。

---

## 归拢时（1.2 收尾）已顺手修掉的（**不留到 1.3**）

| 项 | 处置 | 落点 |
|---|---|---|
| `example-health` 同名两物（美业「AI 型衣」vs 医疗「AI 智能健康管理」） | ✅ 已裁决并改名（导出包副本前缀 `health-` → `healthcare-`，核心示例不动） | `references/lessons.md` #44 |
| 大屏基线：1920×1080 vs 3840×2160 | ✅ 已裁决为 **1920×1080 是设计基准，3840×2160 仅 @2x 出图倍率** | `references/design-system.md` **LD-10** |
| `design-system.md` 的 Brief schema 路径写错（`assets/templates/` → 真身在根 `templates/`） | ✅ 已修（**B4 的实例**；同类已全仓库扫过，仅此一处） | `references/design-system.md` §8 |
| `lessons.md` 守卫覆盖表两处陈旧（重份节点检查标「待收编」、`lessons` 累计条数） | ✅ 已改判 | `references/lessons.md` 附 |
| 一条目录里的**闸门参数与规格不同源**（`minTouch:0` 等） | ⚠️ 未修，**属 A2** | 见上 |

---

## 1.3 开工第一刀（2026-09-17）

范围：**B1 + B2 两个工具层缺陷**（`1.2-plan.md` §7.1 之后的第一个动作），外加**回归时暴露的改名涟漪**。
三件都属「已定位、可机械修」，无新判据设计。

| 项 | 内容 | 落点 | 验证 |
|---|---|---|---|
| **B1** | `layout-audit` 新增第五个留痕段 `alignmentCoverage` —— 把「edge 比了几对 / 因跨轴尺寸不同没比几对 / 是哪些对」写进产物。**修法是留痕不是放宽判据**（放宽实测 100% 假阳性） | `tools/layout-audit.mjs`（walk 循环 + 报告段 + 控制台行） | 变异测试 **60 PASS / 0 FAIL**（原 51，+9 条：留痕 7 + 反向闸门 2） |
| **B2** | `precheck --live` 核对前那行加「离线汇总」限定词，核对后**必再打一行**「最终汇总（含在线核对）」，并给出 `已核对 / 核出不存在 / 未核对` **三个数**（三路径分别落位 1/0/0 · 0/1/0 · 0/0/1） | `tools/precheck.mjs`（两行汇总 + 三计数） | 变异测试 **75 PASS / 0 FAIL**（原 66，+9 条）· 另用**真 Bridge + 真画布**实测一次（`7:328` 存在 → 已核对 1；`999:999` → 未核对 1） |
| **改名涟漪** | `tools/qa-export-mutation.mjs` 里 `["saas","health","highway"]` **一个列表兼两用**（核心示例目录名 + 随包清单名），改名后两者不再同名 ⇒ 拼出已不存在的 `example-health-export.json`，整份变异测试 **ENOENT 崩掉**（上一轮改名后没复跑，故一直潜伏） | `tools/qa-export-mutation.mjs` 拆成 `CORE_EXAMPLES` / `EXPORT_NAMES` + `manifestKey()` 显式转名 | 复跑 **56 PASS / 0 FAIL** |
| **新发现 D3** | 排查 B2 时顺带实测出来：**不存在的 node id 让插件挂 10s 后报「请检查网络」**（非网络问题）。已记入本文 D 组 + `lessons.md` #76，**未改任何冻结文件** | 根因在冻结区 `figma-plugin/code.js` | 3/3 复现（10.1~11.0s vs 存在的 id 54ms） |

**教训回写**：`lessons.md` #67c 改判为 ✅ 已收编（附实测数据）；新增 **#75**（改名的涟漪要追到代码字面量 + 改名后**必须复跑全套闸门**）、**#76**（宿主 API 用错误类别的错误回答"查不到"时，工具层不许用关键词猜）；守卫覆盖表三行改判；`layout-audit-mutation.mjs`(51→60) / `precheck-mutation.mjs`(66→75) 计数订正。

**1.2 收尾补做（同日做，属 1.2 不属 1.3）**：把运行 B 的 L4/L5 出口补了出来 —— `critic-report.json`
（`qa-critic` **45 PASS / 0 FAIL / 0 WARN**）与 `export-manifest.json`（`qa-export` **409 PASS / 1 FAIL**，
唯一 FAIL 为 `QA3 figmaFileKey`，见 **D2 / D4**）。净效果：运行 B 的「未核对」**2 条 → 0 条**。
对应本节 **A3**（结构化落点：报告已补出，但「让读图结论自动落到该字段」仍未做）与
**A4**（降级路径：**仍未做**，且新得的输入是它得同时管「无清单」与「有清单但缺环境凭证」）。

**未做（仍在 1.3 待办）**：~~B3~~ · ~~B4~~ · ~~C1（效果类参数可见性收编）~~ · ~~A2（闸门参数从 L2 Spec 派生）~~ ——
**这四项已于同日「债线打包」做掉，见下节**；随后 **B5 / B6 / A1（残余）/ D1（改判）** 又于同日「债线尾巴」
（第三刀）做掉或改判，见文末。**范围与优先级已于同日立项定稿**（`1.3-charter.md`，B 档「收口 + 空环」），
其 **§2.2 第 0 项（D4 · QA3 判据修正）也已当场做掉**（第四刀，见文末最后一节）。
**仍未做**：**F4 筛子 → F3 装置**（立项书的第 1 / 2 项，正题）· A3/A4/A5 · D2/D3 向冻结区提需求 ·
**H 组分类收紧 / D 组需求单** · **E/F/G 各组**。

> ⚠️ **本文的性质仍是「输入」，不是计划。** 已开工的**四刀**记录在下文各节；
> **1.3 做什么、按什么顺序、怎么算做完，以 `1.3-charter.md`（立项书，已签字）为准**。

---

## 1.3 债线打包（2026-09-17，第二刀）

**为什么是这四项一起做**：都是「已定位、可机械修」的债，且 **A2 必须在加任何新检查类之前完成** ——
否则新检查的档位参数又会变成手敲的（#73 根因重现）。故一轮打包：**B4 收紧引用兜底 → B3 补齐留痕 →
A2 参数同源 → C1 装置收编**，做完地基干净，再进空环线不返工。

| 项 | 内容 | 落点 | 验证 |
|---|---|---|---|
| **B4** | 引用解析四规则收紧：带目录的引用**不再 basename 兜底**；产物**类型名**判 `artifact`（指向 `assets/templates/` 定义）；部署路径 / 占位符根**单列豁免**；同名多份 → `ambiguous` **WARN 不静默取首** | `tools/check-refs.mjs`（`resolveRef` 重写 + `isDeployPath/isPlaceholderRoot/isArtifactName`） | 变异测试 **31 PASS / 0 FAIL**（原 18，+13 条 / 10→15 类）；收口 658 已解析 · 悬空 **0** · 歧义 **0** |
| **B3** | `qa-critic` / `qa-l2` / `qa-export` 各加「**评估覆盖**」段：报出「缺 `critic-report.json` / 缺 DS Spec / `source.dsSpec` 不可读」的**样例名与数量**，并写明「哪些 QA 项因此整体跳过」 | 三个校验器的汇总前输出 | 变异测试 **52 / 20 / 57 PASS**（各 +1 条覆盖留痕用例） |
| **A2** | `layout-audit` 新增 `--spec` 从 L2 Spec 派生档位；`paramSource` 逐项记来源；`paramGaps` 记「spec 声明了但取不到」 | `tools/layout-audit.mjs`（`deriveFromSpec` / `tokenNumbers` / `typographySizes` / `baselineOf`） | 变异测试 **76 PASS / 0 FAIL**（原 60，+16 条 / 新增用例 25–29 含**反向对照**） |
| **C1** | 把运行 A 的一次性装置（Python + PIL）收编为随包发布的零依赖 node 工具 | **新增** `tools/pixel-proof.mjs` + `tools/pixel-proof-mutation.mjs` | 变异测试 **46 PASS / 0 FAIL**；真实截图回归与收编前**逐位一致**（198 色 / 底色 `#f7f7f9` 46.45% / 非底色 53.55%） |

### 本轮的实证（不是「照着做完了」，是「做了之后被数据改了认知」）

1. **B4 的定性被实测推翻。** 原描述说「近半数引用靠兜底解析、目录写错**全**漏」。
   实测：263 条 basename 命中里 **254 条是纯文件名**（兜底本就合理），带目录的仅 9 条、其中 8 条是合理豁免。
   **真正的规模是 38 条**（产物类型名在 `assets/templates/` 有官方同名 → 旧行为静默指向第一个实例，
   17 条**全**指向 `example-health`，制造「具体文件」假象）。
   ⇒ 教训：**兜底规则的危害不在"用得多"，而在"它悄悄替一个类型名挑了一个实例"**。

2. **A2 当场抓到一条已归档结论的偏差。** 运行 B 原始 `layout-audit.json` 的 `spacing` 档位手敲为
   `[2,4,8,16,24]`，而 Spec 只声明 `[8,16,24]` —— **多出的 2/4 是手敲时混入的 radius 值**。
   结论条数没变（均 1 条 spacing），但**尺子不同 ⇒ 数字不可比**。#73 的根因被复现，不是推测。

3. **C1 收编时抓到工具自身的「静默降级」四处。** 且其中一处**违背它自己写在头部的契约**：
   `--json` 声称「机器可读」，却在 JSON 之后又打一行人话结论 ⇒ `JSON.parse(stdout)` 与 `| jq` **全废**。
   另三处：`--grid abc` 让密度图静默变空、`--region` 给了没给值被当成「没写」、`--expect-content` 落在末尾抛 TypeError。
   ⇒ 全部改为**退出码 2 + 可读原因**，并把「选项取值不合法一律退出 2、不静默回落」写进头部契约。
   **这是变异测试第一次跑就抓到的**（46 条里 16 条初始 MISS），不是事后补的。

4. **收编一个新工具，连带暴露了「编排文档漏列闸门」这个老洞（新记 A6）。** 登记 `pixel-proof` 时才发现
   `acceptance-criteria.md` §5 那张「**可直接复制**」的命令表里**连 `layout-audit` 都没有** ——
   而同一份文档 §8.2 早就诊断过「运行 A 漏跑 3 条：`precheck --live` / `layout-audit` / `check-refs`」。
   ⇒ **诊断写了、修的是别的层**：命令表就是「知道要跑它」的唯一载体，漏一条等于没这条闸门。
   两处命令表（`acceptance-criteria.md` §5 · `SKILL.md`）已补，并把「**收编新工具必须同步登记进命令表**」
   写成纪律（否则收编等于没收编）。

**收口实测（2026-09-17，全绿）**：
主闸门 8 个 —— `check-refs` 658 引用 / 悬空 0 / 歧义 0 ｜ `qa-l2` **47** ｜ `qa-critic` **134** ｜
`qa-export --strict` **935** ｜ `qa-install` **100** ｜ `contrast-audit` OK ｜
`layout-audit --spec`（运行 B readback，1 条 medium）｜ `precheck`（基准样例 42 步 / 错误 0）。
变异测试 **9 套 / 475 条断言 / 0 FAIL**：check-refs 31 · contrast-audit 36 · layout-audit 76 ·
precheck 75 · qa-l2 20 · qa-critic 52 · qa-export 57 · qa-install 82 · **pixel-proof 46**（新增）。

**顺带修掉的文档陈旧项（同批，均当场复跑核实）**：`README.md` / `README_EN.md` 的工具清单有**一行重复**
（`qa-export-mutation` 连写两遍）和**五处条数陈旧**（`qa-install` 96→**100**、`qa-install-mutation` 78→**82**、
`precheck-mutation` 63→**75**、`qa-critic-mutation` 51→**52**、`qa-export-mutation` 56→**57**、
`lessons.md` 「48 条」→**「83 条」**，实测 `grep -cE "^[0-9]+\. "` = 83）；两个 README 均补登
`pixel-proof` / `pixel-proof-mutation` / `layout-audit-mutation`（后者**从未进过 README**）。
⇒ 这批陈旧数字本身是 `lessons.md` #74 的又一实例：**数字当时是对的，之后内容长了没人回头改**。

---

## 1.3 债线尾巴（2026-09-17，第三刀）

**范围**：A1（`layout-audit` 吃 `get-node`）+ D1（`paintToHex` 补 opacity）+ B5 / B6（发版守卫）——
即本文 A 组与 D 组里标着「可直接做 / 独立可做」的那几项。**开工前的预期是三件真活；实测后两条半站不住**
（详见上面各候选行的改判，与 `lessons.md` **#84**）。

| 项 | 原预期 | 实测后 |
|---|---|---|
| **A1** | 「readback 形状与 `depth` 语义不匹配，整条 L4 布局闸门**当场跑不了**」 | **需求早已满足**（`31c68cd` 引入即带 `shallowWarning`；`depth` 语义由 `e445a7f` 修好）⇒ 只剩残余：几何覆盖从「容器数」升为「节点数」 |
| **D1** | 「改 `figma-harness.mjs`（**非冻结区，可直接做**）」 | **落点错**（真身在**冻结区**）⇒ 与 D2/D3 同类，**须向冻结区提需求** |
| **B5** | 「不校 `VERSION` ↔ `CHANGELOG` 一致」 | **前提不成立**（文档明写两者允许不同）⇒ 重述为**校序关系** |
| **B6** | 「无 `VERSION` ↔ `git tag` 守卫」 | ✅ 成立，照做 |

| 项 | 落点 | 验证 |
|---|---|---|
| **A1 残余** | `tools/layout-audit.mjs` 新增 `geometryCoverage`（`auditedNodes` / `totalNodes` / `unauditedNodes` / `shallowParents` / `shallowNodes` / `auditedDepth` / `inputDepth`）+ 警告文案改为给出**节点数** | 变异测试 **76 → 89 PASS / 0 FAIL**（+13 条，含反向对照） |
| **A1 顺带** | `tools/figma-harness.mjs` 桩补 `page.selection`（`code.js` 的 `get-node` / `get-page-summary` 都读它 —— **装置缺口被当产品缺陷报出去**的形态） | 探针由崩栈转为正常返回；**`qa-install` / `precheck` 已复跑**（二者共享该装置），均绿 |
| **A1 证据固化** | **新增** `tools/layout-audit-readback-probe.mjs` —— 把 A1 复核从 `.vibe/` 一次性探针提成仓库内可重跑的**跨模块契约探针**（真 `code.js` ←→ 真 `layout-audit`），4 条判据 | **4 PASS / 0 FAIL**：`1/2 · 2/5 · 5/5`，且钉住「depth1 与 depth2 的浅容器数都是 1」 |
| **B5 + B6** | **新增** `tools/release-guard.mjs`（六条判据）+ `tools/release-guard-mutation.mjs` | 变异 **39 PASS / 0 FAIL**；真实仓库 **6 PASS / 0 FAIL**（`v1.2.0` → `9828edb`，`v1.2.0:VERSION` = `1.2.0`） |

**方法上的收获**：A1 的结论**不是读代码猜的**，而是在真执行路径上验 —— `figma-harness.mjs` 加载
**一字未改**的 `code.js`、在桩画布上造一棵 5 节点树、走真实 `execute("get-node", …)`、
把响应落盘后喂给真工具。三组深度得覆盖率 **1/2 · 2/5 · 5/5**，
而前两组**浅容器数都是 1**，漏掉的却分别是 1 个与 3 个节点。

> ⚠ 定稿时改掉两个**照推**出来的数（`lessons.md` #84 的现场实例）：depth1 覆盖率是 **1/2 而非 1/5**
> —— 浅回读把子树整段截掉，漏掉的节点**不在响应里**，工具报的是**下界**；第 3 种回读的问题条数
> **随树与档位变**（3 或 5），**不是契约**。复核已固化为 `tools/layout-audit-readback-probe.mjs`。

**收口实测（2026-09-17，全绿）**：主闸门 **11** 个（新增 `release-guard` / `layout-audit-readback-probe`）—— `check-refs` **751 引用 /
悬空 0 / 歧义 0** ｜ `qa-l2` **47** ｜ `qa-critic` **134** ｜ `qa-export --strict` **935** ｜
`qa-install` **100** ｜ `contrast-audit` OK ｜ `layout-audit --spec`（1 条 medium）｜
`precheck`（基准样例，错误 0）｜ `release-guard`（6 PASS）｜ **`layout-audit-readback-probe`（4 PASS）**。
变异测试 **10 套 / 527 条断言 / 0 FAIL**：check-refs 31 · contrast-audit 36 · layout-audit **89** ·
precheck 75 · qa-l2 20 · qa-critic 52 · qa-export 57 · qa-install 82 · pixel-proof 46 ·
**release-guard 39**（新增）。
> ⚠️ 以上是**第三刀收口当时**的数；**第四刀后为 533 条 / 0 FAIL**（`qa-export` 57 → 63），
> 引用数 **751 → 782**（增量同样是「写记录这件事本身」推动的，见 `lessons.md` #74）。

**第四刀（立项第 0 项 · QA3 判据修正）见文末最后一节。**

**文档改判（本轮的另一半产出）**：`references/acceptance-criteria.md` §8.1（表第 2 行 + 「⚑ 1.3 复核」小节）·
§8.3（「未核对 · 1 条」成因改判）· §5 命令表（登记 `release-guard`）；`SKILL.md` / `README.md` /
`README_EN.md`（登记新工具 `release-guard` / `layout-audit-readback-probe`、`layout-audit-mutation` 76 → 89）；
`references/lessons.md`（新增 #84 + 守卫覆盖表）。

---

## 1.3 立项第 0 项 · QA3 判据修正（2026-09-17，第四刀）

**来源**：立项书 §2.2 第 0 项（用户裁决 ②）——「撤掉 `live-build` 档位对 `figmaFileKey` 的强制要求」。
**约束**：必须作为一次正式的**判据修正**走流程 —— **改判据不改编造 + 变异测试覆盖 + 文档改判留痕**，
**不许顺手改**。本刀**只有这一件**，是立项书定序后开工的第一个动作（顺序见 `1.3-charter.md` §3）。

**为什么它不是「放宽校验器」**（与 A4 教训的关系必须分清）：A4 的教训（`lessons.md` #34 与「**改措辞不放宽校验器**」）
治的是「**文档措辞不严谨，却去松校验器**」；本条病灶相反 —— **判据引用了一个本拓扑根本得不到的外部凭证**。
`figmaFileKey` 的用途是「按 fileKey+nodeId 回读」，那在**基于 REST API 的上游**里是必要条件；本管线的回读通道是
**Bridge 直连插件**（`get-node` 凭 `nodeId` 单键即可），且插件**没有任何 op 返回 `figma.fileKey`**（D2）。
⇒ 在本拓扑下它**不是判据，是噪音**（运行 A / 运行 B 各撞一条永久 FAIL）。系统教训见 `lessons.md` **#85**。

| 项 | 原判据 | 现判据 | 验证 |
|---|---|---|---|
| **D4 · QA3 的 live-build 分支** | `必须有 figmaFileKey`（无条件硬要求） | **如实标 `null` + 在 `_meta.note` 交代缺口**即放行；**已声明就必须非空**（空串 `""` ≠ `null`）；静默留空 / 全空白 / 空串 **三条仍拦** | 变异 **57 → 63 PASS / 0 FAIL**；运行 B `409 PASS / 1 FAIL` → **`410 PASS / 0 FAIL`** |
| **不变式（守住「不是删判据」）** | —— | **两条路径各带一条**凭证判据（数量必须相等，且各自断言行可见） | 运行 A `364 PASS` **不变**、自带样例 `--strict` **`935 PASS` 不变** —— **数字不变正是「换判据」的证据** |
| **D2 · 插件侧** | 向冻结区提需求（立项书 §2.2 第 5 项） | **不变** | 该缺口仍是事实，只是不再表现为一条永久 FAIL |

**盲区（如实记）**：新判据（`_meta.note` 非空）**不证明画布真的存在**，只证明执行者**显式交代了缺口** ——
画布是否存在由 `precheck --live` 的**实际回读**证明，那是另一条时刻的判据（`acceptance-criteria.md` §3.3 / §1.3）。

**文档改判**：`assets/templates/export-manifest.json`（字段描述）· `references/acceptance-criteria.md`
（§1.3 加「别反推」注 + 盲区 · §5 L5 命令块 · §8.3 边界表「环境不可达 · 1 条」**移出并留痕为一条注**）·
`SKILL.md` · `README.md` / `README_EN.md`（`qa-export-mutation` 57 → 63）· `references/lessons.md`（**#85** + 守卫覆盖表）·
`1.3-charter.md` §2.2 第 0 项标已完成 · `1.3-plan.md` §2.6 · `1.3-status.md` 基线。

**实测数字（当场复跑）**：`qa-export --strict` **935 PASS / 0 FAIL / 0 WARN** ｜ 运行 A **364 PASS / 0 FAIL** ｜
运行 B **410 PASS / 0 FAIL** ｜ `qa-export-mutation` **63 PASS / 0 FAIL**（+6）。

---

## F4 第 2 步 · 三份来源合并过筛（2026-09-17 · 立项书 §2.2 第 1 项，⏳ 进行中）

**产出**：`references/design-criteria-intake.md` 由「1 份样例」扩为 **3 份来源 / 46 条原则** +
**回读契约表** + 第二批八条发现。**本轮零工具代码改动**（纯文档轮）。

**筛选口径扩两档**：新增 **已有**（闸门 / 审计器已在做 ⇒ **不重做**，否则重复造轮子 + 两套判据口径漂移）
与 **拆**（一条规则内部结论不同 ⇒ **还要再分一次**，不是一种落点）。

| 来源 | 搬 | 译 | 交人 | 无对象 | 拆 | 已有 | 合计 |
|---|---|---|---|---|---|---|---|
| `better-ui`（17） | 2 | 2 | 1 | 9 | 3 | 0 | 17 |
| `better-layout`（10） | 1 | 2 | 0 | 2 | 4 | 1 | 10 |
| `better-typography`（19） | 0 | 1 | 0 | 9 | 8 | 1 | 19 |
| **合计** | **3** | **5** | **1** | **20（43%）** | **15（33%）** | **2** | **46** |

### 本轮新增的 D 组需求单（**本次性价比最高的一笔** · 立项书 §2.2 第 5 项）

`better-typography` 里有一批规则**判据明确、值也给全了**，却**全部死在「字段不回读」**上 ——
这不是「判据不可算」，而是 **S1.5「能力前提不成立」的新子类：不是缺 op，而是缺回读字段**：

| 需求（`get-node` 新增回传字段） | 卡住的外部规则 | 依据 | 备注 |
|---|---|---|---|
| **数值 `fontWeight`**（现只有 `fontName.style` 字符串如 `SemiBold`） | 「`18px` 以下字重 ≥ `400`」·「`300` 以下只配 `28px`+」 | 契约表 §1.5 · 发现 §4.1 | **冻结区**（`figma-plugin/`）⇒ 立项书 §2.3：**只允许提需求，不许构建期改** |
| **`lineHeight`** | 「行高按角色（`1.1` / `1.5–1.6` / 换行 3 行以上 ≥ `1.4`）」 | 同上 | 替代路径「单行 `height ÷ fontSize` 反推」**明确不采用**（多行需估行数，与「从 padding 反推内边距」同类误报风险） |
| **`letterSpacing`** | 「字距按字号」 | 同上 | 该规则**同时**还有「原文未给值」的问题（"often look better" / "a little"）⇒ 即使补了字段也只能到「**译**」 |

> **一句话**：**一次加这三个回读字段，解锁 4~5 条外部规则**。
> 与 **D1**（`paint.opacity` 回传）/ **D2**（`ping` 返 `fileKey`）/ **D3**（未知 `nodeId` 挂起）**同类同区**，
> 合并成一张需求单提交 —— 这是立项书 §2.2 第 5 项「D 组需求单」在本轮**新增的两条**（原只有 D1/D2/D3）。

**另一条比继续筛更有用的动作**：**先列「回读契约里有哪些字段没有任何检查在读」，再按这个清单去找规则**
（`textAutoResize` / `textAlign` / `clipsContent` / `layoutSizing` 全属此类，其中 `textAutoResize` **一条解锁三条规则**）——
F4 的产出密度取决于「未使用字段清单」，而不是「来源数量」。**✅ 本条已于次日（2026-09-18）执行**，
见下节。

**顺带订正一处陈旧错引**：`better-ui` 表第 1 行原写「见 §四.1」（§四 是结论）→ 应为 **§3.1**「连续公式撞离散白名单」。

**实测（当场复跑，纯文档轮）**：`check-refs` **826 引用 / 悬空 0 / 歧义 0**（800 → 826，`lessons.md` #74 同族）｜
`qa-install` **100 PASS / 0 FAIL**。

---

## F4 第 3 步 · 「未使用字段清单」执行完毕（2026-09-18 · 立项书 §2.2 第 1 项的并行产出线，✅）

> 承接上节末尾的建议。**两个工具层提交**（`1ce6953` 契约使用矩阵 / `62b8d83` 三类新检查）+ 文档层。
> 完整记录在 `references/design-criteria-intake.md` **§七**；此处只留结论与指针。

| 产出 | 内容 |
|---|---|
| **新工具 `tools/contract-usage.mjs`**（+ 变异测试 **42 PASS**） | 现场**解析** `code.js` 序列化段得 **33 个契约字段** × `tools/*.mjs` 属性访问；三档：用 / 待复核 / **零引用**。判据双向（有消费者 **或** `UNUSED_OK` 写明理由），不一致 **exit 1** |
| **首跑结果** | **20 在用 / 3 待复核 / 10 零引用**；待复核 3 个（`opacity`/`fills`/`parentId`）人工核实全是**写侧**，不是读者 |
| **收编 5 字段 → `layout-audit`** | 3 类新检查：`text-container-fixed` / `text-justified` / `font-family-count`（**10 → 13 类**，全 medium）+ `overflow` 在父级 `clipsContent=true` 时升 **high**（静默隐藏）+ `TRUNCATE` 只进 `textTruncated` 清单。变异测试 **89 → 125 PASS** |
| **真实数据零扰动** | run-B 归档回读（377 节点）改动前后 `summary` **完全一致** —— 新检查全部因前提不成立而不触发 = **哨兵，不是噪音** |
| **修文档 3 处** | 契约表「谁在读」至少 4 处凭印象的错（重写 §1.5）· 新增**第三态**：`layoutSizing` 恒为常量 ⇒ 「能回读 ≠ 有信息量」，§2.3 #9 落点收缩 · 新增 §七 |

**对 D 组需求单的影响**：无新增 —— 但**反向验证**了它的价值：零引用清单里 `strokes` / `strokeWeight` /
`vectorPathCount` 判据可写，本轮**刻意不做**（先有判据来源再写检查的顺序纪律），`UNUSED_OK` 逐条写明理由 ——
**清单是待办有理由，不是忽略**。
