# 1.3 候选清单

> **来源**：1.2「可信版」收尾验收（2026-09-17）时归拢。1.2 期间按 `1.2-plan.md` §8「**新增能力项 → 默认拒绝，记入 1.3 候选**」积累的条目，以及两次实测（D2 运行 A / 运行 B）暴露的缺口，**全部收在这里**，1.2 的文档不再散记。
> **性质**：这是**输入**，不是计划。1.3 的范围、优先级与取舍在 1.3 立项时另行定稿。
> **分组**：A 已定案 / B 已定位缺陷 / C 待收编装置 / D 装置缺口 / E 能力边界 / F 新能力 / G 缓做债项；组内按建议优先级排。
> **本文件属开发者内部文档**（`tools/check-refs.mjs` 的 `DEV_ONLY` 模式 `^\d+\.\d+-[^/]*\.md$` 自动豁免），允许引用包外路径与运行产物。

---

## A. 已定案：命令接入点对齐

**来源**：D1 §8 的待定强化项（「薄编排器」做不做）已由 D2 两次实测回答 ——
**立项，但范围重定义为「命令接入点对齐」，不是薄编排器**。

**判据**（不是感觉，是两次实测的数据）：

- 运行 A 漏跑 3 条、运行 B 漏跑 4 条，**成因都不是「命令多到记不住」，而是「不知道该跑它」**
  （`1.2-plan.md` §7.1；`references/acceptance-criteria.md` §8.1 / §8.2）。
- 更根本的一条：**闸门参数与规格不同源** —— 上一轮「全绿」的真相是
  `--min-touch 0` / 无 `--baseline` / `--font` 含 16，**三条检查缺席或放宽**（`lessons.md` #73）。
  ⇒ 光把命令串起来没用，**得让参数本身有源**。

**证据**：`CHANGELOG.md` 运行 B 一节 · `references/acceptance-criteria.md` §8.2 · `1.2-plan.md` §7.1

| # | 子项 | 现象 / 出处 | 建议优先级 |
|---|---|---|---|
| **A1** | 让 `layout-audit` 直接吃 `get-node` 输出 | 运行 A 的 readback 形状与 Bridge `depth` 语义不匹配，整条 L4 布局闸门**当场跑不了**，只能改走自建几何脚本 ⇒ 运行 A 的 `layout-audit` 至今**「未核对」且事后不可补** | 高 |
| **A2** | **闸门参数从 L2 Spec 派生** | 手敲 CLI 是 #73 的根因。应读 L2 的 `spacing` / `typography` 直接生成参数，比靠纪律可靠 | 高 |
| **A3** | L4 读图结论要有**结构化落点** | `critic-report._evidence.visionObservations` 字段已开，但运行 B 的 L4 走读图时**没有产物承载** ⇒ `qa-critic` 无输入，只能记「未核对」 | 中 |
| **A4** | `qa-export` 需要诚实的「**无 manifest**」降级路径 | 运行 B 的 L5 改走逐组件 PNG，`export-manifest.json` 未生成 ⇒ 闸门无输入。**「没查」要有专门的输出形态**，不能只靠人记得 | 中 |
| **A5** | `precheck --live` 接受**增量计划** | 运行 A / B 的实际操作都是「逐批小计划」，而 B2 的核对入口是按「完整计划」设计的 | 低 |

---

## B. 已定位缺陷（可机械修，无需新设计）

**1.3 开工第一刀已做掉 B1 / B2**（2026-09-17，见本文末「1.3 开工第一刀」），B3 / B4 待做。

| # | 缺陷 | 守卫现状 | 1.3 状态 | 出处 |
|---|---|---|---|---|
| **B1** | `alignment` 在**跨轴长度不等**时**静默跳过** —— 不报，也不计入 | ✅ **已收编（留痕式）**：`layout-audit.mjs` 第五个留痕段 `alignmentCoverage`（`compared` / `skippedCrossSizeDiff` / `samples`）+ 控制台「对齐覆盖」行 + 变异测试 #23/#24 | ✅ **已做** | `lessons.md` #67c |
| **B2** | `precheck --live` 的**汇总行不随后续在线核对更新** —— 汇总打印 `待核对 1`，紧随其后的 B2 段却已 `ok 存在`；而 §6 要求留痕的恰是**汇总行** ⇒ 把「**已核过**」读成「**没核**」 | ✅ **已收编**：「离线汇总 / 最终汇总（含在线核对）」两行 + `已核对 / 核出不存在 / 未核对` 三计数 + 变异测试 F 组 | ✅ **已做** | 1.2 收尾新发现 |
| **B3** | 其余校验器缺 `unchecked` / `skipped` / `inputCoverage` 同类留痕 | ✅ 仅 `layout-audit` 有（现为**五个**留痕段）；**其余全都没有** | ⏳ 待做 | `lessons.md` #72 |
| **B4** | `check-refs` 的**全仓库同名兜底**会让「**目录写错、文件名对**」通过 | ❌ **无守卫**（**近半数**引用靠 basename 兜底解析——精确占比会随文档增补而变，**别写死**；复核：跑 `tools/check-refs.mjs` 读它打印的 `已解析 N：path=… / basename=… / glob=…` 一行） | ⏳ 待做 | 1.2 收尾新发现：`design-system.md` 曾把 Brief schema 写成 `assets/templates/design-brief.json`（真身在仓库根的 `templates/`），`check-refs` **未报** |
| **B5** | **`VERSION` 与 `CHANGELOG` 可各说各话** —— `qa-install` 只校「探针 ↔ `VERSION` 文件」一致，**不校「`VERSION` ↔ `CHANGELOG` 顶部」** | ❌ **无守卫**（1.2 定版前 `VERSION` 停在 `1.2.0-dev`、CHANGELOG 却已开到 1.3 段，两处对不上时全部闸门照绿） | ⏳ 待做 | 1.2 定版时发现（2026-09-17） |

> **B4 值得单独看**：它与 `qa-export` QA9 抓到的「`preset:` 前缀合法但路径不存在」是**同一类** ——
> **格式对了，指的东西不一定在**。校验器的兜底规则越宽，「指错地方」越容易漏。

> **B1 的修法值得单独看（反直觉，别再"修"回去）**：`alignment` 跨轴尺寸不同就跳过，看着像"漏检"，
> 于是最自然的动作是**放宽判据**。实测（运行 B readback）这个动作**是错的**：
> 真正比过 **3 对**、因跨轴尺寸不同没比 **121 对**，放宽后在 121 对里新增 **5 条报告 —— 5 条全是假阳性**
> （时间戳 231×27 vs 在线圆点 10×10：圆点**居中**对齐文字，上边缘当然差 8px）。
> **跨轴尺寸不同时视觉惯例是居中，边缘对齐不是正确判据。** 故正确修法是**留痕**：
> 把「没比多少对、是哪些对、差多少」全部写进产物，让人知道这块没被边缘判据覆盖。
> 反向闸门见 `layout-audit-mutation.mjs` #24（居中对齐的合法布局**不得**报 alignment）。

---

## C. 待收编的临时装置

| # | 装置 | 现状 | 出处 |
|---|---|---|---|
| **C1** | **效果类参数的可见性检查**（像素直方图 + 密度图） | ⚠️ 只在运行 A 运行目录下，**不随包发布** | `lessons.md` #56 / #57；守卫覆盖表 |
| **C2** | **增量补齐工具** —— 喂「期望节点清单 + 目标父级」，它自己回读、算差、只补差集 | 📏 纪律，无工具；运行 B 的临时脚本已完成使命 | `lessons.md` #65 / #71 |

> **C1 是当前最该收编的一条**：#56 是运行 A「最值钱的发现」（负载参数缺少能否证它的观察通道），
> 但它至今只活在运行目录里 —— **下一次跑还会重新踩**。

---

## D. 装置缺口（受冻结区或宿主 API 限制，非产品缺陷）

| # | 缺口 | 影响 | 处置 |
|---|---|---|---|
| **D1** | `paintToHex()` 不回传 `paint.opacity` | 填充透明度**不可结构化观测**，只能读图 ⇒ L4「材质一致性」缺通用档证据 | 改 `tools/figma-harness.mjs`（**非冻结区**，可直接做） |
| **D2** | 插件 `ping` 不返回 `figma.fileKey`，且 `code.js` 属冻结区 | **L5 Export Gate 的 `live-build` 档位在本环境无法通过**（运行 A 唯一 FAIL） | 向冻结区**提需求**，不在构建期改（运行 A #11 已因当场改冻结文件被 QA7 拦下） |
| **D3** | **不存在的 node id 让插件挂满 10s，然后报「无法连接 Figma，请检查网络」**（`figma-plugin/code.js` 的 `getNodeByIdAsync` 在 `documentAccess: "dynamic-page"` 下对缺失 id 会去加载对应页面，失败后由 Figma 宿主抛出这句网络错误；代码里 `if (!node) throw fail(..., "NODE_NOT_FOUND")` 的分支因此**永不触发**） | ① **诊断陷阱**：把「id 不存在」说成「网络问题」，把排查引向无关方向；② 每次核对缺失 id 白花 10s；③ `precheck --live` 设计里的「核出不存在」专门路径**在本环境永不触发** ⇒ 所有缺失 id 落进「意外响应」并判 FAIL，`核出不存在` 计数恒为 0 | 根因在**冻结区**，本轮**未改**任何冻结文件。工具侧**不应用关键词猜**（会把真·连接故障一起吞掉）——如实报 FAIL 是本环境唯一不撒谎的选择，已在 `lessons.md` #76 写明该边界。可向冻结区提需求：`get-node` 对未知 id 先查页面清单再决定，或限定加载范围 |

---

## E. 能力边界（明确「补不了」，记录以免反复立项）

| # | 边界 | 说明 |
|---|---|---|
| **E1** | **语义性缺失**：无轴标 / 无单位 / 无空态提示 / 无图例 | 几何**完全合法**，数值审计在原理上不可见（运行 B 的 168 格热力图无一根轴标）。只能靠读图评审，**且目前没有机械裁判** |

> 记在这里的用途是**防止它被当成待办反复立项**：它是**判定的能力边界**，不是实现缺口。
> 真正可做的是「把读图评审变成流程必选项」，而不是「写个脚本查它」——后者写不出来。

---

## F. 新能力（需评估，不是债）

| # | 能力 | 状态 | 出处 |
|---|---|---|---|
| **F1** | **MCP 外壳「门铃式」中间态** | 1.3 单独评估。MCP server **不内化规则**，只暴露「按本 SKILL.md 执行」的入口，规则留文档形态 | `1.2-plan.md` §3.1.3 / §5 |
| **F2** | **图片填充** | 实现路径已定（见 `1.2-plan.md` 附录）。受 `figma-plugin/manifest.json` 的 `allowedDomains` 只允许 `localhost:45677` 限制 —— 插件拉不了任意 URL，须走本地中转 | `1.2-plan.md` §5 + 附录 |

---

## G. 缓做的债项（1.2 明确划出，防止范围膨胀）

| # | 项 | 出处 | 备注 |
|---|---|---|---|
| **G1** | #11 sha256 清单机制 | `1.2-plan.md` §3.2.3 | 有增益但非阻塞 |
| **G2** | #12 op 映射对照 | 同上 | 同上 |
| **G3** | #14 selftest 反向用例 | 同上 | 同上 |

---

## H. 文档盲区（来自 A1 独立盲测的实测清单）

**来源**：2026-09-17 的 A1 独立盲测（见 `1.2-plan.md` §4.A1 与 `CHANGELOG.md` 的 1.2 段）。
三轮执行者（均为全新 context、只读材料）各自记下「文档没说清、只能靠猜」的点。
**去掉第一轮「材料不全」造成的色值类缺口**后，剩 **6 处两轮以上共识的盲区** ——
这是 `references/design-system.md` 的**实测缺口清单**，可直接作为改文档的输入。

| # | 盲区 | 出现轮次 | 说明 |
|---|---|---|---|
| **H1** | **typography 字阶映射规则** | 一 / 二 / 三 | Brief 给的是 12 段数字串（如 26-32-16-24-14-20-13-20-12-18-11-16），文档只说有五类 Typography 子组，**没给「数字串 → Display/Heading/Body/Caption/Number 各取哪个」的规则**。三轮执行者各猜一套，结果互不相同 |
| **H2** | **pagePadding / spacing.base 等非 cellPadding 档位** | 一 / 二 / 三 | §7 只由 SP-2 规定了 cellPadding（dense=12），其余 spacing 档位没有取值规则 |
| **H3** | **web-admin 的响应式断点** | 一 / 二 / 三 | 文档未对 web-admin 平台规定 breakpoints |
| **H4** | **status.warning 取黄还是橙** | 二 / 三 | ST-1 说「按色相从 chartColors 映射」，但黄/橙二选一无判据 |
| **H5** | **focusRing 与 touchTarget 的具体数值** | 一 / 二 / 三 | 字段存在，取值规则缺失 |
| **H6** | **字重取值** | 二 / 三 | §6 未给字重规则 |

> **这 6 处都不是 bug，是「约束内的自由」。** 三轮里第二、三轮均 `qa-l2` **18 PASS / 0 FAIL** ——
> 即文档规范了**必须满足什么**，但没规定这些具体值取多少。所以这是一批
> **「要不要把自由收紧」的产品决策**，不是「缺陷修复」批次：
> 收紧 → 降方差但文档更厚；不收紧 → 保留执行者的判断空间。
> **建议在 1.3 定稿范围时一并裁决**，别直接塞进"修缺陷"的批次里做掉。

---

## 归拢时（1.2 收尾）已顺手修掉的（**不留到 1.3**）

| 项 | 处置 | 落点 |
|---|---|---|
| `example-health` 同名两物（美业「AI 型衣」vs 医疗「AI 智能健康管理」） | ✅ 已裁决并改名（导出包副本前缀 `health-` → `healthcare-`，核心示例不动） | `references/lessons.md` #44 |
| 大屏基线：1920×1080 vs 3840×2160 | ✅ 已裁决为 **1920×1080 是设计基准，3840×2160 仅 @2x 出图倍率** | `references/design-system.md` **LD-10** |
| `design-system.md` 的 Brief schema 路径写错（`assets/templates/` → 真身在根 `templates/`） | ✅ 已修（**B4 的实例**；同类已全仓库扫过，仅此一处） | `references/design-system.md` §8 |
| `lessons.md` 守卫覆盖表两处陈旧（重份节点检查标「待收编」、`lessons` 累计条数） | ✅ 已改判 | `references/lessons.md` 附 |
| 一条目录里的**闸门参数与规格不同源**（`minTouch:0` 等） | ⚠️ 未修，**属 A2** | 见上 |

---

## 1.3 开工第一刀（2026-09-17）

范围：**B1 + B2 两个工具层缺陷**（`1.2-plan.md` §7.1 之后的第一个动作），外加**回归时暴露的改名涟漪**。
三件都属「已定位、可机械修」，无新判据设计。

| 项 | 内容 | 落点 | 验证 |
|---|---|---|---|
| **B1** | `layout-audit` 新增第五个留痕段 `alignmentCoverage` —— 把「edge 比了几对 / 因跨轴尺寸不同没比几对 / 是哪些对」写进产物。**修法是留痕不是放宽判据**（放宽实测 100% 假阳性） | `tools/layout-audit.mjs`（walk 循环 + 报告段 + 控制台行） | 变异测试 **60 PASS / 0 FAIL**（原 51，+9 条：留痕 7 + 反向闸门 2） |
| **B2** | `precheck --live` 核对前那行加「离线汇总」限定词，核对后**必再打一行**「最终汇总（含在线核对）」，并给出 `已核对 / 核出不存在 / 未核对` **三个数**（三路径分别落位 1/0/0 · 0/1/0 · 0/0/1） | `tools/precheck.mjs`（两行汇总 + 三计数） | 变异测试 **75 PASS / 0 FAIL**（原 66，+9 条）· 另用**真 Bridge + 真画布**实测一次（`7:328` 存在 → 已核对 1；`999:999` → 未核对 1） |
| **改名涟漪** | `tools/qa-export-mutation.mjs` 里 `["saas","health","highway"]` **一个列表兼两用**（核心示例目录名 + 随包清单名），改名后两者不再同名 ⇒ 拼出已不存在的 `example-health-export.json`，整份变异测试 **ENOENT 崩掉**（上一轮改名后没复跑，故一直潜伏） | `tools/qa-export-mutation.mjs` 拆成 `CORE_EXAMPLES` / `EXPORT_NAMES` + `manifestKey()` 显式转名 | 复跑 **56 PASS / 0 FAIL** |
| **新发现 D3** | 排查 B2 时顺带实测出来：**不存在的 node id 让插件挂 10s 后报「请检查网络」**（非网络问题）。已记入本文 D 组 + `lessons.md` #76，**未改任何冻结文件** | 根因在冻结区 `figma-plugin/code.js` | 3/3 复现（10.1~11.0s vs 存在的 id 54ms） |

**教训回写**：`lessons.md` #67c 改判为 ✅ 已收编（附实测数据）；新增 **#75**（改名的涟漪要追到代码字面量 + 改名后**必须复跑全套闸门**）、**#76**（宿主 API 用错误类别的错误回答"查不到"时，工具层不许用关键词猜）；守卫覆盖表三行改判；`layout-audit-mutation.mjs`(51→60) / `precheck-mutation.mjs`(66→75) 计数订正。

**未做（仍在 1.3 待办）**：B3 · B4 · C1（效果类参数可见性收编） · A2（闸门参数从 L2 Spec 派生） · A1/A3/A4/A5 · D1 · D3 向冻结区提需求 · E/F/G 各组。

> ⚠️ **1.3 的完整范围尚未定稿** —— 本表只记录**已开工的这一刀**。范围、优先级与取舍待立项时定稿，
> 输入就是本文 A–G 七组。
