# 1.4 候选清单

> **来源**：v1.3.0「裁判版」定版（2026-09-18）后归拢。1.3 期间按纪律「**范围外请求 → 默认拒绝，记候选**」
> 积累的条目，加上定版当天的**回指复核**（`lessons.md` #84：候选是时点快照，动手前必须回指产物核**定性 / 前提 / 落点**）。
>
> **性质**：这是**输入**，不是计划。⚠️ **1.4 尚未立项** —— 是否纳入、按什么顺序、怎样算做完，
> 须先写 `1.4-charter.md`（立项书）并由决策者签字。`lessons.md` **#9**：**「能开工」不等于「该开工」**。
>
> **分组**：A 判据收编 / B 参数同源余量 / C 生成侧 / D 新维度 / E 零引用字段 / F 缓做债 / G 遗留口径。
> **证据强度标记**：✅ 已实测（附取数时点）· ⚠️ 事实成立但未观察到后果 · ❓ 待复核。
>
> **本文件属开发者内部文档**（`tools/check-refs.mjs` 的 `DEV_ONLY` 模式 `^\d+\.\d+-[^/]*\.md$` 自动豁免）。

---

## 零、先定尺子，再挑条目

1.3 的代价是实测过的：三刀都符合「已定位 / 可机械修 / 无新判据设计 / 有实测数据可验」，
**每刀单独成立、合起来拼不成一个可验收的范围**，且「两条半站不住」。⇒ 所以本清单**不按优先级排序**，
而是先给出四条**互斥的主线**（见文末 §八），主线定了再筛条目。

**1.2 是「可信版」**（留下可引的痕迹）· **1.3 是「裁判版」**（让每一环有可证伪的裁判）。
1.4 的主题**待定** —— 这是本次商量的核心问题。

---

## A. 判据收编（F4 已筛、尚未收编）—— 证据最硬的一组

> **共同前提已全部就位**：① 判据来源在 `references/design-criteria-intake.md` 里**逐条给了落点**；
> ② 所需字段在回读契约里（FR-1~5 已于 2026-09-18 真机验证）；③ 装置已在仓库里（`layout-audit` / `contract-usage`）。
> **intake §六 与 `1.3-status.md` §六 里「筛完 ≠ 收编完」被写了三遍** —— 这句本来就是留给 1.4 的。

| # | 条目 | 证据与现状 | 落点 | 代价 |
|---|---|---|---|---|
| **A1** | **文案 tell 审计器**（AI 味检查） | taste-skill 过筛产出约 **12 条搬**共享同一落点形态（`characters` 正则 / 模式表）；intake §2.4 **明确「本轮刻意不实施」**。⚠️ 关键事实：该来源的 tell **假定产物是营销落地页**，而运行 A/B 是**仪表盘** ⇒ hero / footer / scroll 域的 tell 在仪表盘上**结构性缺席**，等宽 KPI 卡在仪表盘上还**合法** | 新审计器（或 `layout-audit` 新检查类） | **先决问题是判据校准，不是代码**：必须先定 ① **中文等价 tell 名单**（「张三/李四/示例」「第一步/步骤 1」「某某科技」…）② **产物类型分档**（哪些 tell 对营销类生效、对仪表盘豁免）。两表**定不出来就是「永远报 0 条」的假绿灯**（#87 同族）。两表都需要**人的产品判断** |
| **A2** | **「画布色 ↔ Spec token」一致性信号** | 获**两处独立指认**：intake §1.5（F3② 收编 `fills` 时发现 `contrast-audit` 只读 Spec、**从不看画布**）+ intake §2.5 #4（`review-checklist`「禁裸色阶、必须语义 token」的可判定内核 = 画布 hex ∈ Spec token 集）。intake 已写「**优先级上调**」 | `contrast-audit` 或 `layout-audit` 新信号「板外色计数」 | 判据可算（hex 集合比对）；**校准项**：按 Preset 定「板外色」白名单豁免，防 §3.1 式冲突（`web-marketing` 的 Hero 可有 20–24px 圆角那类例外）。**必须报候选计数交人，不得直接判违规**（§3.1 的「连续公式 vs 离散白名单」哲学冲突未裁决） |
| **A3** | **排版三条业务判据**（行高 / 字距 / 字重） | **字段已补齐**（FR-3 `lineHeight` / FR-4 `letterSpacing` / FR-2 `fontWeight`，真机验证通过）。但 `contract-usage.mjs` 的 `UNUSED_OK` 注释**明写**：「`qa-plugin` 已在读（形状断言）—— 但注意『用』只到 L1，**业务判据仍未立**」 | `layout-audit` 新检查类 | 判据来源齐（`better-typography` #7 / #8 / #4）。⚠️ 三条都带**语言分档或原文未给值**问题：#8 原文只有 "a little"/"often look better" ⇒ 即便有字段也只能到「**译**」（可疑信号，不是判定） |
| **A4** | **F4 → F3 供料第 2 批（3 条读图项）** | 假产品截图（taste-skill #17）· `<br>` 斜体断行标题（#26）· 眉标下微说明（#30）—— intake §2.4 末明确「**待并入** `references/read-image-checklist.md`」，且并入时**按 R 系列续号 + 全线数字联动** | `read-image-checklist.md` | **最小的一条**（纯文档 + 数字联动）。⚠️ 注意 R 系列条数会牵动 `qa-critic` / 变异测试 / README 的数字（#74） |
| **A5** | **字号下限检查 `font-size-floor`** | intake §2.3 #16 判「**搬**」，落点写「`layout-audit` 新检查类 `font-size-floor`」——「现有字阶检查只查『∈ 档位』，若 8 恰在档位里也不报」。⭐ **本次复核新发现：数据源是现成的** —— L2 Spec 的 `accessibility.minFontSize` 是 **required 字段**，运行 B 里写着 `{"value":14,"source":"rule:TY-3(preset avoid 禁 <14px，高于常规 11/12 下限)"}` | `layout-audit` 新检查类 | **便宜**：判据（`fontSize < minFontSize`）+ 数据源（Spec 已有）都齐，且天然**参数同源**（符合 #73）。⚠️ 需与 B1 一起做（见下） |

---

## B. 参数同源余量（A2 未扫完）—— **本次复核新发现** ✅ 2026-09-18 实测

> **A2 的标题是「闸门参数从 L2 Spec 派生」，实际只派生了 4 项**
> （`paramSource` 当次输出：`scale=spec:tokens.spacing` · `radius=spec:tokens.radius` ·
> `font=spec:tokens.typography[].size` · `baseline=spec:responsive.baseline`）。
> 而 L2 Spec 的 **`accessibility` 是 required 的一节，含 4 个字段**（`contrast` / `minFontSize` /
> `touchTarget` / `focusRing`）—— **`tools/layout-audit.mjs` 里 grep 不到 `accessibility` 一词**。

| # | 条目 | 实测（2026-09-18） | 代价 / 判据 |
|---|---|---|---|
| **B1** | **`accessibility` 一节完全未接** —— `--min-touch` 默认 **44 硬编码**（`parseFlag("--min-touch", 44)`），**不进 `paramSource`**；而运行 B 的 Spec **明确声明** `touchTarget:{value:0, source:"rule:AC-4(big-screen 无触控目标要求，值守+遥控场景不适用)"}` | ⚠️ **定性要说准**：这是**参数与规格不同源的事实**，**不是已发生的假阳性** —— 拿运行 B readback 实扫，名字命中控件正则（`TOUCH_NAME`）的节点 = **0**，故触控检查本次结构性不触发。**失真方向仍是「合格产物被误判」**（#85 同族）：一旦产物里出现名字像控件的 <44 节点，闸门会报 **high**，而 Spec 说该产物**不适用触控** | 让 `--min-touch` 走 `CLI > spec:accessibility.touchTarget > 默认` 三档（与其余四项同构）+ 进 `paramSource`。**注意边界**：`value:0` 是「**不适用**」而不是「阈值为 0」—— 必须与「字段缺失」区分（#12 判据 ②：`null`/缺字段/空串/全空白不是一回事），不适用时该检查应进 `unchecked` 而非静默通过 |
| **B2** | **`--font-family-max` 同样硬编码**（默认 3），不进 `paramSource` | 同批实测 | 与 B1 同一处改动；来源是 `tokens.typography` 的字族数或新字段，**若无来源则应如实标 `default`**（不假装来自规格） |

> **为什么这一组值得优先**：A2 的收口记录写着「**A2 必须在加任何新检查类之前完成**，否则新检查的档位参数又会变成手敲的（#73 根因重现）」
> —— 而 A5（字号下限）恰恰需要一个「来自 Spec 的下限参数」。**B 是 A 的前置**。

---

## C. 生成侧（L1 / L2）—— 结构性偏斜 ✅ 2026-09-18 git 实测

| # | 条目 | 证据 | 代价 |
|---|---|---|---|
| **C1** | **「配方」类落点至今无处落地** | intake §1.4 把落点分两类：**检查项 → 闸门；配方 → 生成侧（Style Preset / L2）**。已筛出的配方：`better-ui` #8（图片 1px 描边）· #6（别拿 Inter 当默认族）· `better-layout` #1 后半（分隔线优先级）· `taste-skill` #6 / #32 的替代（动词直接做标签）· `better-typography` #5 后半（语义化命名）。**这些一条都还没有落点** | 改 `assets/style-library/*.json`（4 个 Preset）与 `references/design-intelligence.md`（L1 八节） |
| **C2** | **生成侧自 1.1 起一字未动** | `git log -1`：`references/design-intelligence.md` 与 `references/design-style-library.md` 最后改动均为 **`c9c1f35`（2026-09-16，1.1 收尾）**。1.2 / 1.3 全程只动裁判侧（L4/L5）与文档。⇒ **这条链一直在造「怎么查得更准」，没碰过「怎么做得更好」** | ⚠️ **代价最高的一类**：改 Preset = 改生成规则，会牵连 `qa-l2`（47 断言）与**历史产物的可比性**；且「配方」的验收天然比「检查」软（配方对错要靠读图 / 人判，撞 H3「零目测评分」） |

> **注**：C 与用户在 F3 来历里问过的「**是不是把设计的功能给丢掉了**」是同一个问题的另一半 ——
> 当时的查证结论是「设计产出能力**没丢**，但『设计判断』这一环**没有裁判**」；1.3 补上了裁判。
> **而「产出能力自 1.1 未增」这个事实，至今没被处理。**

---

## D. 新维度（1.3 已裁决「不单独立项」，作为关联发现留下）

| # | 条目 | 现状 | 代价 |
|---|---|---|---|
| **D1** | **F6 · 响应式只落在声明里、不落画布** | DS Spec 有 `responsive.baseline / breakpoints / rules`，但 **L3 只建一个 `frameBaseline`**（LD-10：大屏恒 1920×1080），**不建多断点**。且 **H3 / LD-11 已于 2026-09-18 把 web-admin 断点收紧为确定值（1440 / 1024）** ⇒ **规则已定、画布没落** | **比 F5 便宜**：不需新 op，只须 L3 建多断点帧 + 回读多帧比对 + 一条新闸门（帧间一致性）。⚠️ 但会改 L3 的产出形状 ⇒ 与历史产物不可比 |
| **D2** | **F5 · 动效维度** | `visual-critic.md` 五维（Layout / Color / Consistency / Commercial / Usability）**没有动效维度**。intake 已攒出**完整启动包**（值表：STANDARD.md 的 easing / 时长 / spring；判据来源；反动效默认姿态「第一优先是删掉动效」） | ⚠️ **最贵的一条，且理由不在「没值表」**：intake §3.2 实测 **`bridge-ops.md` 的 op 集里没有任何原型 / 动效操作** ⇒ **画布表达能力层面就没有**。这是「**表达能力缺口 × 维度缺口**」**两层叠加**，不是「L4 少一个维度」⇒ 必须先动 op 面（**冻结区**） |

---

## E. 零引用字段（`contract-usage` 当次输出，2026-09-18）

38 个契约字段中，**零引用 9 个**：
`imageFills` · `strokeWeight` · `primaryAxisAlignItems` · `counterAxisAlignItems` ·
`primaryAxisSizingMode` · `counterAxisSizingMode` · `layoutSizing` · `vectorPathCount` · `vectorData`

| 子类 | 字段 | 可做性 |
|---|---|---|
| **第三态（不可做）** | `layoutSizing` | 376/376 恒 `FIXED` ⇒ **恒真假绿**，明确不得做判据（#15） |
| **无对象** | `primaryAxisAlignItems` / `counterAxisAlignItems` / `primaryAxisSizingMode` / `counterAxisSizingMode` | `layoutMode` 全树 `NONE` ⇒ 整块 auto-layout 域**结构性是暗的**；「连字段都不出现」 |
| **判据可写但缺来源** | `strokeWeight` · `vectorPathCount` · `vectorData` | `UNUSED_OK` 已逐条写明理由（`strokeWeight` 的外部判据 better-ui #14 主体被判**无对象**）⇒ 按 #73 顺序纪律「**先有判据来源再写检查**」，本轮刻意不做 |
| **F2 刚解锁** | `imageFills` | 回读已通，但「Spec 说这里该有图 ⇒ 画布真的是图」的**一致性判据未立**；与 `pixel-proof` 的像素通道交叉判据也未立 |

---

## F. 缓做债项（1.2 划出 → 1.3 仍未纳入 → 明确「留 1.4」）

| # | 项 | 出处 | 备注 |
|---|---|---|---|
| **F1** | `#11` sha256 清单机制 | `1.2-plan.md` §3.2.3 | 「有增益但非阻塞」 |
| **F2** | `#12` op 映射对照 | 同上 | 同上 |
| **F3** | `#14` selftest 反向用例 | 同上 | 同上 |

---

## G. 1.3 遗留的口径不一致（顺手可清，建议无条件带上）

| # | 条目 | 实测（2026-09-18） |
|---|---|---|
| **G1** | **`imageFills` 的「谁在读」两处口径矛盾** | `references/design-criteria-intake.md` §7.2 写「在用 23｜待复核 2｜零引用 9（**`imageFills` 由 `qa-plugin`/precheck 变异消费，非零引用**）」；而 `node tools/contract-usage.mjs` **当次输出把 `imageFills` 列在零引用里**，且 `UNUSED_OK` 明写「**判据还没立**」——**工具与文档说了两套话**。根因可查：`contract-usage` 的扫描范围**显式排除 `*-mutation.mjs`**（「变异测试是造数据，不是在读契约」）⇒ §7.2 用了**被排除的那一侧**当依据。这是 #74 / #83 家族（数字漂移 / 一个符号装两个意思）的又一实例 |
| **G2** | 已完成的 D 组需求单是否仍有「待批准」措辞 | `references/frozen-zone-requests.md` §4 已回填，但 `1.3-candidates.md` D 组表头仍保留「原始记录」；须核对是否有读者会误读成「未决」 |

---

## 八、四条互斥主线（**待决策者选一**）

| 档 | 主线 | 一句话 | 代价 | 我的建议 |
|---|---|---|---|---|
| **A** | **兑现版** —— 把 1.3 筛出的判据真正装进闸门（A1–A5 + B1/B2 作前置） | 1.3 造好了裁判、备好了原料，**收编 0 条** | 中等。真成本在**判据校准**（中文 tell 名单 / 产物类型分档），**需要人的产品判断**；收编须四件齐（#73）⇒ 须控条目数 | ✅ **推荐** |
| **B** | **生成侧版** —— 把「配方」写进 Preset 与 L1 规则（C1 / C2） | 回答「怎么做得更好」，不只是「怎么查得更准」 | 高。牵动 `qa-l2` 契约与历史产物可比性；配方验收偏软（撞 H3） | ⚠️ 有价值但风险最高，建议**另立评估**后再做 |
| **C** | **维度版** —— F6 响应式落画布（D1）；F5 单列（D2） | 补「从没立过的维度」 | **D1 中等**（改 L3 产形）；**D2 很高**（要先动 op 面 / 冻结区） | ⚠️ D1 可考虑；D2 **不建议**本版做 |
| **D** | **债线版** —— 参数同源余量 + 零引用字段判据 + G 组（B / E / F / G） | 把口径与数字弄干净 | 最低。但收益是「**数字可比**」而非「多查出缺陷」，**不容易被感知** | ⚫ 可作 A 的附随，不建议单独成版 |

**明确不建议**（连同理由）：
- **继续扩外部来源** —— intake §4.6 / §七 已证「**产出密度取决于『未使用字段清单』，不是来源数量**」，且 6/6 来源已筛完；
- **同心圆角** —— 形态前提不成立（真实产物上可检查对象 = **0**），除非先裁决「连续公式 vs 离散白名单」哲学冲突（§3.1）；
- **`layoutSizing` 判据** —— 第三态，恒真假绿。

> **本清单的性质仍是「输入」，不是计划。** 主线定了之后，须写 `1.4-charter.md`（立项书：范围 / 优先级 / 验收标准）
> 并签字，才可开工 —— 这是 1.3 用三刀换来的纪律（`lessons.md` #9）。
