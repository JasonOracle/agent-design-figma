# 1.3 立项书

> **用途**：定义 1.3 的 **范围 / 优先级 / 验收标准**，并由决策者签字。
> 它补的是这条链上**从未发生过**的那一步 —— 此前连续三次开工都跳过了它（见 §0）。
> **候选输入**见 `1.3-candidates.md`（A–H 八组，逐条带出处）· **开工记录**见 `1.3-plan.md` ·
> **现状速览**见 `1.3-status.md`。本文件不重复它们的内容，只回答「**做什么 · 先做哪个 · 怎样算做完**」。
>
> **状态**：✅ **已签字（2026-09-17）** —— 范围锁定 **B 档「收口 + 空环」**，主题「**裁判版**」采纳，
> §5 四类裁决**全部按执行者建议通过**。此后**范围外请求一律默认拒绝并记候选**。
> **时点**：2026-09-17（三刀收口后立项）。
> **本文件属开发者内部文档**（`tools/check-refs.mjs` 的 `DEV_ONLY` 模式 `^\d+\.\d+-[^/]*\.md$` 自动豁免）。

---

## §0 为什么现在补这一步

**事实**：1.3 已经做了三刀、11 条改动，但范围、优先级、验收标准**一次都没定过**。
候选清单是 1.2 收尾验收时的产物（`1.2-plan.md` §8「新增能力项 → 默认拒绝，记入 1.3 候选」），
它是**输入**，不是计划。

**三刀是怎么挑的**：由执行者按四条特征筛 ——「已定位 / 可机械修 / 无新判据设计 / 有实测数据可验」，
理由写在 `1.3-plan.md` §1：「**不需要先做范围决策就能开工**」。

**代价在哪**：每一刀单独看都成立，合起来却**拼不成一个可验收的范围**；
后果不是做错事，而是**没有人能回答「1.3 到底要干什么」**。

**第三刀把这个代价变成了实测数字**：那批「已定位、可机械修」的候选里，**两条半站不住** ——
A1 的需求早已满足、D1 的落点写错、B5 的前提不成立（`lessons.md` **#84**）。
⇒ 范围没定，筛选标准就只剩「**能不能做**」，「**该不该做**」这一问没有抓手。
**这就是缺立项的直接代价，也是这份文件存在的唯一理由。**

> ⚠️ **因此本文件落盘之前，不再抢跑任何新条目** —— 哪怕是标注「可缓、无需决策」的 A3/A4/A5。
> 「能开工」不等于「该开工」；三刀已经证明了这一点。

---

## §1 本版主题（**已采纳**）

> **1.2 是「可信版」** —— 让每一步留下**可引的痕迹**。
> **1.3 是「裁判版」** —— 让每一环都有**可证伪的裁判**。

由此得出一条判据，用来回答「这条该不该做」：

> **它是否让某一环从「没有裁判」变成「有裁判」，或让已有的裁判变得可证伪？**

| 已做的 11 条 | 落在这条判据的哪一半 |
|---|---|
| A2（参数同源）· B1（对齐留痕）· B3（评估覆盖）· B4（引用兜底收紧）· B5/B6（发版守卫）· C1（像素证明） | **让已有的裁判变得可证伪** —— 全部是「**裁判自己的可信度**」 |
| A1 残余（几何覆盖量化）· A6（命令表补 `layout-audit`） | 同上（前者补分母、后者补「知道要跑它」） |

⇒ 三刀合起来是判据的**左半边**，做得很扎实。**右半边（补上「没有裁判的那一环」）一条都还没做** ——
那正是 **F3**。这也是它从「感觉少了点什么」变成**必做**的原因（证据见 §2.2）。

---

## §2 范围

### 2.0 三档（**已裁决 ①：采纳 B 档**）

| 档 | 内容 | 代价 | 裁决 |
|---|---|---|---|
| **A · 只收口** | 已知缺陷 / 守卫 / 文档；**F 组一条不立** | 范围最小、风险最低 | ❌ 未采纳 —— 「可直接修」的债三刀已修完，剩余 A3/A4/A5 是「中低优先」的零头，拼不成一个版本 |
| **B · 收口 + 空环** | A 档 + **F3 / F4** | 一个装置 + 一套筛法，**范围可控** | ✅ **采纳** —— 首次给出主题（§1），且补的是链上唯一**结构性无法评估**的一环 |
| **C · 全量** | B 档 + F5 / F6 + F1 / F2 | 范围膨胀到「新能力评估」 | ❌ 未采纳 —— 与「修裁判」不同源；F1/F2 按纪律须**先立评估再动手**，不混进本版 |

### 2.1 已完成（三刀 11 条）

不在此重复 —— 逐条见 `1.3-status.md` §三。本文件只记「从哪继续」。

### 2.2 本版范围（B 档 · 含裁决结果）

| 序 | 项 | 类型 | 过得了 §1 判据吗 | 依赖 / 约束 |
|---|---|---|---|---|
| **0** | **`qa-export` QA3 判据修正** —— 撤掉 `live-build` 档位对 `figmaFileKey` 的强制要求（**已裁决 ②**） | **判据修正** | ❌ 不属主题，但**已获授权** | ✅ **已于 2026-09-17 完成**（第四刀）：**换判据不改编造** —— 改为「如实标 `null` + 在 `_meta.note` 交代缺口」，静默留空 / 全空白 / 空串**三条仍拦**。实测样例 `935 PASS`、运行 A `364 PASS` **均不变**，运行 B `409 PASS / 1 FAIL` → **`410 PASS / 0 FAIL`**，变异测试 **57 → 63**。留痕见 §2.2 表下的注 / `1.3-candidates.md` 文末 / `CHANGELOG.md` |
| **1** | **F4 · 外部设计判据的机械化吸收** —— 逐条过筛 `taste-skill` / `better-ui` / `better-layout` / `better-typography` / `review-checklist` / `review-animations`，能落到画布几何 / 属性的做成新检查类 | **空环供料** | ✅ **正题** | ⏳ **进行中（2026-09-17 起）**：筛法已提为**随包**文档 `references/design-criteria-intake.md`（原先三份试点全在 `.vibe/`、不随包发布，`lessons.md` #81 点名要求）；**6/6 来源已过筛（F4 筛线完成，2026-09-18）**（`better-ui` 17 条 / `better-layout` 10 条 / `better-typography` 19 条 / `taste-skill` 41 条 / `review-checklist` 20 条 / `review-animations` 14 条 = **121 条原则**）→ 搬 17 / 译 14 / 交人 18 / **无对象 48（40%）** / 拆 18 / 已有 6；关键发现见 intake §2.4–§2.7。**收编动作待四件齐裁决**。**并行产出线（2026-09-18）**：筛出的「未使用字段清单」动作已执行（intake §七）—— 新工具 `tools/contract-usage.mjs` + **5 字段收编 → `layout-audit` 3 类新检查（10→13）+ overflow 分级**，真实数据零扰动 |
| **2** | **F3 · L4「设计判断」闭环** —— ① 读图评审从「增益档」升为**有图环境的必选项**；② 建「**锚点产品并置比对**」装置（随包锚点集 + 并置协议）；③ 把 E1 的语义性缺失做成**读图检查清单**并要求逐条留痕 | **空环** | ✅ **正题（本版主题的收口）** | 依赖第 1 项的产出。**进度（2026-09-18）**：① ✅（`visual-critic.md` §1.6 措辞矛盾已消，「能读而不读 = 违规」）· ③ ✅（`references/read-image-checklist.md` 首批 11 条 + 记录 Schema `assets/templates/read-image-review.json`）· ② ✅ **完成**（`tools/anchor-compare.mjs` + 协议 `references/anchor-compare.md` + Schema `assets/templates/anchor-compare-report.json` + 变异 43 PASS；锚点源 `nexu-io/open-design` 151 品牌契约核实为 Apache-2.0、外部可选不 vendored，**「随包锚点集」改判为「随包协议与装置」**；判据 J1–J5 全是装置纪律，相似度结论属 R11 + 人；**「随包锚点集」措辞改判说明见 `references/anchor-compare.md` §一**）。**判据 1 的「无装置可评」已消** |
| **3** | **A3 / A4 / A5 · 命令接入点对齐剩余项** | 债 | ⚠️ 间接（「让裁判真的被跑到」） | A2 已就位 |
| **4** | **H 组分类收紧**（H1–H6 六处「约束内的自由」，**已裁决 ③**） | 文档 | ❌ 不属主题，但**必要** —— 三轮盲测各猜一套，跨产物方差实打实 | 分类处置，见 §5 ③ |
| **5** | **D 组需求单**（D1 / D2 / D3 的**插件侧**部分：`paint.opacity` 回传 / `ping` 返 `fileKey` / 未知 `nodeId` 的 10s 挂起） | 装置缺口 | ⚠️ **不在本版可交付范围内**，只出需求单 | 冻结区解冻（另议） |

> 第 0 项排在第一位不是因为它重要，而是因为它**便宜且污染面大** —— 不清掉它，
> 此后每次收口都要额外解释「FAIL 的那条是环境凭证、不是产物质量」（D 组原文已记两次实测撞上）。
> 注意区分：**D4 的断言侧**由第 0 项处置；**D2 的插件侧**（`ping` 确实不返 `fileKey`）仍留在第 5 项的需求单里。
>
> ✅ **第 0 项已于 2026-09-17 当场完成**（系统教训见 `lessons.md` **#85**）。它**不是「放宽校验器」** ——
> A4 的教训治的是「文档措辞不严谨，却去松校验器」；本条病灶相反：**判据引用了一个本拓扑根本得不到的外部凭证**
> （`figmaFileKey` 是 REST API 上游「按 fileKey+nodeId 回读」的必要条件，而本管线走 Bridge 直连插件、
> 凭 `nodeId` 单键即可，且插件无任何 op 返回它）。裁剪办法是**把「必须持有」换成「必须交代」**，
> 并用数字证明这不是删判据：自带样例 `--strict` **`935 PASS`** 与运行 A **`364 PASS`** **均不变**，
> 运行 B `409 PASS / 1 FAIL` → **`410 PASS / 0 FAIL`**，变异测试 **57 → 63 PASS**。
> **盲区如实记**：`_meta.note` 非空**不证明画布真的存在**（只证明执行者交代了缺口），画布是否存在由
> `precheck --live` 的**实际回读**证明 —— 两条判据分工不同，别互相顶替。

### 2.3 建议明确排除（连同理由 · **含裁决 ④**）

| 项 | 排除理由 |
|---|---|
| **C2** 增量补齐工具（**已裁决 ④：暂不做**） | 依赖的 `get-node` 输入形状经 A1 复核**本来就能用**，故不存在「等接口」的理由；且多一个长期装置要养（#73 四件齐）。⇒ **留作纪律**，本版不做 |
| **F1** MCP 外壳 / **F2** 图片填充 | **新能力**，不是债。纪律要求「先立评估再动手」（`1.2-plan.md` §8）；且与「修裁判」不同源。⇒ 另立评估，不混进本版 |
| **F5** 动效维度 / **F6** 响应式落画布 | **不单独立项**，作为 F3/F4 评估过程中的发现一并称量。⇒ 但**必须写下来**（防丢），处置为「**F3/F4 的关联发现**」 |
| **E1** 语义性缺失（无轴标 / 无单位 / 无空态 / 无图例） | **能力边界，不是待办** —— 几何完全合法，数值审计原理上不可见，写脚本查不出来。它「真正可做的那件事」**已并入 F3③** |
| **G1–G3** 缓做债项（sha256 清单 / op 映射对照 / selftest 反向用例） | 1.2 已明确划出「**防止范围膨胀**」，本版仍不纳入 ⇒ **留 1.4** |
| **当场修改冻结区** | QA7 禁区 = `figma-plugin/` + `bridge/`。D 组**只允许提需求**，不允许构建期改（运行 A #11 已因此被拦下过） |

---

## §3 优先级与顺序

```
三刀 11 条（已做）
   │
   ├─(0) QA3 判据修正 ──▶ 最小、独立，且**不清掉它，此后每次收口都要解释 1 条 FAIL**
   │
   ├─(1) F4 筛子先行 ──▶ 产出「可判定条目清单」
   │        把外部判据逐条过筛，**能落到画布几何 / 属性的才有装置价值**
   │
   ├─(2) F3 装置 ──────▶ 锚点集 + 并置协议 + 读图检查清单（目标见 §4 判据 1）
   │
   ├─(3) A3 / A4 / A5 ─▶ 命令接入点对齐收尾（中低优先，可穿插）
   │
   └─(4) H 组分类收紧 / D 组需求单
```

**为什么 F4 必须排在 F3 之前**：并置比对要先知道「**比什么**」。
没有判据原料就先建装置，只会把 `unassessed` 从「**没装置**」改成「**有装置但不知道该看什么**」——
那是把问题从「看不见」升级成「看起来能看」，比原来更坏（`lessons.md` #82 的同族错误）。

---

## §4 验收标准（什么叫 1.3 完成）

| # | 判据 | 怎么验 |
|---|---|---|
| **1** | **主题验收**：L4 报告 `_evidence.unassessed` 里 **Commercial 的「商业产品相似度」（占该维 40% 权重）不再是「无装置可评」** | 跑一次并置比对并留痕；该项变为「已评」或「**已具备装置、本轮未跑**（并写明原因）」 |
| **2** | 每项收编**四件齐**：代码 + 前提 + 留痕 + 变异测试 | `lessons.md` #73 |
| **3** | 全套闸门 **0 FAIL / 0 未核对**（未核对项必须逐条解释，**不得折算为通过**） | 当场复跑，把**真实汇总行 + 退出码**写进 `CHANGELOG.md` |
| **4** | 变异测试 **0 FAIL**，且**新增检查类必须带反向对照** | 同上 |
| **5** | ~~**冻结区零改动**（`figma-plugin/` + `bridge/`），D 组只出需求单~~ → **经用户授权修订（2026-09-18 定版时留痕）**：F2 与 FR-1~5 先后解冻并改动冻结区，每条均有**显式批准 + 独立提交 + 真机验证**（`references/frozen-zone-requests.md` §4）；结案后 QA7 冻结恢复。判据的本意「不许顺手改」全程兑现，改动无一例外走「批准 → 解冻窗口 → 逐条提交 → 复跑闸门」 | `git log -- figma-plugin bridge`（F2 `5033227` 起，逐条可查）；`qa-export` QA7 恢复后 PASS |
| **6** | 范围外请求**默认拒绝并记候选**，不夹带做掉 | `1.3-candidates.md` |
| **7** | 发版前 `release-guard` **6 PASS / 0 FAIL / 0 未核对**，再打 `v1.3.0`（落点处 `VERSION` = `1.3.0`） | `node tools/release-guard.mjs` |

> 判据 1 是**这版和「没立项」的分水岭**：前六条都是「过程无瑕疵」，只有第 1 条回答
> 「**这版做完多了什么**」。没有它，1.3 收尾时会和三刀一样 —— 每一条都对，合起来说不出主题。

---

## §5 四类裁决（**已签字** · 原始分析见 `1.3-status.md` §6.2）

| 代号 | 待决 | 问题 | **裁决** |
|---|---|---|---|
| **①** | **范围档位** | A 只收口 / B 收口 + 空环 / C 全量 | ✅ **B 档「收口 + 空环」**（连同 §1 主题「裁判版」一并采纳） |
| **②** | **D2 / D4 · `qa-export` QA3 的 `figmaFileKey` 断言** | `live-build` 档位要求 `figmaFileKey`，但本管线回读走 Bridge 直连插件、凭 `nodeId` 单键即可，该凭证**功能上无用途**（疑为从 REST API 上游继承） | ✅ **裁剪**，**但必须走一次正式的「判据修正」流程**（改判据不改编造 + 变异测试 + 文档改判留痕），**不许顺手改** ⇒ 落为 §2.2 第 0 项 |
| **③** | **H1–H6 · 六处「约束内的自由」** | 字阶映射 / pagePadding / web-admin 断点 / warning 取黄还是橙 / focusRing+touchTarget / 字重 —— 三轮盲测**各猜一套且闸门全绿**（文档只规定「必须满足什么」，没规定这些取多少） | ✅ **分类收紧**：**有跨产物一致性需求的收紧**（H1 字阶映射 · H4 状态色 · H3 断点）；**纯风格选择的保留自由**（H2 pagePadding 具体值 · H5 focusRing/touchTarget 数值 · H6 字重 —— 但须给出**合理区间**，不给自由到无参照） |
| **④** | **C2 · 增量补齐工具** | 喂「期望清单 + 目标父级」→ 自动回读、算差、只补差集。当前是纪律，无工具 | ✅ **暂不做** —— 依赖的 `get-node` 输入形状经 A1 复核本来就能用，可等范围定稿后再评估 ⇒ 移入 §2.3 排除表 |

> **F3 / F4 / F5 / F6 的立项范围已并入 ①**：采纳 B 档 = 「立 F3 + F4」；**F5 / F6 不单独立项**，
> 作为 F3/F4 评估过程中的发现一并称量（`1.3-status.md` §6.2 的建议）。

---

## §6 签字

| 角色 | 签署内容 | 状态 |
|---|---|---|
| **决策者** | §1 主题提议 · §2.0 范围档位（B） · §5 四类裁决（②裁剪 / ③分类收紧 / ④暂不做） | ✅ **已签 · 2026-09-17** |
| **执行者**（AI） | 按签定范围执行；**范围外默认拒绝并记候选**；每一刀复跑全套闸门并留痕；冻结区零改动 | ✅ 本文件落盘即生效 |

**签字后已同步更新三处指向**（避免又出现「结论写下了，改动来时没人回头核」）：
`1.3-plan.md` 顶部状态行（「范围未定稿」→ 指向本文件）·
`1.3-status.md` §一 / §二 / §6.2 / §七（「从未立项」→ 已立项，「待拍板」→ 已裁决）·
`1.3-candidates.md` 第 4 行（「在 1.3 立项时另行定稿」→ 已定稿，指向本文件）。
