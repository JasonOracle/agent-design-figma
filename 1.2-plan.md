# 1.2 实施计划 —「可信版」

> **版本**：1.2（代号「可信版」Trustworthy）
> **状态**：已定稿 · 进行中（A1 / A2 / A3 / A4 / A5 / B1 / B2 已完成；C3 进行中——`qa-l2` 已落地）
> **日期**：2026-09-16 ｜ 最后更新：2026-09-16（并入上游资产清查结论）
> **本文档是开工依据。** 四块范围（补契约 / 建拦截 / 立证据 / 定验收）是配套的，不建议单独抽换；变更走 §8。
> **决策输入**：`1.2-upstream-inventory.md`（上游 1.0.0 资产清查）

---

## 1. 核心命题

让「跑一次完整链路」从**可能翻车**变成**可预期、可拦截、可复现、可验收**。

1.2 **零能力新增，纯还债**。不加任何用户可见的新功能，只解决「同一套输入，这次能成、下次为什么翻车」。

---

## 2. 为什么是这个范围

### 2.1 主线判断

截至 1.1 的三轮实测暴露的 P0/P1 问题，**全部是同一类——隐式知识**：op 参数形状、字体在 Windows 的可用性、avoid 仲裁、depth 语义、审查算法，都散在「没人写下来」的地方。

1.1 已把其中一部分变成显式契约（`references/bridge-ops.md` + 三个 `tools/*.mjs` 回归工具）。1.2 把这条路走完，因为：

- 这条路的**边际收益还没吃完**（L2 完全无文档、L4 只有 1/5 维有工具、L5 出口闸门是空的）；
- 而「加能力」的边际收益，在 Figma 官方 `use_figma` 放开后会归零。

### 2.2 三个新发现（1.1 之后核查代码得出）

| 发现 | 证据 | 后果 |
|---|---|---|
| **L2 是链路唯一黑洞** | SKILL.md 有 L0/L3/L4/L5 章节，**唯独没有 L2**；`references/` 无 L2 文档 | DS Spec 有 9 个顶层字段，规则却零文档 → Agent 只能看 Schema 自由发挥，是单次运行不确定性的最大来源 |
| **12 处失效引用** | SKILL.md / critic-mapping / visual-critic / export-mapping 承诺「Python QA 脚本 / QA1-QA7」；**全仓库 0 个 .py** | 违背本 Skill 第一价值观「未跑的层不得假装跑过」，且实测 Agent 真的会去找 |
| **无版本号 / CHANGELOG** | 完全没有 | 已发生过「安装副本停在旧版本 + 带未提交补丁，说不清哪个是正式版」 |

### 2.3 定稿后的两次修正（诚实记录）

**第一次——定稿前的自我修正（4 处硬伤）**：

1. **预检能力说过头** —— 纯离线桩查不出「引用的 node 是否存在」。修正为 B1/B2 两档（见 §4.B）。
2. **图片填充混入「可信版」** —— 能力项稀释主题。修正：移出 1.2，单独立项（见 §5）。
3. **L4 假设 Agent 一定能读图** —— 本 Skill 跨环境通用，非所有 Agent 都能读 PNG。修正为 C 块两档。
4. **无成功指标** —— 补 §7 硬验收。

**第二次——开工后清查上游仓库（见 `1.2-upstream-inventory.md`）**：

5. **A1 的性质变了** —— 不是「从零写」，是「搬上游 281 行成熟设计 + 适配」。工作量下降、质量上升（§4.A1）。
6. **A2 的规模被低估** —— 不是 3 处失效引用，是 **12 处**；`build-plan.json` 作为独立文件也不存在。
7. **QA 体系必须纳入** —— 上游有 **845 条断言**的现成设计与实现，打包时被排除，用户一条都拿不到。**L5 Export Gate 是空的**（§4.C3）。
8. **范围已膨胀** —— 定稿 4 块 9 项 → 现 4 块 12 项。因此把 #11 / #12 / #14 明确划为**缓做**（§4 末尾），防止继续膨胀。

另两处方法论缺陷：

- **单次样本做战略判断**：主线由一次实测推出。修正：§7 D2 用两次实测校准。
- **L2「文档」非最优解**：文档只降方差、不消除。修正：A1 明确标注为红线下次优解（见 §3.1）。

**第三次——开工后做 B1 时的修正（输入前提错了）**：

9. **B1 的输入前提不成立** —— 定稿写「消费 DS Spec 的 `buildPlan`（op 序列）」，但开工实测：`buildPlan` 只有 `{seq, target, estOps}` 的批次视图，**没有任何 op 序列**；Schema 自己的 description 就写着「可选：L2 预估的 L3 构建批次视图……本字段仅作交接参考」。**op 序列是 L3 运行期由 Agent 现场构造的，此前从未落盘**。
   - 处置（按 §8）：**置换输入** —— 改为消费「你正要发出去的那串 op」；DS Spec 误入时明确拒收并指向 `qa-l2`。
   - 另一处表述修正：定稿写「参数形状符合 `bridge-ops.md`」，实际改为**以真 `code.js` 为事实源**（见 §4.B1）。文档是给人读的，判定必须以能执行的东西为准；照文档再抄一份形状表只会多一处会腐坏的副本。
   - **为什么值得记**：这是「按计划施工」与「按事实施工」的差别。若照定稿硬做，会写出一个**永远收不到输入**的校验器，而且它还会显得"全都通过了"——正是本版本要消灭的那类失败。

---

## 3. 已拍板的决策

### 3.1 产品方向（3 条，来自定稿评审）

| # | 决策点 | 结论 |
|---|---|---|
| 3.1.1 | L2 守红线用文档，还是破红线做生成器？ | **守红线，文档先行**。生成器是另一量级投入，不进 1.2 |
| 3.1.2 | 图片填充排期？ | **等 1.2 验收数据出来再定值不值**，现在不承诺 |
| 3.1.3 | MCP 外壳形态？ | **「门铃式」中间态，1.3 单独评估**。MCP server 不内化规则，只暴露「按本 SKILL.md 执行」的入口，规则留文档形态 |

### 3.2 执行方式（3 条，来自上游清查）

| # | 决策点 | 结论 |
|---|---|---|
| 3.2.1 | 移植语言 | **Node 重写**。守零依赖红线；上游脚本零第三方依赖，直译成本可控；Skill 已全是 `.mjs`，不引入第二语言 |
| 3.2.2 | QA 体系是否纳入 1.2 | **纳入**，作为 C3 独立交付项。理由：上游 845 条断言的设计现成、零依赖，是 D1「零目测评分」唯一可执行的支撑，也是 L5 Export Gate 唯一可能的实现。**不做就是自欺** |
| 3.2.3 | 移植范围 | **分层处置**（见下）。必做含 QA 本体，非只搬低成本那批 |

**3.2.3 的分层**（上层是下层的前提，不再细分）：

| 层 | 内容 | 说明 |
|---|---|---|
| **必做（1.2 的骨）** | A1 L2 契约 · **C3 QA 体系 4 脚本** · A4 引用校验器 | C3 是本体工作量；A1 与 C3 的 `qa-l2` 配套（上游文档 §9 直接引用该脚本） |
| **顺手做（低成本）** | A5 lessons.md · #9 release-checklist · #13 冻结协议 | 半天内量级，各自独立 |
| **缓做（记入 1.3 候选）** | #11 sha256 清单机制 · #12 op 映射对照 · #14 selftest 反向用例 | 有增益但非阻塞；1.2 结束前不动，防止范围继续膨胀 |
| **不做** | #15 stage7 视觉回归移植 | 依赖 numpy / Pillow（破零依赖红线），且对象不同（Figma↔Vue 两稿对比 ≠ 单稿五维打分） |

---

## 4. 范围内（4 块 12 项 · 已完成 7 项 · 进行中 1 项）

### A. 补契约

**A1 — L2 契约文档** → `references/design-system.md` ｜ ✅ **已完成**

- **性质（清查后修正）**：从「从零写」改为 **搬运 + 适配**上游 `docs/stage10-4-design-system-generation.md`（281 行）。
- **实际交付**：
  - `references/design-system.md`（15 节）：五段流水线 / 三条不变式 / 五类 Token 与禁令 / source 五种前缀 / 白名单派生 RD-1·RD-2·RD-3·ST-1（含公式）/ TY-1..4 / SP-2·SP-3 / 组件决策矩阵 CD-1..CD-4 / 四件套状态矩阵 / **四平台**布局模板 / 八字段映射 / QA 判据 / 出口自检 / 与 L1·L4 接缝
  - `SKILL.md` 补上此前唯一缺失的 **L2 章节**（原先有 L0/L3/L4/L5，唯独没有 L2）
  - `assets/templates/design-system-spec.json` 的 `stylePresetId` 枚举补 `web-marketing`
- **相对上游补的缺口**：① `web-site` 平台布局模板（上游 §10.4 自承缺失）+ LD-6..LD-9 规则；② 字体降级链（Windows 无 PingFang SC）；③ 消费 L1 的 `avoidOverrides` 仲裁结果。
- **明确标注：红线下的次优解** —— 文档降方差、脚本拦越界，但**不消除方差**（上游同样只有设计、无实现，其 §10.4 自述「规则引擎的实现未编码」）。
- **DoD**：文档存在 ✅；「未参与本项目的 Agent 仅凭它 + 一份 Brief 能产出合法 DS Spec」——待 **D2 两次实测**实证（届时若不合格，回炉本项）。

**A2 — 修失效引用** → 改 `SKILL.md` 等 5 个文档 ｜ ✅ **已完成**

- 实际规模：**12 处**（非预估的 3 处），分布在 `SKILL.md`(4) / `critic-mapping.md`(3) / `visual-critic.md`(2) / `export-mapping.md`(1) / 另含 `build-plan.json` 2 处。
- 顺带修正：`build-plan.json` 作为独立文件不存在（Build Plan 实为 DS Spec 的 `buildPlan` 字段）→ 2 处文档与实现对齐。
- **DoD 验证**：扫全仓库 63 处文件引用，真缺失 **0**。

**A3 — 版本治理** → `VERSION` + `CHANGELOG.md` + runtime-check 报版本 ｜ ✅ **已完成**

- `VERSION` = `1.2.0-dev`；`CHANGELOG.md` 追记 1.0.0 / 1.1.0 全部变更；探针输出新增 `version` 字段。
- **DoD 验证**：从仓库根与 `/tmp` 均能读出，路径无关成立。

**A4 — 引用校验器** → `tools/check-refs.mjs` ｜ ✅ **已完成**

- 治 12 处失效引用的**机制性根因**：上游 `release-checklist.md` 把手改引用列为人工勾选项，勾了没做干净。
- 逻辑来源：上游 `tools/stage11-release-package.py` 的「文档引用校验」步骤（Node 化），并按本仓库实际情况扩展——上游只扫「`node`/`python` + 脚本」的命令式引用，抓不到「文档里承诺了一个 Python QA 脚本」这类散文式引用的缺位，故本工具**同时扫反引号里的带扩展名路径**。
- **判定**：相对仓库根 / 相对该文档 / 全仓库同名（basename 兜底）/ glob，四条全不中 → 悬空并非零退出。
- **三类豁免（各写明理由，不是「见了就跳」）**：
  1. **运行时产物**（`.vibe/` 下探针输出等）—— 本来就只在跑完之后才存在，文档引用它是描述产出位置，不构成「仓库里该有」的承诺；
  2. **Token 点路径** —— `tokens.radius.md` 是 radius 下的 md 档，不是 markdown 文件。判据收紧到「无 `/` + 至少 3 段 + 首段是已知对象根」，所以真文件 `manifest.json`（2 段）不会被误豁免；
  3. **角度占位符** —— `<skill 根目录>/tools/runtime-check.mjs`、`<readback.json>` 先剥掉尖括号段再判，剥完以 `/` 开头的按仓库根锚定。
- **DoD 验证**：
  - 零误报 —— 现仓库 15 篇文档 / **134 条引用全部落地**（path=86 · basename=46 · glob=2），豁免仅 运行时=4 点路径=1；
  - 能报警并非零退出 —— 由 `tools/check-refs-mutation.mjs`（**16 例**）持续证明：造含已知错误的样本仓库，断言 exit 1、断言恰好报出该报的 3 条、断言 6 类豁免项一个都不误报，**并断言豁免计数非零（防豁免规则空转）**；
  - 路径无关 —— 从 `/tmp` 跑结果一致。
- **上线即抓到 2 个真实问题**（都不是构造样本）：
  1. `check-refs.mjs` 自身：反引号路径正则的首字符类不含 `.`，导致 `.vibe/runtime-capability.json` 整条**连不上正则**——既不报错也不进豁免，运行时豁免规则实际空转（真实仓库输出「豁免 运行时=0」就是证据）。已修，并加了「豁免计数非零」断言守住；
  2. `references/lessons.md` 初稿：给**不存在**的 `package.json` 加了反引号。按本仓库惯例反引号包路径即声明「仓库内有此文件」，改的是文档措辞而非放宽校验器。
- **配套（非新范围，同 A4 的一致性要求）**：`references/lessons.md` #33 已确立「校验器必须有配套变异测试，否则它的全绿没有意义」——`qa-l2` 与 A4 都按此执行，否则 DoD 里「能报警」这句话事后不可复验。

**A5 — 实测不变量** → `references/lessons.md` ｜ ✅ **已完成**

- 搬上游 `docs/lessons-learned.md` 30 条实测踩坑验证过的不变量，成本极低，防复踩价值最高。
- **适配而非照抄**：去掉上游专属上下文（阶段编号、项目名、个人路径），按本仓库命名与实现改写；每条标注**已被工具守卫（⚙️）还是只能靠纪律（📏）**。
- **另补本仓库 1.1–1.2 新得的 8 条**（共 **38 条**）：安装副本与开发仓库漂移 · 校验器的判断顺序本身即缺陷来源 · 校验器必须配变异测试 · 人工勾选治不住文档与实现脱节 · 打包排除项会悄悄删掉质量资产 · 筛选要用正向形状而非反向排除 · 算得出来的数字必须配复算 · 判定阈值不许先舍入。
- `SKILL.md` 增加入口段（「动手前先读」），避免它成为无人引用的死文件。
- （归入 A 段而非 B 段：本质是「隐式知识显式化」，不是拦截机制。）

### B. 建拦截（构建计划预检）

**B1 — 静态预检（离线）** → `tools/precheck.mjs` ｜ ✅ **已完成**

> **⚠️ 开工即发现定稿的输入前提是错的**（见 §2.3 第三次修正）：DS Spec 的 `buildPlan` **里没有 op 序列**，
> 只有 `{seq, target, estOps}` 的批次视图（Schema 原文写着「仅作交接参考」）。故本项**置换输入**为
> 「你正要发出去的那串 op」（`{steps:[…]}` / `{ops:[…]}` / 裸数组），并**新增输入守卫**：把 DS Spec
> 误当计划喂进来时明确拒收并指向 `qa-l2`，而不是含糊报错。

- **契约的事实源改为真 `code.js` 本身**（原定「参数形状符合 `bridge-ops.md`」）：用 vm 离线跑真源码取判定，
  而不是再抄一份 op 形状表。抄一份就多一处将来必然腐坏的副本；而「必填参数 / 颜色格式 / 参数类型 /
  父级可否容纳子节点」的判定本来就写在 `code.js` 里，抄过去只会抄错。
- **装置抽成 `tools/figma-harness.mjs`**，与 `qa-plugin.mjs` 共用同一份严格效果桩。
  理由：效果字段白名单是**安全关键常量**（写错一个 key 毛玻璃全灭），粘贴第二份将来必然腐化。
- **三档判定，边界如实打印**：① 结构错误（离线可判定）② 契约错误（真源码带 `code` 的裁决）
  ③ **装置缺口**（桩没覆盖到的宿主 API，不算产品缺陷，单列提示补桩——既不能当错误，也不能当通过）。
- **逐步试跑而非整批提交**：`run` 是 fail-fast 的，一次只报第一个；等到它报，画布上已经躺了半张页面。
  预检必须**一次报全**（由变异测试的「一次报全部」断言守住）。
- **额外拦一类跑不出来的错**：`set-effects` 会**静默丢弃**它不读的输入字段（不报错也不生效）。
  这类错试跑一万遍也跑不出来，只能静态拦；依据是 `EFFECT_INPUT_KEYS`，与 `EFFECT_KEYS` 同处一份文件
  （输入侧 / 输出侧契约配套）。1.1 的 P0 就是这个形态。
- **边界（如实写进工具输出）**：**离线查不出**「某个 node id 在画布上到底存不存在」——画布是桩的。
  这类字面 id 一律归入「待核对」，交给 B2。装置另按参数名给期望类型提示（`componentId` → COMPONENT），
  免得撞出 `NOT_A_COMPONENT` 这种假错误。
- **DoD 验证**：注入 13 类已知错误全部报出（`precheck-mutation.mjs`）；对合法计划零误报
  （42 步基准样例 + **36 个 op 全通跑**：0 错误 0 装置缺口）。

**B2 — 在线引用核对（可选，`--live`）** → 同上 ｜ ✅ **已完成**

- 开工前连 Bridge，把计划里引用的画布已有 id 逐个用 `get-node` 问一遍（不求页面摘要，因为
  `get-page-summary` 只给顶层节点，深层 id 读不到）。
- **边界：这是「核对」不是「仿真」**——Figma 无事务，做不了真仿真。已写进工具输出。
- **插件未连接 / 掉线时判失败**，并明确说「核对不了」，**不得**把没查说成查过了。
- **DoD 验证**（`precheck-mutation.mjs` 的 B2 段，真 Bridge + mock 插件端到端）：
  · id 存在 → 通过 · id 缺失 → 报出「step 2 (get-node) 的 id = 9:9 —— 画布上不存在这个节点」
  · 插件掉线 → 失败且不说「全部存在」

### C. 立证据（L4 从主观打分到有据可查）—— 按环境分两档

| 维 | 通用（所有环境） | 环境增益（能读图的 Agent） |
|---|---|---|
| Layout | `layout-audit.mjs`（已有） | — |
| Color | **新增 `tools/contrast-audit.mjs`**（对比度计算 + WCAG AA/AAA 判定） | — |
| Consistency / Commercial / Usability | 结构化回读的启发式规则（如相邻卡片圆角 / 字重一致性） | 导出 PNG + 读图评分，评分附「我看到了什么」 |

- **C1 — `tools/contrast-audit.mjs`** ｜ ✅ **已完成**：从 DS Spec tokens 复算「文本 × 表面」矩阵的 WCAG 比值与 AA/AAA 档位，并把 `accessibility.contrast` 里**手写的每条声称值**与复算逐条比对（另交叉验证声称里的 hex 与 token 实际值）。**上线即抓到实证**：随包 3 份样例的 13 条声称值里 **9 条**与 WCAG 公式不符（偏差 0.17~1.64、方向还不一致），说明那些数字是被「编」的；且同一个错值还被抄进了 `design-system-spec.json` 的字段描述里——**写在样例/文档里的数字会被下游当事实复用**。已修正三份样例与 schema 描述。配套 `tools/contrast-audit-mutation.mjs`（**36 例**）。
- **C2 — L4 规则更新**（`visual-critic.md`）｜ ✅ **已完成**：新增 §1.6「证据两档制（通用 / 增益）」+ 逐维分档表 + 四条硬约束；`critic-report.json` 增开可选 `_evidence`（`mode` / `unassessed` / `visionObservations`）。**读图是增益、不是前提**——通用档恒产出，增益档只加不替；无法评估项**剔除权重并重新归一化**（未评估既不当通过也不当零分）。§4 执行方式前两条同步改写。
- **C3 — QA 体系 Node 化（4 个脚本）** ｜ 🚧 **进行中：`qa-l2.mjs` 已完成并实测（47 断言全绿 + 16 例变异测试验证能抓错）** ← **本次新增，1.2 工作量最大的项**

  | 目标文件 | 来源 | 覆盖 | 断言数 | 说明 |
  |---|---|---|---|---|
  | ✅ `tools/qa-l2.mjs` | `stage10-4-qa.py`（204 行） | L2 DS Spec（Schema / Token 无未知色 / 组件覆盖 / DS 单源） | 47 | 与 A1 配套。**已实测**：`47 PASS / 0 FAIL`（与上游一致）；另经 **16 例变异测试**（逐项注入已知错误）确认能抓错，并修掉一个上游逻辑漏洞（见 CHANGELOG）。额外支持 `--spec` + `--brief` 校验用户自己的产物 |
  | `tools/qa-critic.mjs` | `stage10-6-qa.py`（144 行） | L4 Critic（Schema / 评分 / 证据 / 路由 / loop 自洽） | 90 | 与 C2 配套 |
  | `tools/qa-export.mjs` | `stage10-7-qa.py`（237 行） | L5 Export（Schema / 文件存在 / node id 回读 / 映射完整 / Token 回溯 / 无孤儿 / 冻结零修改） | 652 | **L5 出口闸门** |
  | `tools/qa-install.mjs` | `stage10-7-install-qa.py`（109 行） | 安装体验（README / SETUP / USER_GUIDE / 探针四方契约一致） | 56 | 与 A3 配套 |

  - 上游 `stage10-5-qa.mjs`（L3）**不搬，重写为通用版**——它硬编码了项目名与 token 白名单（`AI Health /`、`Stage 10.5 /`），是项目级审计。
  - 全部零第三方依赖（上游仅用标准库），直译可控。
- **DoD**：L4 产出 critic-report 时五维**每一维都有工具输出或 PNG 作为 evidence**，无目测评分；L5 Export Gate 由 `qa-export.mjs` 实际闸门，不再是空规则。

### D. 定验收

**D1 — 硬验收标准** ｜ *待开工*

Agent 全程满足以下三条，跑完一次完整链路（L0→L5）：

- **零人工补丁**：不手工修改 Brief / DS Spec / Build Plan 的 JSON；
- **零失效引用**：执行中不引用不存在的工具 / 文件 / op（由 A4 守卫）；
- **零目测评分**：L4 每维评分附工具输出或 PNG（由 C1 / C3 支撑）。

**D2 — 校准样本** ｜ *待开工*

跑**两次**不同行业 / 复杂度的真实构建（需 Bridge + 插件环境），记录翻车点，用结果校准「翻车率」——补「单次样本做战略判断」的缺陷。走查步骤可参照上游 `docs/stage10-7-e2e-validation.md`。

---

## 5. 范围外（明确不做）

| 项 | 处置 | 理由 |
|---|---|---|
| 图片填充 | 单独立项，等 1.2 验收数据再定 | 能力项，稀释「可信版」主题；实现路径已定（见附录）。清查佐证：上游 8 个阶段从未做过（`plugin/code.js` 里 `IMAGE` / `setImage` / `imageHash` 零命中），不在主线能力上 |
| MCP 外壳 | 1.3 评估「门铃式」 | A1 已是铺路；1.2 无需额外动作 |
| stage7 视觉回归移植 | 1.2 不做 | 依赖 numpy / Pillow（破零依赖红线）；且对象不同（两稿对比 ≠ 单稿打分） |
| op 军备竞赛 / 通用 Figma MCP / 适配既有 DS | 否掉 | 官方 `use_figma` 放开后归零 / 丢护城河 / 复杂度爆炸违反零前提 |

---

## 6. 开工顺序与依赖

```
A1 / A2 / A3               ✅ 已完成（L2 契约 + 12 处失效引用 + 版本治理）
   ↓
C3·qa-l2                   ✅ 已完成（47 断言全绿 + 16 例变异测试验证能抓错）
   ↓
A5 ─┬─ A4                  ✅ 已完成（lessons 38 条 / check-refs 192 条引用零悬空 + 18 例变异测试）
   ↓
B1 → B2                    ✅ 已完成（precheck：真 code.js 当契约 + 离线试跑；--live 核对画布 id）
   ↓
C1 ─┬─ C2                  ✅ 均已完成（C1 对比度工具 + 36 例变异测试 / C2 L4 证据链两档化）
C3·qa-critic               (L4 校验)
C3·qa-export               (L5 闸门)
C3·qa-install              (安装契约)
   ↓
D1 / D2                    (两次实测验收)
```

**可并行**：A4 / A5 相互独立，且与 C3 其余脚本无依赖；C3 的四个脚本相互独立。
**已实证的依赖**：`qa-l2` 确实须待 A1 的 DS Spec 格式确定——开工时 A1 立即暴露了 Schema 枚举缺 `web-marketing`，两者是配套的。
**本轮新增的实证**：**校验器 + 配套变异测试**这个模式已被应用三次（`qa-l2`、`check-refs`、`precheck`），每次都在写完校验器后立刻用变异测试抓到过东西：
- `qa-l2` → 抓到上游判定顺序漏洞（派生值填色板色可蒙混）
- `check-refs` → 抓到自身正则漏点目录（豁免规则空转）
- `precheck` → 抓到装置"多给属性"凭空造出契约错误 + `--live` 把"没查成"说成"查过了"

`references/lessons.md` #33 已把它立为通则。C3 余下三个脚本须各自沿用（否则"全绿"不可复验）。

---

## 7. 验收

1.2 完成 = **D1 硬验收通过** + **D2 两次实测的翻车点全部归因**（要么是已知边界，要么纳入下一轮）+ **C3 四个 QA 脚本全绿**。

**不通过则不算完成**，回炉对应块。

---

## 8. 变更管理

本定稿四块是配套的。执行中若发现范围需变：

- **新增能力项** → 默认拒绝，记入 1.3 候选；
- **范围内某项做不下去** → 记录原因，降级或置换，改本文档 + CHANGELOG，不留口头变更；
- **新增非能力项的债务项** → 允许，但必须同时指出从哪里压出对应工作量（如 §3.2.3 把 #11 / #12 / #14 划为缓做），**净范围不得膨胀**。

---

## 附录：图片填充的既定实现路径（备 1.3+ 参考）

`figma-plugin/manifest.json` 的 `allowedDomains` 只有 `http://localhost:45677`，插件拉不了任意 URL。符合红线的路径：

**bridge 起本地 asset 端点（同域，已在白名单）→ 插件 UI fetch → `figma.ui.postMessage` 传字节 → 主线程 `figma.createImage`**。不改 manifest。

瓶颈是 postMessage 载荷（base64 膨胀 33%），单图需尺寸上限、大图分片。
