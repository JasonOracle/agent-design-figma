# 1.3 现状说明

> **这份文件的用途**：读完这一份，就知道 1.3 在干什么、做到哪、还差什么、哪些要人拍板。
> 它不是计划也不是候选清单 —— 计划与开工记录见 `1.3-plan.md`，全部候选条目与出处见 `1.3-candidates.md`。
> **时点**：2026-09-17（第三刀「债线尾巴」收口后）。数字均为当场复跑所得。
> **本文件属开发者内部文档**（`tools/check-refs.mjs` 的 `DEV_ONLY` 模式自动豁免）。

---

## 一、一句话现状

**1.3 已经做了三刀，且已于 2026-09-17 补上立项** —— 范围锁定 **B 档「收口 + 空环」**、
主题「**裁判版**」、四类裁决全部通过。逐项见 **`1.3-charter.md`（立项书，已签字）**。

```
1.2 封版 ──▶ 候选清单（只是「输入」）──▶ ┌────────────────┐
   v1.2.0                                 │ 1.3 立项 ✅    │ ◀── 2026-09-17 补上
                                          │ 范围 · 优先级  │     B 档 · 主题「裁判版」
                                          │ 验收标准       │
                                          └────────────────┘
                                                    │
                                    实情：范围未定就先做了三刀，立项补在三刀之后
```

> **第三刀（债线尾巴）另有一个意外结论**：挑出来的四条「已定位、可机械修」的候选里，
> **两条半站不住**（A1 需求早已满足、D1 落点写错、B5 前提不成立），只有 B6 照做——
> 详见 §三末与 §六。

---

## 二、1.3 是谁规划的（如实回答）

| 环节 | 实际情况 |
|---|---|
| **候选清单怎么来的** | 2026-09-17 的 **1.2 收尾验收**时归拢，依据是 `1.2-plan.md` §8 当时立的规则：「**新增能力项 → 默认拒绝，记入 1.3 候选**」。⇒ 它是 **1.2 的产物，是「输入」不是「计划」** |
| **1.3 立项了吗** | ✅ **2026-09-17 已立项**（三刀收口后补上）—— 见 **`1.3-charter.md`**：B 档「收口 + 空环」+ 主题「**裁判版**」+ 7 条验收标准，由决策者签字。此前 `1.3-plan.md` 顶部原文为「已开工，但**范围未定稿**」，现已改为指向立项书 |
| **三刀是谁挑的** | 由执行者（AI）按四条特征挑选：**已定位 / 可机械修 / 无新判据设计 / 有实测数据可验**。挑选理由亦写在 `1.3-plan.md` §1：「**不需要先做范围决策就能开工**」—— 这正是**立项缺位**逼出来的筛选标准 |
| **决策者做了什么** | 立项前只做了一个决策：**「选哪条线」**；立项时一次补签四类裁决（范围档位 / QA3 断言裁剪 / H 组分类收紧 / C2 暂不做） |

> 这条值得单独记下来：**「能开工」不等于「该开工」。**
> 缺了立项这一步，后果不是做错事，而是**没有人能回答「1.3 到底要干什么」** —— 每一刀单独看都成立，
> 合起来却拼不成一个可验收的范围。
> **第三刀把这个代价变成了实测数字**：那批「已定位、可直接做」的候选里**两条半站不住**
> （`lessons.md` #84）—— 范围没定，筛选标准就只剩「**能不能做**」，「**该不该做**」这一问没有抓手。

---

## 三、已做完的条目（四刀）

前三刀（11 条）的挑选标准是「不需要先做范围决策就能开工」，所以它们全部是**债**、没有一条是新能力。
**第四刀不同**：它是**立项书签字后的第一个动作**（立项书 §2.2 第 0 项）—— 这是这条链上**第一次「先定范围、再动手」**。

**第一、二刀（七条）**

| 条目 | 旧行为错在哪（这才是做它的理由） | 落点 | 验证 |
|---|---|---|---|
| **B1** | `alignment` 在跨轴尺寸不同时**静默跳过** —— 不报、也不计入，**看不出「这块没被覆盖」** | `tools/layout-audit.mjs` | 变异 51 → **60 PASS** |
| **B2** | `precheck --live` 的汇总行**不随后续在线核对更新**，把「已核过」读成「没核」 | `tools/precheck.mjs` | 变异 66 → **75 PASS** |
| **B4** | 引用兜底**替一个产物类型名静默挑了一个实例**（17 条全指向同一个样例），制造「指的是某个具体文件」的假象 | `tools/check-refs.mjs` | 变异 18 → **31 PASS** |
| **B3** | 闸门**因缺输入整体跳过时，输出与「查了且干净」长得一模一样** | `qa-critic` / `qa-l2` / `qa-export` | 变异各 **+1 条**覆盖用例 |
| **A2** | 闸门档位**靠手敲**：实测抓到运行 B 的 spacing 写成 `[2,4,8,16,24]`，而规格只声明 `[8,16,24]` —— 多出的 2/4 是手敲时混入的 radius 值 | `tools/layout-audit.mjs` | 变异 60 → **76 PASS**（含反向对照） |
| **C1** | 效果类参数（填充透明度等）**结构回读是盲的**，而原本用于验证它的装置只活在一次性的运行目录里 | **新增** `tools/pixel-proof.mjs` | 变异 **46 PASS**；真实截图回归**逐位一致** |
| **A6** | 编排文档的命令表**漏列** `layout-audit` —— 同一份文档早就诊断过「运行 A 漏跑它」，**修的是别的层** | `references/acceptance-criteria.md` / `SKILL.md` | 当场核对两处 |

**第三刀 · 债线尾巴（四条，直接点名做掉）**

| 条目 | 原描述 | 实测结论 | 落点与验证 |
|---|---|---|---|
| **A1** | 「readback 形状与 `depth` 语义不匹配，L4 闸门**当场跑不了**」 | **需求早已满足** —— 工具引入时就带着 `shallowWarning`；`depth` 语义也已由一次冻结区修复修好。**只补了残余**：几何覆盖从「容器数」升为「**节点数**」 | `layout-audit.mjs` 新增 `geometryCoverage`；变异 76 → **89 PASS** |
| **A1 证据** | —— | 复核原本只活在**不随包发布**的 `.vibe/` 里 ⇒ 提成仓库内可重跑的探针 | **新增** `tools/layout-audit-readback-probe.mjs`（**4 PASS**） |
| **D1** | 「改 `figma-harness.mjs`（非冻结区，可直接做）」 | **落点写错了** —— 那是**加载器**，改它不会改变回读行为；`paintToHex` 真身在**冻结区** ⇒ 它其实是 D2/D3 同类，**须向冻结区提需求**（未做） | 未改任何冻结文件 |
| **B5** | 「`VERSION` 与 `CHANGELOG` 可各说各话」（该校两者一致） | **前提不成立** —— 文档早就写死两者**语义不同、允许并存** ⇒ 「不校一致」本来就对。真形态 = **校序关系** | 并入下方的守卫 |
| **B6** | 「无 `VERSION` ↔ `git tag` 一致性守卫」 | ✅ 成立，照做 | **新增** `tools/release-guard.mjs`（六条判据）+ 其变异测试（**39 PASS**）；真实仓库 **6 PASS** |

> **第三刀真正的产出不是代码，是这条教训**：候选清单是**某个时点的快照**，不是事实 ——
> 引用前必须回指产物复核，复核对象包括「**定性、前提、落点**」三类。
> 其中「**落点错**」最难自查（一个函数名写在候选里，读者会默认作者查过它就在那个文件）。
> 连续两轮出现「前提不成立」（上一轮 B5 已是一例）⇒ 已在 F4 的筛法里新增一道
> **S1.5「前提是否成立?」**，且它**必须排在「可判定？」之前**。

**第四刀 · 立项第 0 项：`qa-export` QA3 判据修正（D2 / D4 · 一条）**

| 条目 | 旧行为错在哪 | 改法 | 验证 |
|---|---|---|---|
| **D4 · QA3 的 `live-build` 分支** | 要求「**必须有** `figmaFileKey`」，而该凭证是「按 fileKey+nodeId 回读」在 **REST API 上游**里的必要条件 —— 本管线走 **Bridge 直连插件**（凭 `nodeId` 单键即可），插件**无任何 op 返回它** ⇒ 在本拓扑它不是判据而是**噪音**：永远 1 条 FAIL，每次收口都要解释「FAIL 的是环境凭证、不是产物质量」 | **换判据，不是删判据**：拿不到就**如实标 `null` + 在 `_meta.note` 交代缺口**；**已声明就必须非空**（空串 `""` ≠ `null`）；**静默留空 / 全空白 / 空串三条仍拦** | 运行 B `409 PASS / 1 FAIL` → **`410 PASS / 0 FAIL`**；自带样例 `935 PASS` 与运行 A `364 PASS` **均不变**（数字不变 = 换判据）；变异 **57 → 63 PASS** |

> **这一刀的意义不在代码量（改了三处），而在它示范了「裁剪一个判据」的正确姿势**：
> **① 先问这个判据是「产物性质」还是「某个拓扑的前提」**（#85）；**② 把「必须持有」换成「必须交代」**，
> 而不是删掉；**③ 用实测数字证明没放宽**（不该动的数字一个都没动）；**④ 把新判据的盲区写下来** ——
> `_meta.note` 非空**不证明画布真的存在**，它只证明执行者**交代了缺口**；画布是否存在由
> `precheck --live` 的**实际回读**证明（两条判据分工不同，不许互相顶替）。
> 另：**D2 的插件侧**（`ping` 确实不返 `fileKey`）**仍是事实**，仍在立项书 §2.2 第 5 项的需求单里。

---

## 四、为什么前两刀都盯着「校验器自己的可信度」

1.2 让流程有了**痕迹**；1.3 前两刀在修「**痕迹本身可不可信**」。做它们，是因为翻旧账时发现四条「**绿灯是假的**」：

1. 旧记录里被标为「全绿」的那次运行，实际是 `--min-touch 0`（**等于没查触控**）、无 `--baseline`、`--font` 含了一个不该有的值 —— **三条检查缺席或放宽**；
2. `layout-audit` 的档位靠手敲 ⇒ **同一份数据用不同档位得出不同告警数**，数字之间不可比；
3. 38 条引用靠兜底解析，**指向了错误的同名实例**；
4. 闸门缺输入时整体跳过，**输出与「查过且干净」完全一样**。

⇒ 结论一句话：**「通过」不算留痕；未核对不得折算为通过。** 这两刀就是把这句纪律落进代码，而不是留在文档里靠人记得。

---

## 五、当前实测基线（2026-09-18 F4 第 3 步收口）

> **F4 第 3 步（未使用字段清单）动了工具层**：`layout-audit` 检查类 **10 → 13**（真实回读
> `summary` 不变 —— 新检查全部因前提不成立而不触发，加的是哨兵不是噪音）、
> 新增 `contract-usage`（+42 断言）、`layout-audit-mutation` **89 → 125**。
> 变异测试 **533 → 611**（×10 → **×11**）。`check-refs` 引用数随文档增补变化，看「悬空」结论。

| 主闸门 | 结果 |
|---|---|
| `check-refs.mjs` | **悬空 0 / 歧义 0**（引用条数会随文档增补变化，看「悬空」这个结论即可） |
| `qa-l2` / `qa-critic` / `qa-export --strict` / `qa-install` | **47 / 134 / 935 / 100 PASS** |
| **`qa-export --manifest`（运行 B 的**真实**清单 · 本刀主产出）** | **`410 PASS / 0 FAIL / 0 WARN`**（原 `409 PASS / 1 FAIL` —— 那条永久 FAIL 已由判据修正消除；运行 A 仍 `364 PASS`） |
| `contrast-audit` / `layout-audit --spec` / `precheck` | OK / 1 条 medium（**不改退出码**）/ 42 步、错误 0 |
| **`release-guard`**（第三刀新增 · 发版一致性） | **6 PASS / 0 FAIL / 0 未核对** |
| **`layout-audit-readback-probe`**（第三刀新增 · 跨模块契约） | **4 PASS / 0 FAIL**（`{depth:1}→1/2` · `{depth:2}→2/5` · `{depth:3,detail:true}→5/5`） |
| **`contract-usage`**（F4 第 3 步新增 · 契约使用矩阵） | **判据通过**：33 字段双向一致（20 在用 / 3 待复核 / 10 零引用） |
| **变异测试 ×11** | **611 条断言 / 0 FAIL** |

> 变异测试是「给校验器造错，看它抓不抓得住」——它才是「闸门可信」的证据。
> 分项：check-refs 31 · contrast 36 · layout-audit **125** · precheck 75 · qa-l2 20 · qa-critic 52 ·
> qa-export **63**（第四刀 +6）· qa-install 82 · pixel-proof 46 · release-guard 39 ·
> **contract-usage 42（F4 第 3 步新增）**。

---

## 六、还剩什么

> **⭐ F4 筛线已完成（2026-09-18，6/6）**：吸纳筛法 + 逐条分档已提为**随包**文档
> `references/design-criteria-intake.md`（四道筛 + **回读契约表** + 六份来源逐条表 + 七条读法 + §七）。
> **6/6 来源已过筛**（`better-ui` 17 条 · `better-layout` 10 条 · `better-typography` 19 条 ·
> `taste-skill` 41 条 · `review-checklist` 20 条 · `review-animations` 14 条 = **121 条原则**；
> 搬 17 / 译 14 / 交人 18 / **无对象 48（40%）** / 拆 18 / 已有 6）。
> **第 4 步**：taste-skill —— **搬率跃升 34%**（判据域换到文本内容）、**语言分档升主杀器**（破折号 =
> 中文破折号本体）、候选新审计器「文案 tell 审计」待判据校准。
> **第 5 步**：review-checklist —— **「已有」首成最大可执行档（20%）**（间距档位 / 触控 44px / WCAG /
> 密度被第三方逐字复刻）；「画布色 ↔ Spec token」缺口获**第二处独立指认**，优先级上调。
> **第 6 步**：review-animations —— 实质 0 条可收但**方法层 4 条佐证**（「方法论与实质分开筛」）；
> **F5 启动包定档**。
> ⚠️ **「筛完」≠「收编完」**：候选检查（文案 tell 审计 / 板外色信号）收编需四件齐（#73），
> 随后续步骤裁决。
> **第 3 步已执行（2026-09-18）**：§4.6 的「未使用字段清单」兑现 —— `tools/contract-usage.mjs`
> （33 字段 × 谁在读，零引用 10 个）+ **5 字段收编 → 3 类新检查 + 1 处分级**（详见 intake §7 与 CHANGELOG）。
> **「筛法已定」≠「F4 已完成」；「清单跑掉」≠「来源筛完」** —— 两条产出线并行。
>
> **✅ F3 三件全清（2026-09-18）**：
> ① ✅ `visual-critic.md` §1.6 消「增益档可合法不读」的矛盾（有图环境必做，能读而不读 = 违规）；
> ③ ✅ 随包清单 `references/read-image-checklist.md`（首批 11 条 = E1×4 + F4 供料×4 + 自有增益×3）
> + 记录 Schema `assets/templates/read-image-review.json`；
> ② ✅ 锚点并置比对装置：`tools/anchor-compare.mjs`（+ 变异 **43 PASS**）· 协议 `references/anchor-compare.md`
> · Schema `assets/templates/anchor-compare-report.json`。锚点源 `nexu-io/open-design`（151 品牌契约，
> Apache-2.0，外部可选不 vendored）；判据 J1–J5 全是「装置有没有如实运转」，相似度结论属 R11 + 人
> —— **立项书 §4 判据 1 的「无装置可评」已消**。首批实测：运行 B × linear-app（13 色 1 命中 / 均距 65.5）。
>
> 本轮沉淀的**下一步动作**（比继续逐条筛命中率高）：**先列「回读契约里有哪些字段没有任何检查在读」，
> 再拿这个清单去找规则** —— `textAutoResize` / `textAlign` / `clipsContent` / `layoutSizing` 都属此类，
> 其中 `textAutoResize` 一条就解锁三条规则。另：`lineHeight` / `letterSpacing` / 数值 `fontWeight`
> **不在契约里** ⇒ 三条排版规则死在数据源，需求进 **D 组需求单**（一次加三个字段解锁 4~5 条）。

### 6.1 执行者可独立推进（不需要决策）

| 条目 | 内容 | 为什么值得做 |
|---|---|---|
| ~~**A3**~~ | ~~读图结论自动落结构化字段~~ | ✅ **已做（2026-09-18）**：`tools/read-image-merge.mjs` + `qa-critic` QA9 + 变异 37 PASS —— 见 `acceptance-criteria.md` §5 与 CHANGELOG |
| ~~**A4**~~ | ~~无清单时的降级路径~~ | ✅ **已做（2026-09-18）**：`qa-export --if-absent not-run`（NOT_RUN 记录 + 退出码 3 四态互斥）+ Schema `export-not-run.json` + 变异 70 PASS |
| ~~**A5**~~ | ~~`precheck --live` 吃增量计划~~ | ✅ **已做（2026-09-18）**：`precheck --after <prior> …`（前置批次=已执行上下文；跨批次 `$name`/字面 id 正常归类「跨批次引用」；前置批次有错 exit 2 拒）+ 变异 63→**101 PASS** —— 见 `1.3-plan.md` §2.8c |
| ~~**H 组**~~ | ~~六处实测盲区分类收紧~~ | ✅ **已做（2026-09-18）**：H1/H3/H4 收紧为唯一解（TY-5 字阶映射 · LD-11 断点 1440/1024 · ST-1 黄/橙按序首个）+ H2/H5/H6 给缺省+区间（SP-4 · AC-3/AC-4 · TY-6），落 `references/design-system.md`；`qa-l2` 样例复跑 18 PASS 不变 —— 见 `1.3-plan.md` §2.8d |
| ~~**F2**~~ | ~~图片填充~~ | ✅ **已做（2026-09-18，用户授权解冻）**：`set-image-fill` op + bridge `GET /v1/asset`（同域白名单内，不改 manifest）+ 回读 `imageFills`（契约 33→34）+ `make-test-asset.mjs` 合成资产；offline 全链路 + bridge 真跑五分支已验证。**✅ 真机复测通过（2026-09-18 19:17，用户操作 Figma + bridge 45677）**：① create-frame `4:2` ✓ ② `set-image-fill` 全链路（bridge `/v1/asset` → 插件 UI fetch → base64 → 沙箱 `createImage`）56ms ✓，`imageHash=c4ff8935…` ③ 独立 `get-node` 回读 `imageFills=["image:c4ff8935…:FILL"]`（第 34 字段真机在契）✓ ④ 反向 `scaleMode:"STRETCH"` → `BAD_PARAM` 拒收 ✓ ⑤ `export-node` 出图肉眼验证渐变填充（`.vibe/f2-retest-proof.png`）✓。
**⚠️ 真机排障教训（值得记住）**：Figma 按 manifest **`id` 缓存开发插件包**——「重跑 Development 插件」甚至「完全重启 Figma」都不重读 `code.js`（旧 36-op 包顽固存活）；**可靠解法 = 改 manifest（名字加 `· F2`）后重新 Import plugin from manifest**，插件窗口日志的 ops 清单是「加载了哪版代码」的指纹（有 `set-image-fill` 即新版）。 |
| ~~**D2 / D3**~~ | ~~向冻结区提需求~~ | ✅ **需求单已定稿（2026-09-18）**：D1/D2/D3 + F4 三条回读字段合并为 **`references/frozen-zone-requests.md`**（FR-1~FR-6，含落点/解锁/验收/风险/批准后纪律）。**✅ FR-1~5 已获用户批准并全部实现 + 真机验证（2026-09-18 19:45，bridge 45677 + 插件 `· F2b`）**：FR-1 `ping.fileKey` 键在（未保存草稿 null 属正常）✓ · FR-2 `fontWeight` Regular→400 / Bold→700 真机映射 ✓ · FR-3 `lineHeight` 真机默认 `{unit:"AUTO"}`（三态形状确认）✓ · FR-4 `letterSpacing={value:0,unit:PERCENT}` ✓ · FR-5 `fillOpacities=[0.5]`（回包+独立回读一致，`fills` 格式未变）✓。**验证口径注**：FR-3 首测期待值写错（以为默认 `{100,PERCENT}`，实际 AUTO 是合法三态）；FR-5 首测参数形状写错（透明度须在颜色 spec 内），均非代码缺陷。契约 33→**38**。FR-6 另行处理 |

### 6.1b 夜班收口（2026-09-18 凌晨 · 自主执行段汇总）

**做完**（按 `1.3-plan.md` 顺序 (2)F3 → (3)A3/A4/A5 → (4)H/D + 用户临睡追加的 F2）：

| 项 | 落点提交 | 验证 |
|---|---|---|
| F4 筛线 4~6 步（taste-skill / review-checklist / review-animations） | `9624f18` `f5f73f7` `cd33eff` | 六源合计 121 原则，分档口径全部落在表头（#89） |
| A3 读图结论结构化落点 + QA9 | A3 提交 | 变异 37 PASS |
| A4 qa-export 无清单降级（NOT_RUN + exit 3） | A4 提交 | 变异 63→70 PASS，基线 935 不变 |
| A5 precheck 增量批次（--after） | `cbde0ee` | 变异 63→**111** PASS（含 F2 后），真批次冒烟过 |
| H 组六盲区裁决（TY-5/LD-11/ST-1/SP-4/AC-3/AC-4/TY-6） | `372b4f7` | qa-l2 18 PASS 不变 |
| D 组需求单定稿（frozen-zone-requests.md FR-1~6） | D 组提交 | 纯文档，冻结区未动 |
| F2 图片填充（**用户授权解冻**） | F2 提交 | 变异 111 PASS · bridge 五分支真跑 · qa-plugin 37 op；**真机复测待用户**（见 F2 行步骤） |

**收口时全套闸门实测**（#74：引用请重跑）：

| 闸门 | 汇总行 |
|---|---|
| `check-refs` | `引用总数 1000 ｜ 已解析 1000 ｜ 歧义 0 ｜ 悬空 0` |
| `qa-export` | `935 PASS / 0 FAIL / 0 WARN` |
| `qa-install` | `100 PASS / 0 FAIL / 0 WARN / 0 装置缺口` |
| `qa-l2`（saas 样例） | `18 PASS / 0 FAIL` |
| `qa-critic`（run-b） | `45 PASS / 0 FAIL / 1 WARN` |
| `qa-plugin` | `21 项全过`（op 面 36 → **37**） |
| 变异 ×13 | `precheck 111 · layout-audit 125 · qa-install 82 · qa-export 70 · contract-usage 42 · qa-critic 52 · anchor-compare 43 · read-image-merge 37 · check-refs 31 · contrast 36 · pixel-proof 46 · release-guard 39 · qa-l2 20(ok)` —— **全 0 FAIL** |

**留给用户的 2 件事**：
1. **F2 真机复测**（约 2 分钟，步骤见上 F2 行）—— 离线桩证明不了 Figma 沙箱行为；
2. **冻结区需求单逐条批准**（`references/frozen-zone-requests.md`）—— 批哪条做哪条。

> **已从本节移出（第三刀已做掉或改判）**：A1 ✅ 已做（残余收编进 `geometryCoverage` + 契约探针）；
> D1 → **改判为「须向冻结区提需求」**，与 D2/D3 合流；B5 ✅ 重述为「校序关系」；B6 ✅ 已落地为 `release-guard`。
>
> **第四刀（立项第 0 项）又移出一半**：**D4 ✅ 已做**（QA3 判据修正，见 §三）；
> **D2 的插件侧仍留在本表** —— 它与 D3 合流为「向冻结区提需求」，仍不需要决策者拍板。

### 6.2 需要决策者拍板（4 类）· **已于 2026-09-17 全部裁决** ✅

> 四类**全部按执行者建议通过**；逐项效力以 **`1.3-charter.md` §5** 为准。下表保留原始分析以便对照。

| 待决 | 问题是什么 | 选项与代价 | 执行者建议 | **裁决** |
|---|---|---|---|---|
| **D2 / D4** | `qa-export` 的 QA3 要求 `live-build` 必带 `figmaFileKey`，但本管线回读走 Bridge 直连插件、**凭 `nodeId` 单键即可**，`fileKey` 在功能上没有用途。该断言疑为从基于 REST API 的上游继承而来 | **保留**：本环境永远有 1 条 FAIL，且每次都要解释「FAIL 的是环境凭证、不是产物质量」<br>**裁剪**：放宽了一条校验，须按 A4 教训走「**改判据不改编造**」的正规路径并留痕 | **裁剪**，但必须作为一次「判据修正」正式走流程，**不许顺手改** | ✅ **裁剪** ⇒ 立项书 §2.2 第 0 项 —— **已于 2026-09-17 走完正式判据修正流程**（改判据不改编造 + 变异测试 + 文档改判留痕，**不是顺手改**）：实测运行 B `409 PASS / 1 FAIL` → **`410 PASS / 0 FAIL`**，而样例 `935 PASS` 与运行 A `364 PASS` **均不变**，变异测试 **57 → 63** |
| **H1–H6** | 6 处「**约束内的自由**」（字阶映射 / pagePadding / web-admin 断点 / warning 取黄还是橙 / focusRing+touchTarget 数值 / 字重）。三轮独立盲测**各猜一套、互不相同**，而闸门全绿 —— 因为文档规范了「必须满足什么」，没规定这些值取多少 | **收紧**：跨产物方差变小，但文档更厚、可能过度约束<br>**不收紧**：保留执行者的判断空间，代价是不同执行者产出不同 | **分类处置**：凡有「跨产物一致性」需求的收紧（如字阶映射、状态色），纯风格选择的保留自由（如 pagePadding 具体值）。**但这是产品判断，不适合由执行者代定** | ✅ **分类收紧**：H1 字阶映射 / H4 状态色 / H3 断点**收紧**；H2 / H5 / H6 **保留自由但须给合理区间** |
| **C2** | 增量补齐（喂「期望清单 + 目标父级」，自动回读、算差、只补差集）。当前是纪律，无工具；运行 B 现写过临时脚本 | **做成工具**：又一个装置要长期维护（收编 = 代码 + 前提 + 留痕 + 变异测试，四件齐）<br>**留作纪律**：每次跑都要现写 | **暂不做**。它依赖 `get-node` 的输入形状，而 **A1 已查明该形状本来就能用**（无需改动）—— 可等 1.3 范围定稿后再评估还有没有必要 | ✅ **暂不做** ⇒ 立项书 §2.3 排除表（**留作纪律**） |
| **F3 / F4 / F5 / F6** | **F3**：L4「设计判断」是整条链里**唯一没有裁判**的一环。**F4**：给它找原料（外部设计判据机械化吸收）。**F5**：动效维度**从没立过**。**F6**：响应式只落在声明里、不落画布 | **全部立项**：范围大幅膨胀<br>**只立 F3+F4**：聚焦空环本身<br>**都不立**：L4 继续只有数值/结构裁判 | **只立 F3 + F4**；F5 / F6 作为它们评估过程中的发现一并称量，**不单独立项** | ✅ **只立 F3 + F4**（= 立项书 **B 档**）；F5 / F6 作**关联发现**不单独立项 |

> **F3 的证据要说清**（这是它值得立项的原因，不是「感觉少了点什么」）：
> 已补做的 L4 报告档位确实走到了 `structured+vision`（**真的读了图**）、五维均分 8.5、`action = PASS`，
> 但 `unassessed` 里躺着 5 项，其中 **Commercial 的「商业产品相似度」占该维 40% 权重**，未评，
> 理由是「需与 Apple / Stripe / Linear / Ant Design Pro / Material 3 **同场景并置比对**，本轮只做了单图」。
> ⇒ **「并置比对」这个动作，链路里根本没有装置**，所以设计感最核心的那一项**结构性无法评估**。

---

## 七、文件索引

| 文件 | 管什么 |
|---|---|
| **`1.3-status.md`** | **本文** —— 给决策者读的现状概览 |
| **`1.3-charter.md`** | **立项书（已签字）** —— 1.3 的**范围 / 优先级 / 验收标准**与四类裁决。**判断「该不该做」以它为准** |
| `1.3-plan.md` | 计划与开工记录：三刀的详细记录、纪律；§3「建议范围」仅为历史对照 |
| `1.3-candidates.md` | 全部候选条目 A–H 八组，逐条带出处、守卫现状与建议优先级（**是输入，不是待办**） |
| `references/lessons.md` | 实测踩坑后验证过的**不变量**（收编纪律与变异测试要求的来源） |
| `references/acceptance-criteria.md` | 验收标准、逐阶段闸门命令表、编排纪律 |
| `CHANGELOG.md` | 每个版本的变更与**当轮实测数字** |
